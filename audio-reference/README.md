# audio-reference

Research code from the local experiment of 2026-09-24/25 that designed the example-audio
workflow (prompt strategies A–H, the checkers, the latent-class analysis). Kept for reference
only; production code is `build/audio_*.py` (see `AUDIO_WORKFLOW.md` §11). The test set and
the regression clips moved to `audio/testset/` and `audio/regression/`.
