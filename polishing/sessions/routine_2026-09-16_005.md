# Routine session — 2026-09-16 #005

**Mode**: systemic-fix
**Reason**: highest scheduler debt among eligible modes
**Backlog item**: `transitivity-review-queue` (reviews/transitivity/disagreements.jsonl)

## Pre-flight

No open routine PRs, no orphan `claude/*` branches. Session branch already up
to date with `main`.

## Work done

Worked `reviews/transitivity/disagreements.jsonl` top to bottom, 100 entries
(05321_doumei through 07776_sapooto). For each: read the entry's own examples,
judged the real valency of the underlying verb (a genuine を-marked patient
vs. a に/と/から-marked target, or a self-contained activity noun that takes
no further object), set `tags.transitivity`, and removed each resolved line
from `disagreements.jsonl`. Four entries in the range (07155_uchiawase,
07167_ketsui, 07425_kainyuu, 07445_torishimari) already carried the correct
tag on inspection — verified against their examples and left unchanged, but
still removed from the queue. No genuine 自他 pair verbs turned up in this
batch (mostly suru-noun compounds and single verbs), so no `prominent_see_also`
links were added.

`scope_estimate` on the backlog item: 2264 → 2164.

## Mechanical pass (§3)

- `normalize_notes.py`: 0 rewrites (pre-existing non-canonical headers in
  these entries are unrelated to this run and were left alone).
- `auto_link.py`: 9 links added across 7 entries.
- `harvest_crossrefs.py`: 0 new cross-references (existing coverage already
  complete for these entries).
- `validate.py --changed-only --ratchet`: 96/96 valid.

## Self-check (§4)

`review_accuracy.py` on all 96 changed entries: 10 flagged, 4 filtered as
noise (breadth=3, style=1) beyond the reviewed set (~$0.046). Adjudicated:

- **Applied (6)**: 05451_yurai (semantic tag `education`→`history`, wrong
  category), 05464_haisou (semantic tag `communication`→`transportation`,
  wrong category), 05928_ankooru (notes wrongly claimed English "encore" is
  primarily the audience's shout; corrected to match Japanese usage — both
  senses exist in English too), 05965_kikyaku (top-level gloss "dismissal;
  rejection (legal)" read as if only "rejection" were legal; both senses are
  legal, so aligned the top-level gloss with the definition's "dismissal;
  rejection (of a lawsuit)"), 06180_teikuauto (politeness tag `polite`
  contradicted the entry's own "Neutral to casual" register note; set to
  `plain`), 07080_kosupure (notes claimed cosplay itself originated in Japan;
  corrected to say the term was coined in Japan while the practice of
  costume dress-up predates it).
- **Rejected (5)**: 05461_jogen and 07518_satei (reviewer wanted a more
  specific formality label than `formal`; no real contradiction with the
  notes, just a preference not supported by the schema), 05462_chuukoku
  (reviewer claimed the notes imply 忠告する is transitive; no example or
  note text actually shows a を-marked object for the verb itself), 05451_yurai
  (reviewer claimed the USAGE section's adverbial sense of 由来 refers to a
  different, archaic word 由來; 由來 is simply old orthography for the same
  word, and this literary adverbial sense is genuine — not an error),
  05966_kouso (reviewer claimed the appeal-path note was wrong; the note
  already states the correct path, District Court → High Court).
- **Flagged to curator (0)**.

`review_links.py` kana-link check on the same 96 entries: 2 flagged, both
kept — 05937_poripori's やめられない (potential-negative inflection of
やめる, correct lexeme) and 07702_yaritori's やり in an etymology breakdown
(the stem form of やる, correct lexeme, not a different word).

Ledger: self-check accuracy review ~$0.046, link review ~$0.005. Daily spend
now $1.56 of $5.00.

## Metrics

`entries_changed=96, flags_applied=6, flags_rejected=5, flags_to_curator=0`
(see `pipeline/metrics-history.jsonl`).

## Curator notes

None this run.

## Cursor / backlog state

`transitivity-review-queue.scope_estimate`: 2264 → 2164 (2164 lines remain in
`reviews/transitivity/disagreements.jsonl`). Next run should continue from
the top of the file (07796_tekagen onward).
