# Routine session — 2026-08-04 (008)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Selector signals**: candidates 990 (16 "seen in entry"), max entry ID 30396,
comprehensive frontier 06781, cross-model-review cursor 25601, unharvested
observations 8, reviewed entries 586, open backlog items 20.
**Params**: `start_id: 25601`, `phase: furigana`,
`openrouter_session_budget_usd: 2.50` (daily remaining at start: $4.70).

## Pre-flight

- §0a: no open PRs — nothing to rescue.
- §0b: no `claude/*` strands to sweep. Lock acquired for
  `claude/determined-rubin-sg3dmc`.
- Branch started at `origin/main` (`bcd2bfa59`).

## Range worked

Both passes were launched over 25601–26200 concurrently and stopped early to
leave wrap-up headroom (§6). Actual coverage:

| Pass | Range covered | Entries | Flagged |
|---|---|---|---|
| `review_accuracy.py` (gloss / translation / tags) | 25601–25871 (contiguous) | 271 | 98 entries, 119 issues |
| `review_runner.py --pass screening` (furigana) | 25601–25792 | 78 | **0** |

Issue mix from the accuracy pass: 49 `tags/error`, 52 `tags/warn`,
11 `gloss/error`, 7 `translation/error`.

The deep furigana pass was **skipped**: screening produced zero flags, so there
was nothing to deep-review. Deep spend for the run was $0.00.

## Adjudication

All 67 error-severity issues were worked individually. Warn-severity issues were
handled per §A: the dominant family (off-vocabulary semantic tags) is
correct-by-definition under the 2026-06-11 semantic-tag policy and was applied
in full; the residual in-list breadth/narrowness nits were bulk-rejected with
one aggregated ledger line.

**Applied — 89 entries changed**

- **Semantic-tag migrations (83 entries)** — tags outside `VALID_SEMANTIC`
  migrated to in-list equivalents chosen per entry, not mechanically:
  `place`→`shopping`/`building`/`geography`, `event`→`building`,
  `body`→`body-part`/`person`, `time`→`time-general`/`time-period`,
  `people`→`person`, `social`→`society`, `emotions`→`emotion`,
  `psychology`/`mental-state`→`cognition`, `medicine`→`health`,
  `crime`→`law`, `tools`→`tool`, `sport`→`sports`, `arts`→`art`/`music`,
  `household`→`furniture`/`daily-life`, `vehicles`/`transport`→`transportation`,
  `writing`/`documents`/`publishing`→`communication`/`media`,
  `food-and-drink`→`food`, `degree`→`quantity`, `mathematics`/`statistics`→
  `number`/`science`, `school`→`education`, `agriculture`→`work`/`nature`,
  `environment`→`nature`, `life-stages`→`person`, `loanword` dropped.
- **Sole-`general` under-specification (7 entries)** — 25656 `business`;
  25664/25665 `economics`; 25810/25819/25820/25824 `law`; 25813
  `communication`; 25821 `action`. Aligned with the open `tag-sole-general`
  backlog item.
- **25632 悪戯好き** — POS corrected to `noun, na-adjective`
  (`["noun","adjective-na"]`). The entry's own notes state it is used
  adjectivally (悪戯好きな), which is what licensed the adjectival glosses the
  model flagged as inconsistent with a noun-only POS.
- **25769 死亡届** — gloss "death certificate; death notification" →
  "death notification; death registration form". 死亡届 is the notification
  filed with the municipal office; the certificate is 死亡診断書. The entry's
  own notes and all three examples already used "death notification" — only
  the gloss was wrong.
- **25812 イナゴ** — gloss reordered to "rice grasshopper; locust" and three
  example translations changed from "locusts" to "rice grasshoppers". イナゴ
  (Oxya spp.) is not the migratory locust; "candied locusts" for イナゴの佃煮
  was actively misleading.
- **25832 席を外す** — `formality: formal` → `neutral`. The entry's own notes
  describe 中座する/離席する as "more formal" alternatives, contradicting the
  label.
- **25842 目障り** — example 4 "a thorn in her boss's side" → "an unwelcome
  presence as far as her boss was concerned"; the idiom overshoots 目障り.
- **25850 ぐずぐずする** — `onomatopoeia` added (ぐずぐず is gitaigo).

**Rejected (20 issues)**

- In-list breadth/narrowness substitutions (25636 `daily-life`→`tool`, 25722
  `culture`, and a 7-issue warn family) — excluded by the §A semantic-tag
  policy.
- 25705 銀河系 "remove galactic system" and 25772 弾丸列車 "mark as archaic":
  both glosses are defensible and 25772's notes already document the term's
  historical origin and current colloquial use.
- 25707 倍速 "gloss too narrow": the gloss already reads "double speed; fast
  speed; multiplied speed" — the model's own suggestion was already present.
- 25736 旅行鞄 "add `clothing` because bags are often used for clothing".
- 25774 駐留する "add `action` to a suru-verb": tautological with the POS, per
  the open `tag-sole-action-verb` backlog item.
- 25802 きちんとする / 25829 燕雀 / 25834 おもねる / 25851 そぐわない: mimetic
  and formality labels supported by the entries' own notes.
- 25769 examples[2] translation: 〜と一緒に is correctly rendered "along with".
- 25842 examples[1]/[2]: literal-rendering preference, existing English is
  idiomatic.

Nothing was escalated to `reviews/needs_curator.txt`.

## §4 self-check

Ran `review_accuracy.py --ids 25632,25769,25812,25842 --budget 0.25` on the four
entries where content (not tag-vocabulary) was edited. Tag-only edits were
skipped per §4 step 3 — they were covered by §A this run and are verified
deterministically against `VALID_SEMANTIC`.

**Result: clean — 0 issues on all 4 entries** (~$0.0018).

## Spend

| Phase | Entries | Est. |
|---|---|---|
| screening + accuracy | 349 | $0.1323 |
| self-check | 4 | $0.0018 |

Daily ledger after this run: **$0.4333 / $5.00**. Session budget was $2.50; the
binding constraint was adjudication effort and context, not dollars, exactly as
§A anticipates.

## Candidates and observations

- No missing-entry words were encountered that were not already candidates.
- Three observations appended to `polishing/observations.md`: the off-vocab tag
  density in this block (`[pattern]`), the first post-fix screening sample
  (`[tooling]`), and the reviewer's inconsistent severity assignment for the
  same defect class (`[tooling]`).

## Notable finding — off-vocab tag density

A deterministic scan of 25601–26200 against `VALID_SEMANTIC` found **197 of ~600
entries (33%)** carrying at least one off-vocabulary semantic tag, across 92
distinct bad tags. This run migrated the 45 the reviewer surfaced plus the 38
caught in error-severity flags; the rest belong to the existing open
`unknown-semantic-tags` backlog item and should be swept in `systemic-fix` mode
rather than entry-by-entry here. `validate_tags.py --check-no-new-unknown`
passes; the repo-wide baseline still reports 2808 affected entries.

## Cursors

- `polishing/tasks/cross-model-review/progress.txt` → `next: 25872`
- `reviews/queue.txt` — 25601–25871 removed (12289 → 12018 lines)
- `pipeline/routine-state.json` — advanced by the selector in §1
