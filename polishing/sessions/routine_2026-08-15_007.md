# Routine session — 2026-08-15 (007)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: candidates 160, seen-in-entry 14, max entry ID 30673, comprehensive frontier 06947,
cross-model-review cursor 11501, unharvested observations 8, reviewed entries 586, open backlog
items 36. OpenRouter: $0.3914 spent today of the $5.00 cap.
**Branch**: `claude/determined-rubin-vsi5y6`

## Pre-flight

- §0a rescue: `list_pull_requests` (open) returned **0 PRs** — nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs, so nothing to close.
- §0b orphan-branch sweep: `list_branches` returned **`main` only** — zero orphan branches.
- Lock acquired on `claude/determined-rubin-vsi5y6`.
- `pipeline/update-brief.py` run before reading the brief.

## Activities

Five: [F] harvest, [F] dictionary-wide measurement, [H] metrics refresh, [D] new priority,
[A] backlog-queue sync (plus [E] three curator escalations).

### [F] Harvest — 12 observations from three runs

All 12 observations from the three Routine runs since the midday harvest were processed and
removed from `polishing/observations.md`. Five of the claims were measured dictionary-wide
before filing; three changed shape under measurement.

| observation | filed as | measured outcome |
|---|---|---|
| `・` note bullets penalised by the scorer | **Cleanup P62** + queue item `notes-nakaguro-bullets` | confirmed: 2,496 entries / 18,089 lines; **130 of top 500** priority entries (26%) |
| zero-link "January 18/19 creation cohort" | **Tooling 124** + Cleanup note | **refuted**: the cliff is the polish frontier seen through the creation-date axis |
| `verify_furigana.py` "does not check headword" | **Tooling 123** | **corrected**: it does check headwords; it exits 0 with a clean summary on an unmatched argument, and the failing form is the documented one |
| `word_id_lookup` slash variants | Tooling de-scope note | **shrank**: 3 keys of 28,546, 4 unindexed sides — real, not a class |
| note-header kanji (`THE 〜物 SERIES:`) | Cleanup note | 8 surviving entries, all already reported by `find_missing_furigana.py` |
| accuracy `tags` dimension noise | Tooling 111/118 update | 74 of 108 flags are one family; 4 sentences of prompt removes 82 of 108 |
| accuracy vs screening throughput | **Tooling 84 update** | two measurements disagreed 14×; reconciled — 4.5/min was measured under contention |
| furigana screening precision ~0 | folded into metrics refresh | 0/48 this window, cause now named (Tooling 119) |
| empty `description`; "casual" formality | recurrence notes | both filed this morning, re-observed within 9 hours |
| 06941 つーか stacked contraction | Entry follow-ups (06920, 16105) | — |
| 06946 `noentry` on 三月末 | Entry follow-ups | — |
| P50 (re-check while measuring) | queue item re-measured | 55 → **54**, and six contiguous ID runs rather than scattered |

### [H] Metrics refresh 37 (runs 510–521)

Due at 521 lines against the page's last refresh at 509. Headline: **the §4 self-check reversed
from "insurance" to the highest-precision source on the page (75.7%, 28 applied / 9 rejected)**,
one window after refresh 36 proposed asking whether it could be trimmed. The cause is a
population split, not an instrument change — `new-entries` self-checks ran 1/0, `polish` and
`systemic-fix` self-checks ran 27/9, because "the entries this run changed" are old,
never-reviewed entries whenever the run is a polishing run. It caught a whole 泊まる sense with
five examples living inside basic-tier 00555 止まる.

Other rows: `accuracy`/`tags` 16.9% [39/192] — a mixture weight for the fifth window, 78.0% once
four named noise families are excluded; `gloss` 5.6%; `translation` 30.0%; `furigana` **0.0%**
[0/48], its first exactly-zero window, with 43 of the 48 attributable to Tooling 119. Review
queue rose a second consecutive window (10,279 → 10,553) and frontier-vs-growth reopened to 65%
— both logged back to `polishing/observations.md` as `[pattern]` items per activity H.

## §4 self-verification

**Not applicable** — this run changed no entry files (`git status --porcelain -- entries/` is
empty). Wiki-mode runs edit markdown and JSON backlog data only. No OpenRouter spend; the
ledger is unchanged at $0.3914 for the day.

## Curator escalations (3)

Appended to `reviews/needs_curator.txt` — all three are edits wiki sessions are not permitted to
make:

1. `prompts/newentries.md` omits the `formality` closed list (`formal`/`neutral`/`informal`/
   `vulgar`) while spelling out `pos`, `semantic`, and `domain`. Filed as a recommendation this
   morning; tripped a session again nine hours later.
2. `build/verify_furigana.py` exits 0 with a "0 checked / 0 issues / 0 OK" summary when its
   argument matches no entry — and the argument form that fails is the one CLAUDE.md documents.
3. A convention question for `.claude/skills/vocabulary-notes/SKILL.md`: whether note section
   headers may embed a Japanese suffix, and if so that it needs furigana.

## Candidates added

None. Wiki mode touches no entries, so no missing-word sightings arose.

## Observations logged

Three appended to `polishing/observations.md` (all from the metrics refresh): review-queue
second consecutive rise, frontier-vs-growth reopening, and a `[tooling]` item proposing that the
decisions ledger carry the triggering mode so the self-check precision split becomes automatic
rather than a hand sort each refresh.

## Files changed

- `planning/wiki/ideas/cleanup-backlog.md` — P62; P50 re-measure pointer; zero-link axis
  correction; note-header queue
- `planning/wiki/ideas/tooling-backlog.md` — items 123, 124; slash-variant de-scope; item 84
  reconciliation; items 111/118 update; two recurrences
- `planning/wiki/ideas/entry-followups.md` — つーか cluster; 06946
- `planning/wiki/ideas/backlog-queue.json` — `notes-nakaguro-bullets` added (82 items);
  `zero-link-behind-frontier` re-measured to 54
- `planning/wiki/topics/quality-metrics.md` — refresh 37 (finding 20 + header)
- `planning/wiki/log.md` — session entry
- `polishing/observations.md` — 12 harvested and cleared; 3 new
- `reviews/needs_curator.txt` — 3 escalations
- `pipeline/metrics-history.jsonl`, `pipeline/routine-state.json` — snapshot and selector state

No `make build` — wiki-only run, no entry or `docs/` changes (§6).

## Next cursors

Unchanged by this mode. Comprehensive frontier stays at `next: 06947`; cross-model review at
`next: 11501`. Metrics refresh 38 is due at ~531 `metrics-history.jsonl` lines.
