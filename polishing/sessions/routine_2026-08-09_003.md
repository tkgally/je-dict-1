# Routine session — 2026-08-09 (003)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Signals**: candidates 976 (5 seen-in-entry), max entry 30525, comprehensive next 6844,
cross-model-review next 28901, unharvested observations 12, open backlog items 27
**Budget**: session cap $2.50; daily remaining at start $4.99

## Range worked

- **Accuracy pass** (`review_accuracy.py`, gloss/translation/tags): entries
  **28901–29294** — 394 entries reviewed, 85 issues raised.
- **Furigana screening** (`review_runner.py --pass screening`): entries
  **28901–29046** — 146 entries screened, 2 flagged.
- Both passes were run concurrently and stopped at the §6 context checkpoint
  rather than at the nominal 550-entry range end. The deep furigana pass was
  **skipped** under §A step 2's known-noise shortcut (see below).

## Changes applied (62 entries)

### Off-vocabulary semantic tags — 54 entries

A scripted scan of 28901–29450 against `VALID_SEMANTIC` found 54 entries
carrying a semantic tag that is not in the closed list. Per the §A semantic-tag
policy ("not in the list is correct by definition"), each was migrated to the
best in-list tag, verified individually against the headword and gloss:

| Family | Migration | Entries |
|---|---|---|
| `medical` | → `health` | 29095–29100 (6) |
| `food-drink` | → `food` | 28908, 28916, 28917, 28918, 28920 |
| `body` | → `body-part` / `appearance` / `health` | 28926, 29022, 29045, 29056 |
| `motion` | → `movement` | 29046, 29052, 29060 |
| `sensation` / `manner` / `physical-property` / `perception` | → `descriptive` | 28975, 29002, 29004, 29047, 29048, 29050, 29057 |
| `people` | → `person` | 28959, 28965 |
| `time` | → `time-general` | 28955, 29015 |
| `housing` | → `building` | 28914, 29014 |
| `animals` | → `animal-bird` | 29101, 29102 |
| `mathematics` | → `science` / `number` | 28901, 29021 |
| `measurement` | → `quantity` / `size` | 29007, 29022 |
| `industry` | → `economics` | 29013, 29020 |
| `safety`, `location`, `place`, `environment` | → `general`/`geography`/`nature` | 29016, 29017, 29020, 29061 |
| others (`abstract-concepts`, `physical-objects`, `office-supplies`, `change-state`, `kitchen`, `performance`, `violence`, `sound`, `shape`, `ethics`, `life`, `relationship`, `equipment`, `eating`, `grammar`) | → in-list equivalents | 28902, 28904, 28905, 28923, 28930, 29012, 29049, 29050, 29051, 29052, 29054, 29084, 29085, 29086, 29123 |

Range now has **zero** off-vocabulary semantic tags;
`validate_tags.py --check-no-new-unknown` passes and the baseline was
regenerated (`--write-unknown-baseline`).

### Individual judgments — 8 entries

| Entry | Change | Rationale |
|---|---|---|
| 28929 {苦悶\|くもん} | `{苦悶}{に\|み}ちた` → `{苦悶}に{満\|み}ちた` | **Furigana screening true positive.** The kanji 満 had been dropped and に was wrapped in its place. |
| 28948 ペアリング | semantic `clothing, general` → `clothing, technology` | Sense 2 (wine/Bluetooth pairing) was uncovered; drops the `general` filler. |
| 28952 ガス料金 | semantic `general` → `money, daily-life` | Sole-`general` on a household utility bill (P13 defect class). |
| 28994 ヒール | semantic `clothing` → `clothing, entertainment` | Sense 2 (pro-wrestling villain) was uncovered. |
| 28999 ぴちゃぴちゃ | gloss += "smacking (while eating)" | Sense documented in example 3 and the notes but absent from the gloss. |
| 29112 コケコッコー | semantic `descriptive` → `onomatopoeia, animal-bird` | Aligns with 29101/29102, which were normalised this run. |
| 29124 喫する | gloss: dropped "to taste" | Unsupported by the entry's own definition and all three examples. |
| 29130 三十三回忌 | semantic `general` → `religion, culture` | Sole-`general` on a Buddhist memorial rite. |

## Flags rejected (18)

- **12 in-list substitution nits** — every "too narrow"/"too broad" suggestion
  between two valid tags (28984, 29040, 29041, 29135, 29159, 29244, 29255,
  29276, 29291, and others). Rejected per the §A policy; 12 for 12.
- **28958 常** — formality `formal` challenged; the entry's own notes call it
  "a somewhat literary noun", so the label stands.
- **28963 七回忌** — gloss called misleading about timing; definition and notes
  already spell out the inclusive counting.
- **29007 縦横比** — reviewer applied the English "aspect ratio = width:height"
  convention; the Japanese term is literally 縦(height):横(width).
- **29073 小吉** (gloss + translations) — "small blessing" is the conventional
  English rendering of omikuji fortune levels.
- **29092 ノーコメント** — formality challenged; notes say "mainly formal and
  media contexts".
- **29118 どかどか** — reviewer claimed `onomatopoeia` is POS-only; it is in
  `VALID_SEMANTIC`.
- **1 furigana family (29006)** — `{梅干|うめぼ}し` okurigana split, a documented
  false-positive family in `reviews/calibration_report.md`.

Flagged to curator: **0**.

All 80 adjudications are logged to `reviews/decisions.jsonl`.

## §4 self-check

Ran `review_accuracy.py` on the 7 content-edited entries plus
`review_runner.py --pass screening` on the furigana fix (28929).
**Result: 28929 clean; 6 of 7 clean; 1 residual flag on 28948 rejected**
(reviewer wanted `clothing` replaced for a ring but conceded `jewelry` is not
in the vocabulary — `clothing` is the closest available tag). Est. cost $0.0032.

## Deep furigana pass — skipped

2 flags out of 146 screened entries (1.4%). One was a true positive (28929,
fixed above); one was a known false-positive family. Per §A step 2 the deep
pass was skipped rather than spending ~$0.01/entry re-checking noise.

## OpenRouter spend

| Phase | Entries | Est. |
|---|---|---|
| accuracy review | 394 | $0.1606 |
| furigana screening | 146 | $0.0181 |
| self-check | 8 | $0.0032 |
| **total this run** | | **$0.1819** |

Ledger `spent_usd` for 2026-08-09: **$0.1935** (cap $5.00).

## Observations logged

Five entries appended to `polishing/observations.md`:

- `[pattern]` off-vocab semantic tags are ~10% dense in this block; the
  migration families are mechanical. **1,848 entries dictionary-wide** still
  carry a baselined off-vocab tag (2,436 uses) — a good systemic-fix target.
- `[tooling]` `validate_tags.py` hides off-vocab tags behind `--verbose`
  (11,013 warnings collapsed to a count); a `--list-unknown` mode would make
  the class actionable.
- `[pattern]` 65 furigana wrappers dictionary-wide wrap hiragana as if it were
  kanji; mostly harmless identity wrappers, but they concealed the real 28929
  error. A "wrapper left side contains no kanji" detector would be cheap and
  high-precision.
- `[pattern]` 274 further wrappers give katakana a hiragana reading
  (`{バイク|ばいく}`) — a separate and probably deliberate class; needs a wiki
  policy decision before any sweep.
- `[tooling]` reviewer precision: the applicable tag flags were almost entirely
  the mechanical "not in the valid list" family; every in-list substitution
  suggestion was rejected. The reviewer prompt could stop emitting them.

## Candidates added

None — no un-entered Japanese words were encountered in the reviewed material.

## Queue and cursors

- `reviews/queue.txt`: 10,561 → **10,167** (removed 28901–29294).
- `polishing/tasks/cross-model-review/progress.txt`: **next: 29295**
- Metrics line appended to `pipeline/metrics-history.jsonl`.

## Next run

Resume the accuracy-review sweep at **29295**. The off-vocab semantic-tag
backlog (1,848 entries) is the strongest candidate for the next `systemic-fix`
run — the migration map is nearly mechanical and the deterministic scan finds
every instance for free.

---

## Addendum — screening tail (post-merge)

The furigana screening subprocess survived the stop signal sent at the §6
context checkpoint and kept running while PR #3151 was in CI, writing 25 more
result files (entries **29089–29113**) after that PR's `git add -A`. They are
committed here as a follow-up.

- **24 clean, 1 flagged.** Entry 29097 {心音|しんおん}: the model challenged
  `{聴|き}く` in the collocation {心音|しんおん}を{聴|き}く, arguing 聴 is not
  read き on its own. **Rejected** — this is the documented okurigana family
  from `reviews/calibration_report.md`: 聴く is きく, so the wrapper covers き
  and く is okurigana. The format is correct. Logged to
  `reviews/decisions.jsonl`.
- No entry changes resulted, so no rebuild was needed.
- Screening coverage for this run is therefore **28901–29046 plus 29089–29113**
  (171 entries). The accuracy-review cursor is unaffected: **next: 29295**.
- Ledger: +$0.0025 (`screening-tail`), day total **$0.1960**.

**Process note** — `[tooling]`: `pkill -f "build/review_runner.py"` reported
success but did not actually stop the process; only `kill -9 <pid>` did. A
Routine that stops a review pass early should verify termination by PID before
committing, or the run's own artifacts will land after its commit.
