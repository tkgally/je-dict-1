# Routine session — 2026-07-22 (006)

**Mode:** accuracy-review
**Selector reason:** "accuracy-review: highest scheduler debt among eligible modes"
**Range worked:** entries 17086–17202 (117 entries; cross-model-review frontier)
**OpenRouter session budget:** $2.50 (daily remaining $4.91 at start)

## What ran

- **Furigana screening** (`review_runner.py --pass screening`, gemini-2.5-flash) over 17086–17600, capped by its 15-min timeout at **117 entries (17086–17202)**. 13 flagged.
- **Accuracy review** (`review_accuracy.py`, gloss/translation/tags dimensions) over 17086–17202: **117 reviewed, 58 flagged**, est. cost $0.0511.

## Furigana flags — all rejected (13)

All 13 screening flags fell within documented false-positive families (partial/okurigana
readings, jukujikun, truncated "followed by" context). Verified by direct inspection of the
actual entry JSON rather than a deep pass (saved budget — deep pass skipped per §A step 2):
- 17117 `お{義父|とう}さん` = おとうさん — legitimate jukujikun address form, not an error.
- 17196 `{通行止|つうこうど}め` — め okurigana present outside the wrapper → つうこうどめ, correct.
- 17122/17124/17147/17169/17171/17174/17186 etc. — actual entries contain the correct full
  readings (`圧倒|あっとう`, `婚約者|こんやくしゃ`, `裸足|はだし`, …); flags were truncation artifacts.
- 17168/17185/17198 — flags self-refuted in their own concern text.
One aggregated §C reject line logged (`src:furigana, n:13`).

## Accuracy (tags) flags — 44 applied, 20 rejected

All 58 accuracy flags were in the `tags` dimension. Root cause: the 17000-cohort predates the
locked `VALID_SEMANTIC` vocabulary. A full off-list scan found **53 of 117 entries carrying
off-list semantic tags** — broader than the model's individual flags.

- **APPLIED — migrated all 53 off-list entries to in-list tags** (off-list = definitionally
  wrong per §A tag policy). Used the documented 1:1 map (time→time-general, people→person,
  social→society, medical/medicine→health) plus obvious synonyms (motion→movement,
  grammar→grammatical, literature→media, environment→daily-life, industry→business,
  hobby→leisure, house→furniture, object→tool, place→building/geography, sensation→health,
  body→body-part/person/health per sense). Each target hand-verified in VALID_SEMANTIC; each
  entry's `modified` timestamp bumped. 44 of these were individually model-flagged (logged as
  apply in decisions.jsonl); 9 were proactive off-list fixes the model didn't flag.
- **REJECTED:**
  - 6 formality flags (`formal`→`neutral` on 17121, 17134, 17154, 17171, 17182, 17185): each
    entry's own USAGE notes explicitly describe the word as formal ("A formal word…", "More
    formal than おかず", "literary or formal word") — notes support the label. Aggregated §C line.
  - 17102 売り出し `entertainment`: in-list, defensible for the 売り出し中 "debut/rise-to-
    prominence" sense. Reject (in-list appropriateness nit).
  - 17139 潜伏 `law`→`action`: in-list narrowness substitution, rejected per policy (only the
    off-list `medicine`→`health` applied).

## §4 self-verification

Covered by the §A review itself (accuracy-review mode; the review of these exact IDs *was* the
independent check — no extra OpenRouter call, per §4 step 3 note). All migrations are
off-list→in-list corrections confirmed by `validate_tags.py` (no unknown-tag warnings) and
`validate.py` (117/117 valid). Clean.

## Validation

- `validate.py --range 17086 17202`: 117/117 valid.
- Off-list semantic tags remaining in range: **0**.

## Capture / observations

- One `[pattern]` + `[tooling]` observation logged to `polishing/observations.md`: the
  17000-cohort off-list tag drift, a recommendation for a targeted `check_tag_drift.py
  --check unknown-semantic` sweep across ~16000–18000, and candidate 1:1 additions for
  TAG_MIGRATION.
- No missing-entry candidate words surfaced (review was tag-focused, examples not deep-read).

## Ledger / metrics

- OpenRouter: +$0.074 (accuracy $0.0511 + screening est.); daily spent $0.1603 / $5.00 cap.
- Metrics line appended to `pipeline/metrics-history.jsonl`.

## Cursors

- `polishing/tasks/cross-model-review/progress.txt` → **next: 17203**
- `reviews/queue.txt`: removed 17086–17202 (98 lines; 12953 → 12855).
