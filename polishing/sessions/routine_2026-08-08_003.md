# Routine session — 2026-08-08 (003) — mode: `polish`

**Selector reason:** "polish: highest scheduler debt among eligible modes"
**Signals:** 985 candidates (14 "seen in entry") · max entry ID 30493 · comprehensive_next 6826 ·
cross-model-review next 28351 · 9 unharvested observations · 586 reviewed entries · 22 open backlog items.
**OpenRouter at start:** $0.2265 spent today, $4.7735 remaining of the $5.00 daily cap.

## Pre-flight

- **§0a rescue**: `list_pull_requests` returned an empty list — no open PRs, nothing to rescue.
- **§0b stranded-PR sweep**: no open `claude/*` PRs, so nothing superseded to close.
- **§0b orphan-branch sweep**: `list_branches` returned **`main` only** — zero `claude/*`
  branches on origin. No orphans, no `prune-branch` lines added to `reviews/needs_curator.txt`.
- Lock acquired for `claude/determined-rubin-ponhdz`. Branch was even with `origin/main`.

## Range worked

Two lanes, 15 entries total.

- **Priority lane** — `polishing/priority/notes.txt` lines 268–276 (file dated 2026-07-28,
  11 days old, so still inside the 14-day freshness window). Six entries processed; three
  skipped as modified within the last 30 days (02877 今朝, 02927 七日, 02969 三日).
- **Frontier lane** — 06826–06834, sequentially from `params.start_id` 6826.

## Per-entry changes

### Priority lane (all six had *zero* `cross_references` before this run)

- **02274 英語**: refreshed three stale *split* links in the LANGUAGE PATTERN section —
  フランス+語, ドイツ+語 (was `noentry`), スペイン+語 (was `noentry`) now link to the whole-compound
  entries 24509_furansugo / 28074_doitsugo / 25980_supeingo, matching how 日本語・中国語・韓国語
  were already linked in the same list. Added 4 `cross_references` (日本語, 外国語, 英会話, 英文).
  Left `semantic: ["communication"]` alone — 00614 日本語, 03023 外国語 and 02717 言語 all use it,
  so the family is internally consistent.
- **02646 筋肉**: refreshed the stale 筋肉+痛 split → ⟦{筋肉痛|きんにくつう}→06298_kinnikutsuu⟧.
  Added 5 `cross_references` (筋肉痛, 筋トレ, 腹筋, 骨, 肉 — the last completing the back-link
  from 02240 肉, which already pointed here).
- **02907 背**: added 4 `cross_references` (背中, 身長, 背丈 as synonym, 背骨); retagged
  `semantic` from the template-default `["general"]` to `["body-part"]`.
- **02920 どうぞ**: linked the naked これ in ex3; added 4 `cross_references` (どうか and ぜひ as
  contrasts, どうも, ください); corrected `politeness` from `plain` to `polite` — the entry's own
  first line reads "どうぞ is a polite expression for inviting, offering, or giving permission",
  which is exactly the §A condition for applying a register change.
- **02938 半分**: linked six previously-naked note items (半 → 10460_han, 分 → 26216_bun,
  くらい → 02900_gurai, 半日 → 27978_hannichi, 二分の一 as 二分 `noentry` + の + 一, 半数 `noentry`);
  added 5 `cross_references` (半, 全部 as antonym, 部分, 半額, 半年); retagged `semantic`
  `["general"]` → `["quantity"]`.
- **02976 もう一度**: linked the naked COMPONENTS entries もう → 00602_mou and 一度 → 01312_ichido;
  added 5 `cross_references` (一度, また as contrast, 再び as synonym, 改めて, 一回).

### Frontier lane (all nine had *zero* inline links before this run)

- **06826 画期的**: full link coverage across 4 examples and the whole notes field
  (ETYMOLOGY, FORMS, COLLOCATIONS). 科学史 and 画期 marked `noentry` + candidates.
- **06827 徹底的**: full coverage across 4 examples and notes; added a `related` cross-ref to
  21452_tetteisuru, which the RELATED VOCABULARY section already named in prose.
- **06828 決定的**: full coverage across 6 examples and notes; added 2 `cross_references`
  (決定する, 致命的 as contrast). 確定的 marked `noentry` + candidate.
- **06829 衝撃的**: full coverage across 4 examples and notes.
- **06830 劇的**: full coverage across 4 examples and notes; added a `related` cross-ref to
  02713_geki; **dropped a spurious `domain: ["medical"]` tag** — 劇的 is general-register and
  only one of its four examples is medical. 逆転勝利 and ドラマチック marked `noentry` + candidates.
- **06831 左遷**: full coverage across 4 examples and notes (including the ETYMOLOGY and
  SIMILAR WORDS sections); added a `contrast` cross-ref to 04555_tenkin, which the notes
  already contrast in prose. 遷 and 食らう marked `noentry`; 食らう added as a candidate.
- **06832 栄転**: full coverage across 4 examples and notes; added a `related` cross-ref to
  04555_tenkin. 栄 and 転 left `noentry` (bound morphemes in the etymology line).
- **06833 手取り**: full coverage across 4 examples and notes; added 2 `cross_references`
  (給料, 節税). 総支給額 marked `noentry` + candidate.
- **06834 顔見知り**: full coverage across 4 examples and notes; added 2 `cross_references`
  (友達 as contrast, 見知る); retagged `semantic` `["general"]` → `["person"]`.

Post-edit integrity: `build/validate.py` reports 9/9 valid over 6826–6834 and 790/790 valid
over the 2200–3000 range that contains the priority lane; `verify_furigana.py` reports OK on
all five 〜的 entries; `check_link_targets.py` reports 0 dead targets and
`check_link_baseform.py` reports 0 DISAGREE.

## §4 self-check

`build/review_accuracy.py --ids <15 ids> --budget 0.25`, est. $0.0066 (google/gemini-2.5-flash).

**Completely clean — all 15 entries returned 0 issues.** 0 applied, 0 rejected, 0 flagged to
curator, so no lines were added to `reviews/decisions.jsonl` this run. Spend logged to the
ledger with `phase: "self-check"` (daily total now $0.2331 of the $5.00 cap).

## Candidates added (9)

- 半数 (はんすう): half the number; seen in 02938
- 二分 (にぶん): division into two; halving (as 二分の一); seen in 02938
- 科学史 (かがくし): history of science; seen in 06826
- 画期 (かっき): epoch; new era (bound morpheme in 画期的); seen in 06826
- 確定的 (かくていてき): definite; certain; settled; seen in 06828
- 逆転勝利 (ぎゃくてんしょうり): come-from-behind victory; seen in 06830
- ドラマチック (どらまちっく): dramatic (loanword na-adjective); seen in 06830
- 食らう (くらう): to suffer, to take (a blow, a penalty); seen in 06831
- 総支給額 (そうしきゅうがく): gross pay before deductions; seen in 06833

## Observations logged (5)

- `[pattern]` The 06826–06834 block is uniformly pre-inline-link — all nine entries had zero
  links. Budget ~2 entries per 10% of context across the whole 06800+ stretch.
- `[pattern]` The `〜的` na-adjective family (06826–06830) shares a fixed note skeleton and
  needed the identical five links each time; a templated linker for that family would be a
  safe mechanical sweep, since the pattern is positionally fixed.
- `[tooling]` Stale *split* links are a second detectable family alongside stale `noentry`
  markers (フランス+語, 筋肉+痛). Proposes flagging adjacent link pairs whose concatenated surface
  matches an existing headword in `word_id_lookup.json`.
- `[pattern]` Basic-tier entries keep arriving with empty `cross_references` and template-default
  tags — all six priority-lane entries, plus three carrying a wrong default.
- `[entry]` 06830 carried `domain: ["medical"]` from a single medical example; worth checking
  whether other `〜的` adjectives picked up a domain tag the same way (the P11 contamination shape).

## Next cursors

- `polishing/tasks/comprehensive/progress.txt` → `next: 06835`
- `polishing/tasks/comprehensive/priority-cursor.txt` → `line: 277`

Priority file left in place: it is 11 days old (inside the 14-day window), and every one of the
six priority-lane entries needed substantive changes — far from the "more than half needed no
changes" trigger — so no regeneration or cursor reset this run.
