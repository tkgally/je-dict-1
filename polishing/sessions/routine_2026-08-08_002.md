# Routine session — 2026-08-08 (002) — mode: `accuracy-review`

**Branch**: `claude/determined-rubin-t5ofru`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Params**: `phase: furigana`, `start_id: 27646`, `openrouter_session_budget_usd: 2.50`
**Signals**: candidates 985 (14 "seen in entry") · max entry ID 30493 · comprehensive frontier 06826 ·
cross-model cursor 27646 · 4 unharvested observations · 586 reviewed entries · 22 open backlog items
**OpenRouter**: $0.2265 spent this run of the $5.00 daily cap (started at $0.00)

## Pre-flight (§0)

- **§0a rescue**: `list_pull_requests` (state `open`) returned **0 open PRs**. Nothing to rescue.
- **§0b stranded-PR sweep**: no open `claude/*` PRs. No-op.
- **§0b orphan-branch sweep**: `list_branches` returned **`main` only** — zero `claude/*` branches on
  origin, so no orphans and no `prune-branch` lines added to `reviews/needs_curator.txt`.
- **Lock**: acquired cleanly (exit 0). Branch level with `origin/main` at `729c3e676`.

## Range worked (§A)

| Pass | Coverage | Result |
|---|---|---|
| `review_runner.py --pass screening` | 27646–**27712** (67 entries) | **0 flags**; stopped early (see below) |
| `review_accuracy.py` | 27851–**28350** (500 entries) | 174 entries flagged, 180 issues, $0.2167 |

The accuracy pass started at 27851 because the 2026-08-07 run had already carried its accuracy
coverage to 27850; only the furigana screening lagged at 27646.

**Deep furigana pass skipped** under §A step 2 — screening produced zero flags, so there was nothing
to deep-review. That is the fourth consecutive zero-to-low-yield screening result over
already-polished ranges (2026-06-10/11, 2026-08-07, today).

**Screening stopped early and deliberately.** Run concurrently with the accuracy pass against the
same OpenRouter model, it managed ~1.2 entries/min against the accuracy pass's ~19/min; it was
still starving the pass that carries the actual surveillance value, so it was killed at 27712 and
the accuracy pass finished its full 500 entries in a few minutes once alone. Logged as `[tooling]`.

**Cursor decision.** Set to **28351** — past the accuracy coverage — rather than to 27713 (the range
covered by *both* passes, which is the stricter reading the 2026-08-07 run used). Rewinding to 27713
would make the next run re-review 27713–28350 for accuracy at ~$0.25 for nothing. The residual
**furigana-screening gap 27713–28350** is recorded here and as a `[pattern]` observation instead.

## Changes applied (124 entries)

**Gloss / translation (8 fixes across 7 entries):**
- `27929` サービス{料|りょう} — "gratuity" removed; a service charge is mandatory and non-discretionary,
  which the entry's own explanation already said
- `27948` {計算式|けいさんしき} — bare "calculation" removed from the gloss (that is 計算); example 0
  "I write out the calculation" → "I write out the formula"
- `27952` ギプス — "splint" removed; it contradicted the entry's own "hard casing / plaster cast"
- `27990` {都立|とりつ} — example 1 "run by the metropolis" → "run by the Tokyo metropolitan government"
- `28050` {神経科|しんけいか} — example 2 "at neurology" → "at the neurology department"
- `28253` {涼|すず}しげ — "It makes a cool, refreshing sound" → "It makes a sound that feels cool and
  refreshing"; the old translation dropped the げ "seems/looks" nuance the entry itself teaches
- `28268` {四年生|よねんせい} — "senior (in elementary, junior high, or university)" → "fourth-year
  student; fourth grader; final-year student (university)"; "senior" is simply wrong for a 9-year-old

**Semantic tags (118 entries):**
- **120 off-vocab instances migrated in-list** across 114 entries — the whole `unknown-semantic`
  population of 27851–28350. Range off-vocab: **120 → 0**. Corpus `unknown-semantic`: 2,886 → 2,766.
- Guard applied per the 2026-08-07 precedent: **no migration whose destination would be `general`**
  (lossy; manufactures the sole-`general` defect). Where the reviewer proposed `general`
  (27851 `misfortune`, 28011 `place`, 28071 `manner`, 28002 `object`, 28003 `conflict`) a specific
  in-list destination was chosen instead (`abstract`, `abstract`, `descriptive`, `communication`,
  `idiom`). One reviewer suggestion was refused outright as itself off-vocab: 28044 → `energy`.
- Four named-species entries retagged off the too-broad `nature`: `27869` 黒猫 and `27875` 日本猿 →
  `animal-mammal`; `27872` 隼 and `27873` 鳶 → `animal-bird`.
- Every destination validated against `VALID_SEMANTIC` before writing; `validate.py` clean over
  27600–28400 (801/801).

## §4 self-check

Ran `review_accuracy.py --ids 27929,27948,27952,27990,28050,28253,28268` ($0.0030) — the seven
entries whose **English text** changed. The 118 tag-only changes were skipped: they were driven by
the deterministic `check_tag_drift.py` queue and re-verified by it returning **0 remaining off-vocab
instances** in range.

**No regressions from this run's edits** — not one flag landed on text this run wrote. Four flags
came back, all repeats of claims already rejected on the merits in §A step 4, and all rejected again:
- `27990` gloss — the repeat escalated to a factually wrong version ("any prefectural capital's
  government"). 都 is not a prefectural capital; Tokyo is the only 都.
- `28050` gloss ×2 — "神経科 means neurologist, not the field"; a neurologist is 神経科医.
- `28050` example 0 — "To see a neurologist" is idiomatic English for 受診する, not an error.

One fix round, no re-verify.

## Adjudication summary

- **Applied**: 132 — 12 individual (8 gloss/translation, 4 species tags) + 120 aggregated tag migrations
- **Rejected**: 93 — 7 individual + three aggregated families:
  - 77 in-list narrowness/broadness substitutions (incl. sole-`general` → specific), deferred per §A
  - 5 formality flags contradicted by the entry's own REGISTER note
  - 4 self-check repeats (counted as 2 ledger lines)
- **Flagged to curator**: 0

Notable rejections worth recording because the model was confidently wrong:
- `27921` はとこ — "a second cousin is not necessarily the child of a parent's cousin". It is exactly that.
- `28053` {男児|だんじ} — model read 日本男児 as "Japanese boy". Idiomatically it is "a Japanese man".
- `28069` ご{迷惑|めいわく} — model reversed the causative-permission direction of 案内させてください.
- `28194` {白票|はくひょう} — model called "blank ballot; vote in favor" contradictory and proposed a
  gloss that would have deleted the entry's real second sense (the Diet's white approval slip).
- `28184` {三|み}つ{子|ご} — the entry already separates "triplets" and "three-year-old" into two senses.

## §3 Always-on capture

No new candidate words: this mode reads reviewer reports rather than walking examples, and every
word encountered in the flagged entries already had an entry. **5 observations** logged to
`polishing/observations.md`: the concurrency/throughput finding `[tooling]`; the missing `sound`
semantic category `[pattern]`; the formality-flag noise family `[pattern]`; the screening-yield
trend and its 27713–28350 gap `[pattern]`; and the place-name policy contradiction `[entry]`.

## §5 Metrics

Appended: `mode=accuracy-review, entries_changed=124, flags_applied=132, flags_rejected=93,
flags_to_curator=0, entries_total=30284, review_queue_len=10299, openrouter_spent_today_usd=0.2265`.

## §7 Wrap-up

- Cursor: `polishing/tasks/cross-model-review/progress.txt` → **`next: 28351`** (see decision above).
- Queue: 27646–28350 pruned from `reviews/queue.txt` (11,004 → 10,299 lines).
- Ledger: two lines, `accuracy-review` $0.2235 / 567 entries and `self-check` $0.0030 / 7 entries.
- `make build` run once; PR created, CI polled over MCP, squash-merged; lock released.

## Curator items

1. **Place names exist in the dictionary.** 横浜, 札幌, 青森県, オーストラリア, イギリス, 中国 are all
   live entries, but the standing "no person or place names" note is being used to reject
   proper-noun candidates (C22806, C22820 — filed twice now). The corpus and the policy disagree.
2. **`VALID_SEMANTIC` has no `sound` category**, so 爆発音 and 銃声 took weaker destinations.
3. **Furigana screening's cost/benefit** is now four consecutive near-zero-yield runs. Worth deciding
   whether it stays in the accuracy-review mode at all, or moves to new-entry blocks only.
