# Routine session 2026-09-17 #005

**Mode**: systemic-fix
**Reason**: highest scheduler debt among eligible modes
**Backlog item**: `transitivity-review-queue` (reviews/transitivity/disagreements.jsonl)

## Pre-flight

No open PRs to rescue or absorb (`list_pull_requests` returned none). `list_branches`
returned only `main` — no orphan `claude/*` branches to sweep. Branch was already
up to date with `origin/main`.

## Work done

Worked the transitivity disagreement queue top to bottom, 100 entries (09468_houtou
through 12098_hensou). For each: read the entry's gloss, part of speech, and examples,
judged the verb's own valency (whether the action takes a genuine patient/theme
object, not just the light-verb を that every ~suru noun can carry) rather than the
surface pattern of any one example, and set `metadata.tags.transitivity` to
`transitive` / `intransitive` / `both`. As in the last two runs, no real 自他
(transitive/intransitive) verb pairs turned up needing `prominent_see_also` — this
batch was almost entirely two-character kango + する nouns (懲戒, 起訴, 受賞, etc.)
and loanword/slang suru-verbs (エントリー, フォロバ, コラボ), none with a native
paired counterpart in the dictionary.

Removed all 100 resolved lines from `reviews/transitivity/disagreements.jsonl`
(2084 → 1984 remaining). Updated the backlog item's `scope_estimate` in
`planning/wiki/ideas/backlog-queue.json`.

## Mechanical pass (§3)

- `normalize_notes.py`: 0 rewrites. 16 unknown headers found, all pre-existing
  (sense-numbered headers like "SENSE 1 - COSMETIC SURGERY", comparison headers
  like "リスペクト VS 尊敬"), not introduced by this run.
- `auto_link.py`: 65 links added across 33 entries.
- `harvest_crossrefs.py`: 0 new cross-references — the SIMILAR WORDS/RELATED WORDS
  bullets in the changed entries were already linked or excluded (self/no-entry).
- 100 changed entries total, all validate clean.

## Self-check (§4)

`review_accuracy.py` on the 100 changed entries: 5 flagged (~$0.048), all adjudicated.

**Applied (4):**
- `09764_ninmei`: formality tag was `neutral` but the entry's own REGISTER note
  says "Formal. Used in political, governmental, and organizational contexts" —
  changed the tag to `formal` to match.
- `09918_reishou`, `09919_chouraku`, `10033_ikei`: each carried one off-vocabulary
  semantic tag (`attitude`, `literary`, `interpersonal`) alongside a valid one
  already covering the same ground (`emotion`, `change`, `emotion` respectively) —
  dropped the off-vocab tag, kept the valid one.

**Rejected (1):**
- `11601_riido`: reviewer flagged the top-level `gloss` field ("lead, advantage;
  leash") as improperly combining two senses — but the entry already splits them
  properly into `definitions` sense 1 and sense 2; the top gloss field is a
  semicolon-joined summary, the dictionary's normal convention for multi-sense
  entries (matches e.g. 09493_seikei's "plastic surgery, cosmetic surgery;
  orthopedics" in this same batch). No actual error.

All 5 decisions logged to `reviews/decisions.jsonl`.

`review_links.py` kana-link check: 5 flags, all `other`, all on かける
(→ 00854_kakeru) across three entries (09481_bousou, 11951_shoushuu, 11960_gourei).
Reviewed each in context: 歯止めをかける ("put a stop to"), 召集をかける ("issue a
summons"), 号令をかける ("give a command") are all genuinely 掛ける, just senses the
target entry's three definitions (hang / call / put on glasses) don't cover —
kept per policy, all 5 logged as `keep` in `reviews/link_decisions.jsonl`.

## Needs curator

Logged one consolidated sense-gap note: `00854_kakeru` has now accumulated at
least four uncovered collocational senses across three self-check runs
(検索をかける, 歯止めをかける, 召集をかける, 号令をかける) — worth a dedicated
revision pass adding a broader "to apply/set in motion (an action)" sense rather
than one-off additions.

## Checks

`make gate`: see below (run at wrap-up). `make index` rebuilt `entries_index.json`,
`word_id_lookup.json`, and the kanji index.

## OpenRouter spend

$0.0479 (accuracy self-check) + $0.0020 (link self-check) against a $5.00 daily
cap; today's running total $0.9485.

## Next

Backlog cursor: `transitivity-review-queue` next starts at `12100` (top of the
remaining 1984-line queue after this run).
