# Routine session — 2026-09-16 #003

**Mode**: systemic-fix
**Reason**: highest scheduler debt among eligible modes
**Backlog item**: `transitivity-review-queue` (reviews/transitivity/disagreements.jsonl)

## Pre-flight

No open routine PRs, no orphan `claude/*` branches. Session branch already up to
date with `main`.

## Work done

Worked `reviews/transitivity/disagreements.jsonl` top to bottom, 105 entries
(02673_kunren through 05319_jikaku). For each: read the entry's own examples,
judged the verb's real valency (a genuine を-marked patient vs. a に/と-marked
or objectless pattern, cross-checked against already-tagged precedent such as
電話/相談/連絡 = intransitive, 料理/掃除/我慢 = transitive), set
`tags.transitivity` (2 of the 105 came out `both`: 停止, 接続), and removed
each resolved line from `disagreements.jsonl`. No real 自他 pairs among this
batch, so no `prominent_see_also` links were added.

`scope_estimate` on the backlog item: 2369 → 2264.

## Mechanical pass (§3)

- `normalize_notes.py`: 0 rewrites (pre-existing non-canonical headers in these
  entries are unrelated to this run and were left alone).
- `auto_link.py`: 7 links added across 4 entries.
- `harvest_crossrefs.py`: 0 new cross-references (existing coverage already
  complete for these entries).
- `validate.py --changed-only --ratchet`: 105/105 valid.

## Self-check (§4)

`review_accuracy.py` on all 105 changed entries: 8 flagged, 0 filtered as
noise beyond the usual breadth/absent-tag/register-noquote families
(~$0.05). Adjudicated:

- **Applied (8)**: 03203_shakkin (semantic tag `commerce`→`finance`,
  off-vocab; notes overstated 借金 as informal-only, example 3 shows business
  debt), 04109_ousetsu (translation implied the furniture set *was* the room),
  04563_saiyou (gloss missing the "adoption" sense already in definition 2),
  04653_iiwake (semantic tag `geography` was nonsensical for "excuse" →
  `expression`), 04775_yuushi (notes conflated "lending" with "financing"),
  04776_hensai (formality tag `neutral` contradicted the entry's own
  "REGISTER: Formal/business vocabulary" line), 04778_gappei (etymology gloss
  for 併 corrected from "merge" to "combine, annex").
- **Rejected (1)**: 05150_enzan — reviewer wanted to add a `mathematics`
  semantic tag; not in the closed vocabulary, so rejected.
- **Flagged to curator (0)**.

`review_links.py` kana-link check on the same 105 entries: 5 flagged.
Adjudicated:
- **Retargeted (2)**: 05141_ryuunen's ために and 04293_suitei's だろう were
  linked to the plain-morpheme entries ため/だ; both have their own dedicated
  entries (28332_tameni, 09576_darou) and were repointed there.
- **Unlinked (1)**: 02673_kunren's で (in 技術だけでなく) had been linked to
  the copula だ; it belongs to the set phrase だけでなく (own entry
  18373_dakedenaku), not the bare copula, so unlinked.
- **Kept (2)**: 04729_kensaku's かける (検索をかける) is the right lexeme but
  a sense the target entry (00854_kakeru) doesn't cover — logged to
  `needs_curator.txt` instead of unlinking. 04789_teigi's よって (人によって
  違う) is a standard te-form use of よる — kept.

Ledger: self-check accuracy review ~$0.051, link review ~$0.006. Daily spend
now $1.02 of $5.00.

## Metrics

`entries_changed=105, flags_applied=8, flags_rejected=1, flags_to_curator=0`
(see `pipeline/metrics-history.jsonl`).

## Curator notes

One line added to `reviews/needs_curator.txt`: 00854_kakeru (かける) is
missing the "run a search" sense (検索をかける) used in 04729_kensaku's
notes.

## Cursor / backlog state

`transitivity-review-queue.scope_estimate`: 2369 → 2264 (2264 lines remain in
`reviews/transitivity/disagreements.jsonl`). Next run should continue from
the top of the file (05320_... onward).
