# Routine session 2026-09-22_001

**Mode**: systemic-fix — `transitivity-review-queue`
**Reason**: highest scheduler debt among eligible modes (selector output)
**Signals**: candidate_count 164, seen_in_entry_count 5, max_entry_id 31013, comprehensive_next 8116,
cross_model_review_next 9001, unharvested_observations 38, open_backlog_items 32.

## Pre-flight

No open `claude/*` PRs, no stray branches. Nothing to rescue or absorb.

## What this run did

Worked `reviews/transitivity/disagreements.jsonl` top to bottom, 100 lines (entries 25362–30600, the
continuation point left by the 2026-09-21 routine). For each: read the entry's own gloss and example
sentences, judged valency from an unambiguous external argument in the entry's own text where one
appeared, falling back to standard Japanese usage knowledge otherwise. Set `tags.transitivity`
(transitive/intransitive/both) and removed the line from `disagreements.jsonl`.

Patterns followed (same as recent runs): compound nouns whose own noun already names the patient
(報連相, 屈伸, 開店準備, 予約確認, 予約変更, 発声練習, 誘客, 吸血, 現状報告, 意思決定, 重ね着,
身辺整理, 打刻, 二足歩行, アイロンがけ, 店舗展開, and others) were tagged intransitive — no further
external を argument exists beyond what the compound names. に/へ-marked-target verbs (接近する,
抗議する, 感染する, 物申す, 噛みつく, 適応する, 対戦する, 服す, 通電する, 追いすがる, and others)
were tagged intransitive per the 対処する/出る precedent. Passive evidence in the entry's own examples
(Xが/はXXされる) was read as implying a transitive active verb for 分泌, 再刊, ドラマ化, 免官,
再評価, 干拓, 刻印, 商品化, 製品化, 脅迫. Two verbs (討論する, 開始する) got `both`: both models
agreed neither is intransitive-only, and their own usage patterns support both an object-less
reciprocal/change-of-state use and a transitive use. `マイニング` also got `both` (both "マイニング
する" as a bare activity and "暗号資産をマイニングする" with an external object are natural).

No real 自他 pairs surfaced (all `pairs` hints in this batch's disagreement lines were empty `{}`,
mostly noun+suru compounds), so no `prominent_see_also` links were added.

`scope_estimate` in `planning/wiki/ideas/backlog-queue.json`: 1484 → 1384.

## Mechanical pass (§3)

- `normalize_notes.py --apply`: 0 rewritten; 6 non-canonical header lines found (pre-existing,
  unrelated to this run's transitivity edits — not touched, out of scope for this backlog item).
- `auto_link.py --apply --confirm-real-entries`: 86 links added across 32 entries.
- `harvest_crossrefs.py --apply`: 0 entries rewritten (242 bullets parsed, all already had
  cross-references or had no matching entry).
- `validate.py --id` on each of the 100 changed entries: all valid.

## Self-check (§4)

`review_accuracy.py` over the 100 changed entries: 3 flagged (~$0.05). All three adjudicated REJECT
after independent verification:
- 25511_妄信する: reviewer claimed 盲信する "is not a standard word" — it has its own entry
  (09239_moushin); the note is correct.
- 25596_予約確認: reviewer's suggested translation was not an improvement over the existing one and
  its stated reasoning was self-contradictory.
- 28986_干拓: reviewer said the "(by draining)" qualifier was misleading, but the entry's own
  definition correctly distinguishes 干拓 (reclamation by draining) from 埋め立て (reclamation by
  filling) — a real technical distinction, not an error.

`review_links.py` over the same entries: 3 flags, all on 28529_airongake's かける→00854_kakeru links
(アイロンをかける, がけ). Same lexeme (掛ける), but 00854 (core tier, closed) only lists 3 senses
(hang / phone call / put on glasses) and doesn't cover "to apply/run over." Per §C, kept all three
links (right word, entry doesn't cover the sense) and logged the sense gap to
`reviews/needs_curator.txt` rather than unlinking, since the core tier is closed and adding the sense
needs a curator call.

## Ledger / metrics

OpenRouter spend today: $0.0527 (self-check $0.0487 + link-check $0.004), well under the $5 daily cap.
Metrics snapshot: entries_changed 100, flags_applied 0, flags_rejected 3, flags_to_curator 0,
entries_total 30804, candidates 164.

## Cursor

`transitivity-review-queue.scope_estimate`: 1484 → 1384 in `planning/wiki/ideas/backlog-queue.json`
(notes field carries the full per-run history, updated with this run's entry range and patterns).

## Needs curator

One item logged to `reviews/needs_curator.txt`: 00854_kakeru (core, closed) is missing the
"apply/run something over something" sense of 掛ける, surfaced by 28529_airongake's links. The links
are correct and were kept; adding the sense to a closed-tier entry needs a curator decision.
