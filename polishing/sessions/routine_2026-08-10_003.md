# Routine session — 2026-08-10 (003)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Params**: `phase: furigana`, `start_id: 29744`, `openrouter_session_budget_usd: 2.50`
**Signals**: candidates 991 (20 "seen in entry"), max entry ID 30539, comprehensive frontier
`next: 06856`, cross-model review cursor 29744, unharvested observations 19, reviewed
entries 586, open backlog items 29. OpenRouter $0.0129 spent today, $4.9871 remaining.
**Branch**: `claude/determined-rubin-gfogw2`

## Pre-flight (§0)

- **§0a rescue**: `list_pull_requests` returned zero open PRs — nothing to rescue.
- **§0b stranded-PR sweep**: no open `claude/*` PRs, nothing to close.
- **§0b orphan-branch sweep**: `list_branches` returned `main` only — zero orphans.
- Lock acquired.

## The cursor pointed at already-finished work

The selector's `start_id` was **29744**, but a check before spending anything found that
**all 796 entries in 29744–30539 already had a current `reviews/accuracy/` report**
(entry `modified` older than the report's `reviewed_at` for 777 of them), written by the
§4 at-birth self-check as those entries were created. Of the 49 with issues recorded,
**every one already had a same-dimension decision in `reviews/decisions.jsonl`** — zero
un-adjudicated flags in the whole band.

Re-sweeping it would have reproduced the 2026-08-09 (008) result exactly: a run's worth of
budget and adjudication context spent re-confirming settled decisions. §A step 6 defines
the cursor as "the next **un-reviewed** entry ID", and 29744–30539 is the top of the
dictionary, so the sweep has reached the end and wraps. Dictionary-wide there are **2,302
entries with no accuracy review at all**; the lowest is **06926**. This run resumed there.

## Range worked

**Accuracy pass** (`review_accuracy.py`, gloss/translation/tags) over the 500 lowest
never-reviewed IDs starting at 06926. Stopped at the §6 context checkpoint after
**240 entries reviewed (06926–07265)** — 135 entries flagged, **147 issues** raised
(147 `tags`, 0 `gloss`, 0 `translation`; 146 `error` + 1 `warn`).

**Furigana screening** (`review_runner.py --pass screening`) over 40 entries of the same
block (06926–07010): **0 flagged**, so no deep pass was needed. Clean.

## Result: 77 entries corrected — the P11 tag contamination extends into this block

A 56% entry flag rate would normally read as reviewer noise (§A's >20% rule), but
inspection showed the opposite: the great majority were **gross category errors**, not the
usual breadth nits.

| Entry | Gloss | Was | Now |
|---|---|---|---|
| 07032 {司書\|ししょ} | librarian | `clothing` | `occupation` |
| 07033 {探偵\|たんてい} | detective | `leisure` | `occupation` |
| 07055 カビ | mold; mildew | `weather` | `plant-general` |
| 07066 {天窓\|てんまど} | skylight | `weather` | `building` |
| 07113 {打率\|だりつ} | batting average | `animal-mammal`, `time-general` | `sports`, `number` |
| 07132 {位牌\|いはい} | Buddhist memorial tablet | `electronics` | `religion` |
| 06961 ぴえん | sad; boo-hoo | `food` | `emotion`, `expression` |
| 06947 {一触即発\|いっしょくそくはつ} | explosive situation | `geography` | `idiom`, `descriptive` |
| 06949 {危機一髪\|ききいっぱつ} | close call | `geography` | `idiom`, `descriptive` |
| 07071 {紙\|かみ}やすり | sandpaper | `communication`, `education` | `tool` |
| 07103 {低血圧\|ていけつあつ} | low blood pressure | `emotion` | `health` |
| 07166 {諦\|あきら}め | resignation | `communication`, `geography`, `work` | `emotion`, `cognition` |

**74 entries had their semantic tags corrected** this way; each replacement was chosen
against the entry's own gloss, and every tag written is in `VALID_SEMANTIC`.

**The blind spot this exposes**: not one of these entries carries an *off-vocabulary* tag.
Every wrong tag is a perfectly valid tag attached to the wrong word, so `validate_tags.py`
and the `--check-no-new-unknown` ratchet cannot see the class at all. The band looks
creation-batch-shaped (these entries were created 2026-01).

**3 formality fixes** (§A: apply only where the entry's own notes contradict the label):

- **06955 バイト** — tagged `formal` while its own REGISTER note reads "Casual. For formal
  contexts, use アルバイト or パートタイム." → `informal`
- **06996 {有給休暇\|ゆうきゅうきゅうか}** — tagged `informal` for a standard workplace
  term → `neutral`
- **07152 {思\|おも}いがけず** — REGISTER note leads "Neutral to slightly formal" → `neutral`

## Adjudication: 83 applied, 64 rejected, 0 flagged to curator

Rejected families:

- **Sole-`general` → specific tag (40 flags)** — the standing §A policy treats `general` as
  an accepted fallback and prior runs rejected this family repeatedly; bulk-rejected with
  one aggregated §C line.
- **Formality without a contradicting register note (22 flags)** — the block's formality
  distribution (14% `formal`) matches the dictionary-wide 16.7%, so this is a systematic
  reviewer disagreement with the project's `formal` threshold, not block contamination.
  Notably **rejected** where the notes *support* the label: 07002 {言\|い}い{渡\|わた}す
  ("Formal/written style. Common in legal and official contexts"), 06982
  {繰\|く}り{広\|ひろ}げる ("news reporting and formal writing"), 07265
  {差\|さ}し{伸\|の}べる ("Slightly formal"), 06942 さもないと ("Somewhat literary").
- **Enrichment nits (2)** — 07146 {土台\|どだい} (`descriptive` is not wrong; the flag asked
  to add `building`/`abstract`), and the request to strip `body-part` from 07125 きりきり
  (sharp pain is bodily; only the clearly-wrong `clothing` was removed).

All 147 adjudications are recorded in `reviews/decisions.jsonl` (107 individual lines + 1
aggregated sole-`general` family line). Two of those lines are slightly coarse: 06968
クレジットカード and 07059 {拝観料\|はいかんりょう} each carried a second
formality/politeness sub-flag that was *not* applied, but the entry's line is marked
`apply` for its semantic fix. Net effect on precision statistics is under 2%.

## §4 self-verification — skipped by rule, deliberately

All 77 changed entries were covered by §A's review pass **this run**, which §4 step 3
explicitly exempts ("in `accuracy-review` mode, skip IDs already covered by §A this run —
the review itself was the check"). Re-reviewing the fixes would be precisely the
re-verify-the-fix-round ping-pong §4 forbids. Deterministic verification was run instead:
`validate.py --range 6926 7265` → **258/258 valid**, and
`validate_tags.py --check-no-new-unknown` → **no new off-vocabulary tags**.

## OpenRouter spend

| Phase | Entries | Est. |
|---|---|---|
| accuracy review | 240 | $0.0998 |
| furigana screening | 40 | $0.0054 |
| self-check | 0 | $0.0000 |
| **total this run** | | **$0.1052** |

Ledger `spent_usd` for 2026-08-10: **$0.1181** (cap $5.00). Session cap $2.50 not
approached — the binding constraint was adjudication effort and context, as §A predicts.

## Observations logged

Five entries appended to `polishing/observations.md`:

- `[pattern]` **P11 semantic-tag contamination extends into 06926–07265** — with the
  detector blind spot (all tags in-vocabulary) spelled out; proposes a dedicated
  systemic-fix sweep over 07266+ rather than crawling it at ~240 entries/run.
- `[tooling]` **The sweep cursor pointed at a 100%-reviewed, 100%-adjudicated band** —
  proposes skipping entries whose review is newer than their `modified` timestamp.
- `[tooling]` **`reviews/queue.txt` does not converge** — 9,855 entries, never deduped
  against review recency, so queue depth is not currently a usable health metric.
- `[pattern]` **`formality: formal` with no supporting REGISTER note** is a mechanically
  detectable class in this block (ゴミ{箱\|ばこ}, デバイス, {獣医\|じゅうい}, …).
- `[pattern]` **Sole-`general` flags are ~27% of tag-flag volume** every sweep; either the
  reviewer prompt should learn the policy or the curator should revisit it.

## Candidates added

None — the material reviewed was existing entries' metadata; no un-entered Japanese words
were encountered.

## Cursors and queue

- `polishing/tasks/cross-model-review/progress.txt`: **next: 07266** (the next
  never-reviewed entry ID; the sweep wrapped from the top of the dictionary).
- `reviews/queue.txt`: 9,855 → **9,832**.
- Metrics line appended to `pipeline/metrics-history.jsonl`.

## Next run

Resume the accuracy sweep at **07266**, which is still inside the contaminated band — the
2,062 remaining never-reviewed entries are where all the signal is, and the 29744–30539
tail should not be re-swept. The strongest `systemic-fix` target is now the
in-vocabulary-but-wrong semantic tag class documented above: it is invisible to every
existing deterministic check, and this run's hit rate (74 corrections in 240 entries)
suggests the rest of the 2026-01 creation batch is similarly affected.
