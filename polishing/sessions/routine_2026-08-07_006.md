# Routine session — 2026-08-07 (006)

**Mode**: `accuracy-review` (selector: "highest scheduler debt among eligible modes")
**Branch**: `claude/determined-rubin-1nnj89`
**Params**: `phase: furigana`, `start_id: 27314`, `openrouter_session_budget_usd: 2.50`
**Signals**: candidates 993 (22 "seen in entry"), max entry ID 30473, comprehensive frontier 6820,
cross-model cursor 27314, unharvested observations 8, reviewed entries 586, open backlog items 21

## Pre-flight

- §0a rescue: no open PRs at all — nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs.
- §0b orphan-branch sweep: `list_branches` returned `main` only. Zero orphans.
- Lock acquired.

## Range worked

Targeted 27314–27850 (537 entries).

| Pass | Coverage | Result |
|---|---|---|
| `review_runner.py --pass screening` | 27314–**27645** (332 entries) | 29 flagged; killed at its own 50-min `timeout` before reaching 27850 |
| `review_accuracy.py` | 27314–**27850** (534 entries) | 221 flagged, 257 issues, $0.2344 |

The two passes ran concurrently. Per §A step 2's resilience clause the partial screening
results were kept rather than retried. **Cursor set to the range covered by *both* passes.**

The screening deep pass was **skipped** under §A step 2's known-noise shortcut: after
individually checking every one of the 29 screening flags, only 2 were real (both fixed below)
and the other 27 fell entirely inside the documented false-positive families.

## Changes applied (172 entries)

**Furigana (2, both genuine):**
- `27367` カスタム — `{通知|つうちょう}` → `{通知|つうち}` (つうちょう is 通帳)
- `27447` {弟|おとうと}さん — `{弟|おとうと}{さん|くん}` → `{弟|おとうと}くん`; the wrapper put ruby くん over the kana さん

**Gloss / translation (9 across 8 entries):**
- `27316` {通常|つうじょう} — a Korean string (`평상시`) had leaked into an English note → `{平常時|へいじょうじ}`
- `27365` スピン — main gloss promised a "spin (PR/political)" sense the entry never defines; removed
- `27564` {長財布|ながさいふ} — "bifold" contradicted the entry's own "without folding it"
- `27594` {南蛮漬|なんばんづ}け — main gloss omitted "fried" and vegetables; aligned with its own explanation
- `27796` {谷間|たにま} — dangling gloss "gap between" completed to "gap (between two tall things)"
- `27801` {貯|た}める — gloss said "(money)" where the explanation also covers points
- `27401` {許|ゆる}し — example translation "her father's blessing" → "permission" (obscured the sense)
- `27458` {上手|うわて}` — kamite/shimote restated audience-relative; the performer-relative English
  terms are genuinely reversible and the entry had them the other way round
- `27688` {渋|しぶ}{柿|がき} — "bitter" contradicted the headword's own astringency gloss

**Semantic tags (172 entries touched in total; 183 off-vocab instances migrated):**
- `check_tag_drift.TAG_MIGRATION` 1:1 map: 45 instances / 46 entries
- Reviewer's per-entry `suggestion` destination: 138 instances / 126 entries
- Off-vocab in range: **239 → 55 instances** (201 → 51 entries). Corpus baseline 2,530 → 2,252 entries.
- Two guards applied: refused all 13 migrations whose destination was `general` (lossy; would
  manufacture the sole-`general` defect), and overrode `drink`/`beverage` → `food` where the
  reviewer proposed `consumption` (a substance is not an act).
- `27564` `clothing` → `tool` (a wallet is not clothing), `27367` off-vocab `design` → `technology`

## §4 self-check

Ran `review_accuracy.py` on the 11 entries whose text I edited (`--budget 0.25`, $0.0049).
**No regressions from this run's edits.** 6 flags returned: 2 applied (the two tag fixes above),
4 rejected — one formality nit contradicted by the entry's own notes, one in-list tag judgment,
one wording nit on my own fix, and one conflating 貯める with 溜める. One fix round, no re-verify.

## Adjudication summary

- **Applied**: 151 (13 individual + 138 aggregated tag migrations)
- **Rejected**: 86 (13 individual + 73 aggregated across four noise families)
- **Flagged to curator**: 1 — `27564` 長財布 may take rendaku (ながざいふ). Correcting it would change
  the reading, romaji, filename and the live URL slug, so it is a curator decision, not a run's.

Notable rejections worth recording because the model was confidently wrong:
- `27402` {大尉|たいい} — model claimed 大尉 = "lieutenant commander". It is O-3 (army captain =
  navy lieutenant); lieutenant commander is 少佐. The entry was already right.
- `27333` スリーボール — model said a 3-1 count "is not simply three balls". It is.
- `27599` — `{二百五十千|にひゃくごじゅっせん}` is a deliberate contrast form in a note about
  ten-thousand grouping, not an error.

## Capture

- Candidates added: none — no un-entered Japanese words surfaced in the flags worked.
- Observations: 5 appended (2 `[pattern]`/`[tooling]` on the tag block and the flag-rate heuristic,
  1 `[tooling]` on screening throughput and parse failures, 2 `[entry]`).

## Spend

| Phase | Entries | Est. |
|---|---|---|
| furigana-screening | 332 | $0.0700 |
| accuracy | 534 | $0.2344 |
| self-check | 11 | $0.0049 |

Ledger for the day: **$0.4817** of the $5.00 cap. Session budget was $2.50; throughput, not
dollars, was the binding constraint (see the `[tooling]` observation).

## Next cursors

- `polishing/tasks/cross-model-review/progress.txt` → **`next: 27646`**
  (accuracy covered through 27850, but furigana screening only reached 27645; the cursor is
  deliberately set to the range covered by both, so 27646–27850 gets its furigana pass next run)
- `reviews/queue.txt`: 11,198 → 10,833 lines (27314–27645 removed)
