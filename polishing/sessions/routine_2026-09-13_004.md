# Routine session 2026-09-13 #004

**Mode**: polish (selector reason: "polish: highest scheduler debt among eligible modes")
**Params**: start_id 7318

## Pre-flight

No open PRs, no stray `claude/*` branches. Merged `origin/main` (already up to date).

## Priority lane

`polishing/priority/notes.txt` is 14 days old (regenerated 2026-09-12), so no regeneration was due.
Started at the persisted cursor (line 671). Nearly the whole file turned out to be inside the
30-day "recently modified" skip window — a systemic pass touched most of the dictionary between
2026-09-02 and 2026-09-11 — so scanning 1,282 consecutive lines yielded only 15 eligible entries.
Logged as a `[tooling]` observation.

Of the 15 entries read (all validated clean before any edit), 9 needed no change. The other 6 all
turned out to carry hand-written `noentry` inline-link markers left over from before that rule was
enforced:

- **02874_gyuuniku**: `神戸牛`/`松阪牛` noentry markers → plain furigana text; both queued as
  candidates (C23421, C23422).
- **02917_tooka**: `ぶり` (duration suffix) noentry marker, two occurrences → plain text; queued as
  candidate (C23423).
- **04291_ari**: `一穴` (from 蟻の一穴 proverb) noentry marker → plain furigana text; queued as
  candidate (C23424).
- **04918_mirin**: `風` (ふう, "-style" suffix) noentry marker → this word already has an entry
  (30892_fuu) that postdates the note, so linked directly instead of queuing a candidate.
- **00152_kako**: `形` (けい, grammar "-form" suffix, as in 過去形) noentry marker → plain text;
  queued as candidate (C23425).
- **06125_ochuugen**: `中元` (the underlying Taoist festival term) noentry marker → plain text;
  queued as candidate (C23426).

A repo-wide grep afterward found **2,111** entry files still carrying `：noentry⟧` markers — this
is a large, dictionary-wide backlog, not a one-off. Logged as a `[tooling]`/`[entry]` observation
recommending a dedicated `systemic-fix` backlog item.

Cursor advanced: `priority-cursor.txt` → line 1953.

## Frontier lane

Read entries 07318 through 07339 (22 entries, contiguous IDs, all files present). All were already
in good shape from the same recent systemic pass — correct glosses, complete conjugation tables
where needed, populated notes with canonical section headers, sensible cross-references and
`prominent_see_also`. No edits needed.

Cursor advanced: `progress.txt` → next: 07340.

## Mechanical pass (changed entries: 00152, 02874, 02917, 04291, 04918, 06125)

- `normalize_notes.py --apply`: 0 rewritten (11 non-canonical headers detected but pre-existing,
  not touched by this run's edits).
- `auto_link.py --apply --confirm-real-entries`: 0 new links (all remaining unlinked tokens are
  genuinely no-entry, now queued as candidates).
- `harvest_crossrefs.py --apply`: 0 new cross-references.
- `validate.py --id` on each of the 6: all valid.

## Self-check

- `review_accuracy.py` on the 6 changed entries: 0 issues, 0 filtered. Clean.
- `review_links.py --skip-decided` on the same 6: 12 links judged, 0 flagged. Clean.
- Ledger: +$0.0029 (self-check phase); today's total $0.5074 of $5.00 cap.

## Metrics

`metrics_snapshot.py --mode polish --changed 6`: entries_total 30683, candidates 211 (was 205 at
session start; +6 net after 0 auto-resolved), review_queue_len 10638.

## Candidates added

C23421 神戸牛, C23422 松阪牛, C23423 ぶり (duration suffix), C23424 一穴, C23425 形 (けい,
grammar suffix), C23426 中元.

## Observations logged

Two `[tooling]`/`[entry]` observations in `polishing/observations.md`: the stale priority-file
skip-window issue, and the dictionary-wide `noentry`-marker backlog (2,111 files).

## make index

30683/30683 entries valid (pre-existing warnings unchanged). Articles: 10/10 ok. Indexes, word-ID
lookup, and kanji index (2,801 files) all regenerated cleanly.

## For the curator

- The `：noentry⟧` backlog (2,111 files) is worth a dedicated systemic-fix pass — it's larger than
  any of today's other backlog items and mechanically detectable.
- Nothing else needs attention; self-check was clean and no flags were raised.
