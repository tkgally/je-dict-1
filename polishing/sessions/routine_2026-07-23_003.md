# Routine session — 2026-07-23 (003)

- **Mode**: accuracy-review (selector: "highest scheduler debt among eligible modes")
- **Params**: phase=furigana, start_id=17203, openrouter_session_budget_usd=2.5
- **Range worked**: 17203–17350 (128 entries with files)

## Pre-flight
- §0a rescue: no open `claude/*` routine PRs — nothing to rescue.
- §0b sweep: no open PRs to sweep. Lock acquired for `claude/modest-keller-92rf28`.

## Work done

### Cross-model accuracy review (glosses / translations / semantic tags)
- Ran `review_accuracy.py --range 17203 17350` (128 reviewed, 36 flagged, est $0.056).
- **Applied 30 tag/content fixes:**
  - **Off-vocab semantic-tag migrations (correct-by-definition, APPLY)** — 28 entries: `time→time-period` (17203), `time→time-general` (17204), remove invalid `thinking` (17205), remove invalid `decision` (17209), `learning→cognition` (17210), remove invalid `body` (17214), `relationships→society` (17218), `time→cognition` (17245), `people→person` (17252); and warn-severity: `space`→(drop) (17206), `behavior→action` (17207), `news`→(drop) (17208), `vehicle/transport→transportation` (17213), `cleaning`→(drop) (17215), `manufacturing`→(drop) (17217), `life-stage`→(drop) (17219), `documents`→(drop) (17222), `energy/infrastructure→technology` (17225), `writing/calligraphy→communication,art` (17226), `academic→science` (17227), `martial-arts→sports` (17228), `hospitality→entertainment` (17230), `position→direction` (17232, 17236), `place/house→building` (17243, 17258), `sensation`→(drop) (17244), `time-frequency→time-general` (17266).
  - **Gloss** (17301 落葉): removed misleading adjective "deciduous" from a noun/suru-verb gloss → "fallen leaves; leaf fall; shedding of leaves".
  - **Translation** (17324 陰りが見える, ex2): "A shadow appeared in his smile" (over-literal) → "His smile showed a hint of sadness."
- **Rejected 8 error-severity flags:** formality flags where the entry's own notes affirm the label (17222, 17276, 17301, 17332); `general→X` narrowing between in-list tags (17290, 17349, 17350); and a false "suru-verbs must carry action" convention (17347).

### Furigana screening
- `review_runner.py --pass screening` covered 17203–~17311 (stopped early per §6 time discipline; the pass runs slowly). 11 flags, **all false positives** within documented families (screener truncation dropping trailing mora, rendaku か→が in 縦書き/横書き, word-formation and okurigana-split displays like `{肩書|かたが}き`/`{適齢|てきれい}{期|き}`, and index-confusion hallucinations e.g. 心臓→よ, 落葉→お). Bulk-rejected with one aggregated ledger line; deep pass skipped (§A known-noise shortcut). **0 real furigana errors.**

## §4 self-verification
- Skipped re-send: accuracy-review mode — every changed entry was itself the subject of the §A cross-model review this run (the review *is* the check). All 30 changed entries pass `validate.py` and carry no unknown semantic tags (`validate_tags.py`).

## Ledger / metrics
- OpenRouter est spend this run ≈ $0.067 (accuracy + partial screening). Ledger `spent_usd` → $0.0735 for the day.
- Metrics line appended: 30 applied / 19 rejected / 0 to curator.

## Always-on capture
- `[pattern]` observation logged: dense off-vocab semantic tags across ~17203–17301 → good candidate for a `check_tag_drift.py` systemic-fix sweep.
- No missing-word candidates surfaced in this mode.

## Cursors / next
- `cross-model-review/progress.txt` → **next: 17311** (accuracy covered the full 17203–17350; furigana screening reached ~17311, so next run resumes there to close the furigana gap).
- `reviews/queue.txt`: removed 17203–17350 (85 lines).
