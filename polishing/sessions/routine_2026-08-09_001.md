# Routine session — polish

Date: 2026-08-09
Mode: `polish` (selector reason: "polish: highest scheduler debt among eligible modes")
Selector signals: candidates 983 (12 "seen in entry"), max entry ID 30511,
comprehensive_next 6842, cross-model-review next 28901, unharvested observations 6,
open backlog items 27, OpenRouter remaining $5.00.

Pre-flight: no open PRs (nothing to rescue, nothing to sweep); `list_branches` showed
only `main`, so zero orphan branches. Lock acquired.

## Priority lane (`polishing/priority/notes.txt`, lines 284–295 → next 296)

Priority file is 4 days old, so the lane ran. Four of the twelve lines in range were
skipped as modified within 30 days (03027, 04900, 06485, 00478). All 8 processed
entries needed changes, so the priority rankings are **not** stale — no regeneration.

- **03743 味方**: particle error throughout. The entry taught を as the object particle
  for 味方する; the standard particle is に. Fixed ex8, ex9, ex10 and rewrote the USAGE
  section, adding the 〜の味方をする noun pattern and explaining that を belongs to the
  separate 味方につける idiom (linked 00761_tsukeru, 16145_yoron).
- **04729 検索**: `formality: informal` → `neutral` (a technical/neutral term, not slang);
  added `technology` to semantic tags. Candidates added for the two `noentry` words.
- **05834 しょんぼり**: malformed furigana wrapper `{うつむ|うつむ}` (hiragana wrapped with
  itself) → plain `うつむ`; stale `noentry` on しょげる → 29029_shogeru; semantic tags
  `action/descriptive` → `onomatopoeia/descriptive/emotion`, matching sibling mimetic
  05893_gakkuri; ex2 English no longer invents a "you" subject.
- **00492 思う** (basic): sense 2 had only 4 examples against the 5-per-sense basic-tier
  minimum. Added ex16 (母のことを思って、手紙を書きました) with full link coverage.
- **01099 答える** (basic): removed the 電話が鳴ったけど誰も答えなかった example and the
  "電話に答える (answer the phone - less common)" note line — answering a phone is
  電話に出る, never 答える. Replaced with a natural 名前を聞かれて、すぐに答えた example,
  added the 〜と答える pattern, added an explicit NOT USED FOR PHONES note, and added a
  `related` cross-ref to 00790_kotae (+ back-link on 00790).
- **01282 赤ちゃん**: inline link `つける→付ける` pointed at 00562_tsukeru (headword つける)
  instead of 00761_tsukeru (headword 付ける); corrected to match the written base form.
- **01910 起こる** (basic): stale `noentry` on 勃発する → 11823_boppatsu; added the missing
  `pair` cross-reference to 01234_okosu (+ `pair` back-link on 01234, which previously
  had only a prominent_see_also).
- **02980 やる** (basic): sense 2 is defined as giving to "animals, plants, or those of
  lower status" but all five sense-2 examples were animals/plants. Replaced the third
  feeding example (金魚) with 弟に古い本をやった, and added a register warning that sense 2
  sounds blunt with people plus the 小遣いをやる collocation (12363_kozukai).

## Frontier lane (from 06842 → next 06844)

- **06842 叩き落とす**: full inline-link coverage added to all 6 examples and the whole
  notes block (previously zero links). Added a SENSE 2 REGISTER paragraph noting the
  price sense is pushy haggling vocabulary, with 値切る / 値下げを求める as neutral
  alternatives.
- **06843 這い出る**: full inline-link coverage on all 4 examples and the notes.
  Corrected a factual error — the notes claimed "Transitive pair: 這い出す (to cause to
  crawl out)"; 17010_haidasu is itself intransitive and glosses "to crawl out". Replaced
  with a NEAR-SYNONYM section distinguishing the two, added a `synonym` cross-ref, a
  FIGURATIVE USE section (貧困から這い出る), and "No transitive pair."

Stopped the frontier at 06843: 06844 切り崩す has 15 examples and a full notes block with
zero inline links, which does not fit the remaining budget. The cursor is parked at 06844
and it is logged as an `[entry]` observation so the next polish run can spend its frontier
budget on that single entry.

## §4 self-check

`review_accuracy.py` over exactly the 12 changed IDs (~$0.0055, ledger updated).
**11 of 12 clean; 1 flag, rejected.**

- 02980 `tags.formality` (error): model argued やる is `neutral`, not `informal`.
  REJECTED — §A's formality rule applies a flag only when the entry's own register
  description contradicts the label, and here the notes explicitly support `informal`
  ("やる (sense 1) is informal. In formal contexts, use する instead."). Logged to
  `reviews/decisions.jsonl`.

Notably, the model raised nothing on the two prose-particle corrections (03743, 01099),
which were this run's highest-value fixes.

## Candidates added

- ボックス (ぼっくす) — box; seen in 04729
- ワード (わーど) — word/search term; seen in 04729
- 瓦礫 (がれき) — rubble, debris; seen in 06843
- 言い値 (いいね) — asking price; seen in 06842

(しょげる was *not* added — the check revealed it already had entry 29029, which is how
the stale `noentry` was caught.)

## Observations logged

- `[entry]` 06844 parked with zero inline links, 15 examples
- `[pattern]` two stale `noentry` markers in one 8-entry priority batch — the priority
  lane reaches entries the frontier cannot revisit, so P35 residue is likely larger than
  the frontier's own rate suggests
- `[pattern]` the 2026-01-18 compound-verb creation block (06842–06844) has zero inline
  links at all; a cheap "examples containing no ⟦⟧" detector would size the block exactly
- `[pattern]` particle errors in *notes* outlive those in examples because nothing
  validates prose claims; cross-model accuracy review is the only instrument that catches
  them

## Cursors

- `polishing/tasks/comprehensive/progress.txt` → `next: 06844`
- `polishing/tasks/comprehensive/priority-cursor.txt` → `line: 296`
