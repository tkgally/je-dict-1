# Routine v2 session — 2026-07-26 (006)

**Mode:** `polish` (selector reason: "polish: highest scheduler debt among eligible modes")
**Selector signals:** candidates 1040 (9 "seen in entry"), max entry ID 30144, comprehensive_next 6645,
cross-model-review next 19179, 27 unharvested observations, 586 reviewed entries, 7 open backlog items.
OpenRouter: $0.4444 spent today, $4.5556 remaining.

## Pre-flight

- §0a: `mcp__github__list_pull_requests` returned **no open PRs** — nothing to rescue.
- §0b: no `claude/*` strands to sweep. Lock acquired for `claude/determined-rubin-v1z3u6`;
  branch level with `origin/main` (d976213e1).

## Priority lane (notes.txt, lines 67–78; file generated 2026-07-24, fresh)

Five of the twelve lines in range were eligible; 04376, 04767, 05766, 05775, 02355, 02900, 03423
were skipped as modified within the last 30 days. **All five needed real changes**, so the
rankings are not stale — priorities were not regenerated and the cursor advances normally.

- **05889 ぱちぱち**: the two `noentry` markers in RELATED WORDS now resolve (ぱちん → `29036`,
  バチバチ → `29120`); replaced the thin GRAMMAR paragraph with a COMMON PATTERNS section
  (と-adverbial / する / 目をぱちぱちさせる) plus the katakana-spelling note; added `onomatopoeia`
  to the semantic tags; added three cross-references (29036, 29120, 06485 瞬き) with back-links
  on all three.
- **05933 稲刈り**: added a COMMON PATTERNS section; linked the previously naked うちに (`28396`)
  in ex5 and split its `終わらせなければならない` link into 終わる + なければならない (`27600`);
  added cross-references to 田植え (`22734`, contrast), 収穫 (`03207`), 稲 (`01820`) — all three
  had empty `cross_references` and got back-links.
- **00765 易しい**: added a USAGE section (predicate vs. modifier, the やさしく adverbial being
  restricted to the "kind" sense, に marking the person treated kindly).
- **02841 青**: added a USAGE section distinguishing the noun 青 from 青い + の-modification, and
  the traffic-signal 青になる pattern; added two examples that actually use the noun headword
  (the sense-1 examples were nearly all 青い).
- **02842 赤**: same USAGE treatment (including 赤の他人 as the "plain, utter" sense rather than
  the color); added two noun examples.

Note-quality scores rose 50 → 80 on all five. The missing 5 points in every case are the
known scorer bug (see observations).

## Frontier lane (06645 → 06649)

The same zero-inline-link creation batch as 06631–06644: every entry had links in neither
examples nor notes, and all used `・` bullets instead of the house `- `.

- **06645 口下手**: full link coverage across 3 examples and the whole notes field; 弁が立つ
  turned out to have an entry (`20076`) so the CONTRAST line links rather than spells it out;
  added antonym cross-references to 話し上手 (`08467`) and 弁が立つ, both back-linked.
- **06646 せっかち**: full link coverage; removed the wrong `style: ["literary"]` tag and the
  dubious せっかちを起こす collocation; rewrote ETYMOLOGY around 急く (marked `noentry`, added as
  a candidate); added cross-references to のんびり (`01596`), 気長 (`20721`), そそっかしい
  (`06647`), all back-linked.
- **06647 そそっかしい**: full link coverage; `formality` corrected `formal` → `neutral`;
  reorganised notes into FORMS / USAGE / NUANCE / CONTRAST / RELATED WORDS; added
  cross-references to 慎重 (`03264`), 軽率 (`06866`), せっかち, with back-links.
- **06648 悔い**: full link coverage across 5 examples and notes; expanded USAGE to contrast
  悔い with the everyday 後悔する; added cross-references to 悔いる (`25247`) and 悔やむ
  (`14725`), both back-linked; 出し切る marked `noentry` and added as a candidate.
- **06649 情け**: full link coverage including the proverb and the classical-poetry example;
  added the condescension nuance of 情けをかける; added cross-references to 情け深い (`16074`),
  情けない (`03129`), 人情 (`11437`), all back-linked.

All inline-link targets in every changed file were verified to resolve against the entry
filesystem — two wrong IDs written during this run (`01188_nokosu`, `00939_made`) passed
`validate.py` and were caught only by that check (standing `[tooling]` observation).

## §4 self-check (cross-model, `review_accuracy.py`, 27 entries, $0.0120)

19 of 27 entries came back clean. Ten flags adjudicated: **7 applied, 3 rejected, 0 escalated.**

- APPLY 01820 gloss ×2 — 稲 was glossed "rice plant, paddy" / "rice paddy, rice crop"; 稲 is the
  plant and the standing crop, never the field (that is 田/田んぼ). Now "rice plant, rice crop"
  and "rice crop (the standing rice of a field)".
- APPLY 01820 translation — ex11 "the rice paddies shine golden" → "the rice plants shine golden".
- APPLY 02842 translation — removed ex4 (赤ちゃんが泣いている): it illustrates a derived word,
  not the color headword. 赤ちゃん remains in COMMON EXPRESSIONS and as a cross-reference; the
  entry still has 6 examples, above the basic-tier minimum of 5.
- APPLY 05889 translation — ex11 "loud applause was sent from the audience" was a literal
  rendering of 送られた; now "loud applause came from the audience".
- APPLY 20076 tags — `ability` is not in `VALID_SEMANTIC`; removed (`communication` covers it).
- APPLY 20721 tags — `time` is not in `VALID_SEMANTIC`; removed rather than migrated to
  `time-general`, since 気長 is a temperament, not a time word (`personality` retained).
- APPLY 22734 tags — `agriculture` is not in `VALID_SEMANTIC`; migrated to `work` (`culture` kept).
- REJECT 03207 tags — model wanted `action`/`cognition`/`general` added to cover 収穫's
  figurative sense; in-list breadth nit, `nature` is defensible.
- REJECT 05889 translation — "Half-asleep, I'm blinking my eyes." is a fine rendering of
  寝起きで目をぱちぱちさせている.
- REJECT 06648 tags — model wanted `formality` `formal` → `neutral`; the entry's own notes
  describe 悔い as more literary than 後悔, so the label matches the entry's register description.

Ledger: $0.0120 recorded with `phase: "self-check"`; daily total $0.4564 of $5.00.

## §3 always-on capture

**2 candidates added**: {急|せ}く (せく — the older verb behind せっかち, seen in 06646),
{出|だ}し{切|き}る (だしきる, seen in 06648).

**6 observations logged** (see `polishing/observations.md`): the notes-scorer furigana bug,
the productive priority lane, Cleanup P21 band continuing through 06649, Cleanup P17 register
tags in the same band, the three-way やさしい consolidation question, and the standing
`validate.py` link-resolution gap.

## Totals

- **10 entries polished** (5 priority + 5 frontier), **17 further entries touched** for
  back-links or self-check corrections — 27 entry files changed.
- Full validation: 29,935/29,935 valid; `validate_tags.py --check-no-new-unknown` clean.

## Cursors / state

- `polishing/tasks/comprehensive/progress.txt` → `next: 06650`
- `polishing/tasks/comprehensive/priority-cursor.txt` → `line: 79`
- Selector state advanced in §1; metrics line appended in §5.
