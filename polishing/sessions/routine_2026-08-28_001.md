# Routine session — 2026-08-28 (001)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Params**: `phase: furigana`, `start_id: 13900`, `openrouter_session_budget_usd: 2.50`
**Signals**: candidates 157, seen-in-entry 19, max entry ID 30753, comprehensive frontier 7043,
cross-model cursor 13900, unharvested observations 14, open backlog items 41

## Pre-flight

- §0a rescue: no open PRs — nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs.
- §0b orphan-branch sweep: `list_branches` returned `main` only — zero orphans.
- Lock acquired on `claude/intelligent-curie-wosxt0`.

## Range worked

Screened **13900–14399** (500 entries) for furigana; accuracy-reviewed **13900–14329**
(430 entries) for glosses, example translations, and semantic tags.

The accuracy pass was stopped deliberately at 14330. It ran at ~7 entries/min for the first
~300 entries, then degraded to ~1.4/min (apparently upstream rate limiting) and stalled at
14330 for roughly ten minutes. Rather than hold the run open indefinitely, it was cut at that
clean boundary — 430 entries is inside §A's 400–600 target band — and the cursor set to 14330.
Entries 14330–14399 have screening results on disk but no accuracy review; the cursor sends the
next run back over them.

## Furigana screening — 42 flags, 0 applied

42 of 500 entries flagged (8.4%). **Every flag was rejected**, and the deep pass was skipped
under §A step 2's known-noise shortcut, saving the ~$0.80 deep budget.

| family | n | why rejected |
|---|---|---|
| confabulated truncation | 25 | model claimed readings were "cut off" (`衛生→えいせ`, `被害者→ひがいし`, `談笑→だんし`); the entries hold the full correct readings |
| okurigana splits | 10 | partial readings are correct by design |
| rendaku / compound vs standalone | 4 | documented false-positive family |
| reading the entry itself discusses | 2 | 13942 contrasts 脅す; 14186 discusses the on-reading えい |
| unparseable model response | 1 | tooling noise, not an adjudication |

The dominant family was verified against ground truth: grepping every flagged compound out of
`entries/13500` and `entries/14000` returned the full readings in every case
(`{衛生|えいせい}`, `{被害者|ひがいしゃ}`, `{美少女|びしょうじょ}`, …).

Importantly this is **not** the `trim_context()` display-truncation bug that was fixed earlier.
Rebuilding the screening prompt for 14337 shows the context rendered intact —
`6. 変身 → へんしん  (followed by: 「する{美少女|びしょうじょ}」)` — and the prompt already carries
an explicit "Never infer that a reading is truncated or incomplete from that excerpt" line. The
model ignores it. Logged as a `[tooling]` observation.

## Accuracy review — 57 flags, 9 applied

Of 430 entries reviewed, 57 flagged (~13%), overwhelmingly on the `tags` dimension.

**Applied (9):**

| entry | headword | change |
|---|---|---|
| 13920 | {肥満|ひまん} | formality `formal` → `neutral` — the entry's own notes call it "a clinical, neutral term" |
| 13981 | {義弟|ぎてい} | example 1 English: "My wife's younger brother" → "My wife's younger brother-in-law" (it had rendered 義弟 as plain 弟) |
| 13983 | {純文学|じゅんぶんがく} | semantic `education` → `art` — literary fiction is not an education word |
| 14009 | {花道|はなみち} | example 4 English: "He adorned his final stage with a retirement match" → "He made a glorious exit in his retirement match" (the で phrase was inverted) |
| 14072 | {老年|ろうねん} | semantic dropped `person` — the entry defines it as a period of life |
| 14118 | {薪|まき} | semantic `tool` → `daily-life` — firewood is not a tool |
| 14152 | {英国|えいこく} | sense 1 gloss dropped "England" — the entry's own explanation defines it as the UK |
| 14209 | {誘惑|ゆうわく} | semantic `emotion` → `action` — both senses define it as an act |
| 14216 | {読|よ}み{物|もの} | semantic `education` → `media` — the entry explicitly contrasts itself with study materials |

**Rejected (48)**, in three families plus singletons:

- **62 in-list breadth/narrowness nits** across both halves of the range (37 + 25) — "the tag
  'general' is too vague, replace it with X" and its mirror ("'food' is too narrow",
  "'body-part' is too specific"). §A step 4 directs rejecting substitutions between tags that
  are already in `VALID_SEMANTIC`. Several suggested destinations (`body-general`, `medical` as
  a semantic tag) are not in the vocabulary at all.
- **6 formality flags** (14174 詫びる, 14220 設ける, 14238 記す, 14241 見出す, 14254 諸々,
  14323 遂げる) all arguing "not formal → neutral" on a keigo-only reading of "formal". Every one
  of these entries self-describes as formal in its own notes ("more formal and literary than
  {書|か}く", "literary, formal tone"), so the §A test — apply only when the entry's own register
  description *contradicts* the label — fails in all six. 14091 街路 was the clearest case: the
  model quoted the entry's "More formal than {道|みち} or {通|とお}り" and then argued against it.
- Singletons: 13917 耐性 (notes don't contradict), 13930 背伸び ({棚|たな}の{上|うえ} is the top of
  the shelf; model misread), 13943 脇道 (sense 2 *is* a discourse sense, so `communication` fits),
  13978 自室 ("a slightly formal way"), 13981 gloss (義弟 *is* younger by definition — 義兄 is the
  older one, so the suggested "brother-in-law" would have been wrong), 14026 荒波 (additive
  breadth nit), 14029 蘇生 (breadth nit), 14044 装い ("literary/elegant" has no formality value —
  `formal` is the closest available), 14195 観賞 (the "(nature, beauty)" parenthetical is doing
  the disambiguation work against 鑑賞), 14229 豚汁 (reading nit; the reading field is already
  とんじる), 14235 賑わう and 14289 軒並み (lateral swaps, suggestions no better), 14306 購買
  (additive, not a wrong tag).

## §4 self-check — 8 of 9 clean, 1 flagged

Re-ran `review_accuracy.py --ids` over exactly the 9 changed entries ($0.004). Eight came back
clean, confirming the fixes. One flag on 13981, on the pre-existing "stepbrother" half of the
gloss rather than anything changed this run — a genuine kinship-scope question, escalated to
`reviews/needs_curator.txt` rather than settled unattended.

Result: **0 applied, 0 rejected, 1 flagged.**

## Spend

| phase | entries | est. |
|---|---|---|
| screening | 500 | $0.20 |
| accuracy | 430 | $0.19 |
| self-check | 9 | $0.004 |
| **total** | | **$0.394** of the $5.00 daily cap |

The screening figure is an estimate rounded up: the runner's stdout was lost when the call was
moved to the background, so the exact `Est. cost` line is unavailable. Priced from the measured
per-entry accuracy cost.

## Observations logged

- `[tooling]` furigana screening ran 0% precision over this range; the confabulated-truncation
  family is the model, not the prompt. Third consecutive measurement in the 0–5% band — worth
  costing out a different screening model, or retiring the pass over already-polished ranges.
- `[pattern]` the accuracy reviewer's `tags` dimension flags in-list breadth substitutions at
  high volume and near-zero yield (4 genuine category errors out of 62 tag flags). Teaching the
  prompt that `general` is a legitimate terminal tag would cut this dimension's noise ~90%.
- `[entry]` 13981's example 1 uses the awkward {妻|つま}の{義弟|ぎてい}.

## Candidates

None added — an accuracy review reads entries that are already written, and no example or note
in this range referenced a word lacking an entry.

## Cursors

- `polishing/tasks/cross-model-review/progress.txt` → `next: 14330`
- `reviews/queue.txt` trimmed of 13900–14329 (10,829 → 10,787 lines)
