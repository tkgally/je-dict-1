# Routine session — 2026-08-07 (003)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Params**: `phase: furigana`, `start_id: 27001`, `openrouter_session_budget_usd: 2.50`
**Signals**: candidates 985 (14 "seen in entry"), max entry ID 30473, comprehensive frontier 6813,
cross-model-review cursor 27001, unharvested observations 40, reviewed entries 586, open backlog items 20
**Branch**: `claude/determined-rubin-h9sqqz`

## Pre-flight

- §0a rescue: `mcp__github__list_pull_requests` → **0 open PRs**. Nothing to rescue.
- §0b sweep: no open `claude/*` PRs — nothing superseded to close.
- Lock acquired; branch level with `origin/main`.

## Range worked

**27001–27313** (313 entries reviewed by the accuracy pass).

| Pass | Coverage | Flagged | Est. cost |
|---|---|---|---|
| `review_accuracy.py` (gloss / translation / tags) | 313 | 83 issues | $0.1289 |
| `review_runner.py --pass screening` (furigana) | 14 (27001–27014) | 1 | $0.0014 |
| §4 self-check (`review_accuracy.py --ids`) | 52 | 9 entries / 10 issues | $0.0214 |
| **Total** | | | **$0.1657** |

The range was sized to 313 rather than the nominal 400–600 by **wall-clock, not dollars**: the
accuracy pass sustained ~4 entries/min, so 600 would have consumed the whole run. Spend used 6.6%
of the $2.50 session budget and 3.3% of the daily cap.

**Screening truncated at 27014 and the deep furigana pass skipped** under §A step 2's known-noise
shortcut — see the `[tooling]` observation below.

## Changes applied — 52 entries

### Gloss corrections (1 entry, APPLY)

| Entry | Change |
|---|---|
| 27179 可動式 | `movable type; adjustable style` → `movable; adjustable`; def0 `movable type; adjustable; mobile` → `movable; adjustable; mobile`. "Movable type" is the English printing term (活字); every one of the entry's own three examples renders 可動式 as "adjustable"/"movable". |

### Example-translation corrections (1 entry, 2 APPLY)

| Entry | Change |
|---|---|
| 27143 贈与 ex0 | "To donate property." → "To gift property." — "donate" implies charity; the entry's own notes define 贈与 as a legal transfer of assets. |
| 27143 ex2 | "The transfer of real estate…" → "The gifting of real estate…" — bare "transfer" dropped the gift aspect that distinguishes 贈与. |

### Semantic-tag corrections (50 entries, APPLY)

**Off-vocabulary tags migrated to `VALID_SEMANTIC`** (39) — flag correct by definition per §A step 4:

`objects`→`tool` (27013, 27035 as `money`+`tool`), `description`→`descriptive` (27022, 27123, 27179),
`people`→`person` (27025, 27117, 27138), `place(s)`→`building` (27034, 27118, 27120, 27130, 27134),
`relationship`→`action` (27036), `medical`→`health` (27038, 27043, 27056), `sound`→`onomatopoeia` (27067),
`construction`→`tool` (27068), `tools`→`tool` (27069), `fashion`→`appearance` (27075, 27079),
`service`→`occupation` (27111), `crafts`→`tool` (27135), `government`→`law` (27137),
`location`→`geography` (27180, 27239), `location`→`general` (27246), `infrastructure`→`transportation` (27201),
`conflict`→`action` (27205, 27206), `conflict`→`law` (27210), `psychology`→`emotion` (27231),
`information`→`cognition` (27233), `body`→`body-part` (27234, 27252 — later corrected, see §4),
`body`→`body-internal` (27259), `time`→`time-general` (27202, 27244), `time`→`descriptive` (27177),
`time`→`time-period` (27247), `food-drink`→`descriptive` (27274), `food-drink`→`food` (27277),
`house`→`building` (27276).

Two destinations were overridden against the reviewer's suggestion on semantic grounds:
27201 信号機 → `transportation` (a traffic light is not a building) and 27135 焼き印 → `tool`.

**In-list tags that were category errors, not narrowness** (5): 27108 心的外傷 `emotion`→`health`
(trauma is the injury, not the feeling), 27155 油彩 `leisure`→`art`, 27186 経済大国 `work`→`economics`,
27188 爆風 `weather`→`science` (a blast wave is not a weather phenomenon), 27291 ボーイフレンド dropped
`family`, 27306 開会式 dropped `education`. 27085 こつこつ gained `onomatopoeia` for its documented
second (tapping-sound) sense.

## Rejections — 32 flags

- **`general` is "too broad"** (9: 27047, 27049, 27050, 27052, 27087, 27097, 27100, 27110, 27143,
  27147, 27148, 27154, 27225) — substitutions between in-list tags, rejected per the §A step 4
  policy that usage breadth is not the standard.
- **`formality: formal` → `neutral`** (6: 27098, 27170, 27194, 27208, 27232, 27202) — in five of the
  six the entry's own notes state the word *is* the formal term. New noise family; see observations.
- **"suru-verbs must carry `action`"** (3: 27010, 27043, 27233) — a convention the reviewer invented;
  it appears in neither `validate_tags.py` nor the skills.
- **27003 アドレス gloss "too narrow"** (2) — the model claimed アドレス covers postal addresses; the
  entry's notes correctly document that 住所 fills that role and アドレス is digital-only.
- **27008 着脱する `clothing` too narrow**, **27083 リース `daily-life` too broad**, **27013 入れ物**
  (`tool` vs `general`, plus formality) — in-list aptness nits; `tool` matches the 箱/金庫 precedent.
- **Furigana screening** (1, aggregated) — okurigana/compound-split family.

## §4 self-check — 52 entries, 4 applied / 6 rejected

Not clean, and it earned its keep: **two of the four applied fixes were my own slips.**

- **27068 クレーン** — my `construction`→`tool` migration collided with a pre-existing `tools`,
  leaving `["tool", "tools"]`. Corrected to `["tool"]`. My dedup guard compared parsed list length
  and so was blind to near-duplicate *strings*.
- **27234 身構え / 27252 前傾姿勢** — I migrated off-vocab `body` to `body-part`, but both headwords
  name a *posture*, not an anatomical part. Both corrected to `movement`.
- **27179 可動式** — carried an off-vocab `description` tag the first pass had not flagged (I had
  only touched its gloss). Migrated to `descriptive`.

Rejected: the formality family (27013, 27202), the `general`-too-broad family (27143), and the
invented suru-verb rule (27043, 27233).

## Candidates added

None — the reviewer flags concerned existing entry metadata, and no un-entered Japanese words
surfaced in the material read.

## Observations logged

Three appended to `polishing/observations.md`:

- `[tooling]` The furigana screener ran ~10x slower per entry than the accuracy pass (≈14 entries in
  the time accuracy covered ≈150), contradicting the "cheap bulk screening" cost model in §A. Its
  one flag was a false positive the model itself annotated "technically not an error". This is the
  **third consecutive run** to abort screening and skip the deep pass; the pass is a candidate for
  retirement from the Routine or gating to never-screened entries.
- `[tooling]` New reviewer-noise family: `formality: formal → neutral` proposals that ignore the
  register description in the notes the reviewer was given (4 of 5 entries said "the formal name
  for…", "the formal meteorological term", "used in formal apologies", "more refined than 寝る").
- `[pattern]` Off-vocabulary tags were again the entire applicable yield (39 of 43 applied tag
  fixes). The reviewer resolves a destination *per entry* rather than per tag name, which is
  strictly better than extending `TAG_MIGRATION`'s head for the 2,403-entry baselined backlog —
  though the 27234/27252 slips show the destination still needs a human-judgment check.

## Verification

- `build/validate.py --range 27001 27320` → **320/320 valid**
- `build/validate_tags.py --check-no-new-unknown` → **no new off-vocabulary tags**; baselined
  backlog 2,530 → **2,403**
- Metrics line appended: 58 applied / 32 rejected / 0 to curator

## Next cursors

- `polishing/tasks/cross-model-review/progress.txt` → **`next: 27314`**
- `reviews/queue.txt` drained of 27001–27313 (11,449 → 11,136 lines)
- `pipeline/openrouter-ledger.json` → $0.1657 spent today of the $5.00 cap
