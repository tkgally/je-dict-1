# Routine session 2026-09-20_001

**Mode**: accuracy-review
**Reason**: accuracy-review had the highest scheduler debt among eligible modes.
**Range**: entries 05001–06000 (1,000 entries), budget $2.50 (spent $0.4812).

## Pre-flight

No open routine PRs to rescue or absorb, no orphan `claude/*` branches. Branch
`claude/modest-dijkstra-6ruyra` was already at `origin/main` (no divergence).

## What happened

Ran `build/review_accuracy.py --range 5001 6000 --budget 2.5`: 996 entries
reviewed (4 already covered/failed), 127 flagged (12.8%, within the noise
threshold), 135 issues, est. cost $0.4812.

Adjudicated every surviving issue by family, opening each entry and checking
the reviewer's claim against the entry's own text and my own knowledge of
Japanese before acting:

- **offvocab** (21 issues, 20 entries): all applied — migrated off-list
  semantic tags to the closest in-list tag (`commerce`/`commerce-marketing`/
  `business-marketing` → `business`, `endurance` → `personality`,
  `psychology` → `cognition`, `journalism` → `media`, `mathematics` (13
  geometry/arithmetic entries) → `science`, `plant` → `plant-flower` or
  `plant-general`).
- **register** (35 issues): 25 applied, 10 rejected. Applied where the tag
  plainly contradicted the note's own directionality (e.g. お尻 tagged
  formal/polite when its note calls it the everyday word; 決起 tagged neutral
  when its note calls it formal). Rejected where a note's hedge ("somewhat
  formal") still falls on the same side of the tag as before, or where the
  reviewer misread the note.
- **wrong-category** (12 issues): 9 applied, 3 rejected. Applied fixes
  replaced semantic tags that didn't fit the headword (`body-part` on 炎症,
  `leisure` on 体力, `work` on 振込, `emotion` on まあまあ); rejected where the
  existing tag was defensible.
- **gloss-meaning / translation-meaning** (21 issues): 13 applied, 8
  rejected. Applied fixes trimmed a false-friend sense the entry's own notes
  already flagged as inadequate (つるつる "bald", ジャンパー "jumper"),
  corrected a mistranslated example (虚無感 rendered as "nihilism" instead of
  "sense of emptiness"), fixed a mislabeled honorific/humble distinction
  (お願いする is humble, not honorific), and aligned a top-level gloss with
  its own definitions (ツーイン, 通院, コンパス). Rejected flags mostly
  restated content the entry already got right, or misread furigana/kanji
  the reviewer conflated (少数 vs 小数, 茶道's alternate reading).
- **notes-fact** (46 issues): 16 applied, 30 rejected. Genuine fixes
  applied: reversed 暖簾を下ろす/上げる open/close directions in both the
  note and one example, a false claim about a word's typical connotation
  (優越感 called "typically negative" when the entry's own gloss allows a
  healthy positive sense), an inaccurate prescription-terminology note
  (院内処方). Rejected flags mostly restated content the note already had
  right (anatomy in 静脈, a court-level correction that matched the existing
  text in 控訴, an "onomatopoeia" label the dictionary uses broadly for both
  擬音語 and 擬態語 by convention).

No issue reached genuine curator-worthy uncertainty; nothing was flagged to
`reviews/needs_curator.txt`.

Every decision is logged in `reviews/decisions.jsonl` with a `note` giving
the reasoning in ≤10 words (135 lines, one per issue).

## Mechanical pass (§3)

`normalize_notes.py`, `auto_link.py --confirm-real-entries`, and
`harvest_crossrefs.py` ran on all 79 changed entries. `auto_link.py` added 1
new link (the 暖簾 note fix, now pointing at an existing entry);
`harvest_crossrefs.py` found no new reciprocal cross-references to add (all
proposed terms already had links, were self-references, or had no entry).
`validate.py` passed clean on all 79 entries.

## Self-check (§4)

Skipped per §A step 8: the reviewed range's own review was the check, so no
second independent pass was run on these 79 entries.

## Cursor and queue

- `polishing/tasks/cross-model-review/progress.txt` → `next: 06001`
- `reviews/queue.txt`: removed entries in the 05001–06000 range (5468 → 4879
  lines)
- `pipeline/openrouter-ledger.json`: spent_usd 0.4812 today

## Candidates / observations

None logged this run — no unresolved missing words were found while fixing
notes.

## For the curator

Nothing needs Tom's attention this run — every flag was either fixed or
rejected with a documented reason, no genuine uncertainty was left over.
