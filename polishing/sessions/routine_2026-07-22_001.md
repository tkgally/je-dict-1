# Routine session — 2026-07-22 #001

- **Mode**: `systemic-fix` (selector reason: "systemic-fix: highest scheduler debt among eligible modes")
- **Backlog item**: `furigana-cosmetic-wrappers` (P9 — non-standard but mostly-rendering furigana wrappers)
- **Slice worked**: 18000–18999 (the next ready slice per the item's running notes)

## Pre-flight
- §0a: `list_pull_requests` returned `[]` — no open PRs, nothing to rescue.
- §0b: no `claude/*` strands to sweep. Lock acquired for `claude/modest-keller-jpvrlr`.
- §1: selector chose `systemic-fix`; state persisted. OpenRouter remaining today: $5.00.

## Changes — 28 instances across 20 entries (detect filtered total 1109 → 1081)
Filter: subpattern ∈ {o-go-prefix, pure-kana, over-wrapped}. Each rewrap verified per-entry.

**pure-kana de-wraps** (katakana/kana wrapped in furigana → bare surface):
- 18034 `{パクリ|ぱくり}`→`パクリ` · 18082 `{リスク|りすく}`→`リスク` · 18095 `{シーエー|しーえー}`→`シーエー`×2 · 18120 `{クロール|くろーる}`→`クロール` · 18208 `{イメージ|いめーじ}`→`イメージ` · 18762 `{システム|しすてむ}`→`システム` · 18790 `{イベント|いべんと}`→`イベント` · 18458 `{やつ|やつ}`→`やつ`

**o/go-prefix repositions** (honorific prefix moved outside the wrapper):
- 18081 `{お返|かえ}`→`お{返|かえ}`×2 · 18192 `{お菓子|おかし}`→`お{菓子|かし}` · 18328 `{お茶|おちゃ}`→`お{茶|ちゃ}` · 18374 `{お疲|おつか}`→`お{疲|つか}`×3 · 18693 `{お守|おまも}`→`お{守|まも}`×3

**over-wrapped okurigana/compound splits**:
- 18207 `{花びら|はなびら}`→`{花|はな}びら`×2 · 18371 `{やり直|やりなお}`→`やり{直|なお}` · 18561 `{寄り合い|よりあい}`→`{寄|よ}り{合|あ}い` · 18665 `{賭け事|かけごと}`→`{賭|か}け{事|ごと}` · 18716 `{差し水|さしみず}`→`{差|さ}し{水|みず}` · 18746 `{決め手|きめて}`→`{決|き}め{手|て}` · 18976 `{思い出横丁|おもいでよこちょう}`→`{思|おも}い{出|で}{横丁|よこちょう}`×2

**Extra correctness fix**: 18374's `contrast` cross_reference headword `{お疲|おつか}れ{様|さま}` → `お{疲|つか}れ{様|さま}`, matching target **05208_otsukaresama**'s canonical headword (a 4th お疲 fix beyond the 3 the detector flagged in notes).

`modified` timestamp bumped on all 20 entries.

## Safety
- No `⟦...⟧` inline links in any changed entry — `word_id_lookup.json` unaffected (surfaces/readings unchanged).
- Detector re-check on the 18000–18999 filtered subpatterns: **0 remaining**.
- Validation: **998/998** entries valid in range 18000–18999.

## §4 self-verification (format-only → furigana screener)
- `review_runner.py --pass screening` on the 20 changed IDs: **20 screened, 0 flags, 0 skipped** — clean pass. Est. cost $0.0024, logged to the OpenRouter ledger (phase `self-check`). No `reviews/decisions.jsonl` lines (nothing to adjudicate).

## §5 metrics
- Appended one line to `pipeline/metrics-history.jsonl` (mode systemic-fix, changed 20, 0/0/0 flags).

## Always-on capture (§3)
- No missing-entry words encountered (all edits were cosmetic wrapper fixes to existing/loanword surfaces). No new systemic observations beyond the backlog note updates.

## State updates
- `planning/wiki/ideas/backlog-queue.json`: `furigana-cosmetic-wrappers` scope_estimate 1109 → 1081; notes appended with this sweep.
- `planning/wiki/ideas/cleanup-backlog.md` Priority 9: added the 2026-07-22 sweep record.
- Selector already advanced `pipeline/routine-state.json` in §1.

## Next ready slice
19000+ and the larger pre-06000 backlog.
