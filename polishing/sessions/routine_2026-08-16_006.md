# Routine session — polish

Date: 2026-08-16
Mode: `polish` (selector reason: "polish: highest scheduler debt among eligible modes")
Params: `start_id: 6957`
Selector signals: candidates 163, seen-in-entry 5, max entry ID 30693, comprehensive next 6957,
cross-model-review next 12342, unharvested observations 25, open backlog items 37.
OpenRouter at start: $4.7809 remaining of the $5.00 daily cap.

## Pre-flight

- §0a rescue: no open PRs — nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs.
- §0b orphan-branch sweep: `list_branches` returned only `main`. Zero orphans.
- Lock acquired for `claude/determined-rubin-0hflyl`.

## Lanes

**Priority lane** (`polishing/priority/notes.txt`, generated 2026-08-11, 5 days old — fresh),
starting at line 213. Eight lines consumed, six processed: `00036_dezaato` and `00284_miruku`
were skipped under the 30-day recency rule (both modified 2026-08-08).

**Frontier lane** from 06957, entries 06957–06966 (10 entries).

## Per-entry changes

### Priority lane

- **01207 熱心 (nesshin)**: reviewed against the full tier-1/tier-2 checklist — schema valid, furigana
  complete, inline links complete, cross-references present and resolving. No changes needed.
- **02009 至る (itaru)**: `cross_references` was empty even though the notes' CONTRAST section already
  named and linked three neighbours. Added three structured cross-references — 着く (contrast,
  everyday physical arrival), 達する (related, formal, reaching a level), 届く (related, to reach or
  be delivered). Also fixed an example translation flagged by the self-check (see below).
- **02132 眺める (nagameru)**: removed a CONJUGATION block from the notes that duplicated the entry's
  own `conjugation` field; renamed `COMMON EXPRESSIONS:` to `COMMON COLLOCATIONS:` (the project's
  standard heading, and what the consistency checker looks for); added three cross-references
  already named in the notes — 眺め (noun form), 見つめる (contrast), 見る (related).
- **03682 署名 (shomei)**: flagged as missing transitivity. 署名する takes に for the document signed,
  not を, so it is intransitive. Added the `transitivity: intransitive` tag and a matching line in
  the notes' GRAMMAR section.
- **05685 受け持つ (ukemotsu)**: notes opened directly on `COMPOUND VERB:` with no statement of what
  the word means. Added an opening description (assigned, ongoing responsibility — a class, a
  district, a set of patients; usually seen as 受け持っている). The entry had no `cross_references`
  field at all; added one with 受け持ち (noun form), 担当 (more formal), 任せる (to entrust).
- **07441 迷走 (meisou)**: flagged as missing transitivity. Added `transitivity: intransitive` plus a
  TRANSITIVITY note explaining that the を in 迷走を続ける belongs to 続ける, not to 迷走する — the
  most likely source of confusion for a learner reading the collocation list.

### Frontier lane

- **06957 いける**: added `transitivity: intransitive`; added an opening description sentence
  (previously opened on `ORIGIN:`); added a COMMON COLLOCATIONS section.
- **06958 アジェンダ, 06959 ストレージ, 06960 デバイス**: reviewed, no issues found, no changes.
- **06961 ぴえん**: added an opening description sentence and a COMMON COLLOCATIONS section.
- **06962 ママ友**: added an opening description sentence and a COMMON COLLOCATIONS section.
- **06963 就学**: added `transitivity: intransitive`, an opening description sentence documenting the
  が/に particle pattern, and a COMMON COLLOCATIONS section.
- **06964 奏者**: added an opening description sentence and a COMMON COLLOCATIONS section.
- **06965 不協和音**: removed a `cross_references` item that had **no `target_id`** — it pointed at
  協和音, which has no entry. Replaced the naked 協和音 in the notes' OPPOSITE line with a `noentry`
  marker and queued the word as a candidate. Added an opening description sentence. Semantic tag
  corrected from `general` to `music` per the self-check (see below).
- **06966 切符売り場**: added an opening description sentence and a COMMON COLLOCATIONS section.

All 12 changed entries validate; every consistency flag that was open on them at the start of the
run (`missing-transitivity` ×4, `note-structure` ×4, `no-collocations` ×7) is now clear.

## §4 self-check outcome

Sent all 12 changed entries to an independent model via `review_accuracy.py` (est. $0.0054).
**10 of 12 came back clean.** Three issues raised across two entries; adjudicated:

- **1 applied** — 02009 example 6: `その問題は解決に至っていない` was translated "The problem has not
  led to a resolution", which reads as though the problem were the cause. The Japanese says the
  problem has not *reached* resolution. Corrected. Good catch, and pre-existing rather than
  introduced this run.
- **1 applied** — 06965: sole semantic tag `general` replaced with `music`, which is in
  `VALID_SEMANTIC` and accurate for 不協和音's primary sense.
- **1 rejected** — 02009: the model wanted `formality: formal` relaxed to `neutral`. The entry's own
  notes describe 至る as a formal/literary verb "far more common in written Japanese, news, and
  formal speech than in casual conversation", so the existing tag is right and the §A rule (apply a
  formality flag only when the entry's own register description contradicts the label) says reject.

All three decisions logged to `reviews/decisions.jsonl`; spend recorded in the ledger.

## Candidates added

- 協和音 (きょうわおん): consonance, harmonious chord; antonym of 不協和音; seen in entry 06965 → C23205

## Observations logged

- `[pattern]` low note scores correlate with empty `cross_references` on entries whose notes already
  name the related words — a cheap, high-yield systemic-fix candidate
- `[pattern]` older entries opening notes on a bare ALL-CAPS header with no definition sentence;
  the existing `note-structure` check catches only a subset
- `[tooling]` `no-collocations` keys on the literal heading `COLLOCATION`, so entries using
  `COMMON EXPRESSIONS:` / `USAGE PATTERNS:` are false positives — heading standardization would fix
- `[entry]` 06965 had a `cross_references` item with no `target_id` that passed schema validation —
  worth confirming `check_artifacts.py`'s P2 check covers cross-references, not just inline links
- `[tooling]` entry-ID guesses written from memory were wrong 10 of ~30 times this session; a
  headword→ID resolver at write time would remove a whole fix round

## Next cursors

- Comprehensive frontier: `next: 06967`
- Priority lane: `line: 221`
