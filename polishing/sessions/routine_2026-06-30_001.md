# Routine session — 2026-06-30 #001

- **Mode**: `systemic-fix` (selector reason: "highest scheduler debt among eligible modes")
- **Backlog item**: `furigana-cosmetic-wrappers` (Priority 9 — non-standard but mostly-rendering furigana wrappers)
- **Slice worked**: 07300–07600 (next ready slice per the item's notes)

## Pre-flight
- §0a rescue: no open PRs — nothing to rescue.
- §0b sweep: no open `claude/*` PRs — nothing to sweep.
- Lock acquired for `claude/modest-keller-h1btwt`.

## Changes — 24 instances across 18 entries (`check_furigana_format.py` total 1488 → 1463)
Filter: `subpattern in {o-go-prefix, pure-kana, over-wrapped}`. Each rewrap verified per-entry; none sat inside an inline link.

- **pure-kana de-wraps** (hiragana ruby over katakana/kana is redundant):
  - 07452 `{プロジェクト|…}`→`プロジェクト`; 07453 `{スキルアップ|…}`→`スキルアップ`;
    07457 `{メンバー|…}`→`メンバー`; 07458 `{チーム|…}`→`チーム`; 07459 `{バー|…}`→`バー`;
    07464 `{リストラ|…}`→`リストラ`; 07470 `{タオル|…}/{ティッシュ|…}/{ボールペン|…}/{エコバッグ|…}/{ノベルティ|…}`→bare;
    07519 `{データ|…}`→`データ`; 07520 `{イメージ|…}`×2→`イメージ`; 07529 `{ボケ|…}`→`ボケ`
- **o-prefix repositions**: 07433 `{お金|かね}`→`お{金|かね}`; 07459 `{お茶屋|おちゃや}`→`お{茶屋|ちゃや}`;
  07470 `{お菓子|おかし}`→`お{菓子|かし}`
- **over-wrapped okurigana splits**: 07346 `{取り消し|とりけし}`→`{取|と}り{消|け}し`;
  07364 `{女の子|おんなのこ}`→`{女|おんな}の{子|こ}`; 07479 `{すり身|すりみ}`→`すり{身|み}`;
  07555 `{届け出|とどけで}`→`{届|とど}け{出|で}`; 07577 `{太っ腹|ふとっぱら}`→`{太|ふと}っ{腹|ぱら}`
- **malformed-wrapper repairs** (surface≠reading / empty reading): 07556 `{できる|できない}`→`できる/できない`
  (restores the can/cannot contrast its gloss describes); 07587 `{どこ|}`→`どこ`

All 18 entries' `modified` timestamps bumped to 2026-06-30T00:18:41Z. `validate.py --range 7300 7599`
passed (300/300); detector recheck on the slice shows 0 remaining filtered instances.

## §4 self-verification (furigana screener, `review_runner.py --pass screening`)
- Re-screened all 18 changed IDs (cleared their stale 2026-06-19 cache first). Screened 18, **2 flagged, both REJECTED**:
  - 07459 — model saw `{一見|いっけん}` and called it an inconsistency, but it is the entry's own
    "NOTE ON READING" deliberately contrasting いっけん (at first glance) with いちげん. Correct as written.
  - 07364 — model claimed a translation error reading "to sing" as "to sin"; the 歌=うた furigana is
    correct and the model admits it is "not a furigana error." Misreading.
- Neither flag touched an edited furigana. Both logged to `reviews/decisions.jsonl` (src=self-check, dim=furigana, reject).
- Spend: $0.0022 recorded in `pipeline/openrouter-ledger.json` (phase self-check).

## Always-on capture
- No new candidates (format-only sweep; candidate list already at 1204).
- No new systemic observations beyond the per-run note recorded on the backlog item.

## State updated
- `planning/wiki/ideas/backlog-queue.json`: `furigana-cosmetic-wrappers` scope_estimate 1485→1463,
  notes appended with this slice; `updated` → 2026-06-30.
- `planning/wiki/ideas/cleanup-backlog.md` Priority 9: added 2026-06-30 update paragraph.
- `pipeline/metrics-history.jsonl`: one line appended (systemic-fix, 18 changed, 0 applied / 2 rejected).
- Selector state already advanced by `routine_next.py` in §1.

## Next
- Next ready slice for `furigana-cosmetic-wrappers`: **07600+** and the larger pre-06000 backlog.
