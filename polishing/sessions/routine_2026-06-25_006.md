# Routine session — 2026-06-25 (006)

- **Mode**: `systemic-fix` (selector reason: "highest scheduler debt among eligible modes")
- **Backlog item**: `furigana-cosmetic-wrappers` (Priority 9 / cleanup-backlog.md) — non-standard but mostly-rendering furigana wrappers (o/go-prefix, pure-kana, over-wrapped okurigana)
- **Scope worked**: the predicted scoped **06000–06400 slice** (`build/check_furigana_format.py --range 6000 6400`)

## Pre-flight
- §0a rescue: no open PRs → no-op. §0b sweep: no open `claude/*` PRs → no-op.
- Lock acquired for session `claude/modest-keller-3mgraf`.

## Changes — 18 instances across 16 entries (all per-entry verified)

Pure-kana wrappers de-wrapped to bare kana (katakana loanwords / kana with identical reading):
- 06171 `{リーダー|りーだー}` → `リーダー` (inside inline link to 24312)
- 06263 `{ボランティア|ぼらんてぃあ}` → `ボランティア` (link to 04834)
- 06277 `{おかわり|おかわり}` → `おかわり`
- 06311 `{メガネ|めがね}` → `メガネ`
- 06312 `{ブラッドムーン|ぶらっどむーん}` → `ブラッドムーン`
- 06320 `{サインポール|さいんぽーる}` → `サインポール`
- 06321 `{タッチ|たっち}` → `タッチ`
- 06327 `{ばさみ|はさみ}` → `ばさみ` (rendaku of はさみ; all-kana, no furigana needed)
- 06354 `{タバコ|たばこ}` → `タバコ`
- 06392 `{すぐ|すぐ}` → `すぐ`

お/ご-prefix wrappers repositioned outside the kanji group:
- 06131 `{お問|おと}` → `お{問|と}` (×2, examples 4 & 5; notes were already correct)
- 06208 `{ご飯|ごはん}` → `ご{飯|はん}` (link to 00776)

Over-wrapped okurigana/compounds split with the correct (rendaku) reading on each kanji:
- 06019 `{数え|かぞえ}` → `{数|かぞ}え` (link to 01926)
- 06269 `{どん底|どんぞこ}` → `どん{底|ぞこ}` (×2, example & note; link to 08781)
- 06310 `{流れ星|ながれぼし}` → `{流|なが}れ{星|ぼし}`
- 06367 `{横ずれ断層|よこずれだんそう}` → `{横|よこ}ずれ{断層|だんそう}`

All inline-link surfaces preserved (links resolve by explicit `：entry_id`). `modified`
timestamp updated on all 16 entries. Detector total 1565 → 1547; **0 remaining in 6000–6400**.
`validate.py --range 6000 6400` → 401/401 valid.

## §4 self-check (furigana screener variant, format-only batch)
- `review_runner.py --pass screening --ids <16 ids> --budget 0.15`
- Result: **clean** — 12 screened, **0 flagged**, 4 skipped (pure-kana headwords with no kanji
  pairs). 0 applied / 0 rejected / 0 flagged. Est. $0.0014 logged to ledger (`phase: self-check`).

## Always-on capture
- Candidates added: none (no notably missing words surfaced; de-wrapped loanwords already have entries).
- Observations: none new logged this run.
- Pre-existing, out-of-scope: 06171 has a bare `実質的に` in `examples[2].notes` (English-note
  furigana gap, unrelated to this cosmetic-wrapper batch) — left for the dictionary-wide
  `find_missing_furigana` sweep.

## State / cursors
- `backlog-queue.json`: item stays **open**, `scope_estimate` 1525 → 1546, notes updated with
  the swept slice; `updated` → 2026-06-25. Prose page Priority 9 got a matching dated update.
- Selector state already advanced in §1 (`pipeline/routine-state.json`).
- Ledger spent today: $0.0485. Metrics line appended to `pipeline/metrics-history.jsonl`.

## Next
- Next ready slice for this item: 06400+ and the larger pre-06000 backlog (~1546 remaining).
