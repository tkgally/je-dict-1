# Routine session — polish

Date: 2026-08-03
Mode: `polish` (selector reason: "polish: highest scheduler debt among eligible modes")
Params: `start_id: 6758`
Selector signals: candidates 997 (13 "seen in entry"), max entry ID 30357, comprehensive next 6758,
cross-model-review next 24570, unharvested observations 5, open backlog items 17,
OpenRouter remaining $4.9441.

Entries changed: 16 (7 priority lane, 5 frontier lane, 4 out-of-range verb-tag repairs)

## Priority lane (notes.txt, lines 169–176 → cursor 177)

Line 169 (`00755_shizuka`) skipped: polished 2026-07-25, inside the 30-day window.

- **02871 嫌い** — `じゃない` resolved from `noentry` to `28483_janai` (example + notes); rewrote the
  particle section (は was called the "subject marker"; it is the topic marker) and added a
  CAUTION — PEOPLE section and a 苦手 contrast; added cross-refs to `03513_nigate` and `12178_daikirai`.
- **03860 デザイン** — sole-`general` semantic tag replaced with `art`/`creation`; added cross-refs to
  `05061_dezainaa` and `12689_ishou` (both already named in the notes but unlinked as references).
- **05100 討論** — added a REGISTER section contrasting it with 議論/話し合い; added cross-refs to
  `25422_touronkai` and `03786_ronsou`.
- **05461 助言** — semantic tag `action` → `communication`; added cross-refs to `05462_chuukoku` and
  `05828_kankoku`.
- **05463 要請** — **furigana error fixed**: notes read `{応|こた}じる` for 応じる (correct reading おう).
  Scanned the whole dictionary for the same defect — isolated to this entry; all 125 other
  occurrences of 応じ are `{応|おう}じ`. Also linked a bare する, and filled in the empty
  `cross_references` with 依頼 / 要求 / お願い, the three words its own formality scale names.
- **05652 電話する** — semantic tag `action` → `communication`; linked the particle-pattern section and
  added a で-marks-the-means line; added cross-refs to `01524_renraku` and `10159_denwabangou`.
- **00669 死ぬ** — the definition claimed the verb is used for plants; it is not (枯れる is). Rewrote the
  explanation, replaced the 花が死んでしまった example with 虫が死んでいる (which also demonstrates the
  ている resulting-state point the notes make), added a WHICH VERB FOR WHAT DIES section, a REGISTER
  section, a conjugation note on the sole ぬ-godan verb, and the 死ぬほど pattern; semantic tag
  `action` → `existence`/`health`; added cross-refs to `04589_kareru` and `02371_inochi`.

## Frontier lane (06758 → cursor 06763)

These entries had **no inline links at all**, so tier-1 link coverage was the bulk of the work.

- **06758 くらくら** — full link coverage on all 3 examples and the whole notes field; sharpened the
  three SIMILAR WORDS entries from bare glosses into actual distinctions (ふらふら = body sways,
  めまい = the medical noun, ぐるぐる = something actually rotating).
- **06759 しれっと** — full link coverage on all 3 examples and notes; added a REGISTER section
  (colloquial only; 平然と is the written equivalent) and a cross-ref to `21639_suzushiikao`.
- **06760 押し入る** — full link coverage; restructured notes to the standard TRANSITIVITY header, added
  a REGISTER section (crime-reporting word) and particle guidance (に, never を); semantic tag
  gained `movement`.
- **06761 降り立つ** — full link coverage (地 marked `noentry` + added as a candidate); restructured
  notes, expanded USAGE with the contrast against plain 降りる; added the missing `00758_tatsu`
  component cross-ref; semantic tag gained `movement`.
- **06762 萎縮する** — **tagged `verb-godan` although it is a suru verb** (`part_of_speech: "godan verb"`,
  `pos: ["verb-godan"]`, `verb_class: "suru"`); fixed both POS fields. Full link coverage on all 6
  examples and notes; restructured notes with TRANSITIVITY / ASPECT / SIMILAR WORDS sections; added
  cross-refs to `00027_chijimu` and `07163_puresshaa` (the entry had none).

## Out-of-range repairs (found by scanning for the 06762 defect)

A dictionary-wide scan for `pos`/`verb_class` disagreement returned 6 entries. `00392_suru` is
legitimately `verb-irregular` and was left alone. The other 5 were real, and split into two families:

- **08736 お供する**, **02941 コピーする** — suru verbs tagged `verb-godan`. POS fields corrected.
- **09300 ディスる**, **09318 ミスる** — godan `-る` verbs whose `verb_class` said `suru`, which had given
  them **broken conjugation tables published on the live site**: `ディスるした`, `ミスるして`. Corrected
  `verb_class` to `godan-ru` and wrote correct godan tables (ディスった / ディスって / ディスれば …).
  `build/add_conjugations.py --force` could not fix these — see the `[tooling]` observation.

## §4 self-check (independent model)

`build/review_accuracy.py --ids <16 changed entries> --budget 0.25` — 16 reviewed, 6 flagged,
7 issues, est. $0.0070. All 7 were `tags`-dimension, `error` severity. **5 applied, 2 rejected, 0 flagged.**

- APPLY 02871 — `descriptive` alone understates an emotion word → added `emotion`.
- APPLY 02941 — `formality: formal` wrong for an everyday loanword → `neutral`.
- APPLY 06762 — physical/atrophy sense untagged → added `health`.
- APPLY 08736 ×2 — missing `action`; `social` is off `VALID_SEMANTIC` → migrated to `society` per the
  check_tag_drift map. Also moved the off-vocab `keigo`/`humble` out of `semantic` and set
  `politeness: humble`, which is where that information belongs.
- REJECT 05461 — flagged `formal` → `neutral`, but the entry's own REGISTER note says 助言 is more
  formal than アドバイス and common in written and official contexts.
- REJECT 06760 — flagged `formal` → `neutral`, but the entry is restricted to crime reporting; a
  marked register is correct and `formal` is the closest available label.

No furigana, gloss, or translation issues were raised on any of the 16 entries.

## Candidates added

- 使いやすさ (つかいやすさ) — seen in 03860
- グラフィックデザイン — seen in 03860
- ディベート — seen in 05100
- 何時間 (なんじかん) — seen in 05100
- 地 (ち) — seen in 06761
- 〜ている — seen in 06762

## Observations logged

- `[tooling]` `add_conjugations.py` misclassifies loanword godan `-る` verbs whose kana reading ends in
  する (`reading.endswith('する')`, line 238) — the root cause of the ディスる/ミスる breakage.
- `[pattern]` `pos` / `verb_class` disagreement — full inventory and proposed `check_tag_drift.py` rule.
- `[pattern]` `part_of_speech` free-text field has no house style; six spellings seen this run alone.
- `[pattern]` inline link coverage stops around ID 06758; expect frontier polish to be link-dominated.
- `[entry]` 00755_shizuka may be mis-scored by the note-quality scorer.

## Next cursors

- Frontier: `06763`
- Priority: `line: 177`
