## Session: Unified Routine v2 — `polish`

Date: 2026-07-29
Mode: `polish` — selector reason: "polish: highest scheduler debt among eligible modes"
Selector signals: candidates 1017 (8 `seen in entry`), max entry ID 30221, comprehensive frontier 06681,
cross-model-review frontier 21301, 29 unharvested observations, 7 open backlog items.
OpenRouter at selection: $0.3265 spent today, $4.6735 remaining.

Pre-flight: no open PRs — nothing to rescue (§0a), nothing to sweep (§0b). Lock acquired.

Entries processed: 8 (5 priority lane, 3 frontier lane)

### Priority lane (`polishing/priority/notes.txt`, lines 43–56)

Priority file regenerated earlier today (2026-07-29), so the lane ran. Lines 43–46 and 48–49, 51–53
were skipped as modified within the last 30 days (03423, 06528, 06973, 06981, 03805, 00475, 01108,
02352, 02857).

- **00713 {簡単|かんたん}**: 7 inline-link baseforms carried furigana braces
  (`⟦{言|い}う→{言|い}う：00515_iu⟧`), which render literally in the hover tooltip — stripped to the
  dictionary form. Examples and cross-references were already sound; sense-1 and sense-2 example
  counts both meet the basic-tier minimum of 5.
- **03328 {象|ぞう}**: `大喜び` was marked `noentry` but now exists (28880) — link repaired. The
  "IDIOM" block contained a **duplicated な** (`⟦{様|よう}な→よう⟧⟦な→な⟧` renders 象のようなな記憶力)
  and presented 象のような記憶力 as a Japanese idiom, which it is not — it is a rendering of the
  English "a memory like an elephant". Rewrote as a MEMORY section that says so explicitly and fixed
  the double な.
- **03694 {発車|はっしゃ}**: completed inline-link coverage — `まで`, `あと` (→ 後 09580) and `もう`
  were naked in ex4/ex5; `{出航|しゅっこう}` in the notes marked `noentry` and added as a candidate.
- **03718 {保証|ほしょう}**: the entry used **kana readings as link baseforms throughout** — 38 links
  (`→きかん：`, `→ほしょうしょ：`) rewritten to the dictionary form (`→期間：`, `→保証書：`). Tag fix
  from §4 below applied on top.
- **06983 {組|く}み{込|こ}む**: had zero inline links. Added full coverage across all 5 examples and
  the notes. Removed the leading duplicate-conjugation block from the notes (the conjugation table
  already carries those forms) and added an ASPECT section documenting 組み込まれている as a
  resultant state rather than an action in progress — the form used in ex5 and the dominant one in
  technical writing. `{予備費|よびひ}` marked `noentry` and added as a candidate.

Priority cursor advances to line **57**. Rankings are not stale — 5 of 5 eligible entries needed real
changes — so no regeneration this run.

### Frontier lane (from 06681)

The 06681– block was created without any inline links at all, so each entry needed full coverage
from scratch.

- **06681 こってり**: full inline-link coverage on 6 examples and the whole notes field. Back-link on
  05650 あっさり verified present.
- **06682 じわじわ**: full coverage on 6 examples and notes. **Tag fix**: `formality` was `formal` on
  an onomatopoeic adverb whose own notes describe 徐々に as "more formal" — corrected to `neutral`,
  and the SIMILAR WORDS line now states the register difference. One dead link I introduced
  (`00464_iku`) was caught by the sweep below and corrected to `00119_iku`.
- **06683 ぽつぽつ**: full coverage on 9 examples and notes; the ～始める auxiliary (18370) used for
  the five 〜始める forms rather than 始める (00531). Dropped the ちらほら cross-reference, which had
  **no `target_id`** (no such entry) — ちらほら and ぽつりぽつり are now noentry-linked in the notes
  and added as candidates.

Frontier cursor advances to **06684**.

### §4 self-verification (independent model, `review_accuracy.py`)

8 entries reviewed, $0.0036, **1 issue** — 7 of 8 came back clean.

- 03718 `tags.semantic`, error, **APPLIED**: sole `action` tag under-describes a noun whose primary
  sense is a concept/document. Added `abstract` to `semantic` and `business` to `domain` (the flag
  suggested `business` as semantic; `domain` is its correct home in this schema). Both values are in
  `VALID_SEMANTIC`; `validate_tags.py` reports no unknown-tag warnings.

Logged to `reviews/decisions.jsonl`; spend recorded in the ledger (`phase: self-check`, day total
$0.3301 of $5.00).

### Systemic findings (detail in `polishing/observations.md`)

Three dictionary-wide problems surfaced from the two baseform families found in 00713 and 03718.
All three are user-visible and none are caught by CI today:

| finding | scope | why invisible |
|---|---|---|
| inline links whose entry ID does not exist | 292 links / 160 entries / 144 dead IDs | `validate.py` doesn't check target existence; `html_utils.process_word_links` degrades to plain text silently |
| baseform in kana instead of the dictionary form | 3,567 links / 1,712 entries | `styles.css:245` renders the baseform as the hover tooltip |
| furigana braces left inside the baseform | 254 links / 39 entries | same tooltip path — shows a literal `{言|い}う` |

Three `batch_ready` items added to `planning/wiki/ideas/backlog-queue.json`
(`link-target-dead` priority 2, `link-baseform-kana` and `link-baseform-braces` priority 3). The two
baseform items are genuinely mechanical — the target entry is ground truth and no Japanese text
changes — but they were **not** applied here, since dictionary-wide sweeps belong to `systemic-fix`
mode, not `polish`. The dead-ID item wants a ratcheted `validate.py` check alongside the data fix so
new dead links can't be added.

### Candidates added

- 予備費 (よびひ): contingency fund; seen in 06983
- 出航 (しゅっこう): sailing, departure of a ship; seen in 03694
- ちらほら: here and there, sparsely; seen in 06683
- ぽつりぽつり: variant of ぽつぽつ; seen in 06683

### Observations logged

- [tooling] `validate.py` accepts inline links to non-existent entry IDs (292 live instances)
- [pattern] link baseforms are user-visible via the CSS tooltip; two wrong families totalling 3,821 links
- [pattern] onomatopoeic adverbs mistagged `formality: formal` from template defaults
- [entry] 06684–06690 have zero inline links; expect ~3–4 entries per run through this stretch

### Next cursors

- `polishing/tasks/comprehensive/progress.txt` → `next: 06684`
- `polishing/tasks/comprehensive/priority-cursor.txt` → `line: 57`
