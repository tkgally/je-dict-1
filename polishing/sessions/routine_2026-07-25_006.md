# Routine v2 session — 2026-07-25 (006)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Selector signals**: candidates 1,063 · seen_in_entry 2 · max_entry_id 30,104 · comprehensive_next 6,631 · cross_model_review_next 18,346 · unharvested_observations 19 · reviewed_entries 586 · open_backlog_items 6
**OpenRouter**: spent today $0.4172 of $5.00 cap → $4.5828 remaining; **this run spent $0.00** (no model calls)
**Branch**: `claude/determined-rubin-1x8x98`

## Pre-flight (§0)

- **§0a rescue**: `mcp__github__list_pull_requests` returned **0 open PRs** — nothing to rescue.
- **§0b sweep**: nothing to sweep (no open `claude/*` PRs). `pipeline/sweep-stranded-prs.py` not run (403s in this environment; the MCP path is the working one).
- **Lock**: acquired cleanly.
- HEAD was identical to `origin/main` at start.

## Work done (§2, `wiki` mode → `planning/maintain-knowledge-base.md`)

### [F] Harvest — 15 loose observations, all cleared

From the 2026-07-25 new-entries / accuracy-review (17911–18345) runs and the 2026-07-25 routine polish run (frontier 06627+). Eleven were reinforcements of already-filed patterns; **four were genuinely new, two of them correctness classes**.

**New items filed:**

| Item | What |
|---|---|
| Cleanup **P24** (new) | Inline-link base forms written with furigana braces (`→{来\|く}る：…`) — 44 entries. Indexed in `backlog-queue.json` as `inline-link-braced-baseform` (priority 24, `mechanical-safe`, batch_ready, scope 43). |
| Cleanup **P25** (new) | Auxiliary verbs written in kanji after the te-form (01092_oku wrote 〜て{置\|お}く in 5 of its own examples). Specified as a review queue with its false-positive boundary (lexical uses are correct). |
| Cleanup **P6** sibling defect + Tooling **item 34** (new) | **Fabricated conjugation tables from a mis-assigned verb class** — 06624 甘える (ichidan) tagged `verb-godan` produced 甘えらない/甘えります/甘えった; 09361 バックレる a second real case found inside a 38-hit/36-FP -eru scan. Detector recorded: cross-check the entry's **own examples** against its declared class. |
| Tooling **item 35** (new) | `CLAUDE.md` documents cross-reference types (`formality_variant`, `transitivity_pair`) that `schema.json`'s enum rejects — curator call between fixing the docs and extending the enum. |

**Reinforcement updates**: Tooling **11** (escalated — 9 wrong-ID inline links written in one run, all validating clean, with the docs advising a check that does not check), **24** (~2% precision *diagnosed*: ~37 of 48 flags traced to the prompt builder truncating its own context string, proven by the stray `)`), **21** (twelfth confirmation + first side-by-side throughput: screening ~8/min vs `review_accuracy.py` ~29/min on the same range), **7** (06626 見栄 sense 2 *is* entry 29012 見得; cheap detection via the existing `find_merge_candidates.py` reading grouping), **23** (~200 candidates scanned to curate 15, + new sub-fix: the "seen in entry" logger must dedupe by reading at add time — 雪ぐ/30089, 裏面/18245); Cleanup **P20** (cohort into the themed 18250–18300 pocket; detector 6,721→6,696) and **P21** (band confirmed *above* the cursor, 06627–06632 spot-checked empty; frontier cost quantified at 10–15 min/entry).

**Entry Follow-ups**: the 00969_mata "bare adverb glossed with a compound-only sense" pattern; **ている has no entry yet is `noentry`-linked in 36 entries** → curator decision (create the grammar entry vs. document a never-link convention).

**Already filed (no new action)**: the 01385/02485 気持ち duplicate; the candidate-junk families (standing <10%-signal finding).

**[skill] recommendation (log only, per the knowledge-base prompt's no-skill-edits rule)**: the `inline-word-links` skill and `cross-reference-entry` skill wording are both implicated by new items P24 and 35 — worth a curator pass to make the base-form format and the legal cross-reference `type` values explicit.

### [H] Metrics trend — twenty-fifth refresh

12 new `metrics-history.jsonl` lines (341→353 runs, ~9,235 adjudicated flags). Runs 342–353 adjudicated **366 flags: 159 applied / 207 rejected / 0 escalated** (polish ×4, accuracy-review ×3, new-entries ×2, wiki ×2, systemic-fix ×1). `tags` apply-among-decided **61.3%** (146/238), flat vs. 60.6%. Review queue **new floor 12,496**. OpenRouter ≤$0.44/day. Detectors: `furigana_format` 1,032, `artifacts` 12, `tag_drift` 11,339 (unknown-semantic 6,696 / sole-general 3,823 / semantic-mismatch 813).

Also wrote an **amendment to Finding 4**: nineteen refreshes have re-measured furigana screening at 0–5% precision and read it as a property of the review problem; this window's evidence shows most of those flags answer a question our own prompt builder corrupted, so the figure is a lower bound — the screener should not be retired on it before the untruncated-field fix ships.

No metric judged to be moving the wrong way → no new `[pattern]` observation logged.

### [A] Sync

`project/overview.md` General-tier count refreshed: 27,092 tagged `general` + the 20 untiered 29181–29200, of 29,895 total (per `build/audit_tiers.py`).

### [E] Maintain

`backlog-queue.json`: `unknown-semantic-tags` scope 6,721→6,696 with a dated note; new `inline-link-braced-baseform` item; `updated` bumped to 2026-07-25. No new wiki pages created, so `index.md` needed no change.

## §3 always-on capture

No Japanese words lacking entries were encountered (markdown-only session; no entry files read for content). No new `[pattern]`/`[tooling]` observations generated — everything this run touched was harvested *into* the backlog pages rather than deferred.

## §4 self-verification

**Skipped — no entries created or modified this run** (wiki mode). OpenRouter untouched; ledger unchanged at $0.4172 for the day.

## §5 metrics

Appended: `mode=wiki, changed=0` (353 → 354 lines).

## §7 cursors

- No mode cursor to advance (`wiki` mode): `polishing/tasks/comprehensive/progress.txt` stays at `next: 06631`; the cross-model-review cursor stays at 18,346.
- `pipeline/routine-state.json` already advanced by the selector in §1.
- **No `make build`** — markdown-only changes, `docs/` untouched.

## Next

The selector picks the next mode. Two items from this harvest are ready for a `systemic-fix` run whenever it comes up: `inline-link-braced-baseform` (priority 24, mechanical-safe, ~43 entries) and the standing `unknown-semantic-tags` sweep of the 17000–18000 band (priority 9, 6,696 flags).
