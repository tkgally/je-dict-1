# Routine session — 2026-08-09 (005)

**Mode**: `systemic-fix`
**Selector reason**: "systemic-fix: highest scheduler debt among eligible modes"
**Signals**: candidates 983 (12 "seen in entry"), max entry ID 30525, comprehensive frontier 06845,
cross-model-review next 29295, 24 unharvested observations, 27 open backlog items.
**OpenRouter at start**: $0.2032 spent today, $4.7968 remaining of the $5 daily cap.

## Pre-flight

- §0a rescue: `list_pull_requests` returned **zero** open PRs — nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs, so nothing to close.
- §0b orphan-branch sweep: `list_branches` returned **only `main`** — zero orphan branches.
- Lock acquired for `claude/determined-rubin-6j5b5x`.

## Backlog item worked

`inline-link-stale-noentry` (Cleanup P35) — stale `noentry` inline links whose word now has
an entry. Detector: `build/check_stale_noentry.py` (read-only). Filter per the item: **A1+A2
only**; classes B, C, D, R stay out. At run start the mechanical queue held **1,603 pairs /
1,854 instances**.

The 2026-08-08 run swept every mechanical pair with a base of 4+ characters plus the
hand-reviewed 2–3 character pairs below entry 00443, and explicitly recorded that the
"mechanical, precision ~1.0" claim does **not** hold for short bases (~4% false positives on a
110-pair hand sample). So this run treated every pair as `verify: per-entry`.

### Method

Worked in entry-ID order from the frontier (00443). For each pair the detector's JSON queue was
joined with a ±45-character context window around the marker in the field it sits in, so the
substitution could be judged against the surrounding sentence or note line — the polysemy trap
(baseball フライ vs the deep-fried-food entry; 海パン vs bread) is only visible in context.

**Two batches of 90 pairs, each read in full before applying**:

- Batch 1 — entries 00443–00947: 90 pairs, 98 instances, 57 entries.
- Batch 2 — entries 00947–01435: 90 pairs, 98 instances, 68 entries.

**Total: 180 pairs / 196 instances across 124 entries; all A1/A2.** Everything below entry
01436 is now swept for A1+A2. Remaining queue: **1,423 pairs / 1,658 instances**.

### False positives found: zero

One pair was pulled aside for a closer look — `01369_suteeki`'s ⟦レア→レア：noentry⟧ in a
steak-doneness note, where `11251_rea` glosses as "rare, uncommon" (the gaming/collecting
sense). Reading the target entry settled it: `11251_rea` carries an explicit **sense 2** for
steak doneness ("レア → ミディアムレア → ミディアム → ウェルダン"), so the link is correct and was
applied. No pair in the 180 was rejected.

The families that recur in this ID band and were verified as a group: counter compounds
(六千, 八百, 三十人, 七人), day-of-month readings (十四日, 十五日), floor/rank counters (一階, 二階,
何位), compass and region compounds (北側, 西日本, 東北), family politeness forms (弟さん, 妹さん,
ご子息), katakana loanwords (チャリ, メトロ, ミトン, ウール, コンポ, ミリ, ロング, ティー), and
homograph-safe alternate readings (一度/ひとたび → `27646_hitotabi`, not `01312_ichido`).

Validation after each batch: `validate.py` clean over both ranges (599/599 and 601/601), and
`check_link_targets.py` reports 0 dead / 0 ambiguous / 0 unresolvable dictionary-wide.

## §4 self-verification

`build/review_accuracy.py --ids <124 changed IDs> --budget 0.60` — 124 entries reviewed,
26 flagged, 44 issues, est. **$0.0550**. Ledger now at $0.2582 for the day.

**Zero flags on the inline-link substitutions themselves.** Because the reviewer reads whole
entries, all 44 issues were pre-existing content observations in entries this run happened to
touch. Adjudicated per §C:

**Applied (6)** — flatly wrong category assignments and one English error:

| Entry | Fix |
|---|---|
| 00663 財布 | semantic `clothing` → `daily-life` (a wallet is not clothing) |
| 00787 けが | semantic `body-part` → `health` (an injury is not a body part) |
| 01064 定期券 | removed semantic `person` (a commuter pass is not a person) |
| 00806 両親 | formality `formal`→`neutral`, politeness `honorific`→`plain` (ご両親 is the honorific form) |
| 00799 息子 | politeness `humble`→`plain` (息子 is the plain term; 愚息 is the humble one) |
| 01058 水道 ex[7] | "Please close the faucet." → "Please turn off the faucet." |

**Rejected (37)**, in four families plus one singleton:

- **00483 聞く, 6 issues** — the model asserted that 聞く does not mean "to hear" and that five
  examples belong to 聞こえる. This is simply wrong: 音を聞く is standard, and 聞こえる is the
  spontaneous/potential counterpart. Rejected in full.
- **16 tag issues** — in-list "too narrow / too broad" substitutions (`leisure`→`music` on 音楽,
  `general`→`nature` on 波, `education`→`science` on 医学, …). Rejected per the §A semantic-tag
  policy: narrowness disputes between in-list tags are not errors.
- **9 translation issues** — stylistic rewordings that do not change meaning.
- **5 gloss issues** — the US/UK "pants" ambiguity on パンツ, where the entry already carries two
  clearly separated senses.
- **01315 糸** — `tool` flagged as wrong for thread. There is no `material` tag in
  `VALID_SEMANTIC` and no better in-list parent, so `tool` stands.

Flagged to curator: 0.

All 11 decision lines (6 individual applies + 4 aggregated rejection families + 1 individual
reject) appended to `reviews/decisions.jsonl`; ledger line appended with `phase: "self-check"`.

## Always-on capture

- **Candidates**: none added — every word encountered in this run's markers already had an
  entry by construction (that is what the detector selects for).
- **Observations** (4 appended to `polishing/observations.md`): `[pattern]` ID-order sweeping
  beats detector-order because families cluster; `[tooling]` a link-only run gets no direct
  verification of its own edits from the whole-entry §4 reviewer — a `--dimensions links` mode
  would be the targeted instrument; `[pattern]` tag errors cluster on the *category* axis
  (wrong parent) rather than the narrowness axis, and a POS/gloss-vs-category detector would
  find them deterministically; `[entry]` keigo-label drift on family terms (00806).

## Backlog state updated

- `planning/wiki/ideas/backlog-queue.json` → `inline-link-stale-noentry`: `scope_estimate`
  1580 → **1423**, `scope_measured_at` 2026-08-09, filter and title updated, and a `cursor`
  note recording that entries below 01436 are swept for A1+A2.
- `planning/wiki/ideas/cleanup-backlog.md` P35 → new dated paragraph recording the batch, the
  ID-order finding, and the zero-false-positive result with the レア near-miss.

Item stays **open** — 1,423 pairs remain, all of them 2–3 character bases needing the same
per-entry read.

## Metrics

`pipeline/metrics_snapshot.py --mode systemic-fix --changed 129` →
entries_total 30316, flags_applied 6, flags_rejected 37, flags_to_curator 0,
review_queue_len 10239, candidates 983, openrouter_spent_today_usd 0.2582.

## Next cursor

Resume the P35 sweep at the **lowest remaining `entry_id`** in
`python3 build/check_stale_noentry.py --mechanical --json` — everything below **01436** is
swept for A1+A2. The comprehensive-polish frontier is untouched at `next: 06845`.
