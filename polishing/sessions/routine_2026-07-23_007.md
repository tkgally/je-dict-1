# Routine session — 2026-07-23 (007)

- **Mode**: accuracy-review (selector reason: "highest scheduler debt among eligible modes")
- **Range**: 17311–17560 (`params.start_id` 17311, session budget $2.50)
- **Signals**: cross_model_review_next 17311, OpenRouter remaining $4.92 at start.

## Pre-flight
- §0a rescue: no open PRs → nothing to rescue.
- §0b sweep: no open `claude/*` PRs → nothing to sweep.
- Lock acquired for `claude/modest-keller-miildc`.

## Work done

### Furigana screening (§A step 2)
- `review_runner.py --pass screening --range 17311 17560` — 241 screened, **37 flagged, 0 applied (0% precision)**.
- Every flag verified against the actual entry file: all false positives. Two FP families —
  (a) **screener context-truncation**: the screener's reading-display window cut off the reading
  (e.g. shows `変化球|へんかき` / `敬称|けいしょ` / `横目|よ)`), but the stored entry has the full correct
  reading (へんかきゅう / けいしょう / よこめ); (b) **okurigana/rendaku splits** correct by design
  (通行止→つうこうど+め, 土砂降→どしゃぶ+り, 良→よ+い okurigana). One flag (17398) was a
  misread "translation" claim — text actually reads `{勝|か}つ/{負|ま}ける: to win/lose`, correct.
- Per §A known-noise shortcut: bulk-rejected all 37, **deep pass skipped**. One aggregated §C line.

### Accuracy review (§A step 3–4)
- `review_accuracy.py --range 17311 17560` — 249 reviewed, 82 flagged (90 issues: 2 gloss, 88 tags).
- **APPLIED — semantic tag migration (56 entries / 76 invalid tags).** Ground-truth scan found all
  current semantic tags not in `VALID_SEMANTIC` and migrated each to the reviewer's suggested / best
  in-list tag (or dropped invalid duplicates). Examples: medicine→(drop, health kept), culture-tradition→culture,
  stationery→tool, social-interaction→society, body-sensation→(drop/health), commerce→business,
  writing→communication/language, place→building, degree→quantity, perception→cognition,
  behavior→action, governance→politics, sun→(drop, weather kept). All 56 revalidate clean;
  `validate_tags --check-no-new-unknown` passes. `modified` timestamps updated.
- **REJECTED** (aggregated §C lines):
  - 22 × "general is too broad → [specific]" — in-list narrowness nits (policy: reject in-list substitutions; `general` kept).
  - 8 × in-list wrong-category/removal substitutions — verified defensible, kept.
  - 3 × formality flags (17332, 17533, 17547) — each entry's own notes describe it as formal/literary,
    so the `formal` label stands (17533 notes: "a formal compound used in official explanations").
  - 2 × gloss (17456 suggestion == current gloss; 17481 "three days ago" is correct for さきおととい).

### §4 self-verification
- Mode = accuracy-review, so per §4's rule the §A cross-model review **is** the independent check for
  these IDs (fixes migrate to the reviewer's own suggested/best in-list tags). All migrated entries
  validate clean; no separate re-review spent. Clean.

## Always-on capture (§3)
- Candidates: none added (tag-only pass; no un-entried words encountered in deep reading).
- Observations: appended `[pattern]` (dense off-vocab tag block in ~17374–17560, suggests a systemic
  tag-drift sweep of the 17000s) and `[tooling]` (furigana screener 0% precision on polished range;
  could suppress flags when stored reading already matches the "should be" full reading).

## Ledger / metrics
- OpenRouter spend this run: $0.0290 (screening) + $0.1087 (accuracy) = ~$0.14; ledger day total $0.2186.
- Metrics snapshot appended (entries_changed 56, applied 56, rejected 72, to_curator 0).

## Cursors
- `polishing/tasks/cross-model-review/progress.txt` → `next: 17561`.
- `reviews/queue.txt`: pruned 17311–17560 (12892 → 12748 lines).
- Selector already advanced `pipeline/routine-state.json` in §1.

**Next accuracy-review start: 17561.**
