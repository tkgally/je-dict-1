# Routine v2 session — 2026-07-26 #007

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Branch**: `claude/determined-rubin-4wpwyh`

**Signals**: candidates 1042, seen-in-entry 11, max entry ID 30144, comprehensive_next 6650,
cross_model_review_next 19179, **unharvested observations 33**, reviewed entries 586,
open backlog items 7. OpenRouter: $0.4564 spent today of $5.00 cap ($4.5436 remaining).

## §0 Pre-flight

- §0a `mcp__github__list_pull_requests` → **0 open PRs**. Nothing to rescue, nothing to sweep.
- Branch level with `origin/main` (0 ahead / 0 behind) after fetch.
- `routine_lock.py acquire` → ACQUIRED.

## §2 Work performed (wiki mode — `planning/maintain-knowledge-base.md`)

Skipped that prompt's own pre-flight sweep (already done in §0).

**Harvest (activity F)** — all **29** loose observations cleared from `polishing/observations.md`,
from three 2026-07-26 routine polish runs, the 2026-07-25 new-entries run, two accuracy-review
sweeps (18346–18652, 18653–19200), and a 21000–21999 systemic-fix sweep.

Two items were promoted from repeated anecdote to **measurement** by running the scans they implied:

1. **Dead inline links** (the scan Tooling 11 has requested since 2026-06-10). Dictionary-wide:
   **292 dead links / 144 distinct bad target IDs / 160 entries** out of 262,189 inline links
   (0.11%). **217 (74%) are unambiguous 1:1 repairs** via `word_id_lookup.json`; 74 are ambiguous
   homographs; 1 (逆転する) has no target. All 292 fall in **00000–07999** — the inline-link-polished
   band, densest at 05000–05999 (112) — so the population is **iatrogenic**, created by polishing
   rather than by entry creation. The failure mode is **number invention, not word confusion**
   (`00433_miru`→`00283_miru`). Filed as **new Cleanup P27**; queued `blocked` on the `validate.py`
   gate, since repairing before the gate ships would refill.
2. **Notes-scorer bug #1** — `has_bare_kanji` counts inline-link base forms as bare kanji,
   mis-scoring **6,351 of 6,555 entries (96.9%)** whose notes contain links (~21% of the
   dictionary), hardest on the most-polished entries. `priority/notes.txt` is therefore **inverted
   with respect to link coverage** — the mechanical explanation for the ~68-run no-op streak. With
   the lane running 5/5 productive for a second consecutive run, the disposition flips to
   **fix the scorer, keep the lane**.

**Other filings**: new Tooling **36** (`verify_furigana.py` blind to same-session entries) and **37**
(copula `で` mislinked to the particle); count-sharpened updates to Tooling **17** (95 `general`
noise instances in one sweep), **24** (two more 100%-FP screens, both families decidable without a
model), **26** (9/9 formality FPs), **21** (throughput + cursor-desync), **23** (three judgment-free
purge rules; seen-in lane self-feeding ~10 in / 9 out), **8**, **34** (third consecutive cycle);
Cleanup **P20** (cohort to 19200; deterministic scan found 51 where the model found 32), **P21**
(band unbroken ~06150→06649 + `・` bullets), **P9** (~2× density above 21000), **P17**, **P18**;
entry follow-ups **18554 訓読**, **00475/00765/02640 やさしい**, **04467_shichou**.

**Activity E**: marked the duplicate 気持ち filing in `entry-followups.md`; synced
`backlog-queue.json` (23 items; verified `routine_next.py --explain` still reports 7 open
batch-ready items, i.e. the `blocked` status is correctly skipped); four "Last updated" headers
refreshed.

**Activity A**: `project/overview.md` counts refreshed (29,935 / 27,132 general / ~116,900 examples
/ ~16,600 cross-references).

**Activity H**: **not due** — 362 metrics lines vs the 353 covered by `topics/quality-metrics.md`
= 9 new, below the ≥10 threshold.

## §3 Always-on capture

No new candidates. A `wiki` run reads no entries, and every Japanese word appearing in the
harvested observations was already captured by the run that logged it.

## §4 Self-verification

**Skipped — no entries created or modified** (markdown only). OpenRouter untouched; **$0 spent this
run**; ledger unchanged at $0.4564/$5.00.

## §5 Metrics

Appended: `mode=wiki, entries_changed=0, entries_total=29935, candidates=1042,
review_queue_len=12447, observations_lines=486 (was 531), comprehensive_next=6650`.

## §7 Wrap-up

- No cursor advance (wiki mode owns none); `pipeline/routine-state.json` advanced by the selector.
- **No `make build`** — markdown-only changes, per §6.
- Files changed: `planning/wiki/ideas/{cleanup-backlog,tooling-backlog,entry-followups,backlog-queue.json}`,
  `planning/wiki/project/overview.md`, `planning/wiki/log.md`, `polishing/observations.md`,
  `pipeline/{routine-state.json,metrics-history.jsonl}`, this log.

**Routed to the curator** (a `wiki` run may not touch prompts, skills, entries, or build scripts):
the one-line `score_note_quality.py` fix; the `validate.py` link-resolution gate that P27 is blocked
on; `prompts/comprehensive_polish.md:107`'s two schema-invalid cross-reference types (third
consecutive cycle — the 2026-07-25 misattribution to `CLAUDE.md` re-verified false this run); and
two `inline-word-links` skill rules (copula `で`, bound morphemes).
