# Routine v2 session — 2026-07-30 (004)

**Mode**: `polish`
**Selector reason**: "polish: highest scheduler debt among eligible modes"
**Params**: `start_id: 6694`
**Signals**: candidates 1,024 (15 `seen in entry`), max entry ID 30,240, comprehensive_next 6694,
cross-model-review next 22,072, unharvested observations 5, reviewed entries 586, open backlog items 11
**OpenRouter**: $0.33 spent today, $4.67 remaining at start

## Pre-flight

- **§0a rescue — one PR rescued.** #3070 (`claude/determined-rubin-epkyjv`,
  "routine(wiki): correct session-log CI section…") was open with `validate` **completed / success**,
  `mergeable_state: clean`, and no human comment or review — the only review on it was the
  `chatgpt-codex-connector` bot with no suggestions, which is not a curator claim. Squash-merged as
  `4cde51e`, then `git merge origin/main` fast-forwarded this branch onto it, so this run builds on
  the rescued work. This is exactly the strand §0a exists for: the previous run did the work and
  earned green CI but never got to the merge call.
- **§0b sweep**: the rescued PR was the only open one, so nothing was left to supersede.
- Lock acquired for `claude/determined-rubin-h3p9dr`.

## Priority lane — `polishing/priority/notes.txt` lines 74–81 (file generated 2026-07-28, fresh)

All 8 lines in range were eligible (none `modified` within 30 days) and **all 8 needed changes**, so
the rankings are not stale and priorities were **not** regenerated. Cursor advances to line 82.

- **01201 苦い**: notes opened by restating the POS and gloss and then re-listed the negative /
  te-form / past that the `conjugation` field already holds. Rewritten around what the table cannot
  say: the two noun forms (苦さ vs 苦味), the fixed 苦い顔をする (which says nothing about taste), and
  the taste-adjective set. Fixed a wrong link base form (`→苦み` → `→苦味`, entry 14101's actual
  headword) and linked the naked をする. Added `see_also` 02346 すっぱい and `related` 14101 苦味;
  back-linked both.
- **00269 稀**: the note claimed "Common in both spoken and written Japanese" while the entry's own
  tags read `formality: formal`, `style: ["written"]` — a self-contradiction. Resolved in favour of
  the tags (稀 is news/official/technical register) and made the register section say where it
  actually appears, with 珍しい/めったに as the conversational alternatives. Sections consolidated
  6 → 3. Added a たまに contrast (ordinary-low vs exceptional frequency). Five examples had a naked
  に; linked to match the notes' own convention. **Furigana fix**: the variant-kanji line was bare
  `{希}` → `{希|まれ}` (the entry had been failing `verify_furigana.py`). Added `related` 03806
  めったに and `contrast` 01276 たまに; back-linked both.
- **06702 打ち切る**: **zero inline links across all 10 examples** — full tier-1 coverage added.
  Notes rewritten: dropped the duplicated conjugation list, `・` → `- `, and added the point the
  entry was missing — the decision comes from an authority, so the passive 打ち切られる is at least
  as common as the active, and the noun 打ち切り is what headlines use. Added `related` 08296 打ち切り
  (the entry's own RELATED NOUN, unlinked and uncross-referenced); back-linked. Queued 時間切れ.
- **06860 見せびらかす**: notes were 471 chars with no word-choice guidance for a word whose whole
  point is register. Added SIMILAR WORDS (自慢する = boasting in words vs 見せびらかす = boasting by
  putting the thing in front of people; 誇る is neutral-to-positive; 見栄 is the motivating vanity)
  and sharpened REGISTER. Reworded FORMATION so it no longer asserts びらかす as a free-standing
  word while keeping the `noentry` marker. Added `related` 14180 誇る and 06626 見栄; back-linked both.
- **07004 呼び戻す**: **zero inline links across all 10 examples** — full coverage added. Notes
  rewritten: dropped the duplicated conjugation list, replaced the 呼び返す `noentry` line (a
  marginal word) with the two contrasts learners actually need — 呼び出す summons someone *to* you,
  取り戻す is what you recover for yourself — and recorded that in sense 2 the subject is typically
  an inanimate trigger (a photo, a song, a scent). Added `related` 04813 呼び出す and 04223 取り戻す;
  back-linked 04813. Queued 青春時代, 本国.
- **00463 分かる**: added the contrast the entry's lone 知る cross-reference implied but never
  explained (知る = taking in information, 分かる = grasping meaning; 知りません can sound curt where
  分かりません is safe; 理解する is the formal counterpart), and explained *why* が rather than を.
  **Corrected a factual error**: the variant spellings were written `{解|わ}かる`/`{判|わ}かる`, which
  render as 解かる/判かる — the real variants are 解る/判る. Added `related` 03674 理解.
- **00477 書く**: folded the trailing unlabelled grab-bag paragraph into proper sections and added
  the 書く/描く distinction that def 2 gestures at but the notes never resolved (絵を描く is the
  conventional spelling; 描く read えがく is the literary one; 記入する is the form-filling register).
  Added `related` 28436 描く; back-linked.
- **00481 作る**: same trailing-grab-bag shape. Split into a real KANJI CHOICE section (作る general,
  造る large-scale/brewed, 創る for original creation) and SIMILAR WORDS (建てる for buildings,
  作成する as the business register of sense 2). Added `related` 01189 建てる and 04186 作成. Queued 創る.

## Frontier lane — 06694 through 06697 (4 entries)

Same create-era signature as 06688–06693 last run: **no inline links anywhere**, `・` bullets, and
in three of four cases a sole semantic tag from an adjacent-sounding category.

- **06694 電子マネー**: full link coverage. Notes had four brand lists (11 product names) and little
  vocabulary; trimmed to one prose line and kept the linkable terms, plus the fact that matters to a
  learner — transport IC cards work at shop tills, so 電子マネー in conversation means tapping a card
  or phone, not an online transfer. `semantic` `["electronics"]` → `["money","technology"]`. Added
  `related` 06096 キャッシュレス and 05466 決済; back-linked 06096.
- **06695 小川**: full link coverage. Notes reorganised into FORMATION / USAGE / BY SIZE (細流 →
  小川 → 川 → 大河) and the surname point promoted into the usage section, since a bare 小川 in a
  text may be a person. `semantic` `["general"]` → `["geography","nature"]`. Added `related` 12197
  大河 and 10358 せせらぎ; back-linked 10358. Queued 水遊び, 細流.
- **06696 トレンド**: full link coverage. Examples were interleaved across senses; reordered to
  sense 1 then sense 2, each short → long. Notes gained the 流行 comparison (流行 = a fashion
  actually spreading; トレンド = the analyst's direction-of-travel word, and in sense 2 the
  platform's own label). `semantic` `["time-general"]` → `["change","society","media"]` — a
  fashion/trending word tagged as a time expression.
- **06697 フェス**: full link coverage. **`formality: formal` directly contradicted the entry's own
  REGISTER section** ("Informal/casual") → `informal`. Notes: dropped the list of four named
  festivals (a maintenance liability, not vocabulary), kept the productive suffix pattern, and added
  that フェス does not replace 祭り for a traditional shrine or town festival. Added `related` 11703
  フェスティバル and `contrast` 03735 祭り; back-linked 11703.

## §4 self-check — clean

The 12 content-changed entries were sent to an independent model
(`review_accuracy.py --ids … --budget 0.40`, glosses / example translations / semantic tags):
**0 issues on all 12**, est. $0.0055, logged to the ledger with `phase: "self-check"`
(day total $0.3355 of the $5.00 cap). No flags, so no `reviews/decisions.jsonl` lines this run.
The other 12 entries touched this run were back-link-only edits (one `cross_references` entry plus
`modified`), which neither §4 bullet covers, so they were not sent.

## Candidates added (7)

- 時間切れ (じかんぎれ) — running out of time, time-up; seen in 06702
- 青春時代 (せいしゅんじだい) — one's youth, adolescent years; seen in 07004
- 本国 (ほんごく) — one's home country, country of origin; seen in 07004
- 創る (つくる) — variant kanji of 作る, for original creation; seen in 00481
- 水遊び (みずあそび) — playing in water; seen in 06695
- 細流 (さいりゅう) — rivulet, streamlet (literary); seen in 06695

## Observations logged

- `[pattern]` the 066xx–070xx create-era signature (no inline links + `・` bullets + notes
  duplicating the `conjugation` field), with a proposed detector for the duplicate-conjugation half
- `[tooling]` a read-only **link suggester** (greedy longest-match over `word_id_lookup.json`,
  proposals + ambiguity list, never writes) — inline linking is ~90% lookup and ~10% judgment, and
  it is the dominant cost of `polish` mode
- `[pattern]` sole-semantic-tag drift is dense in the frontier band too, not only in the 5700–6340
  P11 block (3 of 4 frontier entries), with 06696 `time-general` as the same failure shape as
  朱肉→`animal-mammal`
- `[pattern]` a tag can contradict the entry's own notes with no check seeing it (06697), and the
  check is deterministic
- `[tooling]` **closed by measurement**: wrong okurigana in variant-spelling note lines — grepped
  the corpus after fixing 00463, **0 remaining instances**, so it was a one-off, not a family
- `[tooling]` `cross_references[].reading` is hiragana-only by schema, so back-link helpers must
  transliterate katakana headwords (キャッシュレス → きゃっしゅれす)

## Metrics

`pipeline/metrics_snapshot.py --mode polish --changed 24` → entries_total 30,031, candidates 1,030,
review_queue_len 13,310, observations_lines 580, comprehensive_next 6,698,
openrouter_spent_today_usd 0.3355.

## Next cursors

- `polishing/tasks/comprehensive/progress.txt` → `next: 06698`
- `polishing/tasks/comprehensive/priority-cursor.txt` → `line: 82`
  (line 84, 00550 遊ぶ, will need skipping next run — `modified` 2026-07-25, inside the 30-day window)
