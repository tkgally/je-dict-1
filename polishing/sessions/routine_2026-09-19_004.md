# Routine session — 2026-09-19 #004

**Mode:** accuracy-review (reason: "accuracy-review: highest scheduler debt among eligible modes")
**Range:** entries 04001–05000 (991 entries on disk in range), cursor advances to `next: 05001`

## Review

`review_accuracy.py --range 4001 5000 --budget 2.5` (run in two passes after an
artificial local timeout interrupted the first). 991 entries reviewed, 133
flagged (13.3%, well under the 25% noise threshold) covering 154 issues:
offvocab 12, wrong-category 35, register 22, notes-fact 53, translation-meaning
17, gloss-meaning 15.

## Adjudication

- The 12 `offvocab` semantic-tag flags were migrated directly to the closest
  listed tag (e.g. `medical`→`health`, `economy`→`economics`, `home`→`daily-life`,
  matching precedent from existing entries such as 03544_teiden and
  07486_shinseisho).
- The remaining 142 issues across 121 entries were split into 5 disjoint
  chunks by entry ID and adjudicated by five parallel subagents, each reading
  every flagged entry in full before deciding apply/reject/flag — never
  applying a suggestion blindly. Combined: 91 applied, 62 rejected, 1 flagged
  to curator (04717_tashikani: sense 2 is attributed to 確かに but its notes
  and all three examples actually describe 確か — a possible mis-split that
  needs a curator's call on whether to move it to 00894_tashika or realign
  the examples).
- One additional manual fix during the mechanical pass: 04988_senmendai had
  a notes section headed COMPONENTS (a physical-parts list: basin, faucet,
  mirror, shelf) that `normalize_notes.py` correctly canonicalized to
  ETYMOLOGY per the alias table — but ETYMOLOGY is about word origin, not
  object parts, so re-headed it to RELATED WORDS instead, which fits the
  content ("thematically connected vocabulary") and needs no further
  canonicalization.

87 entries were directly edited; `auto_link.py` and `harvest_crossrefs.py`
then added 47 inline links (9 entries) and 10 cross-reference bullets (7
entries, including reciprocal links on 4 out-of-range neighbours:
02190_kagami, 03243_shoumei, 22083_suisen, 30868_aibii), bringing the total
changed-entry count to 92. All 92 pass `build/validate.py`.

## Self-check

Per §A step 8, the 88 changed entries inside the reviewed range (4001–5000)
skip the self-check — the accuracy review was itself the check. The 4
out-of-range neighbours (02190, 03243, 22083, 30868), which only picked up a
reciprocal cross-reference bullet, were sent through `review_accuracy.py`
and `review_links.py`: 0 issues, 0 flags on both.

## Ledger

OpenRouter spend today: $1.0230 (accuracy review $0.4845 for 991 entries,
self-check $0.0019 for 4 entries, plus $0.5366 already spent earlier today)
against a $5.00 daily cap.

## Queue and cursor

- `reviews/queue.txt`: removed the 4001–5000 range (5872 → 5280 lines).
- `polishing/tasks/cross-model-review/progress.txt`: `next: 05001`.

## Curator flags

- 04717_tashikani — sense-2 attribution mismatch (確かに vs 確か), logged to
  `reviews/needs_curator.txt`.
