# Routine session — 2026-07-10 (001)

- **Mode**: `systemic-fix`
- **Selector reason**: "systemic-fix: highest scheduler debt among eligible modes"
- **Backlog item**: `furigana-cosmetic-wrappers` (Priority 9 — non-standard but mostly-rendering
  furigana wrappers; filter `subpattern in {o-go-prefix, pure-kana, over-wrapped}`)
- **Range worked**: 09400–09999 slice (next ready slice per backlog notes)

## Changes — 24 instances / 22 entries (detect filtered total 1325 → 1301)

Semantic-verification-first: every flagged entry opened and each rewrap confirmed correct for
that entry before applying.

**pure-kana de-wraps** (wrapper had no kanji; surface kept as-is):
- 09485 `{これ|これ}`→`これ`; 09524 `{パーティー|パーティー}`→`パーティー`;
  09582 `{ニュース|にゅーす}`→`ニュース`; 09616 `{ゴミ|ごみ}`→`ゴミ`;
  09767 `{プログラム|ぷろぐらむ}`→`プログラム`; 09796 `{ファン|ふぁん}`→`ファン`;
  09817 `{オン|おん}`→`オン`; 09819 `{なし|なし}`→`なし`; 09822 `{すぐ|すぐ}`→`すぐ`;
  09830/09832 `{エリア|えりあ}`→`エリア`; 09833 `{クーポン|くーぽん}`→`クーポン`;
  09891 `{ある|ある}`→`ある`
- numerals: 09621 `{30|さんじゅっ}`→`30` (in 30分); 09627 `{1|いち}`→`1` ×2 (ex + notes)

**o/go-prefix repositions** (honorific prefix moved outside the wrapper):
- 09589 `{お金|おかね}`→`お{金|かね}` ×2 (ex + notes); 09643 `{ご来店|ごらいてん}`→`ご{来店|らいてん}`;
  09779 `{お好|この}`→`お{好|この}` (お好み焼き); 09882 `{お伝|つた}`→`お{伝|つた}` (お伝え);
  09885 `{お腹|おなか}`→`お{腹|なか}`

**over-wrapped okurigana splits**:
- 09745 `{日替り|ひがわり}`→`{日|ひ}{替|がわ}り` (KANJI-NOTE alt spelling; 替=がわ, り okurigana)
- 09867 `{警備する|けいびする}`→`{警備|けいび}する` (する verb suffix outside wrapper)

**Inline-link safety**: three pure-kana de-wraps sat inside `⟦…⟧` links carrying explicit
`entry_id`s (パーティー→02946_paatii, ニュース→04437_nyuusu, プログラム→01540_puroguramu). These
resolve by ID regardless of the display surface, so de-wrapping the surface is safe.

All 22 entries had their `metadata.modified` bumped to 2026-07-10T00:20:58Z.

## §4 self-verification (furigana screener) — CLEAN

`review_runner.py --pass screening` on all 22 changed IDs: 10 screened, **0 flags**, 12 skipped
(kana/loanword entries carry no ruby to screen). Est. cost $0.0012, logged to the ledger with
`phase: "self-check"`. No decisions to adjudicate → no `reviews/decisions.jsonl` line.

## Verification

- `validate.py --range 9400 9999`: 595/595 valid.
- Detector recheck on the filtered slice: 0 remaining instances in 09400–09999.
- Dictionary-wide filtered total 1325 → 1301.

## Always-on capture

- Candidates: none added (all touched words already have entries).
- Observations: none new (the recurring family is already fully tracked in the Priority-9 backlog).

## Bookkeeping / cursors

- `planning/wiki/ideas/backlog-queue.json`: `furigana-cosmetic-wrappers` scope_estimate 1325 → 1301,
  notes appended with this slice, top-level `updated` → 2026-07-10. Item stays `open` (ongoing).
- `planning/wiki/ideas/cleanup-backlog.md` Priority 9: progress-log entry appended.
- OpenRouter ledger: self-check $0.0012 → daily total $0.0012 / $5.00.
- Metrics snapshot appended (systemic-fix, 22 changed).
- **Next ready slice: 10000+** and the larger pre-06000 backlog.

## Pre-flight note

- §0a: one open routine PR (#2905, `claude/modest-keller-50e61p`, accuracy-review 13300–13334).
  Its only check-run concluded `cancelled` (not green) → not rescuable. Left open.
- §0b: not superseded — its max entry ID (13334) is far above the comprehensive frontier cutoff
  (06438), so the strand-sweep rule leaves it open. Flagged here for curator awareness.
