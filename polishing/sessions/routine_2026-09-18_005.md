# Routine session 2026-09-18 #005

**Mode**: new-entries
**Reason**: new-entries had the highest scheduler debt among eligible modes.
**Params**: internal-closure preferred, approx_count 20

## Pre-flight

No open PRs to rescue or absorb (`list_pull_requests` returned none). `list_branches`
returned only `main` — no orphan `claude/*` branches to sweep. Synced this session's
branch with `origin/main` before starting.

## Work done

Listed the 34 candidates whose notes said "seen in entry" or "used in" (the internal-closure
lane). Batch duplicate-checked all 34 (`check_duplicate.py --batch --skip-candidates`) — all
came back OK, with one duplicate found on inspection (see below).

Created **21 general-tier entries** (IDs 30953–30973), all from that lane: {三峯|みつみね}
(Mitsumine Shrine, Saitama — wolf worship, from 04334 {狼|おおかみ}); a cardigan-cut trio
(ロングカーディガン, ショートカーディガン, ボタンレスカーディガン) from 04520 カーディガン;
a scanner-type trio (フラットベッドスキャナー, ハンディスキャナー, ドキュメントスキャナー)
from 05248 スキャナー; the steak-doneness scale (ミディアムレア, ミディアム, ウェルダン) plus
two cuts (サーロインステーキ, フィレステーキ) from 01369 ステーキ; {能楽堂|のうがくどう} from
01927 {能|のう}; a screen/display cluster (プロジェクター, スクリーンセーバー, ワイドスクリーン,
マルチスクリーン) from 01368 スクリーン; {噛|か}み{傷|きず} from 07340 {切|き}り{傷|きず};
{五位鷺|ごいさぎ} from 04939 {鷺|さぎ}; and a fabric/trim pair (シフォン, フリル) from 04517
ブラウス. One new kanji indexed: {峯|みね} (peak), added to `kanji/kanji_list.json` as
`02804_hou_mine_peak`.

**Caught one romaji error before wrap-up.** `validate.py` flagged 30973's ID
(`30973_firiru`) as not matching its reading ふりる (expected `furiru`, not `firiru` — I'd
confused it with フィレ's "fi"). Renamed the file and fixed the `romaji`/`id` fields; no other
files referenced the wrong ID yet, so no further cleanup was needed.

**One candidate dropped as a duplicate.** {蝦蟇|がま} (toad, C23481) is the same word as the
existing entry 28705 ガマ, whose own notes already document {蝦蟇|がま} as an alternate kanji
spelling — removed from the queue instead of writing a duplicate entry.

**Four candidates set aside for editorial reasons, left in the queue.** {大口真神|おおくちのまかみ}
(a wolf deity — no clean semantic-tag category fits "deity," so I deferred rather than force a
mismatched tag); {師|し} and {両生|りょうせい} (bound morphemes rarely used standalone — too thin
for natural example sentences at this dictionary's standard); {藤田|ふじた} and {藤井|ふじい}
(generic common surnames with no specific cultural referent — the dictionary's existing
person-name entries are all specific historical/cultural figures, e.g. 夏目漱石, 松尾芭蕉, so
these didn't fit the established pattern). None of these are wrong to write up eventually; they
just needed a judgment call this run's budget didn't leave room to make well.

## Post-creation sequence

- `validate.py` (full): caught and I fixed the one romaji mismatch above; final run
  30764/30764 valid, no new warnings.
- `find_missing_furigana.py`: nothing in the new IDs.
- `add_conjugations.py` / `add_adjective_conjugations.py`: no-ops (no verbs or i-adjectives
  created this run).
- `update_indexes.py`: 21 candidates auto-cleared; queue 209 → 188.
- `update_kanji_index.py --check-new`: found 峯, assigned `02804_hou_mine_peak`, reran clean.
- `normalize_notes.py --apply`: 0 rewrites needed.
- `auto_link.py --apply --confirm-real-entries`: 398 links added across all 21 entries
  (19.0/entry, 77.3% of content tokens linked).
- `harvest_crossrefs.py --apply`: 8 proposals accepted (5 direct + 3 reciprocal) — new
  `related` links for 30966 プロジェクター↔01368 スクリーン, 30970 噛み傷↔07340 切り傷, and
  30972 シフォン↔30973 フリル / 30972 シフォン↔04517 ブラウス.
- `check_stale_noentry.py --class A1 A2`: one hit, class A2 — 03853 セット's example 9 had a
  `プロジェクター→noentry` marker in an unambiguous "set up the projector for tomorrow's
  meeting" sentence. Resolved to `30966_purojekutaa`.
- `check_link_newcomers.py --since 2026-09-18`: none.
- `check_link_homophones.py --unscreened`: two unrelated pre-existing bases (おたまじゃくし,
  ぼさぼさ) turned up; screened both with `review_links.py --screen` — both came back `unique`
  (no action needed).

## §4 self-check

Ran `review_accuracy.py` on all 22 touched entries (21 new + 03853, the noentry neighbor).
5 flags, 1 applied, 4 rejected:

- **Applied**: 03853 セット carried leftover `geography` and `leisure` semantic tags that
  fit none of its three senses (a set of items, a sports set, the act of setting up) —
  replaced with `general` and `sports`.
- **Rejected (3, same cause)**: the reviewer flagged the DONENESS SCALE line in 30960/30961/
  30962's notes as missing steps. It misread the inline-link arrow markup (`⟦…→…⟧`) that
  `auto_link.py` had wrapped around ミディアムレア/ミディアム/ウェルダン inside that line —
  all four doneness levels are present in every entry; confirmed by reading the raw JSON.
- **Rejected (1)**: 30972 シフォン's `clothing` semantic tag (for a fabric name, not a garment)
  was flagged as wrong-category. Rejected as consistent with the dictionary's own convention —
  the existing entry ウール (wool, also a fabric) carries the same tag.

In-context kana-link check (`review_links.py --skip-decided`): 3 links reviewed, 0 flagged.

## Checks

`make gate`: clean (unit tests, full validation, ratchet gates, tag/header/link-homophone
gates all passed). `make index` rebuilt `entries_index.json`, `word_id_lookup.json`, and the
kanji index (2804 kanji files, one new).

## OpenRouter spend

$0.0104 self-check + negligible screening cost, logged to the ledger; running total $1.032
against today's $5.00 daily cap across all of today's routine runs.

## Next

`PROJECT_STATUS.md` Recent Changes updated (oldest entry, 2026-09-12, rotated to
`PROJECT_STATUS-archive.md`). Candidate queue: 188 remaining, with 4 internal-closure
candidates set aside above for a future session's judgment call (not more time-sensitive
than the rest of the queue).
