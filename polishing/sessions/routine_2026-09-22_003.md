# Routine session 2026-09-22 003

**Mode**: polish (selector reason: "polish: highest scheduler debt among eligible modes")

## Pre-flight

No open routine PRs to rescue, no red predecessors to absorb, no orphan `claude/*`
branches to sweep. Branch already up to date with `origin/main`.

## Priority lane (cursor started at line 8623)

Scanning from line 8623 to the end of `polishing/priority/notes.txt` (18,859 lines,
the whole remainder of the file) found only 7 entries not modified in the last 30
days — the fifth-plus consecutive run to hit this wall (root cause already on file
in `tooling-backlog.md`; not relogged in full, see `polishing/observations.md`).
Took all 7:

- **04926_panko, 04731_gorufu, 04855_shousou**: already complete and correct; no
  changes needed.
- **05262_zukizuki**: renamed a non-canonical "CONTRAST WITH OTHER PAIN WORDS:"
  header to canonical `SIMILAR WORDS:`.
- **05375_meramera**: merged "LITERAL USE (fire):" / "FIGURATIVE USE (emotions):"
  into one canonical `COMMON COLLOCATIONS:` block (with inline sense labels), and
  renamed "FIRE MIMETIC WORDS (intensity scale):" to `SIMILAR WORDS:`.
- **06920_teiuka**: recategorized "VARIANT FORMS (least to most casual):" — a list
  of separately-entried register variants (というか/っていうか/てか/つーか), not an
  inflectional form — to `SIMILAR WORDS:`, which is what the content actually is.
- **02208_koi**: renamed "CONTRAST WITH ⟦愛⟧:" (a header with a hand-placed link
  inside it) to plain `SIMILAR WORDS:`; the link stays in the bullet where it
  belongs.

Priority cursor wrapped to `line: 1` since the tail is exhausted.

## Frontier lane (08116–08141, next: → 08142)

Read all 26 entries. 25 were already complete, correct, and canonically
structured (this stretch was evidently swept by an earlier systemic/mechanical
pass — most carry a `2026-09-21T12:40:57Z` or `2026-09-02T04:01:xx` modified
timestamp). One real defect:

- **08116_rokku (ロック)**: the entry covered only the "rock (music)" sense, but
  two other entries (28103_oyuwari, 28908_yuwari) already carry `contrast`
  cross-references to it labeled "on the rocks" — a second, common meaning
  (a drink served over ice) that the entry never defined, leaving those
  cross-references pointing at content that wasn't there. Added sense 2
  ("on the rocks") with 3 new examples, updated the top-level gloss, and
  expanded the USAGE note to disambiguate all three same-spelled meanings
  (rock music / on the rocks / to lock).

## Mechanical pass (§3)

`normalize_notes.py`, `auto_link.py --confirm-real-entries`, `harvest_crossrefs.py`
on the 5 changed IDs (02208, 05262, 05375, 06920, 08116). One issue caught and
fixed by hand: `auto_link.py` (running tokenizer-free — SudachiPy unavailable)
mis-split "お湯割り" in 08116's new USAGE text, linking only the "湯割り"
substring to the wrong target (28908 instead of 28103) and leaving "お" bare.
Rewrote the sentence to avoid naming both near-identical compounds together
rather than hand-linking around the bug. Re-ran the pass clean; `validate.py`
and `verify_furigana.py` pass on all 5.

## Self-check (§4)

`review_accuracy.py` on the 5 changed IDs: 1 flag, on 08116's new gloss
("combines two unrelated meanings, confusing for a top-level gloss"). Rejected
— combining distinct senses with parenthetical clarifiers in the top-level
`gloss` field matches existing convention for other 2-sense entries in this
range (08120_shasen, 08128_fuchin, 08125_seigo, 08122_kankyuu).

`review_links.py --skip-decided`: 1 flag, on 06920's example 2 ("な" linked to
だ/09496). Checked the sentence (好きなんだ) — な is the attributive form of だ
before の (んだ), so the link is correct; the model's own reasoning agreed with
this even while flagging it. Logged `keep`.

Ledger: self-check spend ~$0.0035, daily total $0.5315 of $5.00 cap.

## Observations logged

One `[tooling]` entry in `polishing/observations.md`: fifth-plus recurrence of
the 30-day-skip wall thinning the priority lane to near nothing; pointed at the
existing long-form diagnosis in `tooling-backlog.md` rather than repeating it.

## Cursors

- `polishing/tasks/comprehensive/priority-cursor.txt`: `line: 1`
- `polishing/tasks/comprehensive/progress.txt`: `next: 08142`

## Candidates

None added this run (no undefined words surfaced in the entries examined).
