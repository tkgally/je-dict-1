# Routine v2 — 2026-08-07 (005)

**Mode:** `polish`
**Selector reason:** "polish: highest scheduler debt among eligible modes"
**Selector signals:** candidate_count 985, seen_in_entry_count 14, max_entry_id 30473,
comprehensive_next 6813, cross_model_review_next 27314, unharvested_observations 4,
reviewed_entries 586, open_backlog_items 21
**OpenRouter at start:** $0.1657 spent today, $4.8343 remaining of the $5.00 daily cap
**Branch:** `claude/determined-rubin-rltni4`

## Pre-flight (§0) — DEGRADED

**GitHub API was unreachable for this entire run.** Both paths documented in
`CLAUDE.md` failed:

- The GitHub **MCP** tools were not present in the session toolset. The `github`
  MCP server was connected (`ListMcpResourcesTool --server github` returned its
  four UI resources) but no `mcp__github__*` tool was exposed; `ToolSearch` for
  `+github` / `+pull_request` / `select:mcp__github__list_pull_requests` all
  returned nothing, and a direct call to `mcp__github__list_pull_requests`
  returned `No such tool available`.
- Direct GitHub **REST** returned the expected policy 403:
  `GET https://api.github.com/repos/tkgally/je-dict-1/pulls` →
  `403 {"message":"GitHub access is not enabled for this session. An org admin
  must connect the Claude GitHub App for this organization."}`

Consequences, all recorded honestly rather than worked around:

- **§0a rescue was not performed.** No open PR could be listed, so if a previous
  run left a green PR stranded, it is still stranded.
- **§0b stranded-PR sweep was not performed**, for the same reason.
- **§7 PR / CI / merge was not performed.** The work is committed and pushed to
  `claude/determined-rubin-rltni4`; **no PR exists and nothing was merged**.
  `polishing/tasks/comprehensive/progress.txt` on `main` therefore still reads
  `next: 06813`, so the next Routine run will re-select this same frontier range
  unless the curator merges this branch first.

The lock *was* acquired and released normally, and `git push` works (the git
remote goes through the agent proxy with credential injection — it is only the
GitHub API that is blocked), so this is specifically an API-access outage, not a
network outage.

## Range worked

**Priority lane** (`polishing/priority/notes.txt`, generated 2026-07-28, 10 days
old → fresh): lines 253–260, 8 entries. None were skipped for recency (all last
modified May–June 2026).

**Frontier lane** (`params.start_id` 6813): 06813–06819, 7 entries.

**15 entries total.** All 8 priority-lane entries needed changes, so the priority
rankings are **not** stale and were **not** regenerated; the cursor advances
normally.

## Per-entry changes

### Priority lane

- **01080 {気|き}をつけて** — `politeness` was `polite`, but the entry's own
  FORMALITY section lists bare {気|き}をつけて as casual and 〜ください as the polite
  form; corrected to `plain`. Added the two cross-references the notes already
  discussed in prose: `related` → 09771_kiwotsukeru (base verb), `related` →
  01029_ittekimasu (farewell pair).
- **01332 ひげ** — stale `noentry`: `キャラクター` now exists, relinked to
  23518_kyarakutaa. Completed inline coverage in ex11, which had two naked runs:
  `なので` → `な⟦ので：09501_node⟧`, and `はいけない` → `⟦は：00079_ha⟧⟦いけない：02335_ikenai⟧`.
  Added `related` → 02196_kami (髪) and 01392_ke (毛).
- **01385 {気持ち|きもち}** — added a CONTRAST WITH {気分|きぶん} section to the notes
  (気持ち is directed at an object; 気分 is an objectless general state — and the
  {気分|きぶん}が悪い / 気持ち悪い asymmetry, which is the trap for learners), with full
  inline link coverage. Added `contrast` → 01306_kibun, `related` → 09583_kimochiii,
  `related` → 01321_kokoro. (This entry was refiled as an Entry Follow-up by the
  2026-08-06 wiki run; the cross-reference half is now done.)
- **01393 {警察|けいさつ}** — `semantic` was `["building"]`, which describes the
  secondary "police station" reading rather than the headword's primary sense
  (the police force); corrected to `["law", "society"]`. Added `related` →
  05056_keisatsukan, 01078_keisatsusho, 02879_kouban, all three of which the
  notes already listed under RELATED TERMS.
- **01447 {世界|せかい}** — two stale `noentry` markers fixed: `{世界|せかい}⟦{中|じゅう}→中：noentry⟧`
  was already linked correctly as a compound in ex4, so both notes occurrences now
  use `⟦{世界|せかい}{中|じゅう}→世界中：10171_sekaijuu⟧`; and `⟦{全|ぜん}→全：noentry⟧` →
  `⟦{全|ぜん}→全〜：28337_zen⟧`. Added `related` → 10171_sekaijuu, 03461_chikyuu,
  01427_shakai.
- **01606 {字|じ}** — `semantic` `["communication"]` → `["language", "communication"]`.
  Added `contrast` → 03632_moji (the 字/文字 register pair the notes describe),
  `related` → 09480_kanji, 11225_roomaji.
- **01706 {胃|い}** — added `contrast` → 00805_onaka (organ vs. conversational
  "tummy", already contrasted in the notes), `related` → 15503_ichou, 26782_igusuri.
  Left the `{胃癌|いがん}：noentry` marker in place (verified: still no entry) and
  filed it as a candidate.
- **01745 {居間|いま}** — completed inline coverage in the WORD FORMATION section,
  which had two naked morphemes: `{居|い}` → 00495_iru, `{間|ま}` → `noentry`
  (00914_aida is 間/あいだ, a different reading, so it is not a match). Stale
  `noentry`: `リビングルーム` → 28876_ribinguruumu. Added `synonym` → 11198_ribingu
  and 28876_ribinguruumu, `related` → 10660_chanoma.

### Frontier lane (all seven had **zero** inline links — Cleanup P43 block)

- **06813 {妄想|もうそう}** — full inline coverage across 6 examples and the notes
  (~30 links). Fixed a typo in a notes heading: "CAUSAL USAGE" → "CASUAL USAGE".
  Added `medical` to `domain` (sense 1 is explicitly the clinical/psychiatric
  sense; only `internet` was tagged).
- **06814 {痺|しび}れる** — full inline coverage across 10 examples and the notes
  (~55 links). `formality` was `informal`, which mislabels the whole entry for the
  sake of sense 2: sense 1 (a limb going numb) is an ordinary neutral word, and the
  `style: ["slang"]` tag plus the SLANG USAGE note already carry the sense-2
  information; set to `neutral`. `ラスト` in ex9 marked `noentry` (ギター, ソロ and
  シーン all resolved; ラスト did not).
- **06815 かぶれる** — full inline coverage across 10 examples and the notes
  (~60 links). Added the missing `cross_references` (`related` → 04645_arerugii,
  03987_hifu) and `prominent_see_also` → 25271_kabure (noun form), mirroring how
  06814 links 07276_shibire.
- **06816 ガチで** — full inline coverage. Filled in the missing `target_id` on the
  本気で cross-reference (→ 03153_honki, relabelled "standard equivalent (本気 + で)").
  `{勢|ぜい}` marked `noentry`.
- **06817 {遺憾|いかん}** — full inline coverage across 4 examples and the notes.
  `{意|い}` marked `noentry`.
- **06818 {灰汁|あく}{抜|ぬ}き** — full inline coverage. Added `cross_references`
  (the field was absent): `related` → 13457_aku, the harshness the technique
  removes. `{蕨|わらび}` marked `noentry`.
- **06819 {宣告|せんこく}** — full inline coverage across 6 examples and the notes
  (~45 links).

## §4 self-check (independent model, `review_accuracy.py`)

15 entries reviewed, **4 flagged, 4 applied, 0 rejected, 0 escalated**. Est. cost
**$0.0067**; ledger now at $0.1724 of the $5.00 daily cap. All four flags were
`error` severity and all four were correct:

- **01606** (translation) — ex1 `{字|じ}が{読|よ}めない。` was glossed "I can't read
  the character," but the Japanese has no demonstrative. → "I can't read the
  characters."
- **01745** (tags) — `formality: "formal"` on 居間. The model called it an ordinary
  neutral word; that matched my own reading, which I had suppressed during the
  pass only because the entry's REGISTER note asserted "slightly formal or
  literary." Applied: `formality` → `neutral`, dropped `style: ["literary"]`, and
  rewrote the REGISTER note so the prose and the tags agree.
- **06813** (tags) — `emotion` is wrong for 妄想, which names a cognitive state, not
  a feeling. → `["cognition"]`.
- **06815** (tags) — `["health"]` covered only sense 1. → added `cognition` for the
  "infatuated with / influenced by" sense.

Worth recording: three of the four were tag flags, and the 01745 one caught a case
where **the entry's own note had been talking the tag into staying wrong**.

All four decisions logged to `reviews/decisions.jsonl` (`src: "self-check"`).
One verification pass, one fix round, no re-verification.

## Candidates added

- 百十番 (ひゃくとおばん) — 110, the police emergency number; seen in 01393
- 胃癌 (いがん) — stomach cancer; seen in 01706
- ラスト (らすと) — last, ending, finale (loanword); seen in 06814
- 膨らませる (ふくらませる) — to inflate/swell, transitive of 膨らむ; seen in 06813
- 意 (い) — intention, meaning, will (formal, as in 遺憾の意); seen in 06817
- 勢 (ぜい) — suffix: group committed to something (ガチ勢); seen in 06816
- 蕨 (わらび) — bracken fern; seen in 06818
- 米ぬか (こめぬか) — rice bran; seen in 06818

## Observations logged

- `[tooling]` Stale `noentry` markers are a detectable family (3 of 8 priority
  entries this run); includes the `全`/`全〜` tilde-mismatch trap for the lookup.
- `[pattern]` The `notes` priority list keeps surfacing entries whose real defect
  is empty `cross_references` — 8 of 8 this run, with the neighbours already named
  in the notes prose and already carrying resolvable inline-link target IDs.
- `[entry]` 00823_wakai / 01525_wakai look like a duplicate pair (same headword,
  reading, and gloss).
- `[pattern]` Cleanup P43 confirmed at the frontier: 7 of 7 entries zero-link,
  ~4× the cost of a normal frontier entry; batched lookup + scripted whole-file
  write is what makes it affordable.

## Validation

- `build/validate.py` clean on every touched entry and on ranges 1000–1400 and
  1400–1800.
- `build/validate_tags.py --check-no-new-unknown` → **no new off-vocabulary
  semantic tags** (every tag used here — `law`, `society`, `language`,
  `cognition`, `medical` — is in `VALID_SEMANTIC` / `VALID_DOMAIN`).
- Pre-existing repo-wide warnings (the 11,582 count, and the three
  `verb_class`/`verb-irregular` mismatches in 18394/19600/20377) are untouched by
  this run.

## Next cursors

- `polishing/tasks/comprehensive/progress.txt` → `next: 06820`
- `polishing/tasks/comprehensive/priority-cursor.txt` → `line: 261`
