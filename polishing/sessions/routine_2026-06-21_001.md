# Routine session — 2026-06-21 #001

**Mode:** `accuracy-review` (selector reason: "highest scheduler debt among eligible modes")
**Params:** phase=furigana, start_id=7815, openrouter_session_budget_usd=2.50
**Range worked:** 7815–8037 (223 entries; the cross-model furigana screening pass was killed by its 25-min timeout at ~8038, leaving a clean sub-range)

## Pre-flight
- §0a: no open `routine` PRs to rescue.
- §0b: `sweep-stranded-prs.py` closed 0; routine lock acquired.

## Furigana correctness (§A step 2)
- Screening (`review_runner.py --pass screening`) over 7815–8037: 223 screened, **33 flagged**.
- Adjudicated all 33 by inspecting the actual entry files: **every flag a false positive** (truncated-reading rendering artifact in the screener prompt; rendaku in compounds かすれ声/覚え書き/目覚まし時計; okurigana splits; deliberate dual-reading docs 粗利 あらり/そり). Per §A known-noise shortcut: bulk-rejected (1 aggregated decision line, n=33), **deep pass skipped**. ~0% precision, consistent with the 2026-06-10/11 calibration.

## Accuracy (gloss/translation/tags) (§A step 3–4)
- `review_accuracy.py --range 7815 8037`: 223 reviewed, 180 flagged (81% → predominantly reviewer noise / systemic tag drift, not per-entry error).
- **APPLIED: 43** entries — migrated out-of-taxonomy semantic tags flagged at `error` severity to valid in-list tags (architecture/house→building, social→communication, 'change of state'→change, speech→communication, economy→economics, progress→change, effort→action, etc.). All chosen targets verified against `VALID_SEMANTIC`; `validate.py --range 7815 8037` = 223/223 valid; 0 invalid tags remain in the 43.
- **REJECTED** (per §C policy): 07884 gloss (sense-2 "hold sway" is a genuine 憚る sense; model misread); 07839 translation ("wrinkly" is natural English for bath-soaked fingers); 07876 finance (in-list too-narrow nit); 07898 & 07932 formality formal→neutral (entry notes do not contradict the label); a "missing-tag on tagless entries" family (n=17, completeness not drift); and a warn-severity in-list narrowness/stylistic family (n=118).

## §4 self-verification
- Re-reviewed the 43 changed entries (`review_accuracy.py --ids … --dimensions tags`): 43 reviewed, **8 flagged, 0 applied** — all 8 were in-list narrowness (6) or formality (2) nits → rejected (one fix round, stop). Net self-check: **clean** (changes held up).

## Systemic finding (routed to systemic-fix)
- Deterministic scan: **163/223 entries (73%)** in 7815–8037 carry invalid semantic tags. Fixed the 43 error-flagged ones; **120 remain**. Logged a `[tooling]` observation recommending expansion of `check_tag_drift.py`'s `TAG_MIGRATION` map + a deterministic sweep over this (and the adjacent 7000–8500 creation cohort). This is systemic-fix scope, not a single accuracy-review run (§6 context discipline).

## Spend / state
- OpenRouter ledger today: **$0.2246** (furigana screening $0.11, accuracy $0.0969, self-check $0.0177) — well under the $2.50 session cap and $5/day cap.
- Cursor: `cross-model-review/progress.txt` → `next: 08038`.
- Queue: removed 7815–8037 (142 lines) from `reviews/queue.txt`.
- Metrics line appended; 53 decision lines appended to `reviews/decisions.jsonl` (43 apply + 10 reject/aggregated).

**Next un-reviewed entry:** 08038.
