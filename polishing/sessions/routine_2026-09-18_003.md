# Routine session 2026-09-18 #003

**Mode**: systemic-fix
**Reason**: systemic-fix had the highest scheduler debt among eligible modes.
**Backlog item**: `transitivity-review-queue` (reviews/transitivity/disagreements.jsonl)

## Pre-flight

No open PRs to rescue or absorb (`list_pull_requests` returned none). `list_branches`
returned only `main` — no orphan `claude/*` branches to sweep. Branch was already
up to date with `origin/main` (no divergence).

## Work done

Worked the transitivity disagreement queue top to bottom, 100 entries (12100_henkaku
through 14172_kyoyou). For each: read the gloss, part of speech, and examples, judged
the word's own valency (whether it names an action with a genuine patient/theme, not
just whichever surface particle happens to appear in one example) rather than
trusting either model's verdict outright, and set `metadata.tags.transitivity` to
`transitive` / `intransitive` / `both`. As in recent runs, this batch was almost
entirely two-character kango + する nouns (変革, 展望, 管轄, 敗北, 潜在, etc.) with
no loanword/slang mix this time; no real 自他 (transitive/intransitive) pairs turned
up needing `prominent_see_also` — none of the 100 disagreement lines carried a
`pairs` hint, and none of the words have a native paired counterpart in the
dictionary.

Removed all 100 resolved lines from `reviews/transitivity/disagreements.jsonl`
(1984 → 1884 remaining). Updated the backlog item's `scope_estimate` in
`planning/wiki/ideas/backlog-queue.json`.

## Mechanical pass (§3)

- `normalize_notes.py`: 0 rewrites. 3 unknown headers found (SENSE DISTINCTION,
  MODERN USAGE, EXAMPLES IN USE), all pre-existing, not introduced by this run.
- `auto_link.py`: 75 links added across 34 entries.
- `harvest_crossrefs.py`: 0 new cross-references — the SIMILAR/RELATED WORDS
  bullets in the changed entries were already linked, self-referential, or pointed
  at words with no entry.
- `validate.py --changed-only --ratchet`: 100/100 changed entries valid.

## Self-check (§4)

`review_accuracy.py` on the 100 changed entries: 3 flagged (~$0.048), all adjudicated.

**Applied (1):**
- `12872_haiboku`: example 3's Japanese used the unnatural collocation 三連敗北
  (敗北 already means "defeat"; the standard word for "three consecutive losses" is
  三連敗, and stacking 三連 directly onto 敗北 is redundant/odd). Reworded to
  三連続の敗北 — natural, keeps 敗北 as the headword the example is meant to
  illustrate, and the English translation needed no change.

**Rejected (2):**
- `12617_shinjuu`: reviewer objected to glossing 無理心中 as "murder-suicide
  (without the victim's consent)," but "murder-suicide" in English already implies
  the perpetrator also dies by suicide, so the gloss is accurate as written; no fix
  needed.
- `13390_senzai`: reviewer called the notes "contradictory" for saying 潜在 is used
  as a noun/suru-verb but most commonly appears as the modifier 潜在的な — that's a
  statement about relative frequency, not a factual contradiction, and the later
  USAGE section restates the same point consistently. No error.

All 3 decisions logged to `reviews/decisions.jsonl`.

`review_links.py` kana-link check: 1 flag (`other`), on かける
(→ 00854_kakeru) in `12160_bonmisu`'s example 3 (迷惑を〈かけた〉, "caused trouble
for the team"). This is genuinely 掛ける, just another sense the target entry's
three definitions (hang / call / put on glasses) don't cover — kept per policy,
logged as `keep` in `reviews/link_decisions.jsonl`. This sense (迷惑をかける) is
already listed among the accumulated gaps for 00854_kakeru in
`reviews/needs_curator.txt` from an earlier session, so no new curator note was
needed.

## Needs curator

Nothing new. (The standing 00854_kakeru sense-gap note already covers this run's
one link flag.)

## Checks

`make gate`: run at wrap-up, see below. `make index` rebuilt `entries_index.json`,
`word_id_lookup.json`, and the kanji index.

## OpenRouter spend

$0.0484 (accuracy self-check, logged to the ledger) + ~$0.002 (link self-check,
not separately ledgered, matching prior runs' convention) against a $5.00 daily
cap; ledger running total $0.5371 across all three of today's routine runs.

## Next

Backlog cursor: `transitivity-review-queue` next starts at `14598` (top of the
remaining 1884-line queue after this run).
