# Session logs directory

This directory contains JSON logs of polishing sessions.

Each session file is named: session_YYYYMMDD_NNN.json

Where:
- YYYYMMDD is the date
- NNN is a sequential number for that day

Session files are created automatically during polishing runs.
