# Routine session — 2026-09-17 #001

**Mode:** accuracy-review
**Reason:** accuracy-review had the highest scheduler debt among eligible modes (cross-model-review cursor at 30239, 230 candidates queued, 54 seen-in-entry).
**Range:** entries 30239–30932 (694 entries — the tail of the ID space; the cross-model-review cursor wraps to 00001 next run for the second, notes-dimension pass).
**Budget:** $2.50 session cap; spent ≈$0.34 (694 entries × gloss/translation/tags/notes dimensions).

## Pre-flight

No open routine PRs to rescue or absorb (`list_pull_requests` returned none). No orphan
`claude/*` branches besides this session's own. Working branch was already even with `main`.

## Accuracy review

Ran `review_accuracy.py` over the full range. 31 of 694 entries (4.5%) came back flagged —
well under the 25% noise threshold, so no `[tooling]` observation needed. Adjudicated every
issue by opening the entry and checking the claim:

- **6 flags applied** (all `apply`, logged to `reviews/decisions.jsonl`):
  - 30250 三学期: removed inaccurate "winter term" from the secondary gloss (inconsistent with
    the entry's own primary gloss).
  - 30343 何回目: reworded an example translation that had turned the ordinal question ("what
    number time") into an unrelated yes/no question, undermining what the entry teaches.
  - 30391 一昨晩, 30484 連れ合う: the reviewer correctly spotted a register mismatch (both
    tagged `formality: formal` while their notes describe more specific written/literary
    register), but its suggested fix — putting "written" or "literary" into the `formality`
    field — isn't a legal value there. Added the proper `style` tag (`written`, `literary`)
    instead; kept `formality: formal`, which was accurate.
  - 30464 オンラインショッピング: fixed a self-contradictory note ("a neutral, slightly formal
    term") that clashed with its own `formality: neutral` tag.
  - 30500 理想論: reworded four collocation glosses ("nothing but an ideal" → "nothing but an
    idealistic argument", etc.) for consistency with the entry's own headword gloss.

- **30 flags rejected**, logged individually. The large majority (about two dozen) were the
  reviewer model misreading the `⟦before→after：id⟧` inline-link syntax used throughout notes
  for cross-referenced numbers and conjugation examples — it would read the *target* of a link
  as if it were unlinked prose and then "correct" the entry back to what the entry already said
  (examples: 30449/30450/30452/30471/30475/30562/30438, all in the 百/千 number-word cluster,
  plus 30246, 30352). This is worth flagging as a recurring reviewer weakness with number-word
  entries and conjugation notes that lean on the link syntax; logged here rather than as a
  `[tooling]` observation since the overall flag rate stayed low. A few others were genuine
  linguistic misreadings unrelated to link syntax (30328 四時 confused with 四季; 30889 一回忌
  confused with 一周忌; 30705 transitivity of 思いとどまる) or defensible nuance calls where the
  entry's existing wording held up under scrutiny (30271, 30344, 30459, 30472, 30473, 30526,
  30558, 30560, 30585, 30639, 30656, 30674, 30932).

## Mechanical pass (§3)

Ran on the 6 changed entries: `normalize_notes.py` (no rewrites needed), `auto_link.py`
(+11 unambiguous links across 4 entries), `harvest_crossrefs.py` (0 new cross-references —
all candidate terms already linked or not in the dictionary), `validate.py` (all 6 valid).

## Self-check (§4)

Skipped for these entries per §A step 8: they were inside the range the accuracy reviewer just
covered, so the review itself was the check.

## Cursors and queue

- `polishing/tasks/cross-model-review/progress.txt`: `next: 00001` (cursor passed the highest
  entry ID, 30932; wraps for the notes-dimension second pass).
- `reviews/queue.txt`: removed the 694 reviewed entries (30239–30932).

## Candidates / observations

None added or logged this run.

## Next steps for the curator

Nothing needs Tom's attention this run — no flags were uncertain enough to send to
`reviews/needs_curator.txt`.
