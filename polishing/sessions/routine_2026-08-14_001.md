# Routine session — 2026-08-14 — accuracy-review

**Mode**: `accuracy-review` (selector: "highest scheduler debt among eligible modes")
**Range**: entries 09809–10400 (577 entries)
**Session budget**: $2.50 OpenRouter / $5.00 daily cap
**Actual spend**: $0.2629

## Selector signals

`candidate_count` 156, `seen_in_entry_count` 10, `max_entry_id` 30634,
`comprehensive_next` 6919, `cross_model_review_next` 9809,
`unharvested_observations` 26, `reviewed_entries` 586, `open_backlog_items` 33.

## Pre-flight

No open PRs, no orphan `claude/*` branches — nothing to rescue or sweep.
Lock acquired cleanly.

## Furigana pass (§A step 2)

Screening over 09809–10400 found 577 entries already screened, 35 newly screened
($0.0046). 48 entries in the range carried a `flagged` screening result.

**Key finding:** 46 of those 48 flags were written on 2026-06-26 — *before* the
2026-08-11 `trim_context` fix in `build/review_runner.py`, which was written
specifically to kill the truncated-wrapper false-positive family
(`{天文台|てんもんだ)`). Running `--pass deep` would have paid ~$0.01/entry to
deep-review known-fixed noise.

Instead I cleared those 46 from `reviews/screening/screening_status.json` and
re-screened them with the current prompt ($0.0061): **48 flags → 10**, of which
2 were real. The deep pass was skipped under §A step 2's known-noise shortcut.

**Applied (2):**
- `10043` {薫風|くんぷう} — COMPONENTS list gave `{風|ぷう}`, the rendaku'd reading
  from inside the compound. 風 alone is ふう. → `{風|ふう}`
- `10082` {水面|みなも} — ETYMOLOGY gave `{面|なも}`; the な belongs to the archaic
  possessive particle (cf. 港 = み+な+と). → `{水|み} + な (archaic possessive
  particle) + {面|も}`

**Rejected (8 individually):** 09892 (へる is the standard reading of 経る),
09911 / 09985 (partial okurigana readings, correct by design), 10010 (両国 as the
place name Ryōgoku is りょうごく), 10088 (真っ昼間 handakuten rendaku), 10130
(家系ラーメン is いえけい), 10308 (からくり時計 rendaku), 10314 (the entry
explicitly contrasts もぐる).
**Bulk-rejected (36):** stale pre-`trim_context` truncation artifacts.

## Accuracy pass (§A step 3)

`review_accuracy.py --range 9809 10400` — 577 reviewed, 217 flagged ($0.2522).
197 error-severity issues: 161 tags, 26 gloss, 10 translation.

### Semantic tags

180 off-vocabulary semantic-tag uses across 160 entries in the range (checked
directly against `VALID_SEMANTIC`, reading `metadata.tags.semantic`). Per the
2026-06-11 semantic-tag policy, "not in the list" is correct by definition, so
these are APPLY.

**Applied:** migrated 106 tag instances across **100 entries** using a
conservative table limited to exact synonyms and clear parent categories —
`time*`→`time-general`, `description`→`descriptive`, `emotion-feeling`→`emotion`,
`grammar`→`grammatical`, `mimetic`→`onomatopoeia`, `action-general`→`action`,
`food-*`→`food`, `nature-*`→`nature`/`geography`/`weather`, `people*`→`person`/
`family`/`personality`, `commerce`/`marketing`→`business`, and similar.

**Flagged for curator:** 74 instances remain off-vocabulary where more than one
approved tag would fit, or where the right answer may be to add a new approved
tag (`interpersonal` ×8, `body-action` ×6, `administrative` ×5, `degree-extent`
×4, `physical` ×4, `literary` ×4, …). Written to `reviews/needs_curator.txt`.

**Rejected:** 50 in-list narrow/broad substitution nits and 22 formality
formal-vs-neutral nits (both explicitly reject-by-policy), plus 63 warn-severity
tag nits.

### Glosses and translations

**Applied (4)** — all cases where the main gloss contradicted or omitted a sense
the entry itself documents:
- `10119` {坊主|ぼうず} — main gloss omitted the "boy; kid" sense that is def2
- `10148` {駄洒落|だじゃれ} — main gloss omitted "bad joke", present in def0
- `10220` {餡|あん} — main gloss omitted "thick starchy sauce", which is def1
- `10356` すくむ — an example translated 足がすくむ as "my legs went weak" while the
  entry's own gloss is "to freeze; to cower; to be paralyzed" → "my legs froze"

**Rejected (bulk, 18):** the reviewer repeatedly read this dictionary's
deliberate *suffix* entries (〜線, 〜船, 〜状, 〜帳, 〜車) as standalone noun entries
and asked for the "(suffix)" marker to be removed. That is the entry design, not
an error.
**Rejected (8 more):** 09874 (貨物車 does cover road freight vehicles; the
reviewer's "train car only" claim is too strong) and 7 example-replacement
requests, which are out of scope for this mode.

## §4 self-verification

Not run separately: every entry changed this run lies inside the 09809–10400
range that §A itself reviewed, which §4 step 3 designates as the check for
`accuracy-review` mode. The two furigana fixes were verified by the re-screen
described above.

## Observations logged

- `[tooling]` Pre-`trim_context` screening results are still marked `flagged`
  and still drive the deep pass. Suggested a one-time status sweep or a
  `--rescreen-before DATE` flag.
- `[pattern]` COMPONENTS/ETYMOLOGY sections are a furigana blind spot: they give
  a single kanji the reading it takes *inside* a compound (10043, 10082). A
  detector for single-kanji pairs whose reading is voiced/handakuten where the
  standalone reading is not would find this family cheaply.

## Candidates

None added — this mode reviews existing entries and encountered no unglossed
words lacking entries.

## Cursors

- `polishing/tasks/cross-model-review/progress.txt` → `next: 10401`
- `reviews/queue.txt` — 9809–10400 removed (10,515 → 10,457 lines)
- Ledger: $0.2629 spent today of the $5.00 cap
