# Routine session — polish

Date: 2026-07-30
Mode: `polish` (selector reason: "polish: highest scheduler debt among eligible modes")
Params: `start_id: 6698`
Signals: candidates 1014 (5 "seen in entry"), max entry ID 30258, comprehensive next 6698,
cross-model review next 22213, 17 unharvested observations, 11 open backlog items,
OpenRouter remaining $4.519.

Two lanes as specified in routine2.md §2. `polishing/priority/notes.txt` was regenerated
2026-07-28 (2 days old, fresh), so the priority lane ran from `priority-cursor.txt` line 82.

## Priority lane — notes.txt lines 82–89 (6 processed, 2 skipped)

Skipped as modified within the last 30 days: line 84 `00550_asobu` (2026-07-25),
line 88 `00685_warau` (2026-07-28).

- **00503 働く** — added the missing back-link to `00480_shigoto` (shigoto already
  pointed here; the reference was one-directional). Notes: distinguished
  仕事をする (names the activity) from 働く (describes the person), and added a
  BEYOND EMPLOYMENT section for the 頭が働く / 薬が働く uses the entry did not cover.
- **00538 歩く** — added a CONTRAST section against 散歩する (bare motion vs. leisure
  activity), with a `related` cross-reference; added the reciprocal `related` back-link
  on **02885 散歩**, which had `cross_references: []`.
- **00557 できる** — linked a naked が in the notes' pattern line; added a POTENTIAL OF する
  section (できる is する's potential, so every する verb has a ready-made potential —
  勉強できる, 運転できる — preferable to the longer ことができる); `related` cross-refs
  added in both directions with **00392 する**. Semantic tag `action` → `abstract` + `existence`
  (the word denotes ability and coming-into-being, not an action).
- **00649 曲がる** — the transitivity pair with 02529 曲げる was described in the notes and
  listed in `prominent_see_also` but absent from `cross_references` on both sides; added
  `pair` links in both directions. New PARTICLES section covering に (direction) vs. を
  (corner/street passed through) vs. で (landmark), which the entry showed in examples but
  never explained.
- **00684 歌う** — semantic tag `action` → `music` (its own noun 歌/02179 is already `music`).
  Added `related` cross-refs to 02179 歌 and 00197 歌手, plus the reciprocal pair on
  **00197 歌手** (which had none). Notes: added a COGNATE OBJECT section for 歌を歌う,
  which reads as redundant to English speakers but is the normal phrasing.
- **00711 かかる** — every notes link was malformed as `⟦{時間|じかん}→{時間|じかん}：id⟧`
  (furigana braces in the base-form slot); rewrote all five. Linked four naked が.
  Added a `pair` cross-reference to **00854 かける** in both directions (again prose-only
  before). New FRAMES section: resource-spent takes が, activity takes に, verbs need
  nominalizing, bare amounts take no particle. Semantic `action` → `time-general` + `money`.

Priority-lane no-change rate: 0 of 6 — rankings are still live, so no regeneration
(`make priorities`) and no cursor reset. Next priority line: **90**.

## Frontier lane — 06698–06704 (4 changed, 1 reviewed clean)

- **06698 ドリンクバー** — full inline-link coverage added to all 3 examples and the notes
  (was zero). Domain tag `internet` removed: it is a restaurant term, and no valid domain
  in `VALID_DOMAIN` fits, so no tag is more accurate than a wrong one. Three words marked
  `noentry` and queued as candidates.
- **06699 なんだかんだ** — full inline-link coverage across 8 examples and the notes (was zero).
  `formality: formal` → `informal`: the entry's own notes said "Common in conversational
  Japanese", so the tag contradicted the prose. Added a 9th example so sense 2 meets the
  core-tier 5-per-sense minimum (it had 4), and made the register restriction explicit.
- **06700 受け継ぐ** — reviewed, **no changes needed** (polished 2026-07-29; links, notes,
  cross-refs and example counts all in order).
- **06701 差し引く** — full inline-link coverage (was zero). Removed the three-line
  conjugation list duplicating the `conjugation` field. Rewrote USAGE to separate sense 1
  (arithmetic on a stated total) from sense 2 (weighing pluses against minuses) and to
  contrast with plain 引く. Semantic `action` → `finance`.
- **06704 飛び付く** — full inline-link coverage (was zero); same duplicate-conjugation block
  removed. Deleted a **dangling cross-reference** to 飛び掛かる that carried no `target_id`
  at all and still validated (queued as a candidate instead). NUANCE rewritten to explain
  why sense 1 is normally past-tense and why sense 2 carries disapproval.
  Semantic `action` → `movement`.

**06703 振り切る** (15 examples, zero links, core tier) was too large for one slot and is
logged as an `[entry]` observation rather than half-done.

Next frontier entry: **06705**.

## §4 self-check

`review_accuracy.py --ids <15 entries> --budget 0.25` (google/gemini-2.5-flash),
$0.0069, 15 entries reviewed, 3 issues on 2 entries. **1 applied / 2 rejected / 0 flagged.**

- **APPLY** 00854 かける gloss — headline gloss read "to hang, to call, to put on" while the
  sense is specifically glasses; now "to put on (glasses)". Pre-existing, not a slip from
  this run, and the model was right to catch it.
- **REJECT** ×2 00392 する translations — both asked to replace idiomatic English
  ("Please turn the TV down", "Will you have coffee?") with literal renderings that are
  worse English and no more accurate. Same literalism family already documented in the ledger.

All decisions logged to `reviews/decisions.jsonl`; spend recorded in
`pipeline/openrouter-ledger.json` (day total $0.4879 of $5.00).

## Candidates added

- ファストフード (ふぁすとふーど) — fast food; seen in 06698
- 漫画喫茶 (まんがきっさ) — manga cafe; seen in 06698
- ネットカフェ (ねっとかふぇ) — internet cafe; seen in 06698
- 飛び掛かる (とびかかる) — to spring upon, to pounce; seen in 06704

## Observations logged

- `[pattern]` The braced-base-form link malformation is a countable family: **37 entries**
  corpus-wide, invisible to `validate.py`, and fixable by a provably-safe regex — a rare
  qualifier for the §B mechanical-application exemption.
- `[pattern]` The 066xx duplicate-conjugation-block signature continues unbroken through
  06701 and 06704 — third consecutive run to find it in every verb entry it touches there.
- `[pattern]` Sole-tag `action` on verbs is the frontier band's tag drift: 4 of 6 verbs this
  run. In-vocabulary, so no detector can see it; only visible as "carries no information the
  POS didn't already carry".
- `[pattern]` Transitivity pairs documented in prose and `prominent_see_also` but missing from
  `cross_references` on both sides (00649/02529, 00711/00854) — possible hole in
  `check_semantic_clusters.py`.
- `[entry]` 06703_furikiru needs its own pass.
- `[tooling]` A cross-reference with **no `target_id`** validates cleanly; a one-line schema
  `required` would close it corpus-wide.

## Next cursors

- `polishing/tasks/comprehensive/progress.txt` → `next: 06705`
- `polishing/tasks/comprehensive/priority-cursor.txt` → `line: 90`
