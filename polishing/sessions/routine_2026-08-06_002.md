# Routine session — 2026-08-06 (002)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Selector signals**: candidates 991 (20 "seen in entry"), max entry ID 30433, comprehensive frontier 6800, cross-model review cursor 26701, unharvested observations 29, reviewed entries 586, open backlog items 21
**OpenRouter**: $0.2585 spent today of the $5.00 cap before this run; **$0 spent by this run**
**Branch**: `claude/determined-rubin-927wzs`

## Pre-flight

- §0a `mcp__github__list_pull_requests` → **0 open PRs**. Nothing to rescue, nothing to sweep.
- Branch level with `origin/main` (0 ahead / 0 behind); working tree clean.
- `routine_lock.py acquire` → ACQUIRED.

## Work done

Harvested **all 25 loose observations** from the 2026-08-05 accuracy-review (25872–26440), the
2026-08-05 polish / new-entries / systemic-fix runs, and the 2026-08-06 accuracy-review
(26189–26700). Three premises were measured against the whole corpus before filing.

### Measurement 1 — the off-vocabulary semantic-tag queue (P20)

The observing run proposed "~55 more 1:1 `TAG_MIGRATION` mappings" to make "the remaining ~2,500
baselined entries batch-fixable". The repo-wide scan that premise implies had never been run:

- **2,530 entries / 3,208 off-list instances / 687 distinct tag names** (was 2,808 entries — converging)
- the **9 shipped mappings cover 364 instances (11.3%)** → the standing "step-1 sweep", recommended
  and deferred across five harvests, is a **364-instance job**, i.e. one bounded batch
- **55 more head mappings reach 40.6%**, leaving 623 names / 1,540 instances (top 20 names = 28.3%;
  249 names needed for 82%) — a frequency-ranked map cannot finish this queue
- every **judgment-free mechanical rule** covers **78 names / 196 instances (6.1%)**
- the head is the **taxonomy gap**: `place` 54, `location` 50, `object` 46, `state` 32, `quality` 32,
  `manner` 30, `degree` 30, `document` 20, `position` 14, `objects` 14 = **322 instances** in the
  region `VALID_SEMANTIC` has no slot for — the same ten strings P13's 2026-07-30 update reached
  from the *reviewer* side
- new prioritization datum: **1,121 of the 2,530 carry off-list tags and nothing else**
  (functionally untagged) vs 1,409 that also carry a valid tag

Filed: **P20 update**; queue `unknown-semantic-tags` scope 4900 → **2530** with notes.

### Measurement 2 — the sole-`general` gloss-keyword suggester (P13) — refuted

Built empirically (899 gloss tokens concentrating ≥80% on one tag across 20,257 specifically-tagged
entries), run against the **3,741** sole-`general` entries:

| threshold | proposals |
|---|---|
| ≥80%, n≥5 | **554 / 3,741 (14.8%)** |
| ≥90%, n≥10 | 109 / 3,741 (2.9%) |

~1 proposal in 5 is wrong, and every error has the same shape — 眉 → `nature` ("ridge"), 保険料 →
`transportation` (gloss illustrates car insurance), 負け惜しみ → `food`: the tag taken from context
the gloss *mentions* rather than what the headword *denotes*, i.e. the P11 defect the tag lane
exists to clear. Filed as a **P13 update** closing the instrument question; queue scope 3791 → 3741.

### Measurement 3 — pure-kana wrappers inside inline-link surfaces (P9)

**108 instances / 94 entries** (84 katakana, 24 hiragana; all resolve today, 5 target `noentry`) —
a subset of P9's existing 373 `pure-kana` findings, so a targeting hint rather than a new item.
Filed as a **P9 update** + queue note, linked to Tooling 76.

### Pages changed

- `planning/wiki/ideas/cleanup-backlog.md` — P20 update, P13 update, P9 update, zero-link Informational (12th/13th filing)
- `planning/wiki/ideas/tooling-backlog.md` — 46 update, **53 resolved as misdiagnosed**, 24 update (throughput), 59 update (ambiguous bucket)
- `planning/wiki/ideas/entry-followups.md` — 4 new: 26031 格上, 04230 打ち明ける, 02918/10077 とき, missing hundreds 二百…九百
- `planning/wiki/ideas/backlog-queue.json` — 4 items updated (2 rescoped)
- `planning/wiki/topics/schema-tag-reliability.md` — **[C] deepened** (first update since 2026-06-13): new section "The shape of the semantic-tag debt", synthesizing why both instruments failed the same way
- `planning/wiki/log.md` — session entry
- `polishing/observations.md` — 25 observations pruned with harvest note

No new wiki pages, so `index.md` needed no change.

## §3 always-on capture

No new candidate words (wiki mode touches no entries). No new observations logged — this run *was*
the harvest.

## §4 self-verification

**Skipped — no entries created or modified this run** (`git status --porcelain -- entries/` empty).
OpenRouter untouched; $0 spent.

## §5 metrics

Appended: `mode=wiki, changed=0, entries_total=30224, candidates=991, review_queue_len=11568,
comprehensive_next=6800, openrouter_spent_today=$0.2585`.

**Activity H (metrics trend refresh) not run**: 7 new lines since the thirty-first refresh
(2026-08-05), below the ≥10 threshold. The next `wiki` run should expect to do it.

## Build

**Skipped** — wiki-only run (markdown plus one JSON index; `docs/` untouched), per §6.

## Cursors

No mode cursor to advance (`wiki`); `pipeline/routine-state.json` was advanced by the selector in §1.

## Routed to the curator

1. **The taxonomy question** — does `VALID_SEMANTIC` want a spatial/positional tag and a
   document-type tag? Now sized at **322 instances** and identified as the largest single block of
   the P20 migration queue. Blocks both the reviewer and the map; no amount of tooling reaches it.
2. **The 364-instance step-1 sweep** of the nine already-shipped mappings — needs no detector, no
   budget, no decision, and is now known to be small.
3. The **furigana-screener retire-on-cost** call, whose throughput argument has grown a
   second-order cost: in the run that measured it, the *accuracy* pass also fell short.
4. The **frontier-versus-growth rate**, carried forward a fourth time.
