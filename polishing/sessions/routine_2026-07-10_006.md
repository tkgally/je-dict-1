# Routine session 2026-07-10 #006

- **Mode**: `accuracy-review` (selector reason: "highest scheduler debt among eligible modes")
- **Params**: phase=furigana, start_id=13550, session budget $2.50
- **Range worked**: 13550–13649 (accuracy dimensions, 99 entries reviewed); furigana screening scoped to 13550–13576 (27 entries) due to tool slowness (see below).

## Cross-model review

- **Accuracy pass** (`review_accuracy.py --range 13550 13649`, gemini-2.5-flash): 99 reviewed, 26 flagged, est. $0.044. All 29 issues were error-severity (24 tags, 5 gloss); adjudicated each individually per §A step 4.
- **Furigana screening** (`review_runner.py --pass screening`): only 13550–13576 completed reliably; the runner stalled repeatedly (see [tooling] observation). 5 screening flags in that sub-range, adjudicated by reading the entries directly (deep pass skipped — unreliable/slow).

## Changes applied (6 entries)

- **13640** semantic tag `legal` → `law` (not-in-list tag; migrated per §A tag policy).
- **13649** semantic tag `body` → `body-part` (not-in-list tag; migrated).
- **13611** (病む) semantic tag `existence` → `health` (clear mismatch: 病む = to fall ill / be mentally troubled, unrelated to existence).
- **13575** (異端) main gloss `heresy, heterodoxy, unorthodox` → `heresy, heterodoxy` (dropped adjectival term from a noun gloss; def1 keeps "unorthodox view").
- **13593** (町人) main gloss `townsperson, merchant class` → `townsperson, merchant/artisan class` (aligned with def1; 町人 = merchants **and** artisans).
- **13570** (町おこし) notes furigana `{町興|まちお}こし` → `{町興|まちおこ}し` (mis-split; 町興 reads まちおこ with し okurigana).

## Rejected (reviewer noise / defensible)

- **Family (n=12, aggregated)**: "'general' semantic tag is too broad → replace with narrower in-list tag" (health/politics/military/history/etc.). In-list broad/narrow substitution → reject per §A policy.
- Individual: 13556 & 13639 (現時点 / 発する genuinely formal), 13585 (`food` correct for potato sense def2), 13605 (`food` defensible for fermentation), 13566 (`existence` defensible for survival), 13624 ("molester" accurate; stylistic), 13631 & 13635 (tag incompleteness, not error), 13610 (formality supported by its own notes), 13641 (`abstract`→`action` in-list swap).
- Furigana FPs: 13560 (申請→しんせい correct), 13564 (no partial reading; all 異例→いれい), 13566 (登山家→とざんか correct), 13576 (parse-failure noise).

Overall flag rate 26% (>20% threshold) → logged as reviewer noise in observations.

## §4 self-verification

Folded into §A: the accuracy pass **was** the independent-model review of exactly these entries (13570 was covered by furigana screening). No separate verification call — per §4 step 3, accuracy-review mode skips IDs already covered by §A this run. All 6 changed entries validate (200/200 in range 13500–13699) with no unknown-tag warnings.

## Capture

- No missing-word candidates added (review-mode; did not deep-read examples).
- 2 `[tooling]` observations: (1) accuracy-review 26% flag rate driven by the "general too broad" noise family — tune the reviewer prompt; (2) `review_runner.py` furigana screening is slow/unreliable in the Routine environment (~8s/entry with multi-minute stalls) — consider a faster screening model or smaller default range.

## Ledger / metrics

- OpenRouter spend this run ≈ $0.065 (accuracy $0.044 + partial screening ~$0.02); daily ledger now $0.1895 / $5.00.
- Metrics line appended (`entries_changed: 6, flags_applied: 6, flags_rejected: 26`).

## Cursors

- `cross-model-review/progress.txt` → `next: 13650`.
- `reviews/queue.txt` pruned of 13550–13649 (13906 → 13845 lines).
- Selector state already advanced in §1.
