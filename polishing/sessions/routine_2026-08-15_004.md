# Routine session — 2026-08-15 (004)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Params**: `phase: furigana`, `start_id: 11008`, `openrouter_session_budget_usd: 2.50`
**Selector signals**: candidates 166, seen-in-entry 20, max entry ID 30653, comprehensive
cursor 6942, cross-model-review cursor 11008, unharvested observations 4, reviewed entries 586,
open backlog items 36.

## Pre-flight

- §0a rescue: no open PRs — nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs.
- §0b orphan-branch sweep: `list_branches` returned only `main` — zero orphans.
- Routine lock acquired for `claude/determined-rubin-x4hibg`.

## Range worked

- **Glosses / example translations / semantic tags**: entries **11008–11500** (493 entries
  reviewed by `google/gemini-2.5-flash` via `build/review_accuracy.py`, est. $0.2165).
- **Furigana screening**: entries **11008–11105** only (98 entries, est. ~$0.01). The screener
  runs about 7x slower than the accuracy reviewer (≈8.5 vs ≈62 entries/min), so it was stopped
  at the point where continuing would have doubled the run's wall-clock for a pass whose measured
  precision on recent ranges is 0–5%. Logged as a `[tooling]` observation.
- Combined ledger entry: $0.2265, phase `accuracy+screening`. Daily ledger now at $0.3761 of the
  $5.00 cap.

## Results

**Accuracy review**: 493 reviewed, 111 entries flagged (22.5%), 114 issues.

| dimension | severity | count |
|---|---|---|
| tags | error | 107 |
| tags | warn | 1 |
| gloss | error | 4 |
| translation | error | 2 |

**Adjudication: 8 applied, 106 rejected, 0 flagged for the curator.**

Applied:

| entry | headword | change |
|---|---|---|
| 11008 | ダイエットする | semantic `consumption` → `health` — the entry's own note says Japanese ダイエット is about weight loss, not eating habits |
| 11189 | {上場|じょうじょう} | semantic `work` → `finance` (stock-market listing) |
| 11212 | リピーター | semantic `work` → `business` (a repeat customer, not an occupation) |
| 11259 | メロディ | semantic `leisure` → `music` |
| 11431 | {仕切|しき}り | semantic `housing` → `building` (`housing` is not in `VALID_SEMANTIC`) |
| 11486 | ラップ | semantic `leisure` → `music` (rap sense; `tool` kept for plastic wrap) |
| 11496 | {先々週|せんせんしゅう} | semantic `time-relative` → `time-general` (`time-relative` is not in `VALID_SEMANTIC`) |
| 11403 | ワイド | example 3 English "Wide shows cover…" → "Daytime TV shows cover…" — "wide show" is not English; the entry's own cultural note already explains the genre |

Rejected, by family (one aggregated ledger line each, per §C):

- **74** — "the semantic tag `general` is too broad, use X". `general` is an accepted fallback in
  this dictionary and a sole-`general` tag is already tracked at `info` severity by
  `build/check_tag_drift.py` across 3,641 entries. Not an accuracy-review matter.
- **19** — in-list narrowness / polysemy substitutions (`food` on フライ and メニュー, `person` on
  {両者|りょうしゃ}, "add an `action` tag to this suru-verb", and similar). Rejected per §A's rule
  against swapping one valid tag for a narrower valid tag.
- **8** — "formality should be `neutral`, not `formal`". Checked four of them individually
  (11232 {一様|いちよう}, 11302 {不在|ふざい}, 11411 {乗用車|じょうようしゃ}, 11372 {下層|かそう});
  in every case the entry's own REGISTER note states the word is formal, which §A makes
  authoritative.
- **5** individual gloss/translation rejections: 11200 レジャー (top-level gloss is already the
  model's own suggestion), 11268 ポーズ (the "pose; pause" gloss correctly signals two senses),
  11488 プロモーション ×2 (the model wanted a "promotion in rank" sense the entry explicitly
  assigns to {昇進|しょうしん}), 11464 {不愉快|ふゆかい} ("I was in an unpleasant mood" is an
  acceptable rendering of 不愉快な気分になった).

**Furigana screening**: 98 entries, 3 flags, all rejected as documented false-positive families
(腹→なか inside お{腹|なか}; 仕入→しい in {仕入|しい}れ{先|さき}, an okurigana split; はかせ vs
はくし for 博士, where both readings are correct in their own compounds). Per §A's known-noise
shortcut the deep pass was **skipped**.

## §4 self-verification

Skipped by design: every entry changed this run (11008, 11189, 11212, 11259, 11403, 11431, 11486,
11496) was inside the §A review range, so the review itself was the independent check —
routine2.md §4 step 3 says to skip IDs already covered by §A. The changes were seven single-tag
substitutions plus one English example translation; `validate.py` and `validate_tags.py` both pass.

## Candidates added

None. This mode reads entries rather than writing them, and no example or note encountered
contained a Japanese word that lacked an entry and was worth queuing.

## Observations logged

Three, in `polishing/observations.md`:

- `[tooling]` The reviewer's `tags` dimension produces 82 of 108 tag flags from two families the
  project has already decided are not errors (sole `general`, and formality labels the entry's own
  notes support). A two-sentence change to the reviewer prompt in `build/review_accuracy.py` would
  remove them without losing a single applied correction.
- `[tooling]` The furigana screener and the accuracy reviewer differ ~7x in throughput, so one ID
  range is the wrong unit for sizing a run; the selector should give the screener its own range.
- `[pattern]` Furigana screening precision over recent ranges remains at or near zero (0/3 this
  run), consistent with the 2026-06-10/11 measurements.

## Cursors

- `polishing/tasks/cross-model-review/progress.txt`: `next: 11501`
- `reviews/queue.txt`: 11008–11500 removed (10,586 → 10,525 lines)
- Furigana screening coverage stops at 11105; entries 11106–11500 were reviewed for
  glosses/translations/tags only this run.
