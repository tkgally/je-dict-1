# Routine session: polish (2026-09-15, 002)

**Mode:** polish (selector reason: "polish: highest scheduler debt among eligible modes")
**Pre-flight:** PR #3293 (routine(new-entries), head claude/trusting-mendel-1b7wtk) is open with
failing `validate` CI and no human comment; its highest touched entry ID (30912) is not below the
polish frontier (07388), so the sweep rule leaves it open rather than closing it — needs an
independent look. Orphan branches `claude/trusting-mendel-o9cv7c` and `claude/trusting-mendel-u8emu6`
were already swept and logged by an earlier run today (`needs_curator.txt`); no new action taken.

## Priority lane (line 1, cursor -> 1008)

Scanned 1007 lines to fill a 20-entry batch (skipping IDs with no file and entries modified in the
last 30 days). 10 of 20 got real fixes:

- 04959_dango: two stale hand-written `noentry` markers (みたらし, 三色) converted to plain text;
  semantic tag general -> food.
- 04990_suimono: three stale `noentry` markers (椀種, 吸い口, 木の芽) converted to plain text.
- 03114_daiseikou: semantic tag general -> action (matching sibling 02638_seikou).
- 04382_momo: `noentry` marker (桃太郎) x2 converted to plain text; semantic general -> food.
- 04943_koushinryou: `noentry` markers (クミン, ターメリック) converted to plain text.
- 04950_shimauma: `noentry` marker (一頭, a transparent counter phrase) converted to plain text.
- 02768_koya: `noentry` marker (三頭) converted to plain text; five identical redundant
  per-example notes ("[Note: Reading changes to ごや in compounds]") removed — the same fact is
  already in the entry's own NOTE ON READING section.
- 04384_kaki: two `noentry` markers inside a haiku quote (なり, 法隆寺) converted to plain text.
- 02923_donata: was missing a `semantic` tag entirely; added `interrogative` to match sibling
  00534_dare — caught by self-check as off-vocabulary, migrated to `grammatical` (see below; also
  fixed 00534_dare's matching off-vocab tag while at it).
- 02977_moshimoshi: removed a stray `domain: business` tag that contradicted the entry's own notes
  ("In business settings, もしもし is generally avoided").

Ten more (01007_wa, 04935_katsuobushi, 03122_choushoku, 04930_nattou, 04513_benki, 00047_fusai,
06916_ze, 04947_kirin, 00929_gochisousama, 00624_getsuyoubi) were already correct — read, no changes.

New candidates from cleared `noentry` markers: みたらし (C23450), 椀種 (C23451), 吸い口 (C23452),
木の芽 (C23453), 桃太郎 (C23454), クミン (C23455), ターメリック (C23456), 法隆寺 (C23457), and two
more from the frontier lane (below).

## Frontier lane (07388 -> 07405)

Read 07388-07404 (17 entries), 8 got real fixes:

- 07388_shinogu: `noentry` markers (売り上げ, 師) converted to plain text; 売り上げ and 師 queued
  as candidates (C23458, C23459) — 師 was later auto-linked to the existing suffix entry
  30801_〜師, which may or may not fully cover the standalone "master, teacher" sense used here;
  left the candidate queued for a curator to judge, since self-check raised no complaint.
- 07391_fuyoui: removed a `domain: colloquial` tag that contradicted the entry's own formal
  register and notes; self-check then flagged the resulting `formality: formal` as inconsistent
  with the note's own "Neutral to formal" — changed to `neutral`.
- 07393_negoto, 07394_tawagoto, 07395_kurigoto, 07396_herazuguchi: semantic tag general ->
  communication (matching sibling 07022_hitorigoto, and each other — all four are 〜言/〜口
  speech-act nouns).
- 07400_meguriawase: semantic tag general -> abstract, existence (matching sibling 07401_shukumei).
- 07403_nimaime: semantic tag general -> appearance; removed a `style: archaic` tag that
  contradicted the entry's own "MODERN USAGE" note.

Nine more (07389_yarusenai, 07390_munashii, 07392_fukakai, 07397_machiawase, 07398_atomawashi,
07399_oteage, 07401_shukumei, 07402_idobatakaigi, plus 07404_sanmaime left untouched) were already
correct.

## Self-check

`review_accuracy.py` on all 18 entries this run rewrote found 2 real issues, both applied:
- 02923_donata: the `interrogative` semantic tag (copied from 00534_dare above) is off-vocabulary
  — migrated both entries to `grammatical`, the tag used by sibling ko-so-a-do word 00539_doko.
- 07391_fuyoui: `formality: formal` contradicted the note's own "Neutral to formal" — changed to
  `neutral`.

`review_links.py` checked all 68 link occurrences in the 18 entries (in context) and flagged none.

Total entries changed this run: 19 (18 from the two lanes + 00534_dare, fixed alongside its
sibling's off-vocab tag).

## Cost

$0.0118 this run's self-check ($0.0088 accuracy + $0.003 links), on top of $0.49 already spent
today. $0.5018 spent today against the $5.00 daily cap.

## Observations logged

- `[tooling]`: normalize_notes.py left 12 distinct non-canonical note headers untouched across
  this run's entries (pre-existing, not in the alias table) — see polishing/observations.md.
- `[pattern]`: a recurring sole `semantic: ["general"]` tag on entries whose word fits a concrete
  category, plus off-vocabulary/contradictory domain and semantic tags found in three unrelated
  entries — see polishing/observations.md for the full list.

## Next cursors

- `polishing/tasks/comprehensive/progress.txt`: `next: 07405`
- `polishing/tasks/comprehensive/priority-cursor.txt`: `line: 1008`

## PR / CI

PR #3298 pushed and opened. Its `validate` check run stayed `in_progress` (never reached
`completed`) through all 16 polls (~20+ minutes), well past the ~6-minute runtime the previous
routine PR's checks took. Pushed the session-log addendum above as a second commit, which started
a second check run (104317291421) — also still `in_progress` after another ~6 minutes of polling.
Left the PR open; stopped polling well past the routine's cap rather than let this run stall
indefinitely. This looks like a slow or stuck GitHub Actions runner today rather than a content
problem (nothing about this PR's diff differs in kind from prior clean runs). The next run's
pre-flight (§0a) should rescue it if green by then; if both check runs are still stuck, that is
worth a direct look at the Actions run pages linked above.
