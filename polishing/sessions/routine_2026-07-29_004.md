# Routine v2 session — 2026-07-29 (004)

**Mode**: `wiki`
**Selector reason**: `wiki: highest scheduler debt among eligible modes`
**Params**: `{}` (none)
**Signals**: candidates 1021 (12 `seen in entry`), max entry id 30221, comprehensive_next 6684, cross_model_review_next 21301, unharvested observations 33, reviewed entries 586, open backlog items 10
**OpenRouter**: spent today $0.3301 of $5.00 cap; **$0.00 spent this run** (no model calls — wiki mode, no entries changed)

## Pre-flight

- §0a rescue: `mcp__github__list_pull_requests` returned **zero open PRs** — nothing to rescue.
- §0b sweep: no `claude/*` PRs open, so nothing superseded to close.
- Lock acquired for session `claude/determined-rubin-nqg262`.

## Work performed

Followed `planning/maintain-knowledge-base.md`. Four activities: **F** (harvest), **C** (deepen a page), **A** (sync counts), **E** (lint/resync), plus **H** (metrics trend, due at 383 lines vs the 369 the page covered).

### [F] Harvest — 29 observations cleared

Sources: 2026-07-28 accuracy-review (20451–21050); both 2026-07-28 polish runs; 2026-07-29 accuracy-review (20703–21300), new-entries (30203–30221), and polish (06681–06683).

**Three observations reported curator items as fixed. All three were verified against the source in this run rather than accepted on report:**

| Item | Claim | Verification |
|---|---|---|
| Tooling 20 | scorer POS substring bug + inline-link-tail bug fixed | `normalize_pos()` now `re.search(r'\bverb\b', …)`; `has_bare_kanji()` strips `INLINE_LINK_TAIL_PATTERN` first — **confirmed**, marked RESOLVED |
| Tooling 34 | `comprehensive_polish.md:107` corrected | line now lists only schema-valid types and names the invalid two explicitly; grep confirms they appear nowhere else — **confirmed**, marked RESOLVED |
| Tooling 28 | (not claimed — tested) | re-ran all four detectors: two items recorded `resolved / scope 0` came back at **12** and **33** — **counter-evidence**, item amended |

Filed or updated: **Tooling 11** (corpus swept: 292 dead links / 160 entries / 144 nonexistent IDs; reframed as a ratchet so it ships before the cleanup), **19** (first self-inflicted case — a `new-entries` run created 30216/30217 while a polish run read 02985's stale `noentry` markers for those same words), **21** (manual sharding worked → concrete `--workers N` ask), **23** (the candidate pool is two pools; `candidates_low` reads 1,021 while the writable pool is 12), **24** (two more precision measurements plus 20908's `{急|きゅう}がれている`, a true positive no charset lint can see), **28**, **new 42/43/44**; **Cleanup P9/P17/P20/P21/P24 updates**, **new P31/P32**; **Entry Follow-up 20891 紆余**.

Most consequential single fact surfaced: **`docs/styles.css:245` renders `content: attr(data-baseform)` as the hover tooltip**, so inline-link baseforms are user-visible — which promotes 3,567 kana baseforms (P32) and 254 braced ones (P24) from metadata tidying to a learner-facing defect.

### [C] `topics/furigana-wrapper-anomalies.md`

Added the measured 23500–23999 slice: 100 of 110 findings were pure-kana wrappers inside *notes*; the four genuinely-damaged strings all carried **`suggestion: null`** (so sort nulls first, not last); empty-reading wrappers clustered in the contiguous 23798–23809 creation run, so linear extrapolation over-counts. Plus the 丸ノコ mixed-script trap.

### [A] `project/overview.md`

**The dictionary crossed 30,000 entries.** 30,012 total / 801 basic / 1,982 core / 27,209 general / 20 untiered / 117,198 examples / 16,821 cross-references (from `build/report.py`).

### [E] `backlog-queue.json` resync

`unknown-semantic` 6,218→**5,939** (first refresh where the sweeps clearly outrun new-entry growth in that class), `sole-general` 3,789→3,791, `semantic-mismatch` 825→834, cosmetic wrappers 761→763. Reopened `artifact-missing-target-id` (0→12) and re-scoped `example-headword-missing` (0→33). Added `notes-leading-conjugation-bullets`. 31 items total.

### [H] Twenty-seventh metrics refresh

383 runs / ~10,368 adjudicated flags. `tags` apply-among-decided a **new series high 73.9%** [366/129]; furigana screening **5.4%** [10/176]; **largest escalation event in the series, 162**, of which 161 share one cause (off-vocabulary tag with no destination in a 9-pair `TAG_MIGRATION`); **first rise in the review queue** (12,634→12,915, no new floor). Added findings **10** (queue convergence is bounded by the sweep's own apply rate — it falls fastest when the reviewer finds nothing) and **11** (escalation volume is a queue signal, not a quality signal).

## §4 self-verification

**Not applicable — no entries were created or modified this run.** `git status --porcelain -- entries/` is empty; the diff is markdown and JSON in `planning/`, `polishing/`, and `pipeline/` only. No OpenRouter calls were made, so the ledger is unchanged and no §C decision lines were written.

## Candidates added

None. No Japanese words lacking entries were encountered — this run read wiki pages, ledgers, and detector output, not entries.

## Observations logged

Two, both raised by the harvest itself (`polishing/observations.md`):

- `[tooling]` `reviews/decisions.jsonl` allows an aggregated `n` line for bulk **rejections** but not bulk **escalations**, so 161 instances of one cause were written as 161 `flag` lines — making the largest escalation event on record out of a missing lookup table.
- `[pattern]` The review queue rose for the first time in the series; diagnosed as structural (a sweep re-adds the entries it just fixed), so queue depth should now always be read jointly with the apply rate.

## Cursor / state updates

- `pipeline/routine-state.json` — advanced by the selector in §1.
- `pipeline/metrics-history.jsonl` — one line appended (§5).
- `planning/wiki/log.md` — session entry appended.
- No entry-polishing cursors touched (`comprehensive` stays at `next: 6684`, `priority-cursor` at `line: 57`).
- `planning/wiki/index.md` — **not modified**: no new pages were created this run (all work went into existing pages).

## Build

**Skipped** — `wiki`-only run; markdown and JSON-under-`planning/` changes do not touch `docs/`. (§6.)

## Routed to the curator

A `wiki` run may not modify prompts, skills, entries, or build scripts. Outstanding:

1. **Extend `TAG_MIGRATION`** with the ~19-pair table now written out at Cleanup P20 — converts most of 5,939 `unknown-semantic` instances into a mechanical batch and would have prevented 161 of this window's 162 escalations. Highest-value item open.
2. **Ratcheted link-target check in `validate.py`** (~15 lines), plus its base-form-agreement half.
3. **`review_runner.py` context-snippet fix** — never truncate inside a `{kanji|kana}` pair.
4. **`--workers N`** on the two review scripts (after item 24's substitution, not before).
5. **Pre-install `jsonschema` and `pytest` in the Routine image** — the latter means no unattended run has ever executed `build/tests/`.
6. **`check_duplicate.py`: `LIKELY VARIANT` verdict** on a reading-identical hit.
7. **Tooling 44 consistency rule** — non-neutral `formality` with no REGISTER note.
8. **P24/P32 baseform sweep** — 3,821 links total, mechanical, user-visible.
9. **20891 紆余** — entry-identity decision.
