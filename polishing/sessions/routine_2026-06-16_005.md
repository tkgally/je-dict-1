# Routine session 2026-06-16_005

- **Mode**: systemic-fix
- **Selector reason**: "systemic-fix: highest scheduler debt among eligible modes"
- **Backlog item**: `furigana-slash-reading` (Priority 12 / `cleanup-backlog.md#priority-12`)
- **Signals**: candidate_count 1314, open_backlog_items 7, comprehensive_next 6151, OpenRouter remaining $4.69

## What was done

Drained the `furigana-slash-reading` backlog item end to end. The read-only
detector `build/check_furigana_format.py --json` (`subpattern == 'slash-reading'`)
flagged **131 wrappers across 118 entries** — all in the `notes` field — using the
non-standard dual-reading furigana form `{漢字|よみ1/よみ2}` (e.g. `{村|むら/そん}`,
`{袋|ふくろ/ぶくろ}`), which the renderer expects a single reading string for.

Each wrapper was verified per-entry against the headword and surrounding note
context, then replaced with the single reading the kanji actually takes:

- **Compound decomposition of the headword** → the reading (incl. rendaku) the
  kanji has in that headword: `{歩|ほ/ぽ}`→`{歩|ぽ}` (散歩), `{袋|ふくろ/ぶくろ}`→`{袋|ぶくろ}`
  (寝袋 etc.), `{顔|かお/がお}`→`{顔|がお}` (ドヤ顔), `{血|けつ/けっ}`→`{血|けっ}` (赤血球).
- **Inside a ⟦…⟧ inline link** → the reading matching the link target:
  `{毎年|まいとし/まいねん}`→`{毎年|まいとし}` (→00731_maitoshi), `{分|ふん/ぷん}`→`{分|ふん}`
  (→00928_fun), `{竹林|ちくりん/たけばやし}`→`{竹林|ちくりん}` (→15110_chikurin).
- **Standalone / related word** → the primary reading, prose keeping the alternate
  where the note already discusses it: `{梅雨|つゆ/ばいう}`→`{梅雨|つゆ}`,
  `{買春|ばいしゅん/かいしゅん}`→`{買春|ばいしゅん}`, `{施行|しこう/せこう}`→`{施行|しこう}`.
- **Notes that explicitly explain the reading variation** (七 なな/しち, 分 ふん/ぷん,
  野 の/や, 額 ひたい/がく) kept their prose; the wrapper dropped to the headword reading.
- **Two malformed English-in-reading wrappers** repaired in 28654 (アプリ内課金):
  `{課金|billing/charging}`→`{課金|かきん}` and `{内|inside}`→`{内|ない}`.
- 09532 (値札): rewrote one confused sentence to drop the slash and state 札 is
  also read さつ ('banknote').

All 118 files had their `metadata.modified` bumped to 2026-06-16T12:23:55Z.

## Verification

- `build/check_furigana_format.py` slash-reading count: 131 → **0**.
- `build/validate.py`: **29076/29076 valid** (warnings unchanged, all pre-existing
  and on unrelated IDs).
- **§4 self-check** (furigana screener, `review_runner.py --pass screening`):
  the 540 s `timeout` wrapper killed the runner at **59/118** IDs (~9 s/entry via
  gemini-2.5-flash). Of the 59 screened, **11 flagged, all REJECTED, 0 applied** —
  every flag was a documented false-positive family (rendaku-in-compound readings
  like ごと/ぷく, okurigana/partial readings "correct by design" like 脆弱性/二泊三日/
  強制執行, and screener input-truncation artifacts such as `そうが)` / `しんりょ` /
  `あいしょ`). Each flagged entry's actual file was spot-confirmed to carry full,
  correct readings. Logged one aggregated reject line (n=11) to
  `reviews/decisions.jsonl` and a `[tooling]` observation about the timeout.
- **Self-check coverage was partial (59/118)** due to the tooling timeout; the
  remaining 59 were not re-screened (all edits are uniform, per-entry-verified
  reading selections; the accuracy-review sweep will reach them later).

## Bookkeeping

- `backlog-queue.json`: `furigana-slash-reading` → status `resolved`, scope 0.
- `cleanup-backlog.md` Priority 12 → marked RESOLVED.
- Ledger: +$0.03 self-check (spent_today $0.3353 / $5.00 cap).
- Metrics snapshot appended (entries_changed 118, applied 0, rejected 11).
- Candidates added this run: none (no missing-entry words encountered in notes).

## Next cursor

- systemic-fix item drained; next open `batch_ready` item by priority is
  `artifact-missing-target-id` (P5) then `unknown-semantic-tags` (P9). The
  selector advances `pipeline/routine-state.json` automatically.
