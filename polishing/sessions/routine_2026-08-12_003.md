# Routine session — 2026-08-12 (003)

**Mode:** `systemic-fix`
**Selector reason:** "systemic-fix: highest scheduler debt among eligible modes"
**Selector signals:** candidates 167 (4 "seen in entry"), max entry ID 30594, comprehensive
frontier 6893, cross-model-review frontier 8347, unharvested observations 21, reviewed entries
586, open backlog items 32.
**OpenRouter:** $0.0154 spent at start, $4.9846 remaining, no session cap.

## Pre-flight

- §0a rescue: `list_pull_requests` returned **zero open PRs** — nothing to rescue.
- §0b stranded-PR sweep: no `claude/*` PRs open, nothing to close.
- §0b orphan-branch sweep: `list_branches` returned **`main` only** — zero orphans.
- Lock acquired for `claude/determined-rubin-bserg0`.

## Backlog item worked

`inline-link-stale-noentry` (Cleanup P35) — inline links still marked `noentry` whose word has
since gained an entry, so the reader sees plain text where a working link exists. `fix_type:
verify`, filter **A1+A2 only**, per-entry context read required because the remaining population
sits on 2–3 character bases with a real polysemy rate. Detector: `build/check_stale_noentry.py`
(read-only). Starting queue: **1,281 pairs / 1,501 instances**; everything below entry 02230 was
swept by the 2026-08-11 run, so this run resumed at the lowest remaining `entry_id`.

## Method

Same instrument as the 2026-08-09 and 2026-08-11 runs: the detector's `--mechanical --json`
queue joined with a ±45-character context window around each marker in the field it occupies,
so every substitution was judged against the sentence or note line it sits in. Four batches of
45, each read in full before anything was applied.

- **Batch 1 — entries 02232–02472**: 45 pairs, 49 instances, 42 entries.
- **Batch 2 — entries 02477–02697**: 45 pairs, 54 instances, 36 entries.
- **Batch 3 — entries 02701–02934**: 45 pairs, 55 instances, 35 entries.
- **Batch 4 — entries 02934–03163**: 45 pairs, 56 instances, 35 entries.

**Total: 180 pairs / 214 instances across 147 entries, all A1/A2, zero rejections** — the first
fully clean band. Everything below entry **03164** is now swept for A1+A2. Remaining queue:
**1,101 pairs / 1,287 instances**.

Validation: `validate.py` clean over 2200–2700 (491/491), 2700–3200 (500/500) and 2800–3200
(401/401) after the §4 fixes; `check_link_targets.py` reports 0 dead / 0 ambiguous /
0 unresolvable dictionary-wide.

## Families verified as groups in this band

Date and counter words (十一日/十四日/七人/六人/二枚/何曜日 → the 30xxx counter entries), meat
compounds (羊肉/馬肉), surname examples in phone-call and self-introduction entries
(田中/山田/鈴木 → 0952x, where the target entries **are** the surnames), place names used as
places (成田/横浜/青森県), and grammar terminology (五段活用 → `28275_godan`, whose entry
carries the grammar sense alongside "fifth dan").

## Figurative compounds — applied, and why they are not the 朝日新聞 family

Several markers sit in sentences that use the word metaphorically while the target entry defines
it literally: 猫に小判 → `27868` (koban coin), 歩行者天国 → `27976` (heaven), 手玉に転がす →
`28101` (juggling ball), 早起きは三文の徳 → `28481` (three mon / a pittance). All were applied.
The discriminator against the proper-name family found on 2026-08-11 is whether surface and
target are the **same lexeme**: in 歩行者天国 the word really is 天国 used figuratively and the
entry is where a learner should land; in 朝日新聞 the surface is a company name that merely
contains 朝日.

One marginal apply, recorded for future runs: ソフト飲料 → `01532_sofuto`, whose sense 2
("soft; not hard or firm") covers the "non-alcoholic" collocation only approximately.

Running false-positive rate across the 538 hand-verified short-base pairs (2026-08-09,
2026-08-11, 2026-08-12): **~0.4%**.

## §4 self-verification

`python3 build/review_accuracy.py --ids <37-entry stratified sample of the 147 changed>
--budget 0.25` — every fourth changed entry in ID order. Reviewed 37, flagged 11 entries /
13 error-severity issues, est. $0.0166.

**No flag concerned a link target** — all 13 were pre-existing gloss/tag observations on entries
this run happened to touch. This is the second consecutive run with that result.

- **4 applied**, all sole-tag defects the deterministic detectors also track:
  - `02830_sanso` 酸素: sole-`general` semantic tag → `science`.
  - `02999_sakuya` 昨夜: sole-`general` semantic tag → `time-general`.
  - `03148_heihoumeetoru` 平方メートル: semantic `tool` → `quantity` (a unit of area is not a
    tool), and formality `informal` → `neutral` (a technical measurement unit is not informal;
    nothing in the entry supported the label).
- **9 rejected**, notably:
  - `02999` formality `formal` → `neutral`: the entry's own REGISTER section says さくや "is
    preferred in news reports, formal writing, and literature". The entry contradicts the flag.
  - `02604_gakumon` formality `formal` → `neutral`: the entry's notes label 学問する as formal,
    which supports the tag rather than contradicting it.
  - `02741_keiji` gloss: the model wanted 刑事 to lead with "detective" — it already does.
  - `02402_hipparu` translation: the flag itself concedes the translation is correct.
  - `02917_tooka` tags: the model's own suggestion concludes `time-general` is the closest
    in-list option, i.e. the flag argues against itself.
  - `03098_soudesu` gloss: asks to split the entry into several, which is not an error.
  - 3 further tag flags bulk-rejected as one family — narrowness swaps between tags that are
    **all already in `VALID_SEMANTIC`** (`work`→`action`, `tool`→`music`, `leisure`→`art`),
    which §A step 4's semantic-tag policy rejects by rule.
- **0 flagged to curator.** All 13 decisions logged to `reviews/decisions.jsonl` (1 aggregated
  line for the 3-item tag family). Ledger updated, phase `self-check`; daily spend now $0.0320
  of the $5.00 cap.

## Always-on capture

- **Candidates**: none added — by construction every word behind a resolving marker already has
  an entry.
- **Observations** (4 appended to `polishing/observations.md`): `[pattern]` the same-lexeme
  discriminator for figurative compounds; `[pattern]` the counter/date entries created by recent
  new-entries runs are what generated most of this band's stale markers; `[pattern]` whole-entry
  accuracy review is a poor §4 instrument for link-only runs (0 of 13 flags touched a link —
  third run to request a `--dimensions links` mode); `[tooling]` three of the four accepted flags
  were sole-tag defects `check_tag_drift.py` would find in bulk for free.

## Backlog state updated

- `planning/wiki/ideas/backlog-queue.json` → `inline-link-stale-noentry`: `scope_estimate`
  1256 → **1101**, `scope_measured_at` 2026-08-12, title/filter/cursor updated, and the
  figurative-compound discriminator recorded in `notes`.
- `planning/wiki/ideas/cleanup-backlog.md` P35 → new dated paragraph with the batch result, the
  family analysis, and the running ~0.4% rate.

Item stays **open** — 1,101 pairs remain, all 2–3 character bases needing the same per-entry read.

## Metrics

`pipeline/metrics_snapshot.py --mode systemic-fix --changed 147` → entries_total 30385,
flags_applied 4, flags_rejected 9, flags_to_curator 0, review_queue_len 10282, candidates 167,
openrouter_spent_today_usd 0.032.

## Next cursor

Resume the P35 sweep at the **lowest remaining `entry_id`** in
`python3 build/check_stale_noentry.py --mechanical --json` — everything below **03164** is swept
for A1+A2. The comprehensive-polish frontier is untouched at `next: 06893`.
