# Routine session — 2026-09-05 (001)

**Mode**: `polish`
**Selector reason**: "polish: highest scheduler debt among eligible modes"
**Params**: `start_id: 7096`
**Signals**: candidates 176, max entry ID 30813, comprehensive next 7096, cross-model-review next
17239, unharvested observations 21, open backlog items 40, days since wiki 5.0. OpenRouter:
$0.00 spent today of $5.00 cap before this run.

## Pre-flight

- §0a rescue: `mcp__github__list_pull_requests` (open) → 0 open PRs. Nothing to rescue.
- §0b sweep: `mcp__github__list_branches` → only `main` exists on the remote. 0 stranded PRs,
  0 orphan branches.
- `git fetch origin main && git merge origin/main --no-edit` — already up to date.

## Polish work (`comprehensive_polish.md`)

### Priority lane (`polishing/priority/notes.txt`, cursor started at line 1401)

The priority file was regenerated 2026-09-02, just before a systemic mechanical sweep
(`routine(systemic-fix)` #3253 and neighbours) touched a very large fraction of the corpus. Scanning
forward from line 1401, the great majority of entries had `metadata.modified` within the last 30
days (touched by that sweep) and were skipped per the priority-lane rule. Scanned lines 1401–2719;
18 eligible entries (not modified in the last 30 days, file present) were opened and read in full
against the per-entry checklist:

02592_kabu, 03859_tento, 05034_chouryoku, 01541_rajio, 01683_akushu, 04291_ari, 04354_koumori,
04390_bara, 04361_sekkaku, 05921_kifuku, 00288_mokuji, 06125_ochuugen, 00152_kako, 02659_kudari,
00239_kiyoi, 05586_oshougatsu, 04668_hasamaru, 05712_haori.

Most were already solid (correct glosses/examples, tags complete, no factual problems) — no forced
edits. One substantive fix:

- **04361_sekkaku（折角）**: added a SIMILAR WORDS section contrasting {折角|せっかく} with わざわざ
  (a common intermediate-level confusion: せっかく centers on the value of the effort/opportunity
  and often implies regret if wasted; わざわざ centers on someone going out of their way and is
  common in thanks). Added the reciprocal `cross_references` entry. Also opened the neighbour
  **06073_wazawaza** (already recently modified, so not otherwise polished) to add the back-link
  cross-reference only, per the "neighbour" rule.

Four missing words surfaced during this lane (mentioned in notes with no dictionary entry) were
logged as candidates rather than left as hand `noentry` markers or silently dropped:
テント泊, 補聴器, 蝙蝠傘, 羽織袴.

### Frontier lane (sequential from 07096)

Read entries 07096–07115 (20 entries) in full. Two real fixes:

- **07099_nadeorosu（撫で下ろす）**: two example sentences carried stale `noentry` links —
  ⟦にかけて→にかけて：noentry⟧ and ⟦寝ぐせ→寝ぐせ：noentry⟧ — even though both words now have real
  entries (30530_nikakete and 10626_neguse respectively, confirmed with
  `build/check_stale_noentry.py --range 7099 7099`). Repointed both links to the real entries.
- **07110_shukkou（出向）**: the `cross_references` entry for 転籍 (26317_tenseki, a two-sense word)
  carried the label "change of registered domicile" — that's 転籍's koseki-transfer sense, not the
  business-transfer sense actually being contrasted with 出向 (secondment). Relabeled to "permanent
  transfer to another company."

The other 18 frontier entries were already correct and complete; no forced edits.

Two more missing words were found and rejected as candidates because they already have entries
(caught by `manage_candidates.py`'s duplicate check before being added): 寝癖 (10626_neguse) and
にかけて (30530_nikakete) — the discovery of these duplicates is what led to the stale-noentry fix
above, rather than becoming separate candidate entries.

## §3 Mechanical pass (on the 4 changed IDs: 04361, 06073, 07099, 07110)

- `normalize_notes.py --apply`: 0 rewrites (headers already canonical or pre-existing unknowns not
  introduced by this run).
- `auto_link.py --apply --confirm-real-entries`: +30 links across 07099 and 07110 (both had a few
  previously unlinked content words — 撫で下ろし's て-form tail, 先/元/社員, 在籍/転籍 — that this
  pass caught).
- `harvest_crossrefs.py --apply`: 0 new reciprocal pairs (the cross-references added by hand this
  run were already complete/reciprocal).
- `validate.py --id` on each of the 4: all valid. `verify_furigana.py` on each: all OK.

## §4 Self-check

`build/review_accuracy.py --ids 04361,06073,07099,07110 --budget 0.40`: reviewed all 4, **0
issues flagged**, cost $0.0020. Nothing to adjudicate; no `reviews/decisions.jsonl` lines needed
(the self-check was clean).

## §5 Metrics

`pipeline/metrics_snapshot.py --mode polish --changed 4` appended: `entries_changed: 4,
flags_applied: 0, flags_rejected: 0, flags_to_curator: 0, entries_total: 30604, candidates: 180,
review_queue_len: 12322, openrouter_spent_today_usd: 0.002`.

## §7 Wrap-up

- Cursors: `polishing/tasks/comprehensive/priority-cursor.txt` → `line: 2720`;
  `polishing/tasks/comprehensive/progress.txt` → `next: 07116`.
- Candidates added: テント泊, 補聴器, 蝙蝠傘, 羽織袴 (176 → 180).
- Observations: none logged this run (no systemic/tooling patterns found beyond the two individual
  stale-link/mislabel fixes above, which were fixed directly rather than deferred).
- `make index`: refreshed `entries_index.json`, `build/word_id_lookup.json`, `kanji/`.
- Files touched: 4 entry files (04361_sekkaku, 06073_wazawaza, 07099_nadeorosu, 07110_shukkou),
  `candidate_words.json`, `polishing/priority/notes.txt`'s cursor files, `reviews/accuracy/*.json`
  (new, local/gitignored), `pipeline/openrouter-ledger.json`, `pipeline/metrics-history.jsonl`,
  this log.

## Next

Priority-lane cursor now sits at line 2720 — still likely to run into more recently-swept entries
before finding fresh substantive-need candidates; that's expected and not a problem with the
priority list itself. Frontier lane continues at 07116. No curator-facing issues from this run.
