# Routine session — 2026-08-29 (002)

**Mode**: `systemic-fix`
**Selector reason**: "systemic-fix: highest scheduler debt among eligible modes"
**Backlog item**: `inline-link-stale-noentry` (Cleanup Priority 35)
**Signals**: candidates 147, comprehensive_next 7051, cross_model_review_next 14330,
unharvested observations 26, open backlog items 41, OpenRouter remaining $4.9937.

## Pre-flight

- §0a rescue: no open PRs — nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs.
- §0b orphan-branch sweep: `list_branches` returned `main` only — zero orphans.
- Lock acquired for `claude/intelligent-curie-flla3q`.

## Range worked

Entries **06389–06810** — the ninth per-entry short-base band of the stale-`noentry`
inline-link sweep, resuming where the 2026-08-25 run stopped (06388).

**177 A1/A2 pairs, 203 marker instances, across 123 entries** rewritten from
`⟦…→base：noentry⟧` to the real target entry ID. Every pair was judged against a
±70-character context window around the marker, per the item's `verify` rule.

Detector before/after for the band: 179 mechanical pairs → 2 (exactly the two rejections).
Dictionary-wide mechanical backlog: **290 → 123** pairs.

### Rejections (2)

Both are the polysemous-katakana-loanword family; both markers were left as `noentry`.

1. **06464** `二重のロック` (a door's double lock) → `08116_rokku`, *rock (music)*.
   Fifth firing of this one target (after 04562, 05762, 05909, and the original
   2026-08-08 フライ/パン pair). 08116's own notes say outright that "ロック as 'lock'
   is a different word with the same reading", and that declaration names the marker's
   own base form — so the refined detector class proposed on 2026-08-25 would catch it.
2. **06574** `コーラスパート` (chorus part / backup vocals) → `03106_paato`, whose entry
   covers **only** パート = part-time work. A **new false-positive family**: the target
   entry silently covers a different sense, with no self-declaration to scan for and no
   reading or lexeme mismatch. Logged as a `[pattern]` observation.

Running false-positive rate across the 1,603 hand-verified short-base pairs
(2026-08-09 → 2026-08-29): **~0.4%**.

### Marginal applies worth recording

- 06783 `一部始終` → `04192_shijuu`: glossed as the adverb "constantly", but the entry's
  notes carry an explicit second usage, "Noun: the whole story/sequence". Applied under
  the 2026-08-14 open-the-target-entry rule.
- 06450 `終活` decomposition → `29777_shuumatsu`; 06798 `デジタル化` → `28335_ka`
  (bare-kanji surface, tilde base — the established suffix precedent).

## §4 self-check

`build/review_accuracy.py` over all 123 changed entries, ~$0.054, 21 entries flagged
(17% — just under the reviewer-noise threshold), 25 issues, all `error` severity.
None concerned the inline-link rewrites themselves; all were pre-existing entry content.

**5 applied, 20 rejected, 0 escalated to the curator.**

Applied:
- 06435 狂喜 ("wild joy") carried the sole semantic tag `action`; added `emotion`.
- 06693 退去: dropped "departure" from the top-level gloss — too loose for a word that
  means vacating a residence or being made to leave a place.
- 06770 アイス ex[9]: "I always order iced" → "I always order iced drinks".
- 06631 面影 ex[1]: "She has traces of her mother in her features" → "She has her
  mother's features" (the example is sense 2, likeness, not sense 1, traces).
- 06631 面影 ex[7]: the translation made 若かった頃の面影 about how *things* looked;
  it is about the speaker's younger self. Rewritten.

Rejected:
- 14 semantic-tag flags, all the "in-list tag is too broad/narrow" family
  (`general` → `language`/`building`/`science`/`energy`/`technology`/… ), bulk-rejected
  per the 2026-06-11 semantic-tag policy with one aggregated ledger line.
- 3 formality flags (06572 不動産屋, 06739 いたわしい, 06798 押し進める). Policy applies
  a formality flag only when the entry's own prose contradicts the label; here it
  supports it — 06572's notes head a section "FORMAL ALTERNATIVES", listing the formal
  variants of the `informal` headword.
- 2 gloss flags on 06770 arguing that "iced" is an adjective and アイス a noun; the
  existing gloss reads "ice; iced (as in iced coffee)", whose parenthetical already
  carries the attributive use.
- 1 translation flag on 06770 ex[7] (飲み物はアイスにしよう → "let's go with iced drinks",
  which is accurate).

All 11 ledger lines appended to `reviews/decisions.jsonl`; spend recorded in
`pipeline/openrouter-ledger.json` (day total $0.0606 of the $5.00 cap).

## Backlog item state

`status` stays **open**. `scope_estimate` 290 → **123**, `scope_measured_at` 2026-08-29.
Both the queue entry and the Priority 35 prose section were updated.

**The leak is now visible in the residue.** Of the 123 remaining mechanical pairs, **41
sit below 06811 — in territory already swept.** About ten are the accumulated deliberate
rejections; the other ~31 are new arrivals, created by later `new-entries` runs adding a
target entry beneath a marker written months earlier (00622 名様, the 00658 counter words,
01510 北極星, 02273 映画祭, and a run of place names). This is the regrowth this priority
predicted from the start and the standing argument for Tooling 19's
`manage_candidates.py` sync hook.

**Next run should sweep a low-ID catch-up band starting at 00007**, not continue above
06810.

## Always-on capture

- Candidates added: none (this mode reads existing markers rather than prose).
- Observations appended to `polishing/observations.md`: 2
  - `[tooling]` fifth ロック firing; a one-line denylist for that single pair would
    already have saved four adjudications.
  - `[pattern]` the new "target entry silently covers a different sense" family (パート).

## Metrics

`pipeline/metrics_snapshot.py --mode systemic-fix --changed 123` appended:
entries_total 30564, flags applied 5 / rejected 20 / to curator 0, review queue 10833,
candidates 147, OpenRouter spend today $0.0606.

## Cursors

- Comprehensive polish frontier untouched (`next: 07051`) — not this mode's cursor.
- Backlog cursor recorded in `backlog-queue.json`: resume at **00007** for a low-ID
  catch-up band.
