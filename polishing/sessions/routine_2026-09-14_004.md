# Routine session 2026-09-14 #004

## Pre-flight

- Rescued PR #3294 (routine new-entries, green, mergeable, no comments) — merged.
- Swept PR #3290 (routine polish, failing CI, all touched entry IDs ≤ 07350, below the
  `next: 07371` frontier cursor) — closed as stale per the sweep rule.
- Left PR #3293 open (routine new-entries, failing CI on two real furigana-format bugs in its own
  new entries 30906/30907, but its file IDs are all ≥ 30893, above the frontier threshold, so the
  sweep rule doesn't authorize closing or fixing it — it's a different session's unmerged work).
- Orphan branch sweep: `claude/trusting-mendel-u8emu6` (PR #3294's branch) had no residue against
  main post-merge — logged as `prune-branch` in `reviews/needs_curator.txt`. Orphan branch
  `claude/trusting-mendel-o9cv7c` (PR #3290's branch) has real residue (13 entries) but already had
  a PR that was just closed as stale, so it doesn't qualify for a fresh PR under the sweep rule —
  logged as a no-action line explaining why.

## Mode: polish (selector reason: "polish: highest scheduler debt among eligible modes")

**Priority lane** (`polishing/priority/notes.txt`, cursor at line 14005): the 30-day skip-filter
collapse continues — scanning all 13,397 remaining lines to the end of the file found only 14
eligible entries (~0.1% hit rate this run). Cursor reset to `line: 1` since the file was exhausted;
logged as a `[pattern]` observation (won't self-resolve until ~2026-10-02).

Of the 14 priority entries read, 9 got real fixes:
- Six more hand-written `：noentry⟧` markers found and fixed (04520_kaadigan: ロングカーディガン,
  ショートカーディガン, ボタンレスカーディガン; 04407_sasa: 笹かまぼこ, 笹寿司; 05248_sukyanaa:
  フラットベッドスキャナー, ハンディスキャナー, ドキュメントスキャナー, オーシーアール/OCR) —
  converted to plain text, 9 words queued as candidates (C23441-C23449).
- `semantic: ["general"]` replaced with a concrete tag on 04034_shinro (transportation),
  00280_miman (quantity), 05454_houshiki (work).
- `cross_references` type corrected from `synonym` to `related` where the entry's own SIMILAR
  WORDS/RELATED WORDS section explicitly draws a nuance distinction rather than claiming
  interchangeability: 03379_seiseki (結果), 01624_iji (保つ, 保存), 06871_tegakari (糸口, ヒント),
  05454_houshiki (方法, 様式, 形式).

**Frontier lane** (07371-07387, 17 entries): 6 fixed.
- `semantic: ["general"]` replaced: 07372_monooki, 07374_hinanjo → building; 07378_noumu →
  weather; 07377_shinchoku's cross-reference types 進行/進展 corrected synonym → related.
- **Real bug**: 07385_momeru's own headword stem "揉め" (て-form/negative of 揉める, this entry)
  was inline-linked to 00289_momu (揉む, "to knead" — a different word, もむ not もめる) in all
  three examples plus once in notes' RELATED WORDS section (which also had the wrong word entirely:
  "揉め合う" isn't standard; the real word is 揉み合う). Fixed both. Root-caused via sudachipy: this
  container's auto_link.py was running in tokenizer-free fallback (sudachipy missing again, same
  gap as an earlier session today) and re-introduced the same wrong link after a first manual
  fix, matching by kanji alone; with sudachipy installed mid-run it correctly recognized "揉め" as
  self-headword and left it unlinked.

## Self-check

`review_accuracy.py` on all 15 changed entries found 2 flags, both real and applied:
- 07375_shigaisen: my own `weather` tag was wrong for UV rays (electromagnetic radiation, not a
  weather phenomenon like 07378's fog) → corrected to `science`.
- 07377_shinchoku: pre-existing note claimed しんぽ as an informal alternative reading — that's
  actually the reading of the unrelated word 進歩, not 進捗. Removed the false claim.

`review_links.py` checked all 20 link occurrences across the 15 changed entries in context and
flagged none.

## Cost

$0.0072 (accuracy) + $0.001 (links) = $0.0082 this run's self-check, on top of $0.5175 already
spent today. $0.5257 spent today against the $5.00 daily cap.

## Cursors

- `polishing/tasks/comprehensive/progress.txt`: `next: 07388`
- `polishing/tasks/comprehensive/priority-cursor.txt`: `line: 1` (file exhausted)

## For the curator

- Same standing item as prior sessions: the priority-lane skip filter needs a "last substantive
  edit" marker instead of raw file `mtime`; still not expected to recover until ~2026-10-02.
- `sudachipy`/`sudachidict_core` are missing from the container image again despite being pinned
  in `build/requirements.txt` — this is the second time today. Worth checking why the pinned
  dependency isn't already baked into the image runs start from.
- PR #3293 (a different session's new-entries work) still has two real, unfixed furigana-format
  bugs in its own new entries — left untouched per the sweep rule since its IDs are all above the
  frontier threshold this session's sweep covers. Someone (a future new-entries run, or you) will
  need to look at it directly.
- Otherwise nothing needs your attention — both self-check flags were real and fixed, the
  07385_momeru link bug was root-caused and fixed at the source (tokenizer), and the rest came
  back clean.
