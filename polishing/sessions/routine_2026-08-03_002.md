# Routine session — 2026-08-03 (002)

**Mode:** `systemic-fix`
**Selector reason:** "systemic-fix: highest scheduler debt among eligible modes"
**Signals:** candidates 992 (seen-in-entry 8), max entry ID 30357, comprehensive next 06755,
cross-model-review next 24501, unharvested observations 14, open backlog items 16.
**OpenRouter at start:** $0.0087 spent today, $4.9913 remaining.

## Pre-flight

- §0a rescue: `list_pull_requests` returned **no open PRs** — nothing to rescue.
- §0b sweep: nothing to sweep (no open `claude/*` PRs).
- Lock acquired for `claude/determined-rubin-pkn1bb`.

## Backlog item worked

`link-target-baseform-disagreement` — inline links whose declared `entry_id` resolves to a
real entry, but not to the link's own base form. **Third batch.**
Detector at start: 230 disagreements / 168 entries (dead targets 0).

Batches 1 (2026-07-31) and 2 (2026-08-01) cleared the compound-homophone and
kanji-variant-verb classes. This batch took the **noun-homophone class**: base is a
(mostly single-kanji) noun or bound morpheme and the declared headword is a different
word sharing the reading.

### Method note (the thing that made this batch safe)

Before adjudicating, the queue was split by **whether the proposal's reading matches the
surface furigana**. That axis, not "do the base and declared headword look different",
is what separates the mechanical repairs from the traps:

- **44 reading-match findings** — near-mechanical wrong-word repairs; every one still
  read in its example/note context before applying.
- **12 reading-mismatch findings** — where all the judgment lives; adjudicated one at a time.

### Decisions (34 families, 51 links, 36 entries)

**Repaired — reading matches (28 families):**
鮭→`19337_sake` (from 酒, incl. the しゃけ variant reading), 麺→`14644_men` (面),
鼻→`02164_hana` (花), 鐘→`02654_kane` (金), 革→`19310_kawa` (皮), 鯛→`14709_tai` (対),
四季→`11809_shiki` (〜式), 室→`09946_shitsu` (質), 件→`11720_ken` (〜軒),
者→`12734_mono` (物), 家具→`12314_kagu` (嗅ぐ), 都市→`01477_toshi` (年),
位置→`11554_ichi` (市), 街→`17306_machi` (町, 7 links), 費→`09877_hi` (費用),
端→`00936_hashi` (箸), 脚→`28733_ashi` (足), 約→`09563_yaku` (焼く),
級→`28171_kyuu` and 急→`13042_kyuu` (both from 九), 皮→`02187_kawa` (川),
油→`01693_abura` (脂), 奥→`00980_oku` (置く), 根→`03525_ne` (値), 名→`03496_na` (な),
像→`03691_zou` (象), 仲→`09513_naka` (中), 子→`09851_ko` (小〜).

**Repaired — reading mismatch, correct entry exists under the surface's own reading:**
- 者 (しゃ, 担当者・作者) → `04662_sha` 〜者, was `04660_sha` 〜社 — an **off-by-two ID slip**, 2 links.
- 中 (ちゅう, 試合中) → `09840_chuu` 〜中, was `03434_chuu` 注.

**Repaired — same-kanji entry is the same morpheme:**
- 心 (しん, 探究心 "mind, spirit") → `01321_kokoro` 心, was `03888_shin` 芯 (wick).
- 畳 (じょう, 20畳) → `02224_tatami` 畳, was `03343_jou` 情.

**Repaired to `noentry` — no correct target exists and the same-kanji entry would mislead:**
- 上 (じょう, 性質上) — was `09878_jou` 状. `00682_ue` 上/うえ "above" is the wrong sense.
- 帯 (たい, 温帯 "zone, belt") — was `03382_tai` 対. `02610_obi` 帯/おび is a kimono sash.
Both added as candidates (below) so the links can be upgraded later.

**Left alone (benign — family (b)):**
- 下/元 (もと, 月明かりの下で) and 本/元 (もと, 本末転倒) — same word, variant spelling.
- 球/玉 (たま, golf ball) — same word, variant spelling.
- 敵/仇 (かたき, 商売敵) — same word, variant spelling.
- お得 (`07739_otoku`) — the **declared target is correct for the sentence**; only the link's
  base label is loose, because the link wraps `{得|とく}` while the word in the text is お得.
- The whole benign orthographic family the item warns about (頃→〜ころ 24, 上げる→あげる 18,
  付ける→つける 7, 通り→どおり 5, 焼きたて 4, 追いかける 3, いい/良い 4, 被る 3) — untouched.

**Not touched:** the 13 ambiguous (>1 lookup candidate) findings, which belong to the curator.

### Result

- `check_link_baseform.py`: **230 → 179** disagreements (138 entries; 166 resolvable, 13 ambiguous).
- `check_link_targets.py` dead targets: **0** (unchanged).
- `build/validate.py`: 30148/30148 valid.

## §4 self-verification

Format-only (link-target) changes, so the furigana screener variant:
`review_runner.py --pass screening` over the 36 changed entries.
**Clean — 11 screened, 0 flagged** (25 skipped as already screened), est. $0.0015.
No flags to adjudicate, so no `reviews/decisions.jsonl` lines this run.
Ledger after the run: **$0.0102** spent today.

## §3 always-on capture

**Candidates added (2):**
- C22723 〜上 (じょう) — suffix: from the standpoint of, in terms of (性質上, 仕事上); seen in entry 01638
- C22724 〜帯 (たい) — suffix: zone, belt (温帯, 熱帯, 火山帯); seen in entry 04479

**Observations logged (3):** the reading-split triage axis as a reusable `[pattern]`; a
`[tooling]` idea for an adjacent-ID-slip detector variant (prompted by 04660/04662); and the
`[pattern]` that `noentry` is the honest repair when the correct word has no entry.

## State updated

- `planning/wiki/ideas/backlog-queue.json` — `link-target-baseform-disagreement`
  `scope_estimate` 230 → **179**, `scope_measured_at` 2026-08-03, notes extended with the
  third batch and the reading-split method. Status stays **open**.
- `planning/wiki/ideas/cleanup-backlog.md` — prose entry updated to match.
- `pipeline/metrics-history.jsonl` — one line appended (mode systemic-fix, changed 36).

## Next

The remaining 179 are mostly the benign orthographic family (~70 links) that must never be
swept, a thinner tail of real judgment cases (羽/羽根, 後/跡, 欠ける/かける, 利く/効く,
差す/挿す・指す・射す, 応える/答える, 変える/帰る, 切る/着る, 冷める/覚める, 載せる/乗せる,
温かい/暖かい, 長い/長〜), and the 13 ambiguous findings for the curator. A fourth batch could
take the **verb/adjective homophone tail** the same way — reading-split first, then one
decision per base-form family.
