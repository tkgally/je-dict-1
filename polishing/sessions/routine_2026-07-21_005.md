# Routine session — 2026-07-21 (005)

**Mode**: accuracy-review (phase: furigana)
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Range worked**: 16901–16935 (furigana screening + accuracy dimensions)

## Context

Selector gave `start_id: 16901`, phase `furigana`, session budget $2.50. Furigana
screening (`review_runner.py --pass screening`) ran at ~8s/entry and processes
entries out of order, so a full 400–600-entry range was not feasible in one
unattended run; screening was capped at the 16902–16935 prefix actually reviewed
this run (30 entries, filtered by internal `screened_at`, not file mtime). An
accuracy pass (`review_accuracy.py`) was run over the same 16901–16935 range to
add gloss/translation/tag coverage and make the run substantive.

## Furigana screening — 30 entries, 4 flags, all REJECTED

- **16904** 釈然としない — model reported truncated 釈然 readings at "pairs 3/4/9";
  entry contains only the correct `{釈然|しゃくぜん}`. Model index-confusion FP.
- **16928** ひったくり — `{置|お}き{引|び}き`; び is correct rendaku (置き引き = おきびき).
- **16935** 焙煎 — `{度合|どあ}い`; どあ is the kanji reading, い is okurigana (度合い = どあい). Correct okurigana split.
- **16902** 日常的 — model "Parse failure", no real concern.

All fall within documented false-positive families (index-confusion, rendaku,
okurigana split, parse failure). Consistent with calibration finding that
already-polished ranges screen at 0–5% precision.

## Accuracy review — 31 entries, 6 flags, 1 APPLIED / 5 REJECTED

- **16912** 洋紙 — semantic `object` is off-taxonomy (not in `VALID_SEMANTIC`).
  **APPLIED**: migrated `object` → `general` (no in-list material/object tag
  exists; reviewer also suggested `general`). Updated `modified`.
- **16927** 御朱印 — `general`→`culture`: in-list breadth nit → REJECT.
- **16928** ひったくり — `general`→`law`: in-list breadth nit → REJECT.
- **16929** さほど — formality `formal`→`neutral`: entry notes say "slightly more
  formal… common in writing and polite speech" → notes support `formal` → REJECT.
- **16930** 中吊り — `general`→`media`: in-list breadth nit → REJECT.
- **16931** 滅相もない — formality `formal`→`neutral`: entry notes say "somewhat
  formal and old-fashioned" → notes support `formal` → REJECT.

## §4 self-verification

Only entry changed (16912) was itself reviewed by §A's accuracy pass this run, so
per §4 step 3 no separate self-check pass was needed (the review was the check).

## Capture

- No missing-entry words surfaced in the reviewed range.
- 2 `[tooling]` observations appended to `polishing/observations.md`: (1) accuracy
  reviewer keeps flagging `general` as "too broad" (general→specific in-list
  nits, ~half of tag flags); (2) `review_runner` screening is slow/out-of-order
  and leaves non-contiguous partial state — filter by `screened_at`, not mtime.

## Ledger / metrics

- OpenRouter spend: +$0.015 (screening, ~30) +$0.0135 (accuracy, 31); today total $0.2449 (cap $5).
- Decisions logged: 10 (1 apply, 9 reject) → `reviews/decisions.jsonl`.
- Metrics snapshot appended to `pipeline/metrics-history.jsonl`.
- Review queue: 12950 → 12939 (removed 16901–16935).

## Next cursor

`polishing/tasks/cross-model-review/progress.txt` → **next: 16936**
