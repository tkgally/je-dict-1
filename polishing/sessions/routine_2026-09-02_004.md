# Routine session — 2026-09-02 #004

**Mode**: accuracy-review (reason: "accuracy-review: highest scheduler debt among eligible modes")

## Pre-flight

No open `claude/*` PRs, no orphan `claude/*` branches. Nothing to rescue or sweep.

## Accuracy review: entries 15239–16238

Ran `build/review_accuracy.py` over the 994 entries in this range (gloss, translation, tags,
notes dimensions), budget $2.50. The run had to be split into two background batches because a
single 994-entry pass exceeded a 10-minute tool timeout; the second batch picked up cleanly at
the first entry not yet in `reviews/accuracy/`. Combined: 994 entries reviewed, 71 entries
flagged, est. cost $0.4837.

### Adjudication (§C)

Every flag was opened and checked against the entry and my own knowledge before deciding.

**Applied (56 issues on 56 entries):**
- 39 `offvocab` tag migrations (deterministic family) — each off-vocabulary semantic tag replaced
  with the closest term from the closed list (`time`→`time-general`, `medical`/`medicine`→
  `health`, `transport`→`transportation` via the code's own migration table; the rest — `state`,
  `legal`, `sport`, `shape`, `role`, `quality`, `mimetic`, etc. — picked by hand from context).
- 6 `register` formality corrections where the entry's own notes contradicted its `formality` tag
  (e.g. 繰り越し tagged `formal` despite notes calling it common in mobile-plan and vacation-policy
  contexts; 質量 tagged `formal` for what is really a neutral-register scientific term).
- 1 `wrong-category` fix: 塗り物 (lacquerware) was tagged `tool`; retagged `art`+`daily-life`.
- 2 `gloss-meaning` fixes: 老婆 dropped the connotation-heavy "crone" from its main gloss; 四捨五入's
  gloss no longer wrongly limits rounding to whole numbers.
- 甲冑 (kacchuu): both 甲 and 冑 had been furigana'd identically as かぶと — a genuine duplicate-reading
  error; replaced the confusing per-kanji breakdown with links to the existing 鎧/兜 entries.
- 震源 (shingen): gloss and notes conflated hypocenter with epicenter; corrected to distinguish
  震源 (hypocenter) from 震央 (epicenter, a separate existing entry) and softened the 震源地 claim.
- 洞穴 vs 洞窟: notes had the frequency comparison backwards (洞窟 is in fact the more common general
  word); corrected.
- A few smaller notes-fact fixes: 額縁 wrongly listed as a 〜枠-suffix example (it's 額+縁, not 額+枠);
  ならば's note said it needs である before nouns, but it also attaches to nouns directly; an
  internal register inconsistency in 平然と and 至る所 where the notes' own wording contradicted the
  tag.

**Rejected (15 issues):** mostly the reviewer misreading an entry that was already correct —
終始する is standard usage (not disqualified from taking する), 一昨年/義兄/次女/勝者/手落ち's formal tags
already matched their own notes, 大縮尺/小縮尺's "large scale = more detail" is standard cartography
(not reversed), 隠喩's "states the comparison directly" correctly describes metaphor vs. simile
(not reversed), and several `notes-fact` flags pointed at text that already matched the reviewer's
own suggested correction (稲足/すあし, 乳液, 返信不要), suggesting the model compared against a stale
prior version.

All decisions logged individually to `reviews/decisions.jsonl` (`src: accuracy`).

### Mechanical pass (§3)

`normalize_notes.py`, `auto_link.py` (+30 links across 10 entries), `harvest_crossrefs.py` (no
new cross-references — everything already covered) on the 55 changed entries. All 55 pass
`validate.py --changed-only --ratchet`.

### Self-check (§4)

Skipped per §A step 8 — every changed entry falls inside the range this run's accuracy review
already covered.

## Cursor and queue

`polishing/tasks/cross-model-review/progress.txt`: `next: 15239` → `next: 16239`.
`reviews/queue.txt`: removed the 15239–16238 range (10,782 → 10,519 lines).

## Ledger

Recorded $0.4837 spend for 994 entries, phase `accuracy-review`.

## Observations

None logged this run.

## Metrics

entries_changed: 55, flags_applied: 56, flags_rejected: 15, flags_to_curator: 0
