# Routine session 2026-06-28 #005

- **Mode**: `systemic-fix` (selector reason: "highest scheduler debt among eligible modes")
- **Backlog item**: `furigana-cosmetic-wrappers` (P9 — non-standard but mostly-rendering furigana wrappers)
- **Slice**: 06650–07300 (the next ready slice after 2026-06-27's 06400–06650 sweep)

## Pre-flight

- §0a rescue: no open PRs → no-op.
- §0b sweep: no open `claude/*` PRs to sweep → no-op.
- Lock acquired for `claude/modest-keller-rokvux`.

## Work done

Per-entry-verified furigana wrapper fixes: **34 instances across 26 entries**.
Dict-wide detector total `1517 → 1485` (`check_furigana_format.py --summary`).

By sub-pattern:

- **Katakana-loanword de-wraps** (redundant hiragana ruby over katakana → bare katakana):
  06658 (コミュニケーション), 06974 (チーム), 07021 (コーヒー), 07031 (クリニック),
  07034 (タオル), 07036 (ゴキブリ), 07037 (トイレ, ラベンダー), 07056 (シニア),
  07057 (プレゼント), 07140 (ベビーチェア, オストメイト), 07141 (シニア), 07143 (イクメン),
  07154 (カビ), 06860 (ブランド — inside an inline link).
- **Pure-kana de-wraps**: 06912 (やっぱり), 07097 (しまう), 07103 (めまい ×2, ふらふら),
  07130 (ひょっとこ), 07160 (きっかけ).
- **Numeral de-wraps**: 06727 — `{男女|だんじょ}の{比率|ひりつ}は{3|さん}{対|たい}{2|に}だ。`
  → `…は3{対|たい}2だ。`
- **o-prefix repositions**: 06810 (`{お元気|おげんき}` → `お{元気|げんき}`),
  07028 (お{客|きゃく}), 07116 (お{経|きょう}), 07132 (お{盆|ぼん}).
- **Over-wrapped okurigana splits**: 06998 (`{旨み|うまみ}` → `{旨|うま}み`),
  07038 (`{卸し金|おろしがね}` → `{卸|おろ}し{金|がね}`, `{下ろし金|おろしがね}` → `{下|お}ろし{金|がね}`).
- **Detector mis-suggestion corrected by hand**: 07140 `{おむつ替|か}` was flagged `o-go-prefix`
  with suggestion `お{むつ替|か}`, but おむつ is a fixed kana word (not honorific お + むつ) and
  the only kanji is 替, so the correct rewrap is `おむつ{替|か}`. Logged as a `[tooling]`
  observation.

Notes:
- 06860's de-wrap sat inside an inline link: `⟦{ブランド|ぶらんど}→ブランド：05221_burando⟧`
  → `⟦ブランド→ブランド：05221_burando⟧`. Resolves by explicit `：05221_burando`
  (verified the target entry exists), so the link still works.
- Post-fix: detector on 06650–07300 = 0 remaining; `validate.py --range 6650 7300` = 569/569 valid;
  no new missing furigana introduced.

## §4 self-check (furigana screener, independent model)

`review_runner.py --pass screening` over the 26 changed IDs (17 screened, 9 skipped, est. $0.0021).
**3 flags, all REJECTED** — each a screener compound-reading-split false positive
(`{次々|つぎつぎ}`, `{入場料|にゅうじょうりょう}`, `{低血圧|ていけつあつ}` reported as truncated
although correct in the entry), and none touched the furigana I edited. One aggregated REJECT
line logged to `reviews/decisions.jsonl`. My actual edits (o-prefix/okurigana splits) came back OK.

Outcome: **clean — 0 applied, 3 rejected, 0 flagged to curator.**

## Always-on capture

- Candidates: none added (format-only slice; loanwords were in others' examples, not gap signals).
- Observations: 1 `[tooling]` line on the `check_furigana_format.py` o-go-prefix mis-suggestion.

## State updates

- `planning/wiki/ideas/backlog-queue.json`: `furigana-cosmetic-wrappers` scope_estimate `1517 → 1485`,
  notes updated, item stays **open** (dictionary-wide; ~1485 remain).
- `planning/wiki/ideas/cleanup-backlog.md` P9: appended 2026-06-28 slice-swept update.
- `pipeline/openrouter-ledger.json`: +$0.0021 self-check (spent today $0.179).
- Metrics snapshot appended to `pipeline/metrics-history.jsonl`.
- `pipeline/routine-state.json`: advanced by the selector in §1.

**Next ready slice**: 07300+ and the larger pre-06000 backlog.
