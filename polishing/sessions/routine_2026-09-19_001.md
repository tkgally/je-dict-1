# Routine session 2026-09-19 #001

**Mode**: systemic-fix
**Reason**: systemic-fix had the highest scheduler debt among eligible modes.
**Backlog item**: `transitivity-review-queue` (reviews/transitivity/disagreements.jsonl)

## Pre-flight

No open PRs to rescue or absorb (`list_pull_requests` returned none). `list_branches`
returned only `main` — no orphan `claude/*` branches to sweep. The session branch
(`claude/festive-knuth-89f7vl`) already matched `origin/main` HEAD exactly (its
prior PR had already been merged), so no rescue/absorb/reset was needed.

## Work done

Worked the transitivity disagreement queue top to bottom, 100 entries (14177_gaitou
through 16729_sessei). For each: judged the word's own valency from its gloss and
part of speech (rather than trusting either model's surface-pattern verdict), and
set `metadata.tags.transitivity` to `transitive` / `intransitive` / `both`. As in
recent batches, this was almost entirely two-character kango + する nouns (該当,
賞賛, 追放, 適応, 遭遇, 造形, 矯正, etc.), with a few borderline cases resolved as
`both` where standard usage genuinely supports two argument structures (連結,
噴射, 近代化, 順延, 投球, 射撃). No real 自他 (transitive/intransitive) pairs
turned up needing `prominent_see_also`: the one `pairs` hint in the batch
(14598_shuukei → 集まる) was not a genuine transitivity counterpart (集計 "to
tally" and 集まる "to gather" are related in meaning but not a paired
verb-stem-plus-auxiliary jita pair), so no link was added.

Removed all 100 resolved lines from `reviews/transitivity/disagreements.jsonl`
(1884 → 1784 remaining). Updated the backlog item's `scope_estimate` in
`planning/wiki/ideas/backlog-queue.json`.

## Mechanical pass (§3)

- `normalize_notes.py`: 0 rewrites. 4 unknown headers found (JAPANESE COURT
  SYSTEM, ACCOUNTING, MEDICAL CONTEXT, TYPICAL CAUSES), all pre-existing, not
  introduced by this run.
- `auto_link.py`: 56 links added across 32 entries.
- `harvest_crossrefs.py`: 0 new cross-references — the SIMILAR/RELATED WORDS
  bullets in the changed entries were already linked, self-referential, or
  pointed at words with no entry.
- `validate.py --changed-only --ratchet`: 100/100 changed entries valid.

## Self-check (§4)

`review_accuracy.py` on the 100 changed entries: 5 flagged (~$0.048), all
adjudicated.

**Rejected (5), all reviewer misreadings — the entries were already correct:**
- `14864_joukoku`: reviewer said the court-system diagram omits 控訴 (the
  intermediate appeal); the notes' JAPANESE COURT SYSTEM list already includes
  it (第一審 → 控訴 → 上告). No error.
- `15031_kyuusui`: reviewer called the gloss "water supply" too narrow; it is
  a standard, accurate gloss, and the notes already contrast 給水 with 排水.
  No error.
- `15146_taishaku`: reviewer claimed 貸方/借方 were reversed in the ACCOUNTING
  note; the note already correctly states 貸方 = credit side, 借方 = debit
  side (exactly what the reviewer's own suggested fix says). No error.
- `15292_douhan`: reviewer said the `formality` tag contradicted the notes'
  "formal word" claim; the tag is already `formal`. No contradiction.
- `16729_sessei`: reviewer said 節制 and 摂生 are not homophones; both are
  read せっせい, so they genuinely are. No error.

All 5 decisions logged to `reviews/decisions.jsonl`.

`review_links.py` kana-link check on the 56 new links (32 bases): 0 flags.

## Needs curator

Nothing new.

## Checks

`make gate`: run at wrap-up (see commit). `make index` rebuilt
`entries_index.json`, `word_id_lookup.json`, and the kanji index.

## OpenRouter spend

$0.0482 (accuracy self-check) + ~$0.002 (link self-check, not separately
ledgered, matching prior runs' convention) against a $5.00 daily cap; ledger
running total $0.0482 today (first routine run of the day).

## Next

Backlog cursor: `transitivity-review-queue` has 1784 lines remaining, next
starting at whatever line is currently first in
`reviews/transitivity/disagreements.jsonl` (the file is not strictly ID-sorted;
work continues top to bottom).
