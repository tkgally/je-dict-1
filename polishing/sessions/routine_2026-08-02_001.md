# Routine v2 session — 2026-08-02 (mode: `wiki`)

**Selector reason**: `wiki: highest scheduler debt among eligible modes`

**Signals**: candidates 1,001 (seen-in-entry 10) · max entry ID 30,317 · comprehensive frontier 6,739 · cross-model-review cursor 23,608 · unharvested observations 29 · open backlog items 15 · OpenRouter remaining $5.00 of $5.00

**Pre-flight**: `mcp__github__list_pull_requests` returned **zero** open PRs — nothing to rescue (§0a) and nothing to sweep (§0b). Lock acquired for `claude/determined-rubin-0j2zm2`.

## Activities

### [F] Harvest — 25 observations, all cleared

Sources: the 2026-08-01 wiki run's own §7 CI gate, the two 2026-08-01 polish runs, the 2026-08-01 systemic-fix run, the 2026-08-01 new-entries run, and the 2026-08-01 accuracy-review run.

| Filing | Kind |
|---|---|
| **Cleanup P36** — bare-kanji headwords (248) | new |
| **Cleanup P37** — `politeness: "polite"` on plain vocabulary + the detector that reports zero | new |
| **Cleanup P20 update** — dictionary-wide off-vocab tag measurement and three-step sequencing | update |
| **Tooling 56** — nothing checks that a headword carries furigana | new |
| **Tooling 57** — closed-paradigm cross-reference symmetry rule | new |
| **Tooling 58** — review cursor advances to `end+1` even over already-reviewed ranges | new |
| **Tooling 59** — `check_link_baseform.py`: suppress reading-changing proposals; document `--by-base` | new |
| **Tooling 60** — `onomatopoeia` is valid in both `pos` and `semantic`; the convention does not exist | new |
| **Tooling 61** — duplicate-conjugation detector misses the i-adjective variant | new |
| **Tooling 62** — `find_missing_furigana.py` cannot tell "wrap this" from "rewrite this" | new |
| **Tooling 6 update** — the shipped nine mappings have 660 unapplied hits | update |
| **Tooling 21 update** — the screener is serial, `review_accuracy.py` is concurrent (~75 min vs ~13 min for 566 entries) | update |
| **Tooling 48 update** — third failure mode: `get_check_runs` stale in the *success* direction | update |
| **Entry Follow-ups** — the 00680–00760 calendar band; prohibitive な; the 義-family measured clear | new ×3 |

### [C] `topics/inline-link-integrity.md` — new section "The unlinkable residue"

Bound morphemes, copula/auxiliary inflections, morphemes whose surface form is already occupied, and metalinguistic mentions, unified as *Japanese that is not a word-token*. Measured scopes included so the section files each case at its true size.

### [E] `ideas/backlog-queue.json` sync

Three new items (`headword-missing-furigana` scope 248, `tag-politeness-polite-on-plain-vocab` scope 75, `crossref-closed-paradigm-symmetry` needs-detector); `unknown-semantic-tags` rescoped 5,009 → **4,900** against a fresh measurement. JSON validated.

### [H] Twenty-ninth metrics refresh — `topics/quality-metrics.md`

411 runs (16 new lines since the 395-run refresh). Both adverse metrics from the twenty-eighth refresh reversed; a new one appeared underneath. Full table in the page; headline in the wiki log.

## Measurements taken this run

| Claim as observed | Measured | Effect on the filing |
|---|---|---|
| "`27889_ageru`'s headword has no furigana braces; headword position escapes some check" | **248** bare vs **25,773** braced (99.05%); 126 created 2026-07, 13 in 2026-08 | enlarged ~250×, filed as an **active** defect + a ratchet |
| "`TAG_MIGRATION` covers only 9 tags; ~30 more are needed" | the shipped **9 cover 660 live instances (13.5%) and have never been swept**; 818 labels total, top-50 caps at 48.4% | reordered: run it before extending it |
| "~174 `polite` entries are worth a targeted sweep" | 174, but **99** carry an overt honorific marker and much of the other 75 is correct | narrowed to a per-entry review of a few dozen |
| "the tier-1 link rule fails on any sentence with `で、`" | **225** polished examples (0.6%); `⟦で→です⟧` 0 vs 2,384 links to `だ` | narrowed; refiled as rediscovery cost, not corpus damage |
| "`onomatopoeia` lives in `pos`; tell the reviewer" | valid in **both** vocabularies; corpus split **172 / 116 / 71** | reversed — filed as an undecided convention |
| "other 義-family entries may carry the invented address form" | **0** remaining `{義\|ぎ}の` in the corpus | closed |
| `check_tag_drift --check politeness-unsupported` | **0 flags** — predicate never examines `polite` | instrument defect, filed alongside P37 |

## §4 self-verification

**Not applicable** — this is a `wiki`-mode run; no entry files were created or modified (`git status --porcelain -- entries/` is empty). No OpenRouter spend this run; the daily ledger is untouched at $0.00 of $5.00.

## §5 metrics

Appended: `{"mode": "wiki", "entries_changed": 0, "entries_total": 30108, "review_queue_len": 13233, "comprehensive_next": 6739, "observations_lines": 694}` — 412 lines total.

## Observations logged

One `[pattern]` raised by the metrics refresh: the polish frontier advanced 34 entry IDs while the dictionary grew 59 in the same window, so the unpolished remainder widened by 25 entries in two days. Routed to the curator as a scheduling question, not Routine work.

## Cursors

- `planning/wiki/log.md` — appended
- `polishing/observations.md` — 25 cleared, 1 new raised (694 lines)
- No new wiki pages, so `planning/wiki/index.md` needs no change
- No entry cursors touched (`wiki` mode)

## Build

**Skipped** per §6 — wiki-only run, markdown changes only, `docs/` unaffected.
