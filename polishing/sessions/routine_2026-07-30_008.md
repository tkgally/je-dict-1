# Routine v2 — 2026-07-30 session 008 (`wiki`)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: candidates 1,018 (seen-in-entry 9) · max entry ID 30258 · comprehensive next 6705 ·
cross-model-review next 22213 · **unharvested observations 23** · reviewed entries 586 ·
open backlog items 11
**OpenRouter**: spent today $0.4879 of $5.00 cap; **this run spent $0.00** (no model calls)
**Branch**: `claude/determined-rubin-v3zafm`

## Pre-flight (§0)

- §0a `mcp__github__list_pull_requests` → **0 open PRs**. Nothing to rescue, nothing to sweep.
- §0b MCP stranded-PR sweep: no candidates (empty open-PR list). `sweep-stranded-prs.py` not run
  (403s in this environment by design).
- Lock acquired cleanly: `routine.lock` for `claude/determined-rubin-v3zafm`.
- Working tree clean at `c88d8ff17`, level with `origin/main`.

## Work done (§2 — `wiki` per `planning/maintain-knowledge-base.md`)

### [F] Harvest — 19 observations, all cleared

Sources: the day's two polish runs (frontier 06694–06697 and 06698–06704), the new-entries run,
the accuracy-review run, and this Routine's own §7 CI-gate note from run 1.

**Four observations were measured against the corpus (30,049 entries) before filing, and three of
the measurements changed the recommendation:**

| Observation as filed | Measurement | Consequence |
|---|---|---|
| Cross-ref with no `target_id` validates cleanly → add `required: [target_id]` | 64 such refs; **59 carry a `label`** and are intentional pointers to entry-less words; **5** are defects | Rule is "`target_id` **or** `label`" — the proposed one-liner would have broken 59 working refs |
| Sole-`action` tags dense in the frontier band | **2,085** entries dictionary-wide (~7%) | Not a band finding — the largest tag family after sole-`general`; filed as **Cleanup P34** |
| A tag can contradict the entry's own REGISTER note | **6** anchored to the word's position in the REGISTER line; **1,238** unanchored; converse **0** | The anchor is the check; loose form is model work, not checker work |
| Braced inline-link base forms, 37 entries, provably safe | **36**, and already filed twice as Cleanup P24 | Confirmed — the finding is about the *backlog*, not the defect (see below) |

**Filed:**

- **New Tooling 48** — the §7 CI gate cannot distinguish "CI is slow" from "CI never started", and
  its `actions_list` cross-check *agreed with it when it was wrong* (PR #3069: eight polls over
  ~14 min, both endpoints `total_count: 0`, job `started_at` 07:46:00Z). Durable rule: absence of
  a check run is evidence of "not visible yet", never "not queued". Actionable part is log wording.
- **New Tooling 49** — read-only inline-link **suggester** (propose `⟦…⟧`, never write). The
  measured 90/10 lookup-to-judgment split on 06702 is the argument.
- **New Tooling 50** — `find_missing_furigana.py --json` accepted but not machine-readable.
- **New Tooling 51** — the `target_id`-or-`label` schema clause (above); 5 entries to fix.
- **New Tooling 52** — does `check_semantic_clusters.py` treat a `prominent_see_also` mention as
  satisfying the pair requirement? **407** entries carry the suspicious combination.
- **Tooling 24 update** — the post-fix screener measurement this item asked for: **0 applied of 29
  across three consecutive post-fix runs**, a self-refuting flag (22097), and the screener now
  rate-limiting `accuracy-review` at ~1.9 entries/min vs the accuracy pass's ~3.4. Recommendation
  is now **retire or heavily downsample**.
- **Tooling 6 update** — the reviewer's `tags` dimension is a source of *destinations*, not just
  detections: 34 unmappable tags resolved, one block's curator escalation cut **87→46**. Run a
  `--dimensions tags` sweep over the *existing* `needs_curator.txt` backlog, with a standing
  rejection of `→ general` suggestions.
- **Tooling 44 update** — the measured formality-vs-REGISTER slice (6 entries, high precision).
- **New Cleanup P34** — sole-`action` on a verb, 2,085 entries.
- **Cleanup P11 / P13 / P21 / P24 / P31 updates** — frontier wrong-category sole tags (3 of 4
  entries); the `→ general` suggestion family as a taxonomy question; the zero-link band confirmed
  unbroken ~06150→06704 plus the file-don't-half-link rule; P24's third independent discovery; and
  P31's detector specified via the populated `conjugation` field.
- **Entry Follow-up** — 06703 振り切る (15 examples, zero links, core tier; deliberately skipped
  rather than half-linked).
- **`topics/cross-references.md`** — two field-level traps added (katakana headword needs a
  *transliterated* hiragana `reading`; prose + `prominent_see_also` is not a substitute for the
  structured pair link).
- **`topics/instrument-defects.md`** — case **7** (the CI gate), the first case on that page in
  the *merge* path.

**Closed with no action**: the variant-okurigana finding (00463 分かる). The reporting run grepped
the corpus itself and found 0 remaining — a one-off, not a family.

**[skill] recommendations (recorded, not acted on — a wiki run does not modify skills)**:
`vocabulary-notes` — kanji named as objects of discussion in English etymology prose ("the 仮
element") still need furigana wrappers (4 of 18 new entries missed it); `cross-reference-entry` —
a katakana headword's `reading` must be transliterated to hiragana, not copied.

### [H] Metrics trend — twenty-eighth refresh

`topics/quality-metrics.md` refreshed: 12 new metrics lines, **383→395 runs**, ~11,642 adjudicated
flags. Window 384–395 (2026-07-29 09:31 → 2026-07-30 18:28): **785 flags — 515 applied, 95
rejected, 175 escalated**; severity **420 error / 30 warn**; 784 entries changed; dictionary
30,012 → **30,049**.

Two metrics judged to be **moving the wrong way**, both logged as `[pattern]` observations for the
next harvest:

1. **Review queue rising a second consecutive refresh** — 12,352 (floor) → 12,919 → **13,375**
   (+1,023 from floor). First sustained rise in the series.
2. **Curator escalations are a trend** — 337 in four days against 2 in the preceding two windows,
   all off-vocabulary `tags`, against a human loop that closed three items in the same period.

Other figures: `tags` apply-among-decided **93.3%** (new series high, by construction on
not-in-list flags); gloss 35.3%, translation 41.7%; furigana screening **0.0% (0/29)**; OpenRouter
daily peak **$0.65** against the $5 cap.

### [E] Queue sync and link lint

- `ideas/backlog-queue.json`: four new items (`tag-sole-action-verb` 2,085,
  `crossref-missing-target-id-unlabeled` 5, `notes-duplicate-conjugation-block`,
  `tag-formality-contradicts-register-note` 6); one duplicate resolved (`link-baseform-braces` →
  `inline-link-braced-base-form`); one **promotion** — that item from priority 24 to **5**, on the
  grounds that a batch-ready, provably-safe, user-visible, 36-entry item found by three runs and
  worked by none is exactly what should be scheduled first.
- Wiki anchor lint over all `planning/wiki/**.md`: **2 broken anchors found and fixed** (a stale
  Tooling-6 heading slug in `quality-metrics.md`; a hyphen-count error in a P28 self-link).
  Re-run clean at **0**. The slug function was validated against a known-working repo anchor
  before use — the calibration step case 6 of `instrument-defects.md` exists to teach.

## §3 Always-on capture

- Candidates added: **none** — a `wiki` run reads no entries, so no missing-word encounters.
- Observations logged: **2** `[pattern]` lines from the metrics refresh (see above).

## §4 Self-verification

**Skipped — no entries created or modified this run** (markdown, one JSON index, one JSONL append).
OpenRouter untouched; **$0.00 spent**; ledger unchanged.

## §5 Metrics

Appended: `{"ts": "2026-07-30T21:30:04Z", "mode": "wiki", "entries_changed": 0, …,
"entries_total": 30049, "review_queue_len": 13383, "observations_lines": 627}`.

## §6/§7 Wrap-up

- **No `make build`** — wiki-only run, `docs/` untouched.
- Cursors: none to advance (`wiki` mode owns no entry cursor). `pipeline/routine-state.json`
  advanced by the selector in §1.
- Files changed: `planning/wiki/` (index, log, 3 topics, 3 ideas pages, backlog-queue.json),
  `polishing/observations.md`, `pipeline/metrics-history.jsonl`, `pipeline/routine-state.json`,
  this log.

## Next

Next run's mode is the selector's call. Two things this run leaves pointed at whoever picks up
`systemic-fix`: **`inline-link-braced-base-form`** is now priority 5, batch-ready, 36 entries, and
provably safe — it should clear in a single run; and **`crossref-missing-target-id-unlabeled`** is
5 entries. Both are small enough to do together.
