# Routine session 2026-09-22 #005

**Mode**: systemic-fix (reason: "systemic-fix: highest scheduler debt among
eligible modes")

**Params**: backlog_item `transitivity-review-queue`
(`reviews/transitivity/disagreements.jsonl`)

## Batch

Worked the queue file top to bottom, 100 entries (02698-04767 plus a run of
recent-tier entries 30603-30690): `tags.transitivity` set to
transitive/intransitive/both for each, judged from the verb's own valency
(case marking of its patient argument, real-world usage, and the entry's own
examples), not the models' English-semantics readings, then removed from
`disagreements.jsonl`. 94 were "no-example-evidence" (both models agreed but
the entry's own examples didn't demonstrate it); 6 were genuine
"models-disagree" flags.

15 entries were tagged against at least one model's verdict, applying the
dictionary's established に/へ-marked-target convention (a verb whose patient
is marked with に rather than を is intransitive here even where the English
translation reads as transitive): 挑戦, インタビュー, サイン, 販売, 捜査,
発売, 代理, 伝言, 万歳, 洗面, 降格, ジョギング against at least one model's
"transitive" call; 処する and 油を売る (idiom, self-contained を, no external
argument) reset from the disagreement pair; 志望 corrected to transitive on
direct example evidence ("当社を志望したのですか") that the automated
no-example-evidence check had missed. 中止, 回復, 移転, 解散, 消化 kept as
"both" — genuinely dual-pattern, each evidenced by both a が-subject and a
を-object use in the entry's own examples. No real 自他 pairs found needing
`prominent_see_also`; the pairs hints were empty `{}` for all 100.

## Self-check

`review_accuracy.py --ids` (100 entries, ~$0.05): 8 flagged. 2 applied —
03846_sain's gloss ("sign" → "signal", matching sense 3 and the SIMILAR WORDS
line already in the notes); 04085_yudan's notes dropped a claim that 油断 is
used as a na-adjective (油断な), which isn't standard usage (its
`adjective-na` pos tag wasn't touched — flagged to `needs_curator.txt` since
that's a bigger question than a wording fix). 6 rejected as model
misreadings: 03497_chokin (the postal-savings-vs-bank distinction the note
draws is real), 03833_intabyuu and 04688_koukaku (both cases of the model
reading an に-marked example or a causative -させる example as evidence the
base verb takes を), 04189_zaigaku and 04376_senmen (claimed contradictions
that aren't — the note and the flagged text agree), 04700_torihiki
(etymology simplification, not a factual error).

`review_links.py --ids --skip-decided` (155 links, ~$0.006): 8 flagged. 2
kept (いけない×2, both matching a COMMON PATTERNS line already in
02335_ikenai's own notes; ため×1, correctly scoped to just the noun before a
separate に). 6 retargeted, all pre-existing mistargetings unrelated to this
run's transitivity edits: two ために occurrences linked to the noun entry
01145_tame instead of the dedicated expression entry 28332_tameni; one な
(attributive form of 法的だ) linked to the plain copula 09496_da instead of
09497_na; two なので occurrences linked to 09501_node instead of their own
unique-tier entry 29354_nanode. Applying the な retarget had a side effect —
`review_links.py --apply-decisions` also stripped an unrelated, correct
だ→09496_da link elsewhere in 04000_iten because it shared the same
(entry, base, target) key as the retargeted occurrence; caught it in the
post-apply validation pass, added a `keep` decision for that occurrence, and
restored the link by hand.

All decisions logged to `reviews/decisions.jsonl` and
`reviews/link_decisions.jsonl`.

## Mechanical pass

`normalize_notes.py` (0 rewrites), `auto_link.py` (+20 links across 6
entries, mostly notes in the 30600s-tier entries that hadn't been linked
before), `harvest_crossrefs.py` (0 new proposals — everything harvestable
already had its cross-reference), `validate.py --changed-only --ratchet`:
100/100 clean.

## Ledger

openrouter-ledger.json: +$0.0482 (self-check, 100 entries) + $0.006
(link-check, 100 entries). Day total: $1.0609 of $5.00 cap.

## Backlog item

`transitivity-review-queue` in `planning/wiki/ideas/backlog-queue.json`:
`scope_estimate` 1384 → 1284; detector count confirmed at 1284 lines
remaining in `disagreements.jsonl`; run notes appended to the item.

## Observations logged

None this run beyond the curator flag above.

## Entries changed

100 total (the full batch); `03846_sain` and `04085_yudan` also carry a
self-check content fix beyond the transitivity tag; `04000_iten` also carries
the link retarget-and-restore described above.
