# Routine session 2026-09-22_002

**Mode**: accuracy-review — entries 9001–10000
**Reason**: highest scheduler debt among eligible modes (selector output)
**Signals**: candidate_count 164, seen_in_entry_count 5, max_entry_id 31013, comprehensive_next 8116,
cross_model_review_next 9001, unharvested_observations 38, open_backlog_items 32.

## Pre-flight

No open `claude/*` PRs, no stray branches. Nothing to rescue or absorb. (One housekeeping commit was
made mid-run for `pipeline/routine-state.json`, the selector's own persisted state, to satisfy a
repo git-check hook while the external review batch ran in the background; it carries no content
changes.)

## What this run did

Sent entries 9001–10000 (995 entries) to the external reviewer (gloss, translation, tags, notes),
budget $2.50, actual spend $0.4753. 50 entries came back flagged (54 individual issues). Adjudicated
every one by reading the entry:

- **Applied (29 entries edited, 35 issues)**: 11 off-vocabulary semantic tags migrated to the closed
  list (mostly dropped where a sibling tag already covered the concept, e.g. `age`→dropped alongside
  `health`, `attitude`→`evaluation`, `style`/`method`→`abstract`/`culture`, `airport`→`transportation`);
  6 `formality: neutral` tags that flatly contradicted their own entry's "Formal"/"formal" register
  note, corrected to `formal`; several gloss narrowness/scope mismatches where the top-level gloss
  claimed a sense no definition or example actually covered (セーフ too narrow, ダベる and こなす and
  〜度 and どうか and コンテナ and 線 too broad or missing a sense); a handful of factual notes fixes
  (a duplicated bullet in 泣き虫's ～虫 pattern list, an odd-number/even-total mixup in 奇数's gift-money
  note, a wrong "different kanji" claim on 汗を掻く, a stray "masculine" label on だろう, an overclaimed
  relative-commonality note on ビオラ, and a scope-creep "porch" section inside ポーチ's pouch-only
  entry, removed and flagged to the curator since a same-kana candidate can't get past the duplicate
  checker).
- **Rejected (22 issues)**: mostly the reviewer either misread furigana-disambiguated readings (車|しゃ,
  船|せん), asserted a false contradiction that wasn't actually there on a second read (三角形/三角,
  体's style-vs-state senses, 後's register note), suggested a formality/politeness value that isn't in
  the closed vocabulary (`colloquial`, `literary`), or flagged an already-correct translation
  (でかい声で話すな, どのような状況でも).

Every decision logged to `reviews/decisions.jsonl` (53 lines — a couple of multi-tag fixes on one
entry share one aggregated line with an `n` count).

## Mechanical pass (§3)

- `normalize_notes.py --apply` on the 29 changed entries: 0 rewritten; 18 non-canonical header lines
  found, all pre-existing (e.g. "BASEBALL USAGE:", "PLAIN EQUIVALENTS:") and unrelated to this run's
  edits — not touched, out of scope.
- `auto_link.py --apply --confirm-real-entries`: 4 links added across 3 entries.
- `harvest_crossrefs.py --apply`: 0 entries rewritten (54 bullets parsed, all already linked or had no
  matching entry).
- `validate.py --id` on each of the 29 changed entries: all valid.

## Self-check (§4)

Skipped per §A step 8 — these 29 entries are inside the range the external accuracy review just
covered; the review itself was the check.

## Ledger / metrics

OpenRouter spend today: $0.528 (accuracy-review $0.4753 + $0.0527 from an earlier run today).
Metrics snapshot: entries_changed 29, flags_applied 35, flags_rejected 22, flags_to_curator 0,
entries_total 30804, candidates 164, review_queue_len 3320.

## Cursor

`polishing/tasks/cross-model-review/progress.txt`: `next: 09001` → `next: 10001`.
`reviews/queue.txt`: removed all lines for entries 09001–10000 (376 lines).

## Needs curator

One item logged to `reviews/needs_curator.txt`: 09488_poochi (ポーチ) had an out-of-scope "porch"
sense removed from its pouch-only entry. That sense may be real, but the duplicate checker treats any
same-kana candidate as a match against this entry, so it can't be queued as a candidate without a
manual override. Left for a curator call on whether it's common enough to warrant its own entry.
