# Routine session — 2026-08-05 (004)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Selector signals**: candidates 976 (5 "seen in entry"), max entry ID 30414,
comprehensive_next 6787, cross_model_review_next 25872, unharvested observations 4,
reviewed entries 586, open backlog items 21.
**Params**: `phase: furigana`, `start_id: 25872`, session OpenRouter budget $2.50
(daily remaining at start: $4.987).
**Branch**: `claude/determined-rubin-o6528y`

**Range worked**: 25872–26440 (569 entries) for the tag lane; **25872–26188 (316 entries)**
for the paid cross-model review, which is where the cursor lands.

## §0 pre-flight

`mcp__github__list_pull_requests` returned **0 open PRs** — nothing to rescue (§0a), nothing to
sweep (§0b). Lock acquired cleanly.

## §A step 2 — furigana screening

`review_runner.py --pass screening --range 25872 26150`: **0 flags on 69 entries** before this
run **stopped it deliberately** (not a crash) to stop it competing for wall-clock with the
paying pass. Cost $0.0073. Deep pass skipped — there was nothing to deep-review.

This is the **seventh consecutive zero window** for the screener. Logged as a Tooling 24 update:
the precision argument is finished, and this run adds the first evidence that the throughput
ceiling binds the *accuracy* dimension too (below).

## §A step 3 — glosses, translations, semantic tags

`review_accuracy.py --range 25872 26440` reviewed **316 of the 569 entries (25872–26188)** for
**$0.1307** before this run stopped it: its throughput decayed badly in the second half (11
entries in one 25-minute window), and §6's context rule made finishing the range a worse trade
than wrapping up cleanly. Per the §A resilience rule the per-entry results already written were
kept and no retry was attempted; the cursor advances only over what was actually reviewed.

**133 entries flagged of 316 (42%), 153 issues**: 76 tag errors, 70 tag warnings, 5 gloss
errors, 2 translation errors. The 42% flag rate is above §A's 20% "reviewer noise" line, and the
reason is visible in the breakdown — **118 of the 146 tag flags (81%) were the off-vocabulary
tag family that the free deterministic detector already finds**.

### The tag lane — 208 entries migrated (the run's main output)

`check_tag_drift.py` reported **251 `unknown-semantic` flags across 208 of the 569 entries
(37%)** in 25872–26440 — roughly 100 distinct off-list tag names. Per the §A semantic-tag
policy a tag that is not in `VALID_SEMANTIC` is wrong by definition, so all 208 were migrated.
None was in the detector's 9-entry `TAG_MIGRATION` map; every destination was chosen from the
entry's headword, gloss, POS and existing tag set, with the ambiguous families first grounded in
what the corpus already does for comparable words (a keyword survey over all 30k glosses:
`death` → culture/health/existence, `sound` → communication, `building/architecture` →
`building`, `urban/city` → `geography`, `government/administration` → `politics`,
`stationery` → `tool`, `crime` → `law`, `math` → `number`, `gland/organ` → `body-internal`,
`linguistics` → `language`).

Representative choices: `emotions`→`emotion`, `people`→`person`, `actions`→`action`,
`transport`→`transportation`, `time`→`time-general`/`time-period`/`time-season` by sense,
`arts`→`art`, `tools`→`tool`, `medicine`→`health`, `academics`→`education`,
`thought`/`thinking`/`psychology`/`logic`→`cognition`, `attitude`→`personality`,
`spatial`/`position`/`location`→`direction`, `martial-arts`→`sports`, `recreation`→`leisure`,
`grammar`→`grammatical`, `keigo`→`expression`, and per-entry calls where the tag name alone was
not decisive — `body` split across `body-part` (脚線美, 掌紋, 片耳), `body-internal` (鼓膜,
涙腺, 乳腺, 前立腺), `health` (柔弱, シャワーを浴びる), `size` (低身長), `movement` (のけぞり),
`consumption` (腹減る) and `action` (排尿, 排便, 性行為, 横見); `animal`→`animal-bird` for 留鳥
and 幼鳥 but `animal-mammal` for 鹿角; `agriculture`→`animal-bird` for 害鳥 (a bird, not a
farming term). 22 off-vocab tags were dropped rather than mapped, where the surviving tags
already carried the meaning (`customs` beside `culture`, `tourism` beside `travel`,
`linguistics` beside `language`) or where the tag was a register marker rather than a semantic
field (`formal` on 25928 and 26181).

Edits were applied text-surgically rather than by JSON round-trip, so the diff is only the
`semantic` array and the `modified` timestamp — 426/439 lines across 208 files — and each file
was re-parsed and compared field-by-field against its original before writing.

After the migration: `check_tag_drift.py` reports **0 `unknown-semantic` flags in the range**
(down from 251), `validate.py --range 25872 26440` passes 569/569, and
`validate_tags.py --check-no-new-unknown` passes with the baselined off-vocab population down to
**2,600 entries**.

### Applied from the reviewer's own judgment (18 further decisions)

The 28 tag flags that were *not* the off-vocab family are the ones worth paying for:

- **sole-`general` replacements — 11 applied**: 忌中 → `culture`+`religion`, 引き下ろす →
  `movement`+`finance`, 選挙運動 → `politics`, 視覚障害/低体温症/癒合/抗菌薬 → `health`,
  国名 → `geography`, 不敬罪 → `law`, 脱会 → `society`, 相対性 → `science`,
  丸括弧/角括弧 → `language`, 彦星 → `culture` (the reviewer's own suggestion, `astronomy`, is
  off-vocab). Applied rather than rejected as breadth nits because `check_tag_drift.py`'s own
  `sole-general` verify text is "replace with a specific semantic tag if one clearly applies".
- **factually inapplicable tags — 4 applied**: ネイル `clothing` → `appearance`;
  ネガ (film negative) `communication` removed; こいつら (a pronoun) `communication` → `person`;
  25872 放心状態 example 3 "I went into a state of shock" → "I was left in a daze" (放心 is
  dazed/absent-minded, not shock).

### Rejected (10 individual decisions)

- **"suru-verbs should carry `action`"** (26046, 26116, 26138) — no such convention exists in
  `validate_tags.py`; the reviewer invented it. Same rejection as the 2026-08-01 run.
- **Formality flags** (26074 故意に, 26144 抗菌薬, both `formal` → `neutral`) — §A applies a
  formality flag only when the entry's own notes contradict the label; neither entry has a
  REGISTER section at all.
- **In-list breadth substitutions** — 26131 火成岩 `geography` → `science`, 25892 ストライク
  (asks for tags covering a sense the entry does not document).
- **26048 魔法陣 and 26068 害悪** — flagged as sole-`general`, but no in-list field clearly
  applies to either; `general` is the honest tag.
- **25975 衝撃を受ける** "remove 'to be struck by impact' from the gloss" — the physical sense is
  a defined sense 2 with three examples; the gloss correctly summarises both.
- **26011 鍛冶屋** (two gloss flags + one translation flag) — 鍛冶屋 genuinely denotes both the
  smith and the smithy, and "at the blacksmith's" is idiomatic English for the shop.
- **26146 柏** `food` "incorrect for an oak tree" — the entry's own gloss carries the Kansai
  sense "chicken"; the reviewer read only sense 1.

### Flagged for the curator (1)

**26031 {格上|かくじょう}** — the reviewer caught a gloss/headword mismatch; checking it out
found something larger. かくじょう is not a reading of 格上 (格上 is かくうえ, 格上げ is
かくあげ); the gloss and **all three examples** are 格上げ, written with the non-word furigana
{格上|かくじょう}げ; and both real words already have entries (**07405** 格上/かくうえ and
**13122** 格上げ/かくあげ). Every available repair changes `romaji` and therefore the filename
and the live URL, so it is a curator call under CLAUDE.md's never-renumber rule. Written up in
full in `reviews/needs_curator.txt`.

## §4 self-verification

The reviewer had already covered every changed entry up to 26188, so the self-check was pointed
at the **97 changed entries above the review frontier** (26189–26440) — the tag migrations no
independent model had seen. `review_accuracy.py --ids …` covered **all 97 for $0.0423**,
flagging 6.

**Not clean — it caught 6 of this run's own tag choices, and all 6 were applied** (one fix
round, no re-verification, per §4):

| entry | this run wrote | corrected to | why |
|---|---|---|---|
| 26197 超高層 | `urban`→`geography` | drop; `building` alone | a high-rise is not geography |
| 26262 足跡 | `achievement`→`evaluation` | drop; `history` alone | the word is the record, not the judgement |
| 26265 殺戮 | `war`→`military`, dropped `violence` | +`action` | massacre is not only military |
| 26275 回収率 | `environment`→`nature` | drop; `number` alone | a recovery rate is not nature |
| 26330 線分 | `mathematics`→`number` | `science` | geometry is not numeracy |
| 26416 都市景観 | `environment`→`nature` | `building` | a cityscape is built, not natural |

**6 of 97 (6%) of the run's own mappings were wrong and were caught before the PR** — the case for §4
existing. The pattern in all six: mapping the *tag name* rather than the *entry*, exactly the
failure §B warns about for mechanical sweeps — and `environment`→`nature` produced two of them
(26275, 26416), which is the signature of a name-level rule applied without reading the entry.
All six sit in the "ambiguous family" set where the tag name alone was not decisive; **none of
the mechanical plural/synonym normalisations** (`emotions`→`emotion`, `people`→`person`,
`transport`→`transportation`) drew a single flag, which is the cleanest evidence yet for where
the line between mechanical and judgement-requiring migration actually falls.

## §3 always-on capture

- **No new candidates** — this mode reads review reports rather than composing examples.
- **Six observations** appended to `polishing/observations.md`: the P20 band holding at 37% for a
  sixth consecutive high-ID block; Tooling 46's 81% overlap measured a sixth time; a new
  `[pattern]` that sole-`general` looks mechanically fixable (11 of 13 flagged had an obvious
  gloss-derived tag); a correction to **Tooling 53** (the "empty `description`" complaint is a
  reader-side field-name mismatch — the issues carry `concern` — so the item should be reworded,
  not fixed); the Tooling 24 zero-window update with the new throughput evidence; and the 26031
  entry escalation.

## Spend

| phase | entries | est. |
|---|---|---|
| screening | 69 | $0.0073 |
| accuracy | 316 | $0.1307 |
| self-check | 97 | $0.0423 |
| **total** | | **$0.180** |

Ledger `spent_usd` after this run: **$0.194** of the $5.00 daily cap. The session budget was
$2.50, so ~$2.33 went unused — for the third run running, **the binding constraint was runner
wall-clock, not dollars**, and this time it truncated the paying dimension as well as the free
one.

## Cursors

- `polishing/tasks/cross-model-review/progress.txt` → **`next: 26189`** (the review frontier,
  not the tag-lane frontier).
- `reviews/queue.txt`: 25872–26188 removed; depth **12,141 → 11,824**.
- Note: 26189–26440 are **tag-migrated but not cross-model reviewed** (81 of those 97 changed
  entries did get the §4 self-check). They stay in the queue and the next accuracy-review run
  picks them up from 26189.

## Wrap-up

- §5 metrics line appended (mode=accuracy-review).
- `make build` run once.
- PR created, CI polled over MCP, squash-merged, lock released.
