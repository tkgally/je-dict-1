# Routine session 2026-09-21_001

**Mode:** accuracy-review (selector reason: "highest scheduler debt among eligible modes")
**Range:** entries 07001–08000 (params.start_id 7001; 918 entries actually reviewed, cost $0.4453)

## Pre-flight
No open routine PRs to rescue or absorb; no orphan `claude/*` branches. Clean start.

## What I did
Ran `build/review_accuracy.py --range 7001 8000 --budget 2.0` (918 entries reviewed,
72 flagged, $0.4453). Adjudicated all 72 flagged entries (some with multiple issues,
~101 individual issues) against the entry text:

- **24 entries fixed** (APPLY): mostly formality-tag corrections where the tag
  contradicted the note's own register description (e.g. tag "neutral" but note
  says "belongs to formal registers"), plus a handful of real gloss/translation/notes
  errors — a gloss with the wrong honorific/humble label (賜る), a passive-voice gloss
  for an active transitive verb (踏まえる), a mislabeled etymology section that was
  actually a parts list (糸鋸), an internally contradictory transitivity note
  (降り注ぐ), an overstated gloss (除菌 "disinfection" → "sanitization; germ removal"),
  and a few translation/grammar fixes.
- **56 issues rejected**: the largest bucket (22) was the external reviewer not
  knowing the dictionary's closed `formality` enum (formal/neutral/informal/vulgar) —
  it kept flagging "slightly formal" or "neutral to formal" note language as
  contradicting a "formal" tag, when "formal" is simply the correct nearest value
  with no intermediate option. The rest (27 notes-fact, 2 wrong-category, 2
  gloss-meaning, 3 translation-meaning) were the reviewer misreading compound-verb
  grammar (を marking a path, not an object), an entry's own established etymology,
  or natural English phrasing.
- **1 flagged to curator**: 07017_amami — the note says the かんみ reading is used
  for the "sweets as food" sense, but that sense is listed under the あまみ reading
  in this same entry. Needs a judgment call on whether to split the reading or
  soften the note.

Per §A step 8, skipped the separate self-check (§4) for the reviewed range since
the accuracy review already served as the check. Two entries outside the range
(00601_yoku, 04670_ochitsuku) picked up small mechanical reciprocal cross-references
from `harvest_crossrefs.py`; not separately self-checked given the low-risk,
script-generated nature of the change.

## Mechanical pass
`normalize_notes.py` (0 rewrites needed), `auto_link.py` (+11 links across 8 entries),
`harvest_crossrefs.py` (+4 cross-references, 2 reciprocal). All 26 touched entries
validated clean.

## Cursors / queue
- `polishing/tasks/cross-model-review/progress.txt`: `next: 08001`
- `reviews/queue.txt`: removed the 07001–08000 range (4196 → 3650 lines)
- `reviews/decisions.jsonl`: 29 lines appended (24 apply, 4 aggregated reject, 1 flag)
- `reviews/needs_curator.txt`: 1 line added (07017_amami)
- `pipeline/openrouter-ledger.json`: spent_usd 0.4453 today

## Next
Next accuracy-review run starts at entry 08001.
