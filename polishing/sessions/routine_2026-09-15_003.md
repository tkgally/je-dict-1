# Routine session: systemic-fix (2026-09-15, 003)

**Mode:** systemic-fix (selector reason: "systemic-fix: highest scheduler debt among eligible
modes")

## Pre-flight

- **Rescue (§0a):** PR #3298 (routine(polish), head `claude/trusting-mendel-do5fff`) was open,
  green (`validate` check `completed`/`success`), and had no human comments or reviews — merged
  (squash). Merged `origin/main` into this branch afterward.
- **Sweep stranded PRs (§0b):** PR #3293 (routine(new-entries), head
  `claude/trusting-mendel-1b7wtk`) has a `completed`/`failure` `validate` check, so it is not
  rescuable. Its highest touched entry ID is 30912, well above the current polish frontier
  (`next: 07405` after #3298's merge), so per the sweep rule it stays open rather than being
  closed — it needs an independent look.
- **Sweep orphan branches (§0b):** two branches are neither this session's branch nor an open PR's
  head: `claude/trusting-mendel-o9cv7c` and `claude/trusting-mendel-u8emu6`. Both diff with real
  residue against `origin/main` (o9cv7c: ~15 polish edits including 07128_takoage, 07350_sugoroku,
  21737_hassousuru; u8emu6: 18 new entries at 30915-30932 — 亥, シャンデリア, the three tea-ceremony
  schools, 南アフリカ, 神戸牛/松阪牛, jump-rope and memorial-day terms). Both branches already had a
  PR each (#3290 and #3294, respectively — both closed without merging), so per the sweep rule
  this run does not reopen either unilaterally; logged one line per branch to
  `reviews/needs_curator.txt` for the curator to decide (salvage vs. discard).

## Mode work (§B systemic-fix)

**Backlog item:** `stale-noentry-katakana-demotion` (Cleanup/Tooling Backlog 143) — "Demote
katakana base forms out of `check_stale_noentry.py`'s mechanical bucket". `fix_type: tooling`,
`verify: none`: this item's fix is a code change to the detector itself, not a batch of entries to
open and correct.

**Detect:** ran `python3 build/check_stale_noentry.py --summary` and `--class A2`. The live
mechanical bucket (A1+A2) currently holds **0 pairs** — the 22 katakana pairs measured on
2026-08-31 had already been resolved by intervening new-entries/systemic-fix runs. So there was no
batch of flagged entries to fix by hand this pass; the remaining work is exactly the code change
the item describes, to stop future katakana matches from being auto-applied.

**Fix:** in `build/check_stale_noentry.py`, changed `MECHANICAL = ("A1", "A2")` to
`MECHANICAL = ("A1",)`. A katakana-base match still classifies as `A2` (unchanged detection logic)
but no longer reports `mechanical: true`, so it now lands in the same per-entry judgment queue as
A3/B/C instead of being written automatically — the fix the backlog item asked for (base form
matching katakana → judgment queue instead of mechanical). Updated the module docstring, the
`VERIFY` guidance string, the `--mechanical`/usage help text, and the summary print line to match.
Verified: `--summary` and `--class A2` both still run cleanly and report the new `mechanical (A1):`
line; `--json --range 1 500` still parses; the full `build/tests` suite (426 tests) still passes.

No entries were touched (correct for this item's `verify: none` / code-only scope), so §3
(mechanical pass) and §4 (self-check) do not apply this run — nothing to normalize, link, or send
to the reviewer.

**Backlog update:** marked `stale-noentry-katakana-demotion` `status: "resolved"` in
`planning/wiki/ideas/backlog-queue.json`, with a note that the live scope was already 0 by fix
time. Added a matching "RESOLVED 2026-09-15" paragraph under item 143 in
`planning/wiki/ideas/tooling-backlog.md`.

## Self-check

Not applicable — no entries changed this run.

## Cost

$0 (no OpenRouter calls this run; nothing needed review).

## Observations logged

None new this run.

## Next cursors

Not applicable to systemic-fix; no polish/accuracy-review cursor advanced.

## PR / CI

PR #3299 pushed and opened. Its `validate` check run stayed `queued`/`in_progress` and never
reached `completed` through all 16 polls (~8 minutes). Same slow/stuck-runner pattern the previous
run (routine_2026-09-15_002) hit on PR #3298's checks. Left the PR open per the cap; the next run's
pre-flight (§0a) should rescue it once green.

## For the curator

- Two orphan branches (`claude/trusting-mendel-o9cv7c`, `claude/trusting-mendel-u8emu6`) carry real
  unmerged work from closed-but-unmerged PRs (#3290, #3294) — see the two new lines in
  `reviews/needs_curator.txt` for what each contains. They need a decision (salvage or discard);
  this run left them untouched per the sweep rule.
- PR #3293 (routine new-entries, still open, failing CI) still needs an independent look — this
  run's sweep correctly left it alone since its entry IDs are far above the polish frontier.
- The katakana-demotion fix itself needs no follow-up: it's a narrow, tested code change with nothing
  left in its own scope.
