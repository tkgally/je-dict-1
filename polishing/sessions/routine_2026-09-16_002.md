# Routine session 2026-09-16 #002

**Mode**: accuracy-review (selector reason: "highest scheduler debt among eligible modes")
**Range**: entries 28239–29238 (params.start_id 28239, openrouter_session_budget_usd 2.5)

## Pre-flight

- No open `claude/*` routine PRs to rescue or absorb.
- No orphan `claude/*` branches beyond this session's own.
- Local branch was already up to date with `origin/main`.

## Review

`build/review_accuracy.py --range 28239 29238 --budget 2.5`: reviewed 999 entries, 56 flagged
(cost $0.4806). Adjudicated every flag individually against the entry, the dictionary's own
conventions (checked via sibling entries and `build/schema.json`/`build/validate_tags.py`), and
outside knowledge.

- **17 applied** (fixed):
  - 28247_tsutau — `transitivity` tag was "transitive" but should be "intransitive", like other
    path-of-motion verbs that take を for the surface followed (歩く, 飛ぶ); notes text now matches.
  - 28360_mukon — collocation "無根と断つ" isn't idiomatic; corrected to "無根と断じる".
  - 28377_suujuuokuen — USD estimate was off by roughly 10x ($20–700M → $15–60M for 数十億円).
  - 28459_sanbai — cleaned up a garbled rendaku note ("ばい → ばい (actually no rendaku...)").
  - 28474_gokaidate — gloss "five-story building" → "five-story (building)" to match sibling
    entry 二階建て's "two-story (building)" convention.
  - 28504_pikkoro — "Loanword from Italian {piccolo}" used curly braces instead of the dictionary's
    standard quote convention ("piccolo"); fixed in both notes and definition explanation.
  - 28548_shame — notes said "photo mail" but definitions[0].explanation said "photo message" for
    the same 写真メール; standardized to "photo mail".
  - 28652_kyomushugi (nihilism) — semantic tag 'religion' wrong for a philosophy term; changed to
    'abstract' (matching sibling -主義 entries like 悲観主義, 利他主義).
  - 28752_sedaikan — the 人間 example under the 〜間 pattern list was confusing (人間 no longer
    productively means "between people"); replaced with 家族間.
  - 28863_monbukagaku — "Minister of Education" was an incomplete translation of 文部科学大臣;
    spelled out the full ministry title.
  - 28894_koon — added a missing sense 2 ("corn," the grain) with its own definition, example,
    and notes section; the entry only covered "cone" senses despite the notes flagging that コーン
    is also a separate loanword for corn. Added semantic tag 'food'.
  - 28895_kyuu, 28896_kakusui, 28899_kakuchuu, 28900_rokkakukei — semantic tag 'number' was wrong
    for these geometric-shape nouns (球, 角錐, 角柱, 六角形); changed to 'general', matching sibling
    entries 円錐/円柱.
  - 29020_youkihousou — semantic tag 'nature' didn't fit a packaging/recycling term; dropped it,
    kept 'economics'.
  - 29123_gachi — 〜がち note said it only attaches to verb masu-stems, but it also attaches
    directly to nouns (病気がち); broadened the note and added that example.
  - 28253_suzushige — two example translations used "feels ... cool" (tactile) where 涼しげ's
    げ-suffix means "seeming/appearing" (visual/auditory impression, not actual sensation);
    reworded to "sounds cool."

- **1 flagged to curator** (needs_curator.txt): 29224_fusuma — the reviewer claims headword
  麸(ふすま, wheat bran) and 麩(ふ, wheat gluten) are the same kanji, contradicting the entry's
  "not to be confused with" note. I could not confidently verify glyph identity from text alone;
  needs a human/reference check before editing.

- **43 rejected** as model misreadings or defensible existing conventions — most common patterns:
  - Several flags conflated unrelated sections of the same notes field (28843, 28845) or quoted
    text that doesn't match what's actually in the entry (29166, 29168, 28485) — apparent model
    hallucination.
  - Several flagged the dictionary's own established conventions as errors: combining distinct
    senses in the top-level `gloss` while breaking them out properly in `definitions[]` (28358,
    28484); the "(formal)" annotation redundant with the `formality` tag (28402, confirmed as a
    27-entry-wide convention); "formal" as the closest fit when schema has no "written"/"literary"
    formality value (28414, 28448, 28958, 29092 — confirmed against `build/schema.json`).
  - Several were verified correct against outside knowledge: Buddhist memorial-year counting
    (28963, 28964), Heisei-era arithmetic (28485), the 旧字体 pre-1949 cutoff (29209), binary vs.
    decimal storage prefixes (28699), 縦横比 word order (29007), ぱんぱん's clapping-sound sense
    (29037), えらが張る anatomy (28708).
  - Full detail and rationale for every flag is in `reviews/decisions.jsonl` (62 lines, ts
    2026-09-16T10:05:26Z–10:10Z) with a ten-word note each.

Per §A step 8, the §4 self-check was skipped for these IDs (the accuracy-review pass already
served as the check).

## Mechanical pass

`normalize_notes.py`, `auto_link.py --confirm-real-entries`, `harvest_crossrefs.py` run on all 18
changed IDs. auto_link added 14 links (including correctly resolving three placeholder links I'd
written with guessed/wrong entry IDs — the linker overwrote them with the correct targets).
harvest_crossrefs found nothing new to add. All 18 entries re-validated clean.

## Ledger / cursor / queue

- OpenRouter ledger: +$0.4806 (review phase), day total $0.9631 of $5.00 cap.
- Cross-model-review cursor: `next: 28239` → `next: 29239`.
- `reviews/queue.txt`: pruned the 28239–29238 range (9507 → 9094 lines).

## Next

Cursor is ready for the next accuracy-review run at 29239. No open follow-ups besides the one
`needs_curator.txt` line above.
