# Routine session — 2026-07-28 (004)

- **Mode**: `accuracy-review`
- **Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
- **Params**: `phase: furigana`, `start_id: 20451`, `openrouter_session_budget_usd: 2.50`
- **Signals**: candidates 1029 (8 "seen in entry"), max entry ID 30182, comprehensive next 6669, cross-model-review next 20451, reviewed entries 586, unharvested observations 4, open backlog items 7

## Range worked

Target range **20451–21050** (599 entries).

- **Furigana screening** (`review_runner.py --pass screening`): 155 entries screened, 31 flagged.
- **Accuracy review** (`review_accuracy.py`, gloss/translation/tags): **379 entries** reviewed, contiguous through **20702**; scattered extra coverage above that (20721, 20781–20845, 20916–20976).

Both scripts are strictly serial, so the range was sharded across parallel processes on disjoint sub-ranges, then a gap-fill pass made 20451–20702 contiguous. The cursor is set to the end of the contiguous block, not the scattered maximum.

## Adjudication

**97 entries changed.**

**APPLY (97)**
- **95 semantic-tag migrations** — entries carrying tags outside `VALID_SEMANTIC`. Per the 2026-06-11 semantic-tag policy, a "not in the list" flag is correct by definition. 43 were adjudicated individually with hand-picked destinations; 52 more were migrated using the canonical `TAG_MIGRATION` map in `build/check_tag_drift.py` extended with unambiguous 1:1 pairs. Examples: `medical`→`health`, `body`→`body-part`/`body-internal`, `character`→`personality`, `economy`→`economics`, `people`→`person`, `description`→`descriptive`, `time`→`time-general`, `daily life`→`daily-life`, `animal`→`animal-mammal`/`animal-general`, `math`→`science`, `thought`→`cognition`, `degree`→`descriptive`, `social`→`society`.
- **1 formality fix** — 20667 お疲れさまです `formal`→`neutral`. Applied because the entry's own FORMALITY LEVELS block calls it "standard polite" and reserves "very formal" for お疲れさまでございます, contradicting the label. (The companion flag to downgrade `politeness` from `polite` was rejected — です *is* the polite form.)
- **1 furigana fix** — 20908 {省力化|しょうりょくか} had `{急|きゅう}がれている`; the verb is 急ぐ, so the reading is いそ. A genuine screening true positive.

**REJECT (58)**
- **30 furigana flags** (aggregated) — every remaining screening flag fell in the documented false-positive families: okurigana/compound reading splits (`{記入漏|きにゅうも}れ`, `{金持|かねも}ち`, `{通行止|つうこうど}め`, `{一回限|いっかいかぎ}り`), and a *new* artifact family where the screener quotes readings that do not exist in the entry because its "followed by" context is truncated mid-pair (claimed `{過小評価|かしょ}` where the entry has `{過小評価|かしょうひょうか}`; quotes containing a stray `)` such as `{分別|ふんべ)`). Verified against the entry files before rejecting. Screening precision over this already-polished block: **1/31 (~3%)**, consistent with the 0–5% measured 2026-06-10/11.
- **15 "general is too broad" flags** (aggregated) — in-list narrowness substitutions, rejected per policy (20803, 20805–20808, 20811, 20813–20815, 20819, 20820, 20823–20826).
- **7 formality flags** — 20475 洗髪, 20538 復路, 20669 紅, 20676 完了する, 20818 ご苦労さまです, 20835 心得る, 20968 宵の明星. Each entry's own notes explicitly describe the word as formal/literary, so the label stands.
- **6 individual** — 20507 みっちり (a mimetic adverb; `onomatopoeia` is right), 20534 標準化 (`action` on suru-verbs is not a convention — measured at 23% of a 600-entry sample), 20721 気長 (model misread; the entry has `personality`, not `time`), 20889 予約困難 (narrowness nit), 20640 明明後日 (the gloss "two days after tomorrow" is arithmetically correct), 20811 調教 examples[4] (stylistic rewording).

**FLAG (32)** — entries whose off-vocabulary tag has no canonical destination (`body` needing a per-entry body-part/body-internal/health call, plus `agriculture`, `astronomy`, `figurative`, `material`, `manner`, `moral`, `supernatural`, `philosophy`, `equipment`, …). Appended to `reviews/needs_curator.txt`.

## §4 self-check

Skipped by rule: in `accuracy-review` mode §4 explicitly exempts IDs already covered by §A this run, and every one of the 97 changed entries was inside the reviewed range — the review *was* the check.

## Validation

`build/validate.py`: 29973/29973 valid. `build/validate_tags.py --check-no-new-unknown`: no new off-vocabulary semantic tags.

## Candidates

None added — this mode reads reviewer output rather than examples, and no un-entered words surfaced during adjudication.

## Observations logged

Four appended to `polishing/observations.md`:
- `[pattern]` off-vocab semantic tags are dense in this block (237/599 ≈ 40%); expanding `TAG_MIGRATION` beyond its 9 pairs would turn this into a mechanical systemic-fix item.
- `[tooling]` the furigana screener's truncated-context artifact family (~3% precision here).
- `[tooling]` both review scripts are serial (~2–4 entries/min); a `--workers N` flag would make the mode's 400–600 entry target reachable in one run.
- `[entry]` 20908's 急 on'yomi-in-verb-context error may have siblings.

## Spend

~$0.19 this run (610 entry-reviews across both passes); daily ledger at **$0.4635 / $5.00**. The deep furigana pass was **skipped** under §A's known-noise shortcut, since every screening flag fell in a documented false-positive family.

## Next cursors

- `polishing/tasks/cross-model-review/progress.txt` → **next: 20703**
- `reviews/queue.txt`: 12707 → 12683 lines (20451–20702 drained)
