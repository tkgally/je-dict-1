# Routine session 2026-09-17 #003

**Mode**: systemic-fix
**Reason**: highest scheduler debt among eligible modes
**Backlog item**: `transitivity-review-queue` (reviews/transitivity/disagreements.jsonl)

## Pre-flight

No open PRs, no orphan `claude/*` branches. Clean start from `main` at `c954d75d`.

## Work done

Worked the transitivity disagreement queue top to bottom, 80 entries (07796_tekagen
through 09460_manen). For each: read the entry's gloss, part of speech, and examples,
judged the verb's own valency (whether it takes a direct を-object) rather than the
surface pattern of any one example, and set `metadata.tags.transitivity` to
`transitive` / `intransitive` / `both`. No real 自他 (transitive/intransitive) verb
pairs turned up in this batch to add `prominent_see_also` for — the batch was almost
entirely two-character kango + する nouns (梱包, 更迭, 反発, etc.) and slang verbs
(バックレる, ミスる), none with a native paired counterpart in the dictionary.

Removed all 80 resolved lines from `reviews/transitivity/disagreements.jsonl`
(2164 → 2084 remaining). Updated the backlog item's `scope_estimate` in
`planning/wiki/ideas/backlog-queue.json`.

## Mechanical pass (§3)

- `normalize_notes.py`: 0 rewrites (the unknown headers it found were pre-existing,
  not introduced by this run).
- `auto_link.py`: 48 links added across 24 entries.
- `harvest_crossrefs.py`: 2 cross-references added (身支度 ↔ 着替える, reciprocal
  contrast pair), pulling `04603_kigaeru` into the changed set.
- 81 changed entries total, all validate clean.

## Self-check (§4)

`review_accuracy.py` on the 81 changed entries: 16 flagged, all adjudicated.

**Applied (13):**
- `04603_kigaeru`: notes wrongly called 着替える intransitive for "changing one's own
  clothes" — it's transitive with an omissible を-object; corrected the wording.
- `07937_konpou`: register note said "Neutral to formal" against a single `neutral`
  tag — reworded to remove the contradiction.
- `07956_nihodoki`: transitivity was set to `intransitive` in my first pass, but the
  reviewer was right that 荷解き takes a direct object (段ボールを荷解きする) — changed
  to `transitive`.
- `09361_bakkureru`: notes said "conjugated as a godan verb" but the entry's own
  conjugation table (バックレない, not バックレらない) and `verb_class` are ichidan —
  fixed the notes to say ichidan.
- 8 off-vocabulary semantic tags (family `offvocab`, deterministic): `organization`/
  `process` → `business`/`change` (08071); `events`/`theater` → `sports`/
  `entertainment` or `entertainment` (08174, 08175); `competition` → `sports`
  (08201, 08236, 08237, 08238); `film`/`theater` → `entertainment` (08254); `sleep`
  → dropped, kept `health`/`work` (08333).

**Rejected (3):**
- `08403_kougousei`: the flagged "quote" spliced two unrelated note lines together
  (the photosynthesis formula and a separate 呼吸/respiration cross-reference line);
  no actual error in the entry.
- `08796_ritsuiito`: the note's claim that X renamed "Retweet" to "Repost" in 2023
  is accurate; reviewer misread this as wrong.
- `09442_suisatsu`: politeness tag `polite` is fine for the word itself; the
  reviewer conflated one humble construction in an example (推察いたす) with the
  headword's own register.

All 16 decisions logged to `reviews/decisions.jsonl`.

`review_links.py` kana-link check: 2 flags, both `other`. Reviewed both by hand:
- `09375_yarakasu` やっ→やる: correct base verb, just an inflected-form conjugation —
  kept.
- `08375_shinsa` つける→00562_tsukeru (点数をつける, "to give a score"): right word,
  but that sense isn't in the target entry's two definitions (turn on / attach) —
  kept per policy and logged the sense gap to `reviews/needs_curator.txt`.

Both logged as `keep` in `reviews/link_decisions.jsonl`.

## Checks

`make gate`: 440 unit tests pass, full validation 30743/30743 entries valid (all
warnings pre-existing, none introduced by this run), ratchet clean, tag/header/link
gates all clean. `make index` rebuilt `entries_index.json`, `word_id_lookup.json`,
and the kanji index cleanly (no new kanji).

## OpenRouter spend

$0.0391 (accuracy self-check) + ~$0.002 (link self-check) against a $5.00 daily cap.

## Next

Backlog cursor: `transitivity-review-queue` next starts at `09461` (top of the
remaining 2084-line queue).

## Needs curator

- New: `08375_shinsa` — つける (点数をつける, "to give a score") is a sense of つける
  not covered by any definition on `00562_tsukeru`; link kept, entry could use a
  third sense.
- (Pre-existing, unrelated to this run) the ～がる transitivity-convention
  inconsistency flagged 2026-09-16 is still open.
