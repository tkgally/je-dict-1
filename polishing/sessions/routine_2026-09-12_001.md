# Routine session 2026-09-12_001

**Mode**: systemic-fix
**Reason**: selector — "highest scheduler debt among eligible modes"
**Backlog item**: `conjugation-suru-single-kanji-potential` (Cleanup P59 / Tooling 122)

## What this run did

1. Fixed the generator first, per the item's own warning. `build/add_conjugations.py`'s
   `_generate_suru_forms()` always emitted the Potential row as 〜できる/〜できない. Correct for
   漢語+する compounds (勉強できる) but not a possible Japanese form for single-kanji サ変 verbs,
   which take 〜せる (愛せる, not 愛できる). The generator now checks the furigana-stripped length
   of the pre-する stem and emits 〜せる/〜せない when it is one character. Verified against
   08053 察する (single-kanji, now せる), 00527 勉強する (multi-kanji, unchanged), and the one
   entry fixed by hand at creation, 30647 処する (generator output now matches it exactly). Full
   `build/tests` suite (426 tests) passes.

2. Re-ran the item's own detection rule against the live dictionary rather than trusting the
   prose list verbatim, and found two discrepancies:
   - The backlog's "The 32:" list enumerates only 31 IDs — it silently omits 14629 面する, which
     the rule does catch and which was genuinely still broken.
   - 20837 さする, flagged in the original filing as a *separate, worse* defect (a full サ変 table
     on a godan verb), was already fully correct godan by the time of this run. Some other session
     fixed it outside this item; it needed no action here.
   - Net scope: 31 entries (the 30 of "The 32" still affected, plus 14629; minus 20837 already
     fixed and 30647 already correct from the original measurement).

3. Rewrote the Potential row (〜できる→〜せる, 〜できない→〜せない) on all 31 entries. One of them,
   27887 害する, had lost furigana across its *entire* conjugation table — a separate, pre-existing
   defect unrelated to this item — so its whole table was regenerated with the corrected generator
   rather than patched row by row. All 31 pass `build/validate.py --changed-only`.

4. Mechanical pass (§3): `normalize_notes.py` found nothing to rewrite (4 unrelated non-canonical
   note headers noted, not touched — out of scope). `auto_link.py` added 36 inline links across
   11 of the 31 entries (these entries had never been linked). `harvest_crossrefs.py` proposed
   nothing.

5. Self-check (§4): `review_accuracy.py` flagged 3 issues on 2 entries, all adjudicated:
   - 02045 関する: `formality: neutral` contradicted its own REGISTER note ("a formal expression
     common in written Japanese..."). **Applied**: neutral → formal.
   - 02126 対する: reviewer claimed the notes' "intransitive" tag was wrong because examples use
     it "transitively." **Rejected** — every example uses に (dative-marked object), not を;
     对する is genuinely intransitive and the reviewer conflated a に-marked object with
     transitivity.
   - 12567 徹する: semantic tag `action-general` is off-vocabulary. **Applied**: migrated to the
     in-list `action`.
   `review_links.py` checked the 16 new kana-surface links `auto_link.py` added; 0 flagged.

6. Updated the backlog item to `resolved` in `planning/wiki/ideas/backlog-queue.json`, and added
   resolution notes to `planning/wiki/ideas/cleanup-backlog.md` (Priority 59) and
   `tooling-backlog.md` (item 122) recording the two list-vs-reality discrepancies above.

## Entries changed (31)

01811 愛する, 02045 関する, 02126 対する, 02129 達する, 02297 適する, 02401 罰する, 08053 察する,
09142 屈する, 09168 臆する, 09794 熱する, 11664 介する, 11906 制する, 11966 反する, 11970 博する,
11971 即する, 12003 呈する, 12402 属する, 12567 徹する, 12776 扮する, 12920 接する, 13400 有する,
13401 瀕する, 13639 発する, 14342 要する, 14629 面する, 21490 没する, 23992 議する, 25545 値する,
27887 害する (full table regenerated), 28650 脱する, 29124 喫する.

## Self-check outcome

3 flagged / 2 applied / 1 rejected. Logged to `reviews/decisions.jsonl`. Link check: 16 judged,
0 flagged.

## Candidates added

None.

## Observations logged

None new (the two discrepancies above were folded directly into the backlog item's own resolution
note rather than filed as fresh observations).

## Cursors

No cursor applies to `systemic-fix`; the backlog item's own `status` field is the state (now
`resolved`).

## PR / CI

PR #3277 opened. First `validate` run (head 33123627) was still `in_progress` after the full
16-poll wait; pushed a small session-log follow-up commit (6a9392a) which reset CI, then polled
the fresh run — its job log shows it stuck at the "Checkout repository" step for 13+ minutes,
a runner-side stall unrelated to this change. Left the PR open per §7 step 5; the next run's
pre-flight rescue should pick it up.
