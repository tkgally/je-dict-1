# Routine session 2026-07-13 #008

- **Mode**: `systemic-fix` (selector reason: "systemic-fix: highest scheduler debt among eligible modes")
- **Backlog item**: `furigana-cosmetic-wrappers` (Non-standard but mostly-rendering furigana wrappers: o/go-prefix, pure-kana, over-wrapped okurigana). Source: `ideas/cleanup-backlog.md#priority-9`.
- **Slice worked**: 11000–12999 (2,000-ID band; density had dropped to ~5–10 flags per 500-ID block above ID 11000, so a wider slice was taken to reach a batch size comparable to the earlier 500-ID sweeps).

## Changes — 32 instances across 31 entries (detector filtered total 1262 → 1230)

Subpatterns filtered to `{o-go-prefix, pure-kana, over-wrapped}`. Each fix verified per-entry; all preserve the visible surface, so `build/word_id_lookup.json` keys and inline-link resolution are unaffected. No `⟦…⟧` inline-link surfaces were touched in the range.

- **Pure-kana katakana/loanword de-wraps**: 11117 `{クリア|くりあ}`→クリア, 11141 `{チェーン|ちぇーん}`→チェーン, 11563 `{プライバシー|ぷらいばしー}`→プライバシー, 11674 `{ストレス|すとれす}`→ストレス, 12360 `{エース|えーす}`→エース, 12675 `{レスラー|れすらー}`→レスラー, 11311 `{テスト|てすと}`→テスト.
- **Pure-kana hiragana de-wraps**: 12210 `{おもてなし|おもてなし}`→おもてなし, 12417 `{なす|なす}`→なす, 12451 `{こだわり|こだわり}`→こだわり, 12730 `{おいしい|おいしい}`→おいしい, 12746 `{まるで|まるで}`→まるで, 12802 `{もてなし|もてなし}`→もてなし, 12595 `{すぐ|すぐ}`→すぐ.
- **Numeral/symbol de-wraps**: 11579 `{１|いち}`→１, 11582 `{30|さんじゅう}`→30, 11698 `{＆|あんど}`→＆.
- **o/go-prefix repositions**: 11131 `{お弁当|おべんとう}`→`お{弁当|べんとう}`, 11529 `{お坊|ぼう}`→`お{坊|ぼう}`, 12030 `{お七夜|おしちや}`→`お{七夜|しちや}`, 12419 `{お盆|おぼん}`→`お{盆|ぼん}`, 12571 `{お願|ねが}`→`お{願|ねが}`, 12608 `{お化|ば}`→`お{化|ば}`.
- **Over-wrapped okurigana/compound splits**: 11287 `{連れ合い|つれあい}`→`{連|つ}れ{合|あ}い`, 11625 `{鯉のぼり|こいのぼり}`→`{鯉|こい}のぼり`, 11626 `{入り交じる|いりまじる}`→`{入|い}り{交|ま}じる` and `{交じる|まじる}`→`{交|ま}じる`, 11681 `{付け根|つけね}`→`{付|つ}け{根|ね}`, 12053 `{まさに地獄|まさにじごく}`→`まさに{地獄|じごく}`, 12429 `{幼なじみ|おさななじみ}`→`{幼|おさな}なじみ`, 12520 `{ほうれん草|ほうれんそう}`→`ほうれん{草|そう}`, 12539 `{当て字|あてじ}`→`{当|あ}て{字|じ}`.

`modified` timestamp bumped to 2026-07-13T21:19:15Z on all 31 entries. Validation clean (1999/1999 in range 11000–12999). Detector confirms 0 filtered instances remain in the slice.

## §4 self-verification (furigana screener)

`build/review_runner.py --pass screening` on all 31 changed IDs. The runner hit its 2-minute wall-clock twice (writing per-entry results incrementally, so nothing lost), so coverage was completed across two invocations. Outcome: **all 31 entries screened, 0 flags** — clean self-check. No APPLY/REJECT/FLAG decisions to log. Ledger updated (self-check, 31 entries, est. $0.003; daily spent_usd 0.2383, well under the $5 cap).

## Always-on capture

- No missing-word candidates: this was a format-only sweep, not a content read.
- `[tooling]` observation: `review_runner.py` screening 2-min timeout on ~30-entry batches — needs a longer timeout, a `--resume`/skip-already-screened flag, or parallel API calls.
- `[pattern]` observation: cosmetic-wrapper backlog thins sharply above ID 11000; the dense remaining bulk sits in 23000–29999 new-entry blocks and the un-swept pre-06000 backlog — future sweeps should prioritize those over the sparse sequential frontier.

## State updated

- `backlog-queue.json` `furigana-cosmetic-wrappers`: `scope_estimate` 1262 → 1230; notes appended with this slice.
- `cleanup-backlog.md` Priority 9: "Update 2026-07-13 (11000–12999 slice swept)" added. Next ready slice recorded: **13000+ and the larger pre-06000 backlog**.
- Metrics line appended; OpenRouter ledger updated.

## Next

- Next ready slice for `furigana-cosmetic-wrappers`: **13000+** (sparse) or, more efficiently, the dense high-ID new-entry blocks (23000–29999) and the un-swept pre-06000 backlog.
