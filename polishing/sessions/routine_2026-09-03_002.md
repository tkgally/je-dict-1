# Routine session 2026-09-03 002

**Mode**: new-entries (selector reason: "new-entries: highest scheduler debt among eligible modes")

## Pre-flight

One open `claude/*` PR whose title started with `routine`: #3258 (`routine(polish): ...`), from
the prior session in this same run window. Its `validate` check run was `completed`/`success`,
mergeable, no human comments — merged via `mcp__github__merge_pull_request` (squash). Merged
`origin/main` into this branch. No other stranded PRs; `list_branches` showed only `main` — no
orphan `claude/*` branches to sweep.

## Candidate selection

`grep -B1 -A3 -E '"seen in entry|used in' candidate_words.json` found 9 internal-closure
candidates (the entire lane): {色物|いろもの}, {挙式|きょしき}, {間|ま}, {観覧料|かんらんりょう},
{拝観|はいかん}, バーテンダー, {竹細工|たけざいく}, {師|し}, {主夫|しゅふ}. Took the remaining
11 from the general queue in file order — all proper-noun place names: {隅田川|すみだがわ},
{北陸|ほくりく}, {東海|とうかい}, ハワイ, ニューヨーク, ロンドン, {北京|ぺきん}, ソウル,
{台湾|たいわん}, {香港|ほんこん}, ドイツ. Batch `check_duplicate.py --skip-candidates`: all 20
OK (some homophone notes, none blocking).

## Entries created (IDs 30794–30813)

- 30794 {色物|いろもの} — two senses (colored laundry vs. {白物|しろもの}; vaudeville-program
  variety act), contrast cross-ref to 30774_shiromono.
- 30795 {挙式|きょしき} — the ceremony itself, distinct from {披露宴|ひろうえん}.
- 30796 {間|ま} — felt pause/timing, cross-referenced against the unrelated {間|あいだ} reading.
- 30797 {観覧料|かんらんりょう} and 30798 {拝観|はいかん} — both harvested from 07059's
  "RELATED FEE TYPES" note (renamed to canonical "TYPES:" in this run).
- 30799 バーテンダー, 30800 {竹細工|たけざいく} (same {細工|さいく} pattern as
  {木彫|きぼ}り/07063).
- 30801 〜{師|し} — practitioner suffix, contrasted with 〜{士|し} (licensed professional),
  〜{家|か}, 〜{者|しゃ}.
- 30802 {主夫|しゅふ} — coined gender-neutral counterpart of {主婦|しゅふ}.
- 30803 {隅田川|すみだがわ}, 30804 {北陸|ほくりく}, 30805 {東海|とうかい} — Japan's own places.
- 30806–30813 — ハワイ, ニューヨーク, ロンドン, {北京|ぺきん}, ソウル, {台湾|たいわん},
  {香港|ほんこん}, ドイツ, each written for the connotation a Japanese speaker reaches for
  rather than the bare referent.

Two conjugation tables added by `add_conjugations.py` (both suru: {挙式|きょしき},
{拝観|はいかん}). `add_adjective_conjugations.py`: 0 (no new i-adjectives). No new kanji.

## Process note (self-correction)

First drafts of all 20 entries hand-placed inline links (`⟦...⟧`) and, for words without their
own entry yet, hand-written `noentry` markers — both against policy (`build/auto_link.py` and
`build/harvest_crossrefs.py` own that work; `noentry` markers are never hand-written). Caught
before the mechanical pass: stripped all hand-placed `⟦surface→target⟧` markup back to plain
surface text across all 20 files with a regex pass, then re-ran the standard pipeline
(`auto_link.py --apply --confirm-real-entries`, `harvest_crossrefs.py --apply`) to add links and
cross-references the sanctioned way. Also caught two knock-on gaps from the same drafts: missing
furigana on several kanji in the 7 foreign-place-name entries' examples/notes (all-katakana
headwords made this easy to miss) and two malformed furigana braces — fixed by hand before
`find_missing_furigana.py` and `validate.py` both came back clean. Logged as a `[pattern]`
observation for future new-entries sessions to watch for.

## Mechanical pass (§3)

`normalize_notes.py --apply`: 0 rewrites, but flagged 3 non-canonical headers from the first
drafts ("RELATED FEE TYPES:", "SIMILAR SUFFIXES:", "RELATED PLACES:") — renamed by hand to
"TYPES:", "SIMILAR WORDS:", "RELATED WORDS:" respectively, then the pass came back clean.
`auto_link.py --apply --confirm-real-entries`: +389 links across all 20 entries (77.5% of content
tokens linked). `harvest_crossrefs.py --apply`: 8 proposals accepted (4 direct + 4 reciprocal) on
7 entries — {間|ま}↔{タイミング|17683}, {拝観|はいかん}↔{見学|00214}, {拝観|はいかん}↔
{参拝|10634}, {竹細工|たけざいく}↔{工芸|04182}. `validate.py --range 30794 30813`: 20/20 valid.

`check_stale_noentry.py --class A1 A2`: 11 mechanical hits, all fixed in place (marker →
resolved target ID) across 9 pre-existing entries: 02186_kawa (×2, {隅田川|すみだがわ}),
02791_keiyu (×2, {香港|ほんこん}), 03115_daitokai (ニューヨーク, ロンドン), 03225_shufu (×2,
{主夫|しゅふ}), 04456_kyougen (〜{師|し}), 04551_touitsu (ドイツ), 07059_haikanryou (×2,
{拝観|はいかん}; ×1, {観覧料|かんらんりょう}), 07060_baa (バーテンダー), 07063_kibori
(〜{師|し}). `metadata.modified` updated on all 9; all re-validated individually, 9/9 valid.

`check_link_newcomers.py --since 2026-09-03`: 5 hits, all links to どいつ (the informal
"which one" pronoun, 18614_doitsu) in the あいつ/こいつ/そいつ family entries, correctly
distinguished by context from the new ドイツ (Germany) entry sharing the same reading —
no retargeting needed.

## Self-check (§4)

`review_accuracy.py --ids <33: the 20 new entries plus the 9 stale-noentry-fixed entries plus
the 4 harvest-reciprocal neighbors> --budget 0.40`: **0 flags on all 33 entries** (1 filtered as
`register-noquote`, per standing policy). Est. cost $0.0159. Ledger updated (self-check phase).

## Queue note

C23287 ({師|し}) did not auto-clear from `candidate_words.json` via `update_indexes.py`'s sync
(the entry's headword carries a leading 〜 tilde marker the sync's exact-match check doesn't
see) — removed by hand. Candidate queue: 196 → 176 (19 auto-cleared + 1 by hand).

## Observations logged

`[pattern]` new-entries sessions must not hand-place `⟦...⟧` links or `noentry` markers in first
drafts — write plain surface text (with furigana) and let `auto_link.py`/`harvest_crossrefs.py`
do that work in the mechanical pass, per `newentries.md`'s explicit rule. `[pattern]` an
all-katakana headword doesn't exempt the entry's examples/notes from the furigana requirement —
double-check kanji-bearing prose sentences even when the headword itself needs no furigana.

## Cursors

Not applicable to `new-entries` mode; `PROJECT_STATUS.md` Recent Changes section updated instead
(new entry added, oldest two rotated out to keep five).

## Metrics

`entries_changed: 33 (20 new, 13 revised), flags_applied: 0, flags_rejected: 0, flags_to_curator: 0`

## PR / CI

PR #3259 opened. The `validate` check run stayed `in_progress` through all 16 polls (~8 min of
polling at 30s intervals) without completing or failing — same behavior the prior session in
this run window (#3258) saw. Left the PR open per §7 step 5.3; the next run's pre-flight rescue
(§0a) will merge it once CI finishes, provided it comes back green and no human has commented.
