# Routine v2 session — 2026-07-15 (008)

**Mode**: `wiki` (Unified Routine v2)
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Selector signals**: candidate_count 1119, seen_in_entry 6, max_entry_id 29875, comprehensive_next 6500, cross_model_review_next 15254, unharvested_observations 12, reviewed_entries 503, open_backlog_items 6. OpenRouter remaining $4.8861.
**Branch**: `claude/modest-keller-auw8bs`

## Pre-flight
- §0a rescue: `mcp__github__list_pull_requests` (state=open) → **0 open PRs**. Nothing to rescue.
- §0b sweep: no `claude/*` PRs to sweep. Lock acquired cleanly.

## Work (wiki mode)
Markdown-only run — no entries changed, so **§4 self-verification skipped** (OpenRouter untouched), **no `make build`**.

### [F] Harvest — 9 loose observations from three 2026-07-15 routine runs
Eight reinforcements of well-established patterns + one genuinely-new tool bug. Filed:
- **Cleanup P20 update** — off-vocab semantic-tag creation-batch cohort reaches the **15100s** (15100–15153 accuracy-review migrated ~30/54 off-vocab tags in-run); recommends a `check_tag_drift --check unknown-semantic` sweep over 15000–15500. Mirrored in `backlog-queue.json` `unknown-semantic-tags` notes.
- **Cleanup P21 update** — zero-inline-link band reaches **06490–06499**; new tractability datum: example-linking is uniformly cheap across the band, but notes-linking cost tracks each cohort's note density (dense 加湿器/除湿機/踏切 appliance-composition glossaries deferred; 06495–06499 notes completed in-run + candidates added). Band now unbroken ~06150→06499.
- **Cleanup P17 update + Tooling item 6** — fifth formality sub-family (colloquial i-adjectives auto-`formal`, 06498 しつこい `formal`→`neutral`) + a cheap high-precision `adjective-i`+`formality: formal` deterministic drift cut for `check_tag_drift.py`.
- **Tooling item 17 update** — twentieth confirmation: `general`-too-broad in-list-narrowness noise now surfaces on the create-era frontier §4 self-check too (3/5 rejected over 06490–06494).
- **Tooling item 20 update** — fifty-first/fifty-second no-op confirmation (two priority lanes all-no-op on already-polished closed-tier basics; priorities regenerated + cursor reset). Reinforces the two `score_note_quality.py` scorer bugs.
- **Tooling new item 33** — `build/prioritize_polishing.py --task notes` crashes with `KeyError: 'furigana'` (single-task branch indexes hard-coded `results['furigana']`; only arg-less `make priorities` works; `CLAUDE.md`-documented per-task flag broken).

Both backlog "Last updated" headers refreshed; all 9 observations cleared from `polishing/observations.md` (harvest marker appended).

### [A] Light sync
Refreshed `project/overview.md` General-tier count → 26,863 tagged `general` + 20 untiered (29181–29200), of **29,666** total (per `build/audit_tiers.py`).

### [H] Metrics trend — not due
Only 7 new `metrics-history.jsonl` lines (277 total) since the nineteenth refresh at 270 runs (threshold ≥10).

## §4 self-check
**Skipped — no entries modified** (wiki-only run; OpenRouter ledger untouched).

## §5 metrics
Appended one `wiki` line to `pipeline/metrics-history.jsonl` (entries_changed 0, entries_total 29666, review_queue 13316).

## Wrap-up state
- `pipeline/routine-state.json` advanced by the selector in §1.
- No cursor advance (wiki mode has no entry cursor).
- Files touched: `planning/wiki/ideas/cleanup-backlog.md`, `planning/wiki/ideas/tooling-backlog.md`, `planning/wiki/ideas/backlog-queue.json`, `planning/wiki/project/overview.md`, `planning/wiki/log.md`, `polishing/observations.md`, `pipeline/metrics-history.jsonl`, this session log.
