# Routine session 2026-09-05 #002

**Mode**: accuracy-review
**Reason**: highest scheduler debt among eligible modes (selector signal `accuracy-review: 0.361383`)
**Range**: entries 17239–18238 (980 entries reviewed; budget $2.50, spent $0.4713)

## Pre-flight

- Rescued PR #3262 (`routine(polish): ...`, head `claude/amazing-bardeen-g2ge7d`): green, mergeable,
  untouched by any human — merged (squash). Fetched and merged `origin/main`.
- Zero strands, zero orphan branches.

## Accuracy review

`build/review_accuracy.py --range 17239 18238 --budget 2.5`: 980 entries reviewed, 52 issues
flagged (filtered: absent-tag=1, breadth=15, fallback-tag=1, notes-noquote=1).

Adjudication (all 52 flags; full detail in `reviews/decisions.jsonl`):

- **offvocab (25 issues, 20 entries) — all applied.** Migrated off-vocabulary semantic tags to the
  closed list in `build/validate_tags.py` (e.g. `speech`/`manipulation` → `communication`,
  `translation` → merged into existing `language`, `transport` → `transportation`).
- **wrong-category (5) — 4 applied, 1 rejected.** Applied: 万が一 (`quantity`→`general`), 電柱
  (`building`→`daily-life`), 給仕 (dropped `food`, kept `work`), 膝掛け (`furniture`→`daily-life`).
  Rejected: 稲荷 — `food` is legitimate for the entry's own "inari sushi" sense.
- **gloss/translation-meaning (6, 5 entries) — 1 applied, 4 rejected.** Applied: 公爵's gloss
  wrongly included "prince (peerage)" — 公爵 is duke only; removed. Rejected: 億's two flags were
  reviewer arithmetic errors (80億 = 8 billion and 10億円 = one billion yen were already correct);
  つぼむ's flag misread the て-form as non-sequential (the original "closed the umbrella and got on
  the train" is the standard sequential reading); 溶解's flag was a style nitpick matching the
  entry's own established infinitive-label convention; 謝意's "apology" sense is a real secondary
  meaning already backed by the entry's own sense 2.
- **notes-fact (24, 22 entries) — 7 applied, 1 flagged to curator, 14 rejected.** Applied: softened
  an absolute claim about 最小 (size only, not count), fixed 途方に暮れる's etymology for 暮れる
  (was "to be overwhelmed", corrected to the literal "to grow dark" → metaphorical "to lose one's
  way"), removed a misleading "less common" framing on 何度's degrees sense, softened やっちゃった's
  "always implies regret" overreach, and fixed three formality-tag/register-note contradictions
  (問答無用, 汚らわしい, 百獣の王 — each note called itself literary/formal while the tag said
  neutral; tag has no "literary" value in the schema, so set to `formal`). Flagged: 茶店 — the
  entry's さてん reading is not one I could confirm as genuine; see `reviews/needs_curator.txt`.
  Rejected 14 as reviewer misreadings or misattributions: 左記/右記 vertical-writing direction was
  already correct (reviewer had it backwards); 尊重する, 何人, 分割払い/一括払い, 肉じゃが,
  親子丼, そうかも, あざす, 次長, 際立たせる, and 五分咲き notes were all already accurate on
  careful re-reading — several of the "errors" traced back to the reviewer misparsing the
  inline-link markup (`⟦...→...⟧`) rather than the entry.

## Mechanical pass (32 changed entries)

- `normalize_notes.py`: 0 rewrites (7 pre-existing non-canonical headers noted, none introduced by
  this run).
- `auto_link.py --confirm-real-entries`: +37 links across 10 entries (mostly on the rewritten
  途方に暮れる note).
- `harvest_crossrefs.py`: 0 new cross-references.
- `validate.py --changed-only --ratchet`: 36/36 valid.

Self-check (§4) skipped for all 32 changed entries — they fall inside the range the accuracy
reviewer just covered, so per §A step 8 the review itself was the check.

## Cursors / queue

- `polishing/tasks/cross-model-review/progress.txt`: `next: 18239`.
- `reviews/queue.txt`: pruned entries 17239–18238 (12322 → 12062 lines).
- `pipeline/openrouter-ledger.json`: +$0.4713 (review phase).

## For the curator

One judgment call needs you: **18069_saten (茶店)** — I couldn't confirm the entry's さてん
reading is genuine (see `reviews/needs_curator.txt`). Everything else adjudicated cleanly.
