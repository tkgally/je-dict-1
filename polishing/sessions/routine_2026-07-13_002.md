# Routine session — 2026-07-13 (002)

**Mode:** accuracy-review (phase: furigana)
**Selector reason:** accuracy-review: highest scheduler debt among eligible modes
**Range worked:** 13975–14500 requested; screening completed 13975–14186 (214 entries) before timeout
**Session budget:** OpenRouter $2.50 (daily remaining $4.9986 at start)

## What ran

- Furigana screening pass (`review_runner.py --pass screening --range 13975 14500 --budget 0.60`,
  model google/gemini-2.5-flash). **Timed out at the 25-min wrapper limit (SIGTERM/exit 143)**
  after 214 contiguous entries (13975–14186). Per §A resilience, kept the per-entry results
  already written, logged a `[tooling]` observation, and continued — did not retry the whole pass.
- 22 entries carried screening flags (includes a few pre-existing screening files above the
  timeout frontier). **Deep OpenRouter pass skipped**: I verified every flag directly against the
  current entry JSON, which is authoritative and cheaper than a second model round (and avoids a
  second timeout).

## Adjudication (§C logged to reviews/decisions.jsonl)

- **APPLY (1):** 14102 — example 3 had `{衣装|いしょ}{替|が}え` (reads いしょがえ); 衣装替え is
  いしょうがえ, and the entry's own notes correctly write `{衣装|いしょう}{替|が}え`. Added the
  missing う → `{衣装|いしょう}`. Updated `modified`.
- **REJECT (21):** 14203 (`{髪|はつ}` is a valid on-reading in the 茶髪 morpheme-decomposition
  note) logged individually; the remaining 20 bulk-rejected as one aggregated family line —
  partial-reading extraction artifacts (ちめいし/えいせ/ひがいし/しゅうぎ… all correct in-entry),
  compound rendaku (14020 茶屋/ぢゃや — a documented editorial choice in calibration_report.md),
  and model misreads (14488 entry already has the correct ござん, not the claimed ございん).
- Precision ~1/22 (~4.5%), consistent with the documented 0–5% on already-polished ranges.

## §4 self-verification

- Skipped for the one changed entry (14102): it was already covered by this run's §A review, so
  per §4 the review itself was the check. No separate model call.

## Always-on capture

- Candidates added: none (no un-entried words encountered during furigana adjudication).
- Observations: 2 appended to `polishing/observations.md` — `[tooling]` screening-timeout
  (recommend smaller default range / checkpointing) and `[pattern]` low furigana-screening
  precision on polished ranges.

## Metrics

- `metrics_snapshot.py`: entries_changed 1, flags_applied 1, flags_rejected 21, queue 13632,
  openrouter_spent_today $0.1114.

## Ledger

- Recorded screening spend est. $0.11 (214 entries) → ledger spent_usd $0.1114.

## Cursors / next

- `polishing/tasks/cross-model-review/progress.txt` → `next: 14187` (first unscreened entry;
  timeout frontier).
- `reviews/queue.txt` pruned for 13975–14186 (139 lines removed → 13632).
- Selector state advanced by routine_next.py in §1.
