# Routine session 2026-09-13 #006

**Mode**: systemic-fix (selector reason: "systemic-fix: highest scheduler debt among eligible
modes")
**Params**: backlog_item `furigana-katakana-base` — katakana wrapped in furigana braces
(`{カキ|がき}` style), scope estimate 273 instances / entries at last measurement (2026-08-31).

## Pre-flight

No open `claude/*` PRs, no branches other than `main`. Nothing to rescue or sweep.

## Detect

Ran `build/check_furigana_format.py --json`: 239 malformed-wrapper records, but its `pure-kana`
bucket (46 records) also catches numerals, `々`/`〇`/`○`, romaji abbreviations like `BGM`, and other
kana-only-but-not-katakana bases — it does not isolate "surface side is entirely katakana," which
is what this backlog item actually targets. Wrote a direct scan (surface side of every
`{left|right}` wrapper in every entry field, checked against the Unicode katakana block) instead.

**Result: only 1 instance remained in the whole dictionary** — 23394 二枚貝's
`{カキ|がき}` in `definitions[].explanation`, the exact "275th, genuine reading error" the backlog
item's own notes named. The other 274 instances (across the other 228 entries measured on
2026-08-31) had already been stripped by intervening polish/systemic-fix runs; the backlog item
was just never marked resolved.

## Fix

23394_nimaigai: `{カキ|がき}` → `カキ` (dropped the wrapper; カキ is katakana and needs no
furigana, and がき was wrong anyway — the entry's own notes already gloss カキ correctly as かき via
a link to 10786_kaki 牡蠣). Bumped `modified`. Re-ran the direct scan after the fix: 0 katakana-base
furigana wrappers remain anywhere in the dictionary.

## Mechanical pass (changed entries: 23394)

- `normalize_notes.py --apply`: 0 rewritten (2 pre-existing unknown note headers noted, not
  touched — out of scope for this fix).
- `auto_link.py --apply --confirm-real-entries`: 0 new links.
- `harvest_crossrefs.py --apply`: 0 new cross-references.
- `validate.py --id 23394_nimaigai --ratchet`: valid.

## Self-check

`review_accuracy.py --ids 23394 --budget 0.40`: 0 issues. `review_links.py --ids 23394
--skip-decided --budget 0.10 --src self-check`: 1 link judged, 0 flagged. Model found nothing.
Ledger: +$0.0005 (today's OpenRouter total: $0.9969 of $5.00 cap).

## Backlog item

Marked `furigana-katakana-base` **resolved** in `planning/wiki/ideas/backlog-queue.json`
(`scope_estimate` 273 → 0, `scope_measured_at` → 2026-09-13) and updated the Priority 60 section
of `planning/wiki/ideas/cleanup-backlog.md` with the resolution note. Left the separate
`katakana-hiragana-reading-wrappers` merge suggestion (from the 2026-08-31 queue-hygiene note) for
whoever owns that item — out of scope here.

## Metrics

`metrics_snapshot.py --mode systemic-fix --changed 1` (first call mistakenly passed the entry ID
23394 instead of the count 1 to `--changed`; caught it immediately and corrected the appended
`metrics-history.jsonl` row by hand before anything else was appended after it).
entries_total 30683, candidates 211, review_queue_len 10321, flags_applied/rejected/to_curator 0.

## make index

30683/30683 entries valid. Articles: 10/10 ok. Indexes, word-ID lookup, and kanji index
regenerated cleanly.

## PR and CI

PR #3289 opened (`claude/trusting-mendel-qxnphd` → `main`). The `validate` check stayed
`in_progress` through all 16 polls (30s+ apart, ~28 minutes total) — much longer than the ~6
minutes prior runs saw for the same check on a 1-entry diff, and `get_job_logs` returned 404 while
the job was still running, so no way to see why from here. Left the PR open per §7 step 5.3; the
next run's §0a pre-flight will rescue it once CI settles (merge if it went green with no human
comment, otherwise it'll need a look).

## For the curator

- Queue hygiene only: the `katakana-hiragana-reading-wrappers` backlog item (filed as "blocked —
  policy decision, not a defect") should be merged with this now-resolved item's history per the
  2026-08-31 note in `cleanup-backlog.md` — the actual open question is whether any of the
  affected words were a deliberate reading gloss on a hard-to-pronounce loanword, worth putting to
  a sample rather than leaving the twin item blocked indefinitely.
- PR #3289's `validate` CI check ran far longer than usual (28+ minutes, still `in_progress` when
  this run stopped polling at the 16-poll cap) on a 1-entry, few-line diff. Worth a look if it
  recurs — may be an Actions runner/queue issue rather than anything about this change. Left the
  PR open for the next run to rescue.
- No entry-level flags this run.
