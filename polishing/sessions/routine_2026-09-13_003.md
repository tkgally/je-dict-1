# Routine session 2026-09-13 #003

**Mode**: systemic-fix
**Backlog item**: `bare-kanji-headwords` (`ideas/cleanup-backlog.md#priority-52`)
**Reason**: highest scheduler debt among eligible modes

## Pre-flight

- Rescued PR #3284 (`routine(accuracy-review): adjudicate 54 flags across 1,000 entries
  (23239-24238)`): all checks green (`validate`: success), mergeable, no human comments or
  reviews. Squash-merged, then fetched and fast-forward merged `origin/main`.
- No other open `claude/*` PRs to sweep.
- `mcp__github__list_branches`: only `main` remains (the merged PR's head branch was
  auto-deleted) — no orphan `claude/*` branches.

## Systemic fix: bare-kanji headwords — duplicate of an already-resolved item

Re-ran the item's detector (`re.search(r'[一-鿿]', headword) and '{' not in headword`)
dictionary-wide instead of trusting the filed count (259, measured 2026-08-10): found **0**.

Investigated why: this backlog item (`bare-kanji-headwords`, Cleanup P52) is a duplicate filing
of `headword-missing-furigana` (Cleanup P36) — same detector, same population. A 2026-09-06
systemic-fix run already swept P36 to zero (258 entries braced) and marked *that* record
resolved in `backlog-queue.json`, but never touched this duplicate record, so the scheduler
picked it again today. No entry files needed fixing.

**Shipped the validator fix both items called for (Tooling 56 / 96), since it was still open:**
`find_missing_furigana.py` never scanned `headword` (only used it to label output), and
`validate.py` had no check on `headword` at all. Both P36's resolution note and P52 flagged this
as the reason the backlog would refill (~70 bare-kanji headwords/month at creation time).

- `build/find_missing_furigana.py`: added `headword` to `fields_to_scan`.
- `build/validate.py`: added `find_bare_kanji_headword_errors` — a hard ERROR (no baseline; the
  corpus is already at zero) for any kanji in `headword` outside a `{kanji|reading}` group.
  Wired into `validate_entry_file` alongside the existing furigana-brace checks.
- Verified both instruments report 0/30,683 entries after the change; full `build/validate.py`
  run: 30683/30683 entries still valid, no new errors or warnings introduced; `python3 -m
  unittest discover -s build/tests -q`: 426 tests, all pass (25 skipped, pre-existing).
- Updated `backlog-queue.json`: marked `bare-kanji-headwords` (P52) `resolved` (scope 0, noted as
  the duplicate filing) and appended the ratchet-shipped note to `headword-missing-furigana`
  (P36). Updated the prose pages (`cleanup-backlog.md` P36 and P52, `tooling-backlog.md` items
  56 and 96) with resolution notes.

No entry files were changed this run, so §3 (mechanical pass) and §4 (self-check) had nothing
to run on.

## Metrics

`metrics_snapshot.py --mode systemic-fix --changed 0`: entries_changed 0, flags_applied 0,
flags_rejected 0, flags_to_curator 0.

## Cursors

Systemic-fix has no comprehensive-progress cursor; the backlog item's own `status`/`notes` in
`backlog-queue.json` and its prose pages are the record (updated above).

## Indexes

`make index`: `validate.py` (30683/30683 valid) → `validate_articles.py` (10 articles, 1405
links, 0 errors) → `update_indexes.py` → `update_kanji_index.py` (2801 kanji files, no new kanji).

## For the curator

Nothing needs a decision. Two small process notes:

- The `bare-kanji-headwords` (P52) backlog record was a stale duplicate of an item already
  resolved a week earlier (P36) — worth a quick look at `backlog-queue.json` next time a wiki
  harvest runs, in case other items were double-filed the same way and are still marked `open`.
- The recurrence risk both items flagged (new entries shipping with bare-kanji headwords,
  ~70/month) is now closed by a validator, not just a one-time sweep: `validate.py` will hard-fail
  any future entry with this defect, and CI (`build/tests`) covers it.
