# Routine session 2026-09-14 #001

**Mode**: polish (selector reason: "polish: highest scheduler debt among eligible modes")
**Params**: start_id 7340

## Pre-flight

One open `claude/*` PR: #3289 (`routine(systemic-fix): resolve furigana-katakana-base backlog
item`, head `claude/trusting-mendel-qxnphd`). Title started with `routine`, check runs all green
(`validate`: success), no human comments or reviews — merged (squash). Merged `origin/main` into
this session's branch. No other open PRs, no orphan `claude/*` branches. Nothing else to sweep.

## Priority lane

`priority/notes.txt` is 2 days old (regenerated 2026-09-12), well under the 14-day regeneration
threshold. Started at the persisted cursor (line 1953). The 30-day "recently modified" skip filter
is still severely thinning the lane (see observation below): scanning 6,961 consecutive lines
(1953-8913) found only 20 eligible entries.

Of the 20 read (all validated clean before editing), 7 needed no change (00239_kiyoi, 00795_man,
02235_namida, 00412_uisukii, 02713_geki, 05127_susuru, 04926_panko). The other 13 got fixes:

- **02659_kudari**: added missing `transportation`/`language` semantic tags (multi-sense entry had
  only `movement`).
- **05586_oshougatsu**: added missing `culture` semantic tag.
- **04668_hasamaru**: `semantic: ["food"]` was mistagged (only 1 of 6 examples is food-related) →
  `["action"]`, matching its transitive pair 00091_hasamu.
- **04396_fuji**: two hand-written `：noentry⟧` markers (藤田, 藤井) → plain text, both queued as
  candidates (C23427, C23428); fixed lowercase section headers (`Common expressions:` →
  `COMMON EXPRESSIONS:`, `Cultural note:` → `CULTURAL NOTE:`, dropped a stray `Counter:`
  mini-section into the intro sentence); `semantic: ["general"]` → `["plant-flower"]`.
- **01369_suteeki, 01927_nou, 01368_sukuriin**: five more hand-written `：noentry⟧` markers total
  (ミディアムレア, ミディアム, ウェルダン, サーロインステーキ, フィレステーキ, 能楽堂, プロジェクター,
  スクリーンセーバー, ワイドスクリーン, マルチスクリーン — 10 words) → plain text, all queued as
  candidates (C23429-C23438).
- **00021_bunpu**: lowercase headers fixed (`Common collocations:` / `Usage contexts:` →
  canonical); `semantic: ["general"]` → `["science"]`.
- **00086_hakki**: lowercase header fixed; `semantic: ["general"]` → `["action"]`.
- **01944_korosu**: trimmed a redundant "LITERAL MEANING (Sense 1)" / "FIGURATIVE MEANING (Sense
  2)" block that only restated the `definitions` field.
- **00061_geinou**: merged two lowercase, overlapping mini-headers ("Traditional 芸能:" / "Modern
  芸能:") into a canonical `TYPES:` section and removed a duplicated collocation; added `art` to
  `semantic`.
- **06289_shinki**: restructured four non-canonical/lowercase headers (`BUSINESS/FORMAL USAGE`,
  `FAN COMMUNITY USAGE`, `CONTRAST WITH`, `NOTE:`) into canonical `COMPOUNDS:` / `COMMON
  EXPRESSIONS:` / `SIMILAR WORDS:` / `USAGE:` sections.
- **04305_tokage**: removed a stray entry-level `domain: ["business"]` tag that belonged only to
  one idiom in the notes, not the headword; folded a bare `Counter:` line and `Note:` header into
  proper prose under `WRITING:`/`COMMON EXPRESSIONS:`.

Cursor advanced: `priority-cursor.txt` → line 8914.

## Frontier lane

Read entries 07340 through 07354 (15 entries, contiguous IDs, all files present).

- **07340_kirikizu**: no entry changes; two words named in notes without entries (打撲傷, 噛み傷)
  queued as candidates (C23439, C23440).
- **07341_rinku, 07342_sukyan**: `semantic: ["general"]` → `["technology"]`.
- **07343_haraimodoshi**: `semantic: ["general"]` → `["money"]`.
- **07344_hensou**: `semantic: ["general"]` → `["business"]` (later also corrected `formality`
  during self-check, see below).
- **07345_soufu**: non-canonical `FORMALITY SCALE (least to most formal):` header → `SIMILAR WORDS
  (least to most formal):`; `semantic: ["general"]` → `["business"]`.
- **07346_torikeshi**: `semantic: ["general"]` → `["business"]`; `formality: "informal"` was wrong
  for an administrative cancellation term → `"neutral"`.
- **07347_akirakani**: `domain: ["colloquial"]` directly contradicted the entry's own REGISTER note
  ("more formal than はっきり") — removed; `formality: "formal"` also didn't match the note's "used
  in both spoken and written, mid-scale formality" → `"neutral"`.
- **07348_kikizake**: `semantic: ["general"]` → `["food", "leisure"]`.
- **07350_sugoroku**: merged a non-canonical `NEW YEAR GAMES:` header into `RELATED WORDS:` (later
  also corrected a gloss error during self-check, see below).
- **07342/07344/07346/07347/07348/07349/07351-07354**: remainder validated clean, no other changes.

Cursor advanced: `progress.txt` → next: 07355.

## Mechanical pass (changed entries: 00021, 00061, 00086, 01368, 01369, 01927, 01944, 02659,
04305, 04396, 04668, 05586, 06289, 07341-07348, 07350)

- `normalize_notes.py --apply`: 0 rewritten (11 non-canonical headers reported as pre-existing or
  newly-introduced-but-topical, e.g. `SCREEN VOCABULARY:`, `PROVERB:`, `GAMEPLAY:` — left as-is,
  out of scope beyond the lowercase-header fixes made above).
- `auto_link.py --apply --confirm-real-entries`: installed `sudachipy`/`sudachidict_core` (missing
  from the container despite being in `build/requirements.txt`) so the linker ran tokenizer mode
  instead of the degraded fallback; +3 links across 3 entries (06289_shinki, 07344_hensou,
  07346_torikeshi).
- `harvest_crossrefs.py --apply`: +10 cross-reference proposals (5 direct + 5 reciprocal) across 7
  entries — 07345_soufu ↔ 00742_okuru/21737_hassousuru, 07350_sugoroku ↔
  07128_takoage/07129_hanetsuki/21979_karuta. Neighbours touched only for the reciprocal back-link,
  not otherwise edited.
- `validate.py --id` on all 27 touched entries (22 directly edited + 5 reciprocal neighbours): all
  valid.

## Self-check

- `review_accuracy.py --ids <27 ids> --budget 0.40`: 2 flagged, both applied.
  - 07344_hensou: `formality: "neutral"` contradicted its own REGISTER note ("used in formal and
    business contexts") → `"formal"`.
  - 07350_sugoroku: `definitions[0].gloss` called it "Japanese backgammon-style board game" —
    factually wrong for the modern/general term (only the older 盤双六 variant resembles
    backgammon; the common 絵双六 is a roll-and-move game) → "Japanese roll-and-move board game".
  - Both logged to `reviews/decisions.jsonl`.
- `review_links.py --skip-decided --budget 0.10`: 1 flagged (06289_shinki, examples[3], な). The
  model read it as a sentence-final particle; it's actually the attributive copula before ので
  (新規なので), matching 09497_na's own definition exactly — REJECTED the flag, logged a `keep`
  line to `reviews/link_decisions.jsonl`.
- Ledger: +$0.0131 (self-check phase, 27 entries accuracy + 39 links judged); today's total
  $0.0131 of $5.00 cap.

## Metrics

`metrics_snapshot.py --mode polish --changed 22`: entries_total 30683, candidates 225 (was 211 at
session start; +14 net), review_queue_len 10321, flags_applied 2.

## Candidates added

C23427 藤田, C23428 藤井 (surnames, 04396), C23429 ミディアムレア, C23430 ミディアム, C23431
ウェルダン, C23432 サーロインステーキ, C23433 フィレステーキ (01369), C23434 能楽堂 (01927),
C23435 プロジェクター, C23436 スクリーンセーバー, C23437 ワイドスクリーン, C23438 マルチスクリーン
(01368), C23439 打撲傷, C23440 噛み傷 (07340).

## Observations logged

Three entries in `polishing/observations.md`: the worsening priority-lane skip-window hit rate
(~0.3%, down from ~2% on 2026-09-03), six more hand-written `：noentry⟧` markers found (adds to the
2,111-file backlog already flagged), and a pattern of lazy/mistagged `semantic: ["general"]` tags
across several entries this run that had a better closed-list fit.

## make index

Ran `make index` — see PR/CI section for outcome.

## PR and CI

PR opened from branch `claude/trusting-mendel-o9cv7c` → `main`. Awaiting CI, will squash-merge on
green per the routine's polling protocol.

## For the curator

- The priority-lane 30-day skip filter continues to worsen (now ~0.3% hit rate) and won't clear
  until roughly 2026-10-02, when the 2026-09-02 mechanical sweep's timestamps finally age out. If
  the next couple of runs don't show improvement, the filter likely needs a "last substantive
  edit" marker instead of raw `mtime`, per the standing recommendation from session 004.
- `sudachipy`/`sudachidict_core` were missing from this container despite being pinned in
  `build/requirements.txt`; installed for this run so `auto_link.py` used real tokenization
  instead of its degraded fallback. Worth checking whether the container image needs updating so
  future runs don't start in fallback mode.
- The `：noentry⟧` marker backlog (2,111 files as of 2026-09-13) is still open and still growing
  slower than it's being found; the dedicated systemic-fix pass recommended in session 004 hasn't
  run yet.
- Otherwise nothing needs attention this run: self-check found two real errors and both were
  fixed; the one link flag was a model misread, rejected with a documented `keep`.
