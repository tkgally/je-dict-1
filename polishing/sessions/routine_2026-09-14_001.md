# Routine session 2026-09-14 #001

**Mode:** polish (selector reason: "polish: highest scheduler debt among eligible modes")

## Pre-flight

- PR #3290 (routine polish, branch `claude/trusting-mendel-o9cv7c`, priority lines
  1953-8913, frontier 07340-07354) is open but not rescuable: its `validate` check
  fails because `07345_soufu` carries a non-canonical note header
  ("SIMILAR WORDS (least to most formal):") not in the header baseline. Its highest
  touched entry ID (07350) is at/above the comprehensive-polish frontier cursor
  (07340), so it is not stale either — left open per the sweep rule.
- No other stranded PRs. `claude/trusting-mendel-o9cv7c` is the only other `claude/*`
  branch and has an open PR, so no orphan-branch action.
- Because PR #3290 already holds unmerged edits to entries 07340-07354, this run's
  frontier lane started at **07355** instead of the selector's `params.start_id`
  (7340), to avoid two sessions editing the same entry files. See "For the curator"
  below.

## Priority lane

Cursor `polishing/tasks/comprehensive/priority-cursor.txt` from line 1953. The skip
filter (30-day recently-modified, no-file) continues to have a very low hit rate:
scanning 12,052 consecutive lines found 25 eligible entries (~0.2%). Of those 25,
13 got real fixes:

- **Noentry marker cleanup** (dictionary-wide backlog): converted stale
  `⟦word→word：noentry⟧` markers to plain text and queued the underlying words as
  candidates, in 06323_nawatobi (5 markers), 06016_shonanoka (5 markers),
  04325_kani (8 markers), 04334_ookami (3 markers), 01365_sandaru (1 marker),
  00632_inu (2 markers) — C23427-C23440 (several already queued by a prior pass).
- **Stale noentry markers with a real target**: 06774_baree's `部` marker and
  00427_yooroppa's `風` marker both now have entries (00440_bu, 30892_fuu created
  recently) — relinked instead of stripped.
- **Semantic tags**: replaced a sole `general` tag with a concrete one —
  01362_okage → abstract, 00228_kijun → abstract, 03155_mama → family,
  05828_kankoku → politics; 03151_houdai (suffix, no semantic tag at all) →
  grammatical.

## Frontier lane

07355-07370 (16 entries; skipped 07340-07354, see pre-flight). All are
well-constructed existing entries; only one fix found:

- **07358_hirumu**: tagged `formality: informal` + `domain: colloquial`, but ひるむ
  is a common word used in formal writing, news, and literature, not colloquial-only
  (contrast: 07359_todoroku next to it is tagged `formal`, and its own sibling
  07356_tajirogu is `neutral`) → changed to `formality: neutral`, dropped the
  `colloquial` domain tag.

## Self-check

`review_accuracy.py` on the 14 changed entries found 3 flags, adjudicated:

- **05828_kankoku** (apply): `formality: neutral` contradicted its own notes
  ("REGISTER: Formal.") → changed to `formal`.
- **01362_okage** (reject): flagged the お陰 kanji note as "misleading" — お陰 is
  the standard kanji for おかげ, not obscure or wrong.
- **06774_baree** (reject): flagged notes text as "grammatically incorrect," but the
  quoted text was garbled by the reviewer's own inline-link stripping — the actual
  notes read correctly.

`review_links.py` checked all 26 kana/link occurrences across the 14 entries: 0
flagged.

## Mechanical pass

`normalize_notes.py`: 0 rewrites (pre-existing non-canonical headers in these
entries were already in the header baseline; `check_note_headers.py --gate`
confirms no new unknown headers). `auto_link.py` (installed `sudachipy`/
`sudachidict_core` for full tokenizer mode): +3 links across 3 entries.
`harvest_crossrefs.py`: 0 new cross-references (all already present).
`make index`: 30,683/30,683 entries valid.

## Candidates added

C23427-C23440 (前跳び, 後ろ跳び, 交差跳び, 片足跳び, 二七日, 三七日, 四七日, 五七日,
六七日, 土佐犬, スポーツサンダル, カニ, 大口真神, 三峯); several others attempted
were already queued by a prior pass (大縄, ハチ公, and the crab-variety words in
04325_kani).

## For the curator

- **PR #3290 needs a one-line fix to merge**: rename `07345_soufu`'s
  "SIMILAR WORDS (least to most formal):" header to a canonical one (or bless it as
  an alias) so its `validate` check passes. Until it merges (or is closed), the
  comprehensive-polish frontier cursor cannot safely advance through 07340-07354 —
  this run skipped that range and resumed at 07355 to avoid duplicate/conflicting
  edits. If that PR is abandoned instead of fixed, someone should re-open the
  frontier at 07340.
- Otherwise nothing needs attention — self-check found one real error (applied) and
  two model misreadings (rejected with reasons above); no link flags.

**Next cursors:** priority line 14005; frontier next 07371.
