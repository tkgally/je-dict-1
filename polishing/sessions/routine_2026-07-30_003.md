# Routine v2 session — 2026-07-30 (003)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: 25 unharvested observations, 1,024 candidates (15 `seen in entry`), max entry 30,240, comprehensive frontier 06694, cross-model review next 22072, 10 open backlog items
**OpenRouter**: $0.33 spent today, $4.67 remaining — **no OpenRouter calls made this run**

## Pre-flight

- §0a rescue: `list_pull_requests` returned **no open PRs** — nothing to rescue.
- §0b sweep: no open `claude/*` PRs to sweep.
- Lock acquired for `claude/determined-rubin-epkyjv`.

## Work performed (5 activities: F, D, A, E, plus the H check)

### [F] Harvest — all 21 loose observations cleared

From four runs: the 2026-07-29 wiki harvest's own raised observations, the 2026-07-29 polish
and systemic-fix runs, the 2026-07-30 polish run (05xxx onomatopoeia block + 06688–06693
frontier), and the 2026-07-30 accuracy-review (21901–22500).

**Four observations closed items rather than reinforcing them:**

| Item | Outcome |
|---|---|
| Tooling 24 (context-snippet half) | **RESOLVED** after four cycles |
| Tooling 19 | Implementation site found — `manage_candidates.py sync` |
| Tooling 23 | Writable candidate pool reached **zero** |
| Tooling 6 | Map's value re-scoped by the ~100-distinct-string measurement |

- **Tooling 24 — RESOLVED.** `extract_furigana_pairs()` captured only 10 characters of
  following context and `build_screening_prompt()` rendered it as
  `(followed by: {協議会|きょうぎか)`. The window almost always cuts inside the next
  `{kanji|reading}` wrapper, and the annotation's closing paren then reads as the wrapper's
  closing brace — the model saw a well-formed wrapper holding a truncated reading and correctly
  reported what it was shown. Final pre-fix precision: **0/21** on 21301–21900, ~0 on the ~16%
  of 21901–22500 flagged. Consequence recorded: **every screener precision figure in the series
  predates the fix**, so "retire the screener" is suspended, not resolved.
- **Tooling 19.** `manage_candidates.py sync` already computes the exact `(surface, reading)`
  set that just crossed from "no entry" to "has an entry" — that set *is* the stale-`noentry`
  work list, free as a side effect. Split into an incremental hook and the historical sweep.
- **Tooling 23.** The 2026-07-29 new-entries run drained the `seen in entry` pool completely.
  `candidates_low` is now **anti-correlated**: reads ~1,024 while the writable pool is empty.
- **Tooling 6.** 275 off-vocab tags spanning **~100 distinct strings** — map the recurring head,
  review the judgment tail, gate the inflow; none substitutes for the others.

**Also filed**: new Tooling **45** (extend the ledger's `n` aggregation from `reject` to
`flag`), **46** (the reviewer missed ~11% of a set-membership class the regex decides free),
**47** (below); Tooling **43 update** (okurigana variants enter the candidate queue as new
words); Cleanup **P13**, **P21**, **P9** updates; new Cleanup **P33**; Entry Follow-up for **ば**.

**Two new defect classes measured by the harvest itself** (by checking an observation's premise
rather than accepting it):

- **Cleanup P33 — 77 entries** whose notes announce `ONOMATOPOEIA`/`オノマトペ` while
  `tags.semantic` omits `onomatopoeia`. Reaches beyond the 05xxx block the observing run
  supposed, but its *cause* held (19924 ぱらぱら does carry the tag → bounded create-era
  population). Batch-ready systemic-fix; evidence is inside the entry being changed.
- **Tooling 47 — cross-reference `headword` fields are scanned by no furigana instrument.**
  Their redundant `reading` field makes them self-checking: 869 raw → 116 fully-annotated →
  **11 after normalization → 7 genuine defects** (over-wrapped okurigana ×5, missing okurigana
  ×1, unmarked rendaku ×1). Also found that `check_artifacts.py`'s `missing-target-id` *does*
  cover the target-less shape a polish run asked about, but a `ref_is_resolvable` filter
  suppresses the malformed subclass.

### [D] New page — `topics/instrument-defects.md`

The run's main content contribution, answering the 2026-07-30 accuracy-review observation
("second consecutive cycle in which a *systemic errors* signal was an instrument defect").
Six documented cases, what they share, and the core argument: **as corpus quality rises,
P(instrument bug) overtakes P(a swept range is 16% defective), so a flag-rate jump on a polished
range is now a tooling hypothesis first** — inverting the reading that was correct a year ago.
Plus six detection heuristics and the cost asymmetry (a defective instrument does not merely
fail to find defects; it *generates* work that must later be retracted).

### [A] `project/overview.md` counts refreshed

30,012 → **30,031** entries; 27,209 → **27,228** general; 117,198 → **117,263** examples;
16,821 → **16,902** cross-references.

### [E] Lint and sync

`backlog-queue.json` resynced against fresh detector runs:

| Item | Was | Now |
|---|---|---|
| `unknown-semantic-tags` | 5,939 | **5,519** |
| `tag-semantic-mismatch` | 834 | 845 |
| `tag-sole-general` | 3,791 | 3,790 |
| `furigana-cosmetic-wrappers` | 763 | 762 |
| `tag-concrete-noun-domain-mismatch` | 8 | 9 |

The 420-instance `unknown-semantic` drop is the largest single-refresh fall recorded in that
class (the 2026-07-30 accuracy-review migrated 93 off-vocab tags). Two new queue items added:
`tag-onomatopoeia-missing` (77, batch-ready) and `crossref-headword-reading-disagreement` (11).
Wiki-wide cross-reference lint: **0 broken links**.

### [H] Metrics trend — NOT due

390 metrics lines vs the 383 covered by `topics/quality-metrics.md` — 7 new, below the ≥10
threshold.

## §4 self-verification

**Not applicable — zero entries created or modified** (`git status --porcelain -- entries/`
returned nothing). This is a wiki-only run; no OpenRouter calls were made and the ledger is
unchanged at $0.33 spent today.

## §5 metrics

Appended: `mode=wiki, entries_changed=0, entries_total=30031, candidates=1024,
review_queue_len=13310, observations_lines=567, comprehensive_next=6694, spend=$0.33`.

## Verification note

The cross-reference lint written for Activity E reported **119**, then **24**, then **7** broken
anchors before the true count turned out to be **0** — three independent bugs in its own slug
function (anchor table keyed on relative paths but looked up with resolved ones; runs of spaces
collapsed where GitHub maps each separately; en/em dashes converted where GitHub drops them).
Acting on iteration 1 would have "fixed" 119 working links into broken ones. What settled it was
an anchor already in the repo — `#priority-23-20-entries-2918129200-missing-metadatavocabulary_tier`,
whose `2918129200` records a silently-dropped en-dash. Written up as case 6 of the new page
rather than quietly fixed, since it is that page's thesis reproducing live.

## Cursors and state

- `pipeline/routine-state.json` — advanced by the selector in §1.
- No polishing cursor advanced (wiki mode touches none).
- `polishing/observations.md` — 21 observations cleared, harvest note appended.
- `planning/wiki/log.md` — session entry appended; `index.md` updated with the new page and the
  dated summary.

## Build

**Skipped** per §6 — wiki-only run, markdown changes only, `docs/` untouched.

## Routed to the curator

A `wiki` run may not modify prompts, skills, entries, or build scripts:

- **Tooling 45** — the §C one-sentence change extending `n` aggregation to `flag` decisions.
  Smallest item on the list; unblocks honest escalation statistics and makes
  `reviews/needs_curator.txt` (102 lines, mostly one cause) readable again.
- **Tooling 46** — off-vocab pre-scan sequencing, so OpenRouter dollars buy judgment only.
- **Tooling 47** — the `ref-reading-disagreement` sub-check, and the `ref_is_resolvable`
  filter question.
- **Tooling 19** — the `manage_candidates.py sync` stale-`noentry` hook.
- **Tooling 23** — the `source` field on candidates, now with a second justification (the
  selector's `candidates_low` signal).
- **Cleanup P9** — the `pure-kana` subpattern split that makes 258 katakana wrappers
  mechanically fixable.
- **Tooling 21 / 42** — `--workers N`, and the container image's missing `jsonschema`/`pytest`
  (named in the new page as the common ancestor of several instrument defects).
- **ば** — needs a headword-form decision (bound affix vs. bare entry) before it can be written.

## CI outcome — PR left OPEN (no checks ever appeared)

PR **#3069**. The §7 CI gate never reached a verdict: `get_check_runs` returned
`total_count: 0` on **every** poll over ~14 minutes (well past the ~8-minute cap), and
`actions_list --workflow validate.yml --branch claude/determined-rubin-epkyjv` likewise
returned `total_count: 0` — **no workflow run was ever queued for this PR**, as opposed to one
queued and running slowly.

This is not a failing check and not a repository misconfiguration:

- `.github/workflows/validate.yml` triggers on `pull_request` for `branches: ["**"]` with **no
  path filters**, so a markdown-only PR should still queue a run.
- Actions is healthy generally — the most recent `validate.yml` run (2026-07-30 03:55Z, branch
  `claude/determined-rubin-k4862v`) completed `success`, as did every run before it.

Per §7 step 5.3 ("still pending at the cap → leave the PR open and stop"), the PR was **not
merged**. The next Routine run's §0a rescue should merge it once checks appear and go green.

**Risk assessment for the curator**: low. This run changed **no entry files** (`git status
--porcelain -- entries/` empty), so `validate.py` and the off-vocab tag ratchet are evaluating
exactly the corpus already on `main`. The diff is markdown plus `backlog-queue.json`,
`routine-state.json`, and `metrics-history.jsonl`.

**Worth watching**: if the next run also sees `total_count: 0`, the cause is queueing rather
than latency and belongs in the tooling backlog — an unattended run cannot distinguish "CI is
slow" from "CI will never start" without the `actions_list` cross-check used here, which is not
currently part of the documented §7 loop.
