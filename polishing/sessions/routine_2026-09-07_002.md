# Routine session — 2026-09-07 002

**Mode**: systemic-fix (selector reason: "systemic-fix: highest scheduler debt among eligible modes")

## Pre-flight

No open PRs, no orphan `claude/*` branches. Working tree clean at session start.

## Params

- `backlog_item`: `conjugation-potential-of-potential` (priority 41, `planning/wiki/ideas/cleanup-backlog.md#priority-41`)
- Seed list given directly by the selector (curated, not detector-generated): `00557 dekiru`,
  `01165 mieru`, `01229 kikoeru`, `02376 toreru`, `15166 nemureru`, `06957 ikeru`,
  `30367 machikireru`.

## §B systemic-fix playbook

Each of the 7 entries is a verb whose own gloss, definition, or notes already describe it as the
potential/spontaneous form of a base verb (できる = potential of する; 見える/聞こえる = spontaneous
potential of 見る/聞く; 取れる = potential of 取る; 眠れる = potential of 眠る; いける = derived from
the potential of 行く; 待ちきれる = potential of 待ちきる). Despite that, each entry's generated
`conjugation` table still carried `Potential`, `Passive`, and `Imperative` rows — publishing
ungrammatical forms like できられる, できろ, 見えられる, 聞こえられる live on the site. `00557 できる`
is basic tier, the dictionary's highest-traffic band.

Opened all 7 entries by hand, confirmed each one's self-declaration against its own prose, then
deleted the `Potential`, `Passive`, and `Imperative` rows from each `conjugation.forms` array
(14 rows remain per entry, down from 17). No other fields touched. This is the fix prescribed in
the backlog item's "shippable form": suppression is safe by construction, since an omitted row
teaches nothing while a wrong row teaches ungrammatical Japanese.

Per the backlog's own accounting, this seed list of 7 *is* the full curated scope (the
self-declaration detector's 6 true positives plus 見える, which under-generates in prose but is
confirmed lexicalized). Marked the backlog item `resolved` in
`planning/wiki/ideas/backlog-queue.json` and appended a resolution note to
`planning/wiki/ideas/cleanup-backlog.md`. The generator-side guard (so `add_conjugations.py`
doesn't regenerate these rows if the tables are ever rebuilt) remains open as tooling-backlog
item 70 — out of scope for this run.

## Mechanical pass (§3)

- `normalize_notes.py`: 0 rewrites (2 pre-existing unknown headers noted on 01165/06957, unrelated
  to this fix, not touched).
- `auto_link.py --confirm-real-entries`: +1 link, on `30367_machikireru` only.
- `harvest_crossrefs.py`: 0 new cross-references.
- `validate.py --changed-only --ratchet`: 7/7 valid.

## Self-check (§4)

`review_accuracy.py --ids 00557,01165,01229,02376,06957,15166,30367 --budget 0.40` (cost
$0.0036). Flagged 3 issues on 2 of the 7 entries, none related to the conjugation-table change:

- 00557 できる, notes-fact: reviewer objected to "できる is the potential form of する" as
  morphologically imprecise. **Rejected** — this is the dictionary's own stated convention (see
  the entry's `cross_references` entry for 00392 する: "できる is its potential form") and is
  standard pedagogical framing, not an error.
- 01165 見える, gloss-meaning: reviewer wanted "to come (honorific)" changed to "to arrive/visit
  (honorific)". **Rejected** — "to come" is the standard keigo gloss for this sense; the flag was
  a stylistic preference, not a factual error.
- 01165 見える, notes-fact: reviewer objected to "見えている = 'is visible'" as not fully capturing
  the ongoing-state sense. **Rejected** — that's exactly what the stative English gloss captures;
  the note already frames it as the ている ongoing-state pattern.

All 3 decisions logged to `reviews/decisions.jsonl` (ts 2026-09-07T12:25:52Z).

## Ledger

- OpenRouter spend today: $0.4843 of $5.00 cap (self-check phase added $0.0036 to the $0.4807
  already spent by the morning accuracy-review run).

## Candidates / observations

None logged this run.
