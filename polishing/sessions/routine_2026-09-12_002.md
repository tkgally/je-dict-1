# Routine session 2026-09-12_002

**Mode**: accuracy-review
**Reason**: selector — "accuracy-review: highest scheduler debt among eligible modes"
**Range**: 21239–22238 (1,000 entries; 998 reviewed, 2 IDs not found)

## Pre-flight

Rescued PR #3277 (`routine(systemic-fix)`, previous session): check run green, mergeable, no
human comments — merged (squash) and fast-forwarded local `main`. No other stranded PRs or
orphan `claude/*` branches found.

## What this run did

1. Ran `build/review_accuracy.py --range 21239 22238 --budget 2.5`. 136 of 998 entries flagged
   (13.6%, well under the 25% noise threshold), 159 individual issues across six families:
   offvocab 115, notes-fact 15, register 10, wrong-category 7, gloss-meaning 6,
   translation-meaning 6.

2. **offvocab (115 issues, ~100 entries)** — deterministic per the playbook, but each off-vocab
   tag needed a per-entry choice of the best `VALID_SEMANTIC` replacement (no single tag class
   dominated; ~70 distinct off-vocab tags, mostly singletons). Read each flagged entry's gloss and
   full tag set, picked a replacement from the closed vocabulary in `build/validate_tags.py`
   (`transport`→`transportation` and `economy`→`economics` matched the existing 1:1 migrations in
   `build/check_tag_drift.py`; everything else was a case-by-case choice), applied to all 100
   entries, and confirmed `build/check_tag_drift.py --check unknown-semantic --range 21239 22238`
   now reports zero flags.

3. **notes-fact (15)** — checked each claim against general knowledge and the rest of the entry.
   Applied 7 genuine errors: a stray furigana brace (21382 誘い出す), a misdescribed 拘留 penalty
   category no Japanese legal term actually uses (21416), a note presenting 関心する as if it were
   a natural suru-verb (21451), furigana placement described as "alongside" instead of above/beside
   (21516 ルビ), a bogus alternate-kanji claim for ほてり (21722), a note conflating an albino sense
   that belongs to a different reading/word (21830 白子, しろこ vs しらこ), and a collocation gloss
   for 検視官 that called a police/prosecutor role "coroner, medical examiner" (21854). Rejected 8:
   in each case the note already said what the reviewer wanted, or the reviewer's claimed
   contradiction wasn't one (e.g. 21273 樹氷/霧氷, 22073's prefecture count, 21800's formality
   comparison were all already correct on inspection).

4. **register (10)** — 4 genuine gaps (`formality: null` on entries whose own REGISTER section
   says "Formal": 21898, 21903, 21912, 21920) fixed to `formal`. 6 rejected: three asked for a
   `'literary'` formality value that doesn't exist in `VALID_FORMALITY` (formal is already the
   closest valid tag); the rest were the reviewer misreading a relative/comparative register note
   as a contradiction, or citing the wrong tag field (22149: note was about formality, flag named
   politeness).

5. **wrong-category (7)** — 4 genuine mistags fixed (21599 座り方 tagged `body-part` for a manner
   of sitting, not a body part; 21701 お釜 tagged `food` for the vessel itself; 21916 老婦人 tagged
   `family` for a generic "elderly woman"; 22101 本場物 tagged `food` when its own notes says it
   applies to "food and traditional crafts" generally). 3 rejected: the reviewer checked only the
   headword's primary sense and missed a second definition that justified the tag (21566 間引き
   sense 2 is about reduced train service; 21998 ノックする sense 2 is the baseball term; 21999
   突き出し sense 2 is the sumo term).

6. **gloss-meaning / translation-meaning (12 issues, 10 entries)** — 8 applied: an intransitive-
   sounding gloss for a transitive verb (21440), the same coroner/medical framing fixed in notes
   for 21854, a gloss listing "dispatch" (the message) as if it meant "messenger" (22015), and five
   example-sentence translations reworded for naturalness or tense/nuance accuracy (21795 小さめ,
   21810 雨空, 21855 脱色, 22066 整形外科 ×2, 22222 〜たびに). 2 rejected: 21516's gloss listing
   furigana/ruby text as synonyms matches what the entry's own notes say; 21701's combined-sense
   top-level gloss matches the established convention seen elsewhere in the dictionary (e.g.
   21999, 21998).

7. Logged every decision to `reviews/decisions.jsonl` (one aggregated line for the 100 offvocab
   migrations, one line per entry for everything else — 43 lines total, covering all 136 flagged
   entries).

8. §4 self-check was skipped per the playbook: every entry this run touched falls inside the
   21239–22238 range the accuracy reviewer already checked, so the review was the check.

9. Mechanical pass (§3): `normalize_notes.py` found nothing to rewrite (14 unrelated
   non-canonical headers noted, pre-existing, out of scope). `auto_link.py` added 144 links
   across 52 of the 122 changed entries (many of these older entries had never been linked).
   `harvest_crossrefs.py` proposed 12 reciprocal/direct SIMILAR-WORDS cross-references, applied
   across 11 entry files (6 of the 122 plus 5 reciprocal targets outside the range). All 128
   touched entries pass `build/validate.py --changed-only`.

## Entries changed (128)

122 from the reviewed range (100 offvocab tag migrations; 21382, 21416, 21451, 21516, 21722,
21830, 21854 notes fixes; 21898, 21903, 21912, 21920 formality fixes; 21599, 21701, 21916, 22101
tag fixes; 21440, 22015 gloss fixes; 21795, 21810, 21855, 22066, 22222 example fixes — several IDs
overlap categories) plus 6 outside it picked up as `harvest_crossrefs.py` reciprocal targets
(25222, 22357, 02286, 04255, 10466, 01191).

## Self-check outcome

Skipped (in-range review was the check, per playbook §A step 8).

## Candidates added

None.

## Observations logged

None.

## Ledger

`review_accuracy.py`: 998 entries, ~$0.4889 (phase accuracy-review). Daily ledger: $0.5051 spent
of the $5.00 cap.

## Cursors

`polishing/tasks/cross-model-review/progress.txt`: `next: 21239` → `next: 22239`.
`reviews/queue.txt`: reviewed range removed (11,844 → 11,429 lines).

## PR / CI

PR #3278 opened. The `validate` check run (head 30c84d50) stayed `in_progress` through all 16
polls (~40 minutes) without completing — the same GitHub Actions runner-side stall the previous
session (PR #3277) reported at the "Checkout repository" step. Everything ran clean locally
(`build/validate.py --changed-only`, `make index`), so this looks like infrastructure, not this
change. Left the PR open per §7 step 5 rather than reset CI with more commits; the next run's
pre-flight rescue should pick it up once Actions recovers.
