# Routine session — 2026-07-16 #006

**Mode:** accuracy-review
**Selector reason:** "accuracy-review: highest scheduler debt among eligible modes"
**Params:** phase=furigana, start_id=15500, openrouter_session_budget_usd=2.5

## Range worked

Entries 15500–15566 (the span the furigana screening pass covered before its
500s wrapper timed out; accuracy dimension run over the same span).

## Furigana pass (§A step 2)

- `review_runner.py --pass screening --range 15500 15999 --budget 0.30` — the
  wrapper hit its 500s `timeout` after screening 64 entries (15500–15566); kept
  the per-entry results already written and continued (per §A resilience note).
- 10 entries flagged: 15501, 15510, 15511, 15516, 15524, 15532, 15541, 15545,
  15559, 15562.
- **All 10 verified by hand against the actual entry text — every furigana pair
  is correct.** Every flag falls in a documented false-positive family:
  okurigana/compound reading splits (梅干し, 人付き合い, 占星術, 投球+百二十球),
  model truncation-misreads (球場→きゅうじょう misread as きゅうじょ; 必需品 full
  readings misparsed as partial), an outright hallucination (15501 claimed a
  "裕→あ" pair that does not exist in 垢抜ける), and 2 parse failures.
- Bulk-rejected with one aggregated §C line; deep pass skipped (known-noise
  shortcut; matches the 0–5% screening precision on already-polished ranges).

## Accuracy pass (§A step 3–4)

- `review_accuracy.py --range 15500 15566 --budget 0.40` — 66 reviewed, 14
  flagged, all on the `tags` dimension. Est. cost $0.0292.
- Adjudicated per the semantic-tag policy (§A step 4):
  - **8 APPLY** — flags that a tag is *not in* `VALID_SEMANTIC` (correct by
    definition), migrated to the best in-list tag:
    - 15503 胃腸: `body` → `body-internal`
    - 15511 球場: `place` → `building`
    - 15514 暗室: `place` → `building`
    - 15515 編み棒: `object` → `tool`
    - 15516 人付き合い: `social` → `society`
    - 15521 週明け: `time` → `time-general`
    - 15523 背信: `social` → `society`
    - 15525 応戦: `competition` → `action` (kept `military`)
  - **6 REJECT** — "`general` too broad" nits where `general` is a valid in-list
    tag (too-broad substitution between in-list tags is rejected by policy):
    15507, 15513, 15522, 15533, 15555, 15563.
- All decisions logged to `reviews/decisions.jsonl` (10 furigana reject
  aggregated + 8 tag apply + 6 tag reject).

## §4 self-check

Skipped: all 8 changed entries were already covered by §A's accuracy review this
run (the review itself was the check, per §4 step 3).

## Validation

`validate.py --range 15500 15599` → 100/100 valid. `validate_tags.py` → no
unknown-tag warnings for the migrated IDs.

## Capture (§3)

No missing-entry words surfaced; no new systemic observations.

## Ledger / metrics

- OpenRouter: +$0.02 (furigana screening, 64) +$0.0292 (accuracy, 66);
  daily spent_usd now $0.202 (cap $5).
- Metrics line appended: entries_changed=8, flags_applied=8, flags_rejected=16.

## Cursors

- `polishing/tasks/cross-model-review/progress.txt` → `next: 15567`
- Pruned 15500–15566 from `reviews/queue.txt` (13312 → 13269 lines).

## Next

accuracy-review resumes at entry 15567.
