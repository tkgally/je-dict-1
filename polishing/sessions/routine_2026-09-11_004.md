# Routine session — 2026-09-11 #004

**Mode**: accuracy-review (selector reason: "accuracy-review: highest scheduler debt among eligible modes")
**Range**: 20239–21238 (999 entries), `openrouter_session_budget_usd` 2.5

## Pre-flight
Rescued PR #3274 (`routine(systemic-fix)`, head `claude/trusting-mendel-sej05o`): CI (`validate`)
was green, mergeable clean, no human comment or review, so merged it (squash). No other open PRs,
no stranded PRs, no orphan `claude/*` branches. Merged `origin/main` into this session's branch.

## Review
`build/review_accuracy.py --range 20239 21238 --budget 2.5`: 999 entries reviewed, 129 flagged
(12.9%, below the 25% noise threshold), est. cost $0.4884.

By family: offvocab 110, notes-fact 11, register 11, wrong-category 7, gloss-meaning 5,
translation-meaning 5.

## Adjudication

**offvocab (110 tags across 99 entries) — all applied.** Each off-list semantic tag was migrated
to the closest fit in `VALID_SEMANTIC` (`build/validate_tags.py`), reasoned from the entry's gloss
and its other, already-valid tags — not a blind 1:1 substitution. Patterns used repeatedly:
`personal-quality`→`personality`, `physical`→`movement`, `time`→`time-general` (per
`TAG_MIGRATION` in `check_tag_drift.py`), `crime`→`law`, `manufacturing`→`technology`/`business`/
`creation` (entry-dependent), `material`→`clothing`/`building` (entry-dependent), `shape`/`body`→
`appearance`. Where an existing valid tag on the same entry already covered the ground (e.g.
`place` dropped when `building` was already present), the bad tag was simply dropped rather than
padded with a redundant synonym. Two were plain formatting typos (`daily life`, `daily_life` →
`daily-life`). Full per-entry mapping and reasoning is in the decision ledger.

**wrong-category (7 issues, 6 entries) — all applied.** Removed the ill-fitting tag; 20366_kyuutai
(球体, "sphere") had both its `math` and `science` tags flagged by two different checks this run —
replaced with `appearance` (shape-descriptive) instead.

**register (11) — 7 applied, 4 rejected.** `VALID_FORMALITY` only has {formal, neutral, informal,
vulgar} — no "literary" or "slightly formal" — so every genuine mismatch was resolved to `neutral`.
4 were false positives: 20856_ieru, 20859_atsui, and 21130_hiwomiruyoriakiraka all have a REGISTER
section that explicitly says "formal" (e.g. "Formal and literary"), which the `formal` tag matches
exactly — no contradiction, rejected. 20982_mamachichi's register note was left as-is (defensible:
義父 genuinely doubles as father-in-law/stepfather, per the entry's own RELATED WORDS section).

**notes-fact (11) — 5 applied, 5 rejected, 1 flagged to curator.**
- Applied: 20244_zenzai (internal contradiction — REGIONAL DIFFERENCES said Kansai しるこ/ぜんざい
  are distinct by paste texture, SIMILAR WORDS said they're synonymous in Kansai; fixed to agree),
  20502_ganseki ("ignite rock" typo for "igneous rock"), 20612_kousha (降車/下車 usage pairing —
  降車 pairs with buses, not "cars and buses"; 下車 is broader, not train-only), 20667_otsukaresamadesu
  (clarified お疲れさまでした as grammatically past tense rather than an unqualified "past tense"
  claim), 21111_nouri (removed a spurious "脳裏: same word, alternative reading" SIMILAR WORDS
  bullet — a self-referential entry that didn't belong in that section and whose claimed reading
  I couldn't verify).
- Rejected: 20356_rouninsei (note correctly explains 浪人's samurai etymology before the modern
  metaphorical sense — reviewer misread the etymological framing as a claim about current meaning),
  20663_sejutsu (施術 read せじゅつ vs. しじゅつ is a genuine alternate-reading note, not a tautology
  — reviewer's tool apparently didn't register the differing furigana), 20891_uyo (reviewer said
  gloss/examples show only literal meaning, but example 3, 紆余を経て「ようやく結論に達した」, is
  clearly figurative — gloss and notes are accurate), 21166_boukensha (reviewer's own restated
  "correct" fact is identical to what the note already says — self-contradicted, no real issue).
- Flagged: 20361_kisaragi — internally inconsistent about whether 如月 (2nd lunisolar month)
  corresponds to February or March; logged to `reviews/needs_curator.txt` with my reasoning rather
  than picking a side, since I'm not confident enough in either the list-based or offset-based
  convention without a source check.

**gloss-meaning (5, 3 entries) — all applied.** 20640_shiasatte and 21187_kijitsu had misleading
literal glosses ("three days from now" is ambiguous, "memorial day" over-specifies a US holiday)
removed; 20705_hakurikiko's definition gloss dropped the inaccurate "all-purpose flour" claim for
薄力粉 (which is specifically low-gluten).

**translation-meaning (5, 4 entries) — all applied.** 20640_shiasatte's two example translations
corrected to state the day precisely instead of the ambiguous "within three days"; 20766_neesan's
"older girl" softened to "young woman" (matching sense 2's own gloss); 20811_choukyou's "whip the
newcomer into shape" toned down to "put the newcomer through rigorous training" (avoids implying
physical punishment not present in 調教 here); 21085_honsuji's "fundamental perspective" corrected
to "main point."

## Mechanical pass (121 changed entries)
- `normalize_notes.py --apply`: 0 rewrites (18 pre-existing non-canonical headers noted, unrelated
  to this run's edits — out of scope).
- `auto_link.py --apply --confirm-real-entries`: +127 links across 43 entries (deterministic —
  furigana-kanji, function-word, forms-map, katakana rules).
- `harvest_crossrefs.py --apply`: 11 cross-reference proposals accepted (6 direct + 5 reciprocal)
  across 10 entries, harvested from SIMILAR/RELATED WORDS sections.
- `validate.py --changed-only --ratchet`: 146/146 valid. 3 pre-existing warnings noted (not
  introduced by this run): 00319_niou and 21046_utagainai have furigana braces missing a reading
  in fields I didn't touch; 22988_shinraisuru has a part-of-speech/transitivity-tag mismatch. Logged
  as `[entry]` observations rather than fixed inline (out of scope for this run's edits).

Self-check (§4) was skipped for this batch per §A step 8 — all changed entries fall inside the
range the accuracy reviewer already checked.

## Ledger
Review $0.4884 → `pipeline/openrouter-ledger.json` spent_usd 0.5206 today.

## Cursor and queue
`polishing/tasks/cross-model-review/progress.txt` → `next: 21239`. Removed the reviewed range
(359 matching lines) from `reviews/queue.txt`.

## Index refresh
`make index`: run at wrap-up.

## Needs the curator
One flag in `reviews/needs_curator.txt`: 20361_kisaragi's internally-inconsistent 如月↔February/March
calendar note (see Adjudication above) — needs a source check, not urgent.
