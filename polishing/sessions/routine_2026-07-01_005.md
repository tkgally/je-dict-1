# Routine v2 session — 2026-07-01 #005 (wiki)

**Mode**: `wiki` (selector: "wiki: highest scheduler debt among eligible modes")

**Selector signals**: candidates 1188, seen_in_entry 3, max_entry_id 29612,
comprehensive_next 6358, cross_model_review_next 11573, unharvested_observations 8,
reviewed_entries 413, open_backlog_items 5. OpenRouter remaining $4.851.

## Pre-flight
- **§0a rescue**: `mcp__github__list_pull_requests` (open) → **0 open PRs**. Nothing to rescue.
- **§0b sweep**: no `claude/*` PRs open → nothing to close.
- **Lock**: acquired cleanly for `claude/modest-keller-29lg9d`.

## Work done (wiki mode — no entries changed)
Harvested all **4 loose observations** from `polishing/observations.md` (the
2026-06-30 routine_008 polish run + the 2026-07-01 new-entries / accuracy-review /
polish runs) and filed each into the backlog pages:

- **Cleanup Backlog P20 update 2026-07-01** — off-vocab semantic-tag cohort reaches
  the **11515–11553 band** (26/72 entries) with a granular hyphenated tag set
  (`action-*`/`religion-*`/`quality-*`/`person-occupation`/`location`/`behavior`/…)
  from a ~Feb-2026 `claude-opus-4-6` batch; all migrated 1:1 by the observing run.
  Recommends a targeted **11500–12500** `validate_tags.py --check-no-new-unknown` sweep.
- **Cleanup Backlog P11 update 2026-07-01** + **Tooling item 6 update 2026-07-01** —
  06355 どうせ (adverb) tagged `furniture` + a contradictory `formal`; both fixed on
  that entry by the polish run. Filed the cleanup pattern (wrong-category
  concrete-domain drift reaching **function words**) to P11, and the new deterministic
  detector signal (a physical-object tag on an adverb/particle/conjunction/interjection
  POS — single tag sufficient, cleaner than `concrete-noun-domain-mismatch`) as a
  proposed `function-word-concrete-domain` check under Tooling item 6.
- **Tooling item 20 update 2026-07-01 (twenty-sixth no-op)** — routine_008 priority
  `notes.txt` lane 0/6, triggered regeneration; recency-stacking diagnosis restated;
  reinforcement of the `prioritize_polishing.py` recency down-weight.
- **Tooling item 23 update 2026-07-01 (reinforcement)** — the **C05xxx–C14xxx**
  candidate block named as the worst pocket; recommends a scoped
  `clean_up_candidates_list.md` purge; <10%-signal finding reconfirmed.

Also updated the two backlog pages' "Last updated" headers, `log.md`, and `index.md`.

**Activity A (light sync)**: refreshed `project/overview.md` General-tier count
(26,600 of 29,403 total as of 2026-07-01).

**Activity H (metrics trend) not due**: only 7 new `metrics-history.jsonl` lines
(181 total) since the page's last refresh at 174 runs (threshold ≥10).

`backlog-queue.json` needed no edits — all harvested items are incremental updates
under existing open entries with unchanged status; the item-6 check is a not-yet-built
detector suggestion, not a batch-ready sweep.

## §4 self-verification
**Skipped** — no entries were created or modified this run (wiki/markdown-only).
OpenRouter untouched.

## §5 metrics
Appended one line to `pipeline/metrics-history.jsonl` (`--mode wiki --changed 0`).

## Wrap-up
- No `make build` (wiki-only run; markdown changes don't touch `docs/`).
- Observations file: 4 loose bullets removed, replaced with a 2026-07-01 harvest marker.
- Selector state already advanced in §1.
- **Next cursors** unchanged by this mode: comprehensive `next: 06358`,
  cross-model-review `next: 11573`.
