# Routine session — 2026-09-11 #002

**Mode**: polish (selector reason: "highest scheduler debt among eligible modes")
**Params**: `start_id: 7150`

## Pre-flight
No open PRs to rescue, no stranded PRs, no orphan `claude/*` branches. Local branch already
flush with `origin/main`.

## Priority lane (13 entries)
Cursor started at line 8907. Scanning found the lane almost empty: of the first 4,000 lines
scanned, only 13 entries (0.3%) had `metadata.modified` older than the 30-day skip cutoff — the
rest were touched by the 2026-09-02 mass mechanical pass. Logged as a `[pattern]` observation
(confirms and extends a 2026-09-08 observation on the same problem) rather than regenerating the
priority file early, since it is 9 days old (under the 14-day threshold).

Reviewed: 04926_panko, 06323_nawatobi, 06774_baree, 01362_okage, 00228_kijun, 02991_ashioto,
03155_mama, 06016_shonanoka, 00632_inu, 00427_yooroppa, 01365_sandaru, 04313_iruka, 05828_kankoku.

- **04313_iruka** (dolphin): fixed a cross_references entry using a non-schema `description` key
  instead of `label` (added `headword` too); removed a self-referential noentry link on the
  entry's own katakana alternate spelling (イルカ), which was linking to nothing useful.
- All other 12 entries were already correct and complete on inspection — no changes needed.
- Candidates added for words seen with `noentry` markers in notes: 風 (ふう, "-style" suffix),
  ハチ公 (はちこう, Shibuya statue), 鼠海豚 (ねずみいるか, porpoise), 大縄 (おおなわ, group jump
  rope), and 重圧 (じゅうあつ, heavy pressure — no link marker at all in 07163_puresshaa's notes,
  caught by inspection).

## Frontier lane (26 entries, 07150–07175)
- **07150_tagaini** (互いに): fixed `politeness: "polite"` → `"plain"` — an adverb tagging outlier
  (141 of 149 `formal`-tagged adverbs use `plain`; the entry's own note says おたがいに, not 互いに,
  is the more polite form).
- **07155_uchiawase** (打ち合わせ, verb-suru): added missing `transitivity: "intransitive"` (the
  を in 打ち合わせをする marks the headword itself, matching the convention set by 00710_kaigi).
- **07167_ketsui** (決意, verb-suru): added missing `transitivity: "transitive"` (留学を決意する —
  the を-marked object is external to the headword).
- Remaining 23 entries (07151–07154, 07156–07166, 07168–07175) were already correct — no changes.
- Logged a `[tooling]` observation: missing `transitivity` on verb-suru entries looks systemic
  (spot-checked 05323_renshuu, also missing) — candidate for a systemic-fix detector.

## Self-check
`review_accuracy.py --ids 04313,07150,07155,07167 --budget 0.40`: 0 issues on all 4 entries (2
filtered as unquoted-register noise). `review_links.py` on the same IDs: 2 links judged, 0
flagged. Self-check clean.

## Candidates added
C23391 風, C23392 ハチ公, C23393 鼠海豚, C23394 大縄, C23395 重圧 (5 total).

## Ledger
Self-check accuracy review + link review: $0.0019 → `pipeline/openrouter-ledger.json`
spent_usd 0.0204 today (cumulative with the earlier new-entries run).

## Cursors
- `polishing/tasks/comprehensive/priority-cursor.txt`: `line: 12906` (was 8907)
- `polishing/tasks/comprehensive/progress.txt`: `next: 07176` (was 07150)

## Observations logged
Two `[pattern]`/`[tooling]` entries in `polishing/observations.md` (priority-lane staleness
detail, missing-transitivity systemic gap).

## Index refresh
`make index`: 30,663/30,663 entries valid dictionary-wide (pre-existing warnings unrelated to
this run).

## PR and CI
PR #3273 opened. The `validate` check run was still `in_progress` after all 16 polls (roughly
45+ minutes) — unusually long for this check. Left the PR open unmerged per routine2.md §7; the
next Routine run's pre-flight (§0a) will merge it if CI has gone green by then, or it will
surface as a stranded PR for the sweep to handle otherwise.

## Nothing needs the curator this run, beyond PR #3273 still awaiting CI.
