# Routine v2 session — 2026-06-10 (run 003)

**Mode:** `accuracy-review` (forced via `--force-mode accuracy-review` for manual v2 test; `state_persisted: false`, so `pipeline/routine-state.json` was NOT advanced this run).
**Selector reason:** "accuracy-review: forced via --force-mode" — phase `furigana`, `start_id: 451`, `openrouter_session_budget_usd: 1.5`. Signals: cross_model_review_next 451, reviewed_entries 164, remaining daily $4.9881.
**Range worked:** entries **00451–00650** (199 entries present), capped to ~200 per the manual-test bound (well inside the $1.50 session budget).

## Pre-flight (§0)
- §0a rescue: `list_pull_requests` returned no open PRs — nothing to rescue.
- §0b: `sweep-stranded-prs.py` closed 0 stranded PRs; `routine_lock.py acquire` succeeded (held for the run, released at end).

## §A accuracy-review work

### Furigana (§A step 2 — `review_runner.py`)
- **Screening** (gemini-2.5-flash), range 451–650: 199 screened, **45 flagged**, est $0.0226.
- **Deep** (gpt-4.1 + gemini-2.5-pro): first invoked with `--range 451 650`, which (see observation) deep-reviews the *whole* range rather than just the 45 flagged. Stopped it and re-ran with `--ids <45 flagged>` to match §A intent. Result: 45 reviewed, **4 flagged**, est $0.4461. (gemini-2.5-pro returned bare-array JSON on a few entries → "Failed to parse response"; gpt-4.1 covered them, pass exited 0. Documented as a `[tooling]` observation per the crash-resilience rule; not the null-response crash.)
- **Adjudication of the 4 deep flags** (all the documented compound/rendaku/noun-reading false-positive family):
  - 00595 話→はなし — REJECT (correct standalone noun reading of 話, links to 01496_hanashi; not okurigana).
  - 00607 会社→がいしゃ — REJECT (correct rendaku inside 株式会社/有限会社).
  - 00616 先行→さきゆ — REJECT (correct compound reading 先行き=さきゆき).
  - 00617 好→ず in 甘いもの好き — **FLAG** (both gpt-4.1 and gemini-2.5-pro agree 好 should be す; but the 〜好き fondness suffix commonly rendaku's to ずき (cf. 物好き ものずき). Genuine contested reading → `reviews/needs_curator.txt`).

### Glosses / translations / tags (§A step 3 — `review_accuracy.py`)
- Range 451–650 (gemini-2.5-flash): 199 reviewed, **173 flagged**, **413 issues** (109 error-sev, 304 warn), est $0.0679.
- **6 APPLIED** (clear, safe single-field fixes; `modified` bumped on each):
  - 00460 修飾 semantic tag `education` → `grammatical` (education wrong; grammar/ornamentation term).
  - 00639 傘 semantic tag `clothing` → `tool` (umbrella is not clothing).
  - 00637 かばん semantic tag `clothing` → `tool` (bag is not clothing).
  - 00565 とる ex[6] "Can you catch the bug?" → "Can you get rid of the bug?" (虫を取る = remove, not catch).
  - 00514 早い ex[9] "Tomorrow is early…" → "I have an early start tomorrow…" (unnatural literal).
  - 00602 もう ex[12] "wait a little more" → "wait a little longer" (natural collocation).
- **3 FLAGGED** to curator (00472 仕様 gloss/sense-structure: "way, method" may belong only to the 仕様がない idiom, not standalone 仕様=specifications).
- **404 REJECTED**: verbose-gloss / add-a-sense suggestions (restructuring, out of accuracy-review scope, and against the dictionary's concise basic-tier house style); stylistic translation nits ("natural rendering, but consider…"); schema-default politeness/formality tags on nouns; kinship `humble` tags on 母/父/兄/姉 (linguistically correct for in-group reference terms); model misreads of 何か/何も/どうして/どうしても compound examples as headword mismatches.

Every one of the 413 accuracy flags + 4 furigana flags was logged to `reviews/decisions.jsonl` (417 lines this run): accuracy 6 apply / 404 reject / 3 flag; furigana 0 apply / 3 reject / 1 flag.

## §4 self-verification
Not run as a separate pass: all 6 modified entries (00460, 00514, 00565, 00602, 00637, 00639) are inside the 451–650 range that §A already cross-model-reviewed this run — per §4 step 3, IDs already covered by §A are skipped (the review itself was the check). Re-validated the 6 edits with `validate.py` (clean).

## §3 always-on capture
- Candidates added: none (range is basic-tier; examples already carry inline links / have entries).
- Observations appended to `polishing/observations.md` (3): `[tooling]` deep-pass `--range` reviews whole range not flagged-only; `[tooling]` gemini-2.5-pro bare-array parse failures; `[pattern]` accuracy reviewer-flag precision on basic entries ~1.5% (6/413) — tags dimension was the only high-signal lane.

## §5 metrics
Appended to `pipeline/metrics-history.jsonl`: mode accuracy-review, entries_changed 6, applied 6, rejected 407, flagged 4, entries_total 28873, candidates 1464, review_queue_len 19444, openrouter_spent_today 0.5485.

## Spend (OpenRouter ledger)
Three calls recorded this run: furigana-screening $0.0226 (199), accuracy $0.0679 (199), furigana-deep $0.4461 (45) = **$0.5366 this run**. Ledger total today **$0.5485** (prior $0.0119), well under the $5 daily cap and the $1.50 session budget.

## Queue maintenance (§A step 7)
`reviews/queue.txt`: **19627 lines before → 19444 after** (removed the 183 in-range entries that were queued). CI will re-add the 6 entries I edited for a future second look, by design.

## Cursors / state
- `polishing/tasks/cross-model-review/progress.txt` → `next: 00651`.
- `routine-state.json` intentionally NOT advanced (forced-mode manual test).

## Next
Wrap-up: single `make build`, commit/push, PR `routine(accuracy-review): …`, wait for CI, squash-merge, release lock.
