# Routine session — 2026-09-11 #003

**Mode**: systemic-fix (selector reason: "systemic-fix: highest scheduler debt among eligible modes")
**Backlog item**: `inline-link-homophone-target` — Cleanup Backlog Priority 55, "Inline links that
resolve to a homophone of the intended word (23 instances / ~22 entries)"

## Pre-flight
Rescued PR #3273 (`routine(polish)`, head `claude/trusting-mendel-okut4u`): CI (`validate`) had
gone green since the previous run left it pending, no human comment or review, so merged it
(squash). No other open PRs, no stranded PRs, no orphan `claude/*` branches. Merged `origin/main`
into this session's branch.

## Detect
The item's `detect` field was marked `proposed:` — no detector script exists yet. Rather than
build one from a generic heuristic, I used the confirmed, hand-verified 23-occurrence list already
recorded in `planning/wiki/topics/inline-link-integrity.md` (measured 2026-08-13) as the queue,
and re-verified each occurrence still present in the current entries (all 23 were; nothing had
been fixed by an intervening run). Note: `check_link_baseform.py` itself reports 0 unfiltered
disagreements dictionary-wide right now — its `resolve()` falls back to a reading-only match for
single-kanji base forms, which happens to include several of these wrong targets among the
homophone candidates it returns, so it silently "agrees." That is a detector gap worth a future
tooling note, not something I fixed this run (out of scope, and its own allowlist mechanism isn't
the right place for the fix).

## Fixes (22 entries, 23 occurrences)
- **12 repointed** to the entry the base form actually names, discovered via
  `build/word_id_lookup.json`: 深く→深い (01211, matching the 早く→早い adverb convention), 感→〜感
  (28578, ×3 in 00445/02494/02496 — the suru-noun-suffix entry, not the coincidental reading match
  観/缶), 系→〜系 (28466, in 03445's 出会い系), 純→純〜 (28361, in 04875's 純損失).
- **5 consolidated**: a compound had been split across two or three separate link markers when it
  has its own single entry — お好み焼き→10252 (×2 occurrences in 04472), 吸い物→04990 (in 04475),
  終身雇用→13852 (×2 occurrences in 04840), 腑に落ちない→25853 (in 05984).
- **6 unlinked, candidate queued** (no entry exists for the base form): 用地 (00365), 専任 (00451),
  五時 (00504), 詩的 and 詩集 (both in 02139), 詐称 (02628), 天賦 (02772), 進水 (04484), 書架
  (04489), 科す (04955). Added all ten as candidates (C23396–C23405).
- **1 unlinked, no candidate**: 温 in 04479's kanji-component breakdown ("温(warm)+帯(zone)") — a
  bound morpheme gloss, not an independent word.

## Verification
- `build/validate.py --id` on all 20 changed entries: valid.
- `build/check_link_baseform.py --no-allowlist`: 0 DISAGREE cases dictionary-wide (was 98 before
  this run, all 98 already covered by the existing allowlist as separate same-word spelling
  variants unrelated to this batch).
- `build/auto_link.py --ids <20> --apply --confirm-real-entries`: 0 new links; the 56 unlinked
  tokens it reports are exactly the 11 no-entry base forms I unlinked by hand, confirming the
  linker agrees none of them currently resolve.
- `build/harvest_crossrefs.py --ids <20> --apply`: 0 changes.
- `build/normalize_notes.py --ids <20> --apply`: 0 changes (the unknown headers it lists are
  pre-existing custom section titles, out of scope for this run).

## Self-check
`review_accuracy.py --ids <20> --budget 0.40`: 2 issues surfaced, both unrelated to the link
fixes (pre-existing content in fields I didn't touch):
- 00181_kansha (notes-fact): flagged the ASPECT note's ている-vs-plain-form explanation as
  contradictory. **Rejected** — it distinguishes stative (ている) from performative (plain-form
  "I hereby thank you") use; not a factual error.
- 02496_manzoku (gloss-meaning): flagged part_of_speech ("na-adjective") vs. gloss ("satisfaction",
  a noun) as mismatched. **Flagged to curator** — genuine inconsistency (the entry's own notes
  call it noun/na-adjective/suru-verb, and comparable words like 緊張 and 心配 are tagged plain
  "noun"), but changing POS touches the conjugation table and is a convention call, not an inline
  fix for a self-check pass.

`review_links.py --ids <20> --skip-decided --budget 0.10 --src self-check`: 33 links judged
across 18 bases, 0 flagged.

## Candidates added
C23396 用地, C23397 専任, C23398 五時, C23399 詩的, C23400 詩集, C23401 詐称, C23402 天賦, C23403
進水, C23404 書架, C23405 科す (10 total).

## Ledger
Self-check accuracy review $0.0098 + self-check link review $0.002 →
`pipeline/openrouter-ledger.json` spent_usd 0.0322 today (cumulative with the earlier runs).

## Backlog item status
`inline-link-homophone-target` marked `resolved` in `backlog-queue.json` (scope_estimate 0) and
Priority 55 in `cleanup-backlog.md` updated to RESOLVED with a summary of the fix.

## Index refresh
`make index`: 30,663/30,663 entries valid dictionary-wide.

## Needs the curator
One flag in `reviews/needs_curator.txt`: 02496_manzoku's part_of_speech/gloss mismatch (see
Self-check above) — a convention call, not urgent.
