# Routine run — 2026-08-16 (002)

**Mode:** `systemic-fix`
**Selector reason:** "systemic-fix: highest scheduler debt among eligible modes"
**Selector signals:** candidates 168, seen-in-entry 22, max entry ID 30673, comprehensive next 06953,
cross-model-review next 12001, unharvested observations 17, open backlog items 37.
**Backlog item:** `inline-link-stale-noentry` (Cleanup Backlog P35) — stale `noentry` inline links
whose word now has an entry.

## Pre-flight

- §0a rescue: no open PRs at all, so nothing to rescue.
- §0b stranded-PR sweep: nothing to sweep (open-PR list empty).
- §0b orphan-branch sweep: `list_branches` returned only `main`. Zero orphans.
- Lock acquired for `claude/determined-rubin-24p4l2`.

## Range worked

Entries **04459–04999** — the next unswept band. 177 A1/A2 (mechanical) pairs, 212 marker
instances, 120 entries. Everything below 04459 was swept by the 2026-08-09 → 2026-08-14 runs.

Each pair was judged against a ±60-character context window around the marker, with the candidate
target entry's headword, reading, part of speech and full gloss list alongside; marginal cases had
the target entry's notes opened directly.

## Result: 176 applied, 1 rejected

- **176 pairs / 211 instances across 119 entries** had `：noentry⟧` replaced with the resolving
  entry ID. `modified` updated on every entry touched.
- **1 rejection — `04562` 二重⟦ロック⟧ → `08116_rokku`.** The entry is about home security and
  means "double lock"; the only ロック entry is glossed "rock (music); rock 'n' roll" and its own
  notes say *"ロック as 'lock' (to lock something) is a different word with the same reading."*
  Left as `noentry`. This is the documented polysemous-katakana family (フライ, パン), and the fact
  that the target entry declares the split in prose is a mechanically detectable signal — logged as
  a `[pattern]` observation and written into the backlog item as a candidate new detector class.

Marginal applies resolved by opening the target entry rather than trusting its gloss (the
2026-08-14 "notes, not gloss" rule): 連立⟦方程式⟧ → `14492_renritsu`; kindergarten 年少/年長 →
`29613`/`29601`; washing-machine おしゃれ着⟦コース⟧ → `10864_koosu` sense 2; bare-kanji-surface
markers {購読|こうどく}⟦{料|りょう}⟧ → `03797_ryoukin` and ⟦{購|こう}⟧ → `03057_kounyuu`.

Detector after the sweep: **602 mechanical pairs / 685 instances remain** (was 778/896).

## §4 self-check

`build/review_accuracy.py --ids <the 119 changed entries> --budget 0.60`, model
`google/gemini-2.5-flash`, est. **$0.0525**, logged to the ledger with `phase: "self-check"`
(day total now $0.0592 of the $5.00 cap).

**Not clean: 53 of 119 entries flagged (45%), 57 issues — 54 `tags`, 3 `gloss`.** A 45% flag rate
is above the ~20% reviewer-noise threshold, but unusually the flags were not all noise.

- **19 entries fixed (23 issues applied):**
  - Not-in-list semantic tags (mandatory per the 2026-06-11 tag policy): `04796` 関数
    `mathematics`+`computing` → `science`+`technology`; `04800` 内閣 `government` → dropped
    (`politics` already present); `04856` 金利 `economy` → `finance`.
  - Flatly wrong in-list semantic tags — physical categories on unrelated headwords: `04473`
    菜箸 `building` dropped; `04474` 捩じる `tool` → `action`; `04489` 本棚
    `communication`+`tool` dropped; `04495` 縁側 `time-general` → `building`; `04615` 跳ね上がる
    `transportation`+`weather` → `movement`; `04650` 鬱病 `emotion` → `health`; `04651` 関節痛
    `emotion` → `health`; `04659` 組み合わせる `furniture`+`tool` → `action`; `04677` コンロ
    `electronics` → `cooking`; `04682` ストッキング `work` dropped; `04692` 月給 `time-general`
    dropped; `04803` 消費税 `work` → `finance`.
  - Formality mislabels on everyday words: `04489` 本棚, `04517` ブラウス (both `formal` →
    `neutral`), `04973` 電子レンジ (`informal` → `neutral`).
  - Glosses: `04559` 退職 "resignation" → "retirement; resignation; leaving a job" (the entry's
    own definitions already covered the broader sense); `04970` 佃煮 bare romaji → "tsukudani;
    small foods simmered in soy sauce and mirin".
- **34 issues rejected**, mostly one family: "the in-list tag `general` is too broad, use
  <narrower in-list tag>". Per the semantic-tag policy, narrowness substitutions between two
  valid tags are rejected. Also rejected `04923` 片栗粉 — the model wanted "dogtooth violet
  starch"; modern katakuriko is potato starch and the entry's notes already explain the history.
- **0 escalated to the curator.**

Every decision logged to `reviews/decisions.jsonl` (individual lines for applies, aggregated
lines with `n` counts for the two rejection families).

## Always-on capture

- Candidates added: none — this mode reads existing markers rather than new Japanese text, and
  every base form encountered already had an entry by construction.
- Observations appended to `polishing/observations.md`: three.
  - `[pattern]` the ロック rejection and the detectable "different word with the same reading"
    prose signal.
  - `[pattern]` band 04459–04999 looks contaminated with P11 semantic-tag drift (17 genuinely
    wrong tags in 119 entries) — a good next range for `prompts/fix_semantic_tag_drift.md`.
  - `[entry]` `04651` has a malformed furigana wrapper `{痛|いたみ}` (should be `{痛|いた}み`)
    in its notes; class R can't see it because the base reading still matches.

## State updated

- `planning/wiki/ideas/backlog-queue.json` — `inline-link-stale-noentry`: scope 751 → **602**,
  `scope_measured_at` 2026-08-16, cursor now "everything below entry 05000 swept", filter and
  notes extended with this band.
- `planning/wiki/ideas/cleanup-backlog.md` — Priority 35 gains a **2026-08-16 systemic-fix run**
  section (the rejection and the proposed detector class, the marginal applies, the families
  verified as groups, running false-positive rate **~0.2%** across 1,070 hand-verified pairs).
- `pipeline/metrics-history.jsonl` — one line appended (119 entries changed, 19 applied,
  35 rejected).
- `pipeline/openrouter-ledger.json` — self-check spend recorded.
- No polishing cursor to advance (this is not the `polish` mode); the comprehensive frontier
  stays at 06953.

## Next

Resume P35 at the lowest remaining entry_id in `check_stale_noentry.py --mechanical --json`
(currently just above 05000).
