# Routine session — 2026-09-07 001

**Mode**: accuracy-review (selector reason: "accuracy-review: highest scheduler debt among eligible modes")

## Pre-flight

- Rescued PR #3265 (`routine(polish)`, green, mergeable, no human comments) and squash-merged it.
- Merged `origin/main` into this branch. No other stranded PRs; no orphan `claude/*` branches (only `main` and the just-merged head existed).

## Params

- `start_id`: 18239
- `openrouter_session_budget_usd`: 2.5 (daily cap $5.00, $0 spent before this run)

## Review

Ran `build/review_accuracy.py --range 18239 19238 --budget 2.5` (all four dimensions, including notes).

- Reviewed: 998 entries (2 IDs in the range don't exist / are gaps)
- Flagged: 48 entries, 49 issues total
- Cost: $0.4807
- Filtered by family before reaching me: breadth=10, register-noquote=3, style=4

## Adjudication (§C, all logged to `reviews/decisions.jsonl`)

**Applied (22 entries fixed):**
- 7 `offvocab` tag migrations (deterministic): accommodation→daily-life, behavior removed (personality already present), sound removed (music already present), situation→abstract, sport→sports, transport→transportation, people→person.
- 7 `wrong-category` tag fixes verified by hand: removed `building` from 生垣 (hedge), `body-part`→`health` on 発赤 (redness) and 体外 (outside the body), removed `nature` from 再利用 (recycling), added `movement` alongside `furniture` on 流し (its second sense is taxi cruising for customers, not a fixture), `education`→`language` on 借用語 (loanword), removed `weather` from 夕闇 (dusk).
- 3 genuine formality/register contradictions: 競売 (the tagged reading きょうばい is called "everyday" in its own notes; the "formal" tag belonged to the legal reading けいばい — changed to neutral), 充てる and 有力者 (notes explicitly say "slightly formal" / "a formal word" while the tag said neutral — changed to formal).
- 1 notes-fact fix: 未納 vs 滞納 — softened an overstated claim that 滞納 implies "willful" non-payment; it just means overdue/in arrears.
- 4 gloss fixes: 義妹 (reworded "stepsister (younger)" to "younger stepsister" for clarity), 目配せ (removed misleading "wink" — the word means any meaningful glance, not specifically a wink), 排気 (top-level gloss was missing "exhaust gas", now matches the definition), 無農薬 (removed a misleading "organic" gloss for a word that just means pesticide-free).

**Rejected (27 issues, 26 entries):** the largest group was register/formality flags where the entry's own REGISTER note already agreed with its tag (e.g. 渋面 "formal, literary", 断念 "Formal. Common in news...", 受取人 "Formal. Used on forms...") — the reviewer flagged these as contradictions when they were not. A second recurring pattern: the reviewer misread the dictionary's `⟦surface→base：id⟧` inline-link markup in grammar-pattern notes (～やすい, ～終わる, ～直す, 弦, 遺言) and claimed conjugation examples or readings were wrong when the underlying text was already correct. Full list and reasoning for each of the 27 in `reviews/decisions.jsonl` (ts 2026-09-07T00:37:*).

**Flagged to curator:** none.

## Mechanical pass (§3)

- `normalize_notes.py`: 0 rewrites (2 unrelated pre-existing unknown headers noted, not touched).
- `auto_link.py --confirm-real-entries`: +10 links across 8 of the 22 entries.
- `harvest_crossrefs.py`: 0 new cross-references (all SIMILAR WORDS terms already linked or unresolvable).
- `validate.py --changed-only`: 48/48 valid. `make index`: 30604/30604 valid (pre-existing warnings unrelated to this run).

## Self-check (§4)

Skipped per §A step 8 — all 22 changed entries fall inside the just-reviewed range (18239–19238), so the accuracy review itself was the check.

## Ledger / cursors

- OpenRouter spend today: $0.4807 of $5.00 cap.
- `polishing/tasks/cross-model-review/progress.txt` → `next: 19239`.
- `reviews/queue.txt`: removed the 18239–19238 range (11,950 lines remain).

## Candidates / observations

None logged this run.
