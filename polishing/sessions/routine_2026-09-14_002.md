# Routine session 2026-09-14 #002

**Mode**: accuracy-review (selector reason: "accuracy-review: highest scheduler debt among eligible modes")

## Pre-flight

- Rescued PR #3291 (routine polish, priority lane + frontier 07355-07370): all checks green,
  mergeable, no human comments → squash-merged.
- PR #3290 (routine polish, priority 1953-8913, frontier 07340-07354): `validate` check still
  failing (non-canonical note header on 07345_soufu, per its own body). Highest touched entry ID
  is 21979 (well above the comprehensive-polish cursor of 07371), so it is not stale by the sweep
  rule. Left open — needs the one-line header fix PR #3291 already flagged.
- No orphan `claude/*` branches other than #3290's own head.
- Local branch was even with `origin/main` after the rescue merge and a follow-up automated
  commit ("Queue entries for multi-model review"); fast-forwarded to catch up.

## Review

Ran `build/review_accuracy.py --range 25239 26238 --budget 1.5` (999 entries, the cross-model
review cursor's full width). 52 entries flagged (398 raw issue lines once duplicate
gloss/definitions locations are counted), cost $0.4899. Adjudicated every flag individually:

- **44 issues applied** across 39 entries: 12 off-vocabulary semantic tags migrated to the closed
  list (`time`→`time-general`, `transport`→`transportation`, `social`→`society`, and five tags
  dropped outright where a second valid tag already covered the entry), 8 wrong-category semantic
  tags corrected, 6 formality/politeness tags brought into line with their own REGISTER notes
  (25559_kinmitsuna, 25564_shokon, 25575_saikonsha, 25578_enkyokuni formality→formal;
  25581_gokigen, 25973_yoroshikereba politeness→polite), 7 gloss fixes (25707_baisoku dropped a
  redundant "fast speed"; 25770_enbifuku's top-level gloss no longer contradicts its own
  definition; 25793_ryakusou and 25812_inago had misleading gloss words trimmed; 26011_kajiya's
  "smithy" swapped for the clearer "blacksmith's shop"), 11 notes-fact corrections (a mathematical
  wording fix in 25644_heihoukon, an overclaim about 横入り being children's speech removed, the
  預言する/予言する contrast reworded per the reviewer's own suggestion, a wrong "alternate kanji
  spelling" claim about 魔方陣 fixed, a 交通費/交通代 suffix mixup in 26161_buhindai, and others),
  and 3 translation fixes (a baseball strike/strikeout mixup, a passive-voice mistranslation, a
  clarified construction-site sentence).
- **11 issues rejected** as model misreadings or reviewer artifacts: two register flags where the
  entry was already correctly tagged or well-supported by its own notes (25403_sashitsukaenai,
  25432_tayasuku, 26074_koini), a tournament-round-order flag where the entry's list was already
  in the right order (25649_junjunkesshou), a "foreign legion" gloss flag that's a standard
  translation (25700_gaijin), a postal-rule flag where the entry's claim about 配達証明 requiring
  書留 is correct (25813_haitatsushoumei), a cultural-nuance flag on 無冠の帝王 where the existing
  framing is the standard one (25997_mukan), and three cases (25748_maeyaku, 25749_atoyaku,
  25796_shusseuo, 26049_marukakko) where the entry's actual notes are complete and correct and the
  flag appears to be an artifact of the reviewer's own link-stripping/markup handling garbling its
  view of the text.
- **1 flagged to the curator**: 26031_kakujou (headword 格上, reading かくじょう) looks like it may
  be a fabricated or mixed-up entry — its gloss and all three examples only ever use 格上げ
  (かくあげ, "to upgrade"), never plain 格上 by itself, and the dictionary already has a correct,
  separate entry for 格上 read かくうえ ("higher-ranked") at 07405_kakuue. Fixing this would mean
  changing the headword and reading, which is outside what a routine run should do unilaterally.

One entry (25245_secchakusuru, 接着する "to bond/adhere") got a different fix than the flags
suggested: the reviewer's gloss-trim and notes-fact flags both stemmed from the entry mixing
transitive examples (部品を接着する) and intransitive ones (タイルが接着していない) while tagged
`transitivity: "transitive"`. Retagged it `"both"` instead, which resolves both flags at once
without narrowing the gloss.

## Mechanical pass

`normalize_notes.py`: 0 rewrites (one pre-existing non-canonical header noted on
25973_yoroshikereba, not introduced by this run). `auto_link.py --confirm-real-entries`: +54
links across 16 of the 40 changed entries. `harvest_crossrefs.py`: +2 reciprocal cross-references
(26104_sukareru ↔ 01811_aisuru). `validate.py --changed-only`: 41/41 valid.

## Self-check

Per §A step 8, entries inside the reviewed range (25239-26238) skip the separate self-check since
the accuracy review already covered them. The one neighbour opened outside that range
(01811_aisuru, pulled in by cross-reference harvesting) was checked with `review_accuracy.py`:
clean, 0 issues.

`review_links.py --skip-decided` checked all 58 link occurrences across the 41 changed entries: 1
flagged (どちらも in 25897_docchimo's notes, "other" — the model misread which word the note was
calling informal). On inspection the linked surface text is literally どちらも and the link target
is correct; logged as a `keep` in `reviews/link_decisions.jsonl`.

## Cost

$0.4899 (accuracy review) + $0.0025 (self-check: 1 accuracy call + 4 link-review calls) = $0.5024
against a $5.00 daily cap.

## Candidates / observations

None logged this run.

## Cursors

- `polishing/tasks/cross-model-review/progress.txt`: `next: 25239` → `next: 26239`.
- `reviews/queue.txt`: removed 409 lines in the reviewed range (25239-26238).
