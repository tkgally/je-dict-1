# Routine session — 2026-08-09 (008)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Signals**: candidates 989 (18 seen-in-entry), max entry 30525, comprehensive next 6850,
cross-model-review next 29295, unharvested observations 8, open backlog items 29
**Budget**: session cap $2.50; daily remaining at start $4.73

## Range worked

- **Accuracy pass** (`review_accuracy.py`, gloss/translation/tags): entries
  **29295–29743** — **449 entries reviewed**, 26 flagged, 29 issues raised
  (21 tags, 6 gloss, 2 translation; all `error` severity — the pass produced no
  `warn` issues in this range).
- **Furigana screening** (`review_runner.py --pass screening`): entries
  **29295–29322** — 28 entries screened, 1 flagged. Stopped early (see below).
- Both passes were stopped at the §6 context checkpoint rather than at the
  nominal range end.

## Result: zero applicable flags — 29 rejected, 0 applied, 0 flagged to curator

**No entry files were changed this run.** That is the finding, not a shortfall:
every flag was either invalid by the §A tag policy or a re-litigation of a
decision the project had already made.

### The headline measurement

**26 of 26** flagged entries already carried a decision in
`reviews/decisions.jsonl` on the **same dimension**, and **24 of those were
prior REJECTs** with reasoned notes. Examples:

| Entry | Today's flag | Prior decision |
|---|---|---|
| 29345 {能動\|のうどう} | sole-`general` → `grammatical, cognition` | 2026-06-19 **reject** — "general ok fallback; noudouteki is behavioral not grammatical" |
| 29469 {刻印\|こくいん} | sole-`general` → `action` | 2026-06-24 **reject** — "noun-primary; general fallback correct vs action" |
| 29616 {楕円形\|だえんけい} | sole-`general` → `size` | 2026-07-01 **reject** — "size wrong for shape" |
| 29716 / 29718 / 29719 | sole-`general` → `building` | 2026-07-06 **reject** ×3 — "sibling consistency" |
| 29622 {神曲\|しんきょく} | formality `informal` → `slang` | 2026-07-01 **reject** — "slang not valid formality enum" |
| 29647 コンペティション | `leisure` → `action` | 2026-07-03 **reject** — in-list breadth nit |
| 29677 ウォレット | `finance` → `technology` | 2026-07-04 **reject** — "finance justified (holds money)" |

Each of these was re-verified against the entry's **current** content before
being rejected again; the prior reasoning still holds in every case.

### Two reviewer oscillations (flags that would have reverted earlier fixes)

- **29451 塁打** — a 2026-06-24 run APPLIED a gloss change reasoning "ruida is
  total bases, not generic base hit". Today's reviewer flagged the *result* as
  an error and asked to revert to "bases gained by a hit; a base hit".
- **29634 ネガティブ** — a 2026-07-02 run APPLIED "removed technology; core
  sense is attitude". Today's reviewer asked to **add `technology` back** for
  the photographic-negative sense.

Applying either would have undone a deliberate correction, and a later sweep
would presumably flip it back again. Both rejected.

### Invalid-by-construction tag suggestions (6)

Six of the 21 tag flags proposed tags that are **not in `VALID_SEMANTIC`**:
`mathematics` (29329, 29468), `astronomy` (29558, 29564), `literature` (29498),
`manufacturing` (29485). One more (29622) proposed `slang`, which is not in the
formality enum, and one (29378) asserted that `informal` "is not a valid
formality tag" — it is. The remainder were in-list "too narrow / too broad"
substitutions, which §A rejects by definition.

All 30 adjudications are logged to `reviews/decisions.jsonl` (29 individual
accuracy lines + 1 aggregated furigana-family line).

## Furigana screening — stopped early, deep pass skipped

The screener managed 28 entries in ~40 minutes (~1.7 min/entry) while the
accuracy pass covered ~200 in the same window, and the accuracy pass visibly
sped up the moment the screener was killed — the two contend for the same rate
limit. Its single flag (29300 {多色刷\|たしょくず}り, 刷 read as す vs ず) is the
documented rendaku-in-compounds false-positive family from
`reviews/calibration_report.md`. Per §A step 2's known-noise shortcut it was
bulk-rejected with one aggregated §C line and the deep pass was **skipped**.

Termination was verified by PID per the 2026-08-09 (003) process note —
`pkill -f` again failed to stop it; `kill -9 <pid>` did.

## §4 self-check — not applicable

No entries were created or modified, so there was nothing to self-verify.
Est. cost $0.00.

## Deterministic cross-check

A scripted scan of 29295–29849 against `VALID_SEMANTIC` found **zero**
off-vocabulary semantic tags — the migration work done in the 2026-08-09 (003)
run covered the overlapping block, and the rest of this range was already
clean. Dictionary-wide the class still stands at 2,436 uses
(`check_tag_drift.py`), unchanged by this run.

## OpenRouter spend

| Phase | Entries | Est. |
|---|---|---|
| accuracy review | 449 | $0.1810 |
| furigana screening | 28 | $0.0028 |
| self-check | 0 | $0.0000 |
| **total this run** | | **$0.1838** |

Ledger `spent_usd` for 2026-08-09: **$0.4492** (cap $5.00).

## Observations logged

Four entries appended to `polishing/observations.md`:

- `[tooling]` **The accuracy sweep is re-litigating settled decisions** — 26/26
  flagged entries had a prior same-dimension decision, 24 of them REJECTs; net
  applicable flags zero. Proposed fix: have `review_accuracy.py` read
  `decisions.jsonl` and either suppress `(entry, dimension)` pairs with a prior
  REJECT whose entry has not been modified since, or feed the prior decision
  notes into the reviewer prompt.
- `[tooling]` **Reviewer oscillation** (29451, 29634) — the strongest argument
  for the prompt-side variant of the above.
- `[pattern]` **Tag flags keep proposing off-vocabulary tags** — 6 of 21;
  constraining the suggestion field to the `VALID_SEMANTIC` enum, or
  post-filtering off-list suggestions before adjudication, would remove roughly
  half the tag-flag volume for free.
- `[tooling]` **Furigana screening is far slower than the accuracy pass** and
  contends with it for rate limit; running them sequentially, or dropping
  screening from accuracy-focused runs, would buy several hundred more entries
  of coverage per run.

## Candidates added

None — no un-entered Japanese words were encountered in the reviewed material.

## Build

**Skipped** per §6 — no entry files or build artifacts changed, so `docs/`
would be byte-identical.

## Queue and cursors

- `reviews/queue.txt`: 10,284 → **9,834** (removed 29295–29743).
- `polishing/tasks/cross-model-review/progress.txt`: **next: 29744**
- Metrics line appended to `pipeline/metrics-history.jsonl`
  (`flags_applied: 0`, `flags_rejected: 30`).

## Pre-flight

No open PRs, no orphan `claude/*` branches — both sweeps were no-ops. Nothing
to rescue.

## Next run

Resume the accuracy-review sweep at **29744**. Before spending another sweep on
this band, the `[tooling]` suppression fix above is the highest-value change
available: on this run it would have turned $0.18 and the bulk of the
adjudication context into a few seconds of deterministic filtering. The
dictionary-wide off-vocab semantic-tag backlog (2,436 uses) remains the
strongest `systemic-fix` target.
