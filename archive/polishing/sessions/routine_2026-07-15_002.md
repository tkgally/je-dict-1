# Routine session — 2026-07-15 (002)

**Mode:** accuracy-review
**Selector reason:** "accuracy-review: highest scheduler debt among eligible modes"
**Range worked:** 15100–15153 (54 entries)
**Params:** start_id 15100, phase furigana, openrouter_session_budget_usd 2.50

## What ran

The proxy was slow today (~8 furigana-screening entries/min), so the range was
shrunk from the ~500-entry target to the 54 entries that completed cleanly
(15100–15153), per §A ("shrink the range if step 4 is running long").

### Furigana pass (§A step 2)
- `review_runner.py --pass screening --range 15100 15600` (stopped at 15153).
- 6 entries flagged: 15111, 15114, 15130, 15135, 15142, 15148.
- All 6 verified by direct inspection to be **documented false positives**:
  compound-reading / okurigana splits the screening model mis-parsed
  (`{不注意|ふちゅうい}`, `{悪影響|あくえいきょう}`, `{郷土料理|きょうどりょうり}`,
  `{打|う}つ`, `{免許証|めんきょしょう}`), and 15148's `{一文|いちもん}` is a reading
  the entry's own disambiguation note deliberately discusses.
- Deep pass **skipped** (known-noise shortcut). One aggregated §C reject line (n=6).

### Accuracy pass (§A step 3)
- `review_accuracy.py --range 15100 15153` → 54 reviewed, 30 flagged, ~$0.0236.
- 32 tag issues + 1 translation issue.
- **Tags:** 30 entries carried off-vocabulary semantic tags (`people`, `body`,
  `degree`, `sensation`, `manner`, `behavior`, `place`, `state`, `substance`,
  `classification`, `housing`, and coined compounds like
  `administrative-procedure`, `achievement-success`, `social-behavior`,
  `movement-direction`, `language-writing`, `discourse-filler`, etc.). Per §A's
  semantic-tag policy (not-in-list = correct-by-definition flag), all 30 were
  **APPLIED**, migrating each off-vocab tag to the best in-list `VALID_SEMANTIC`
  tag. One in-list correction: 15104 `厄介払い` `communication`→`action` (clearly
  wrong category). `modified` timestamps updated on all 30.
- **Translation:** 15147 `昇降口` — reviewer wanted "for going up and down"
  instead of "(for boarding)". **REJECTED** — "(for boarding)" is a legitimate
  rendering of 昇降口. Stylistic nuance, not an error.

## §4 self-verification
Skipped per §4 step-3 note: in accuracy-review mode the entries changed were
already covered by §A this run (the review itself was the check). All 54 pass
`validate.py`; `validate_tags.py` reports no unknown semantic tags for the range.

## Bookkeeping
- Decisions: 30 apply (tags) + 1 reject (translation) + 1 aggregated reject
  (furigana n=6) → `reviews/decisions.jsonl`.
- Ledger: furigana-screening ~$0.02 + accuracy ~$0.0236 → spent_today $0.0436.
- Queue: removed 15100–15153 from `reviews/queue.txt` (13258 → 13248).
- Metrics snapshot appended.
- Observation logged (`[pattern]`) on the systemic off-vocab tag drift in this
  block — nearby ranges (15000–15500) likely hold more; candidate for a
  systemic-fix `check_tag_drift.py` sweep.

## Candidates added
None (no missing-entry words surfaced in this tag-focused pass).

## Cursor
`polishing/tasks/cross-model-review/progress.txt` → `next: 15154`
