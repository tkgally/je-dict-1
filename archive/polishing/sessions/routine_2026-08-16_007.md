# Routine session — 2026-08-16 (007)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: 30 unharvested observations, 37 open backlog items, candidates 164,
frontier `comprehensive_next: 6967`, max entry ID 30693, reviewed entries 586.
**OpenRouter**: $0.2245 spent today of the $5.00 cap; **$0.00 spent this run**.
**Branch**: `claude/determined-rubin-svl079`

## Pre-flight

- §0a rescue: `mcp__github__list_pull_requests` returned **0 open PRs** — nothing to
  rescue, nothing to sweep.
- §0b orphan sweep: `mcp__github__list_branches` returned **`main` only** — zero orphan
  `claude/*` branches.
- Lock acquired on `claude/determined-rubin-svl079`.

## Work done

Harvested all **30 observations** from the six runs of 2026-08-15/16 (the wiki
metrics-refresh-37 run, two accuracy-review runs over 11501–12000 and 12001–12341, and
three polish runs), then ran **eight dictionary-wide measurements** before filing anything.

### Filed

**Cleanup backlog**
- **P63** — collocation/pattern heading near-synonyms, **730 entries**
  (`COLLOCATIONS:` 330, `PATTERN:` 154, `PATTERNS:` 115, `USAGE PATTERNS:` 92,
  `KEY PATTERNS:` 39). Explicitly excludes the `EXPRESSIONS:` and `PARTICLE PATTERNS:`
  families, which are different sections.
- **P64** — okurigana swallowed into the furigana ruby (`{痛|いたみ}` for `{痛|いた}み`),
  **90 distinct pairs / 123 instances**. Per-entry verification required.
- **P20 relocated** — 934 entries carry off-vocabulary semantic tags; hot spot is
  **08000–08999 (201, 22% of the population)**, not the 11750+ band its filing run named.

**Tooling backlog** — items **125–130** plus an update to **109**:
- 125: `check_consistency.py --issue no-collocations` is **55% false-positive on content**
  (3,717 of 6,759).
- 126: `check_semantic_clusters.py` reads only `prominent_see_also` for transitivity pairs;
  blind spot measured at **17 entries** and de-scoped.
- 127: post-filter `review_accuracy.py`'s own tag suggestions against `VALID_SEMANTIC`.
- 128: carry the run's mode into `reviews/decisions.jsonl`.
- 129: a swapped-translation detector for examples.
- 130: `check_furigana_format.py` is blind to okurigana-in-ruby (verified on 04651).
- **109 update**: the stated root cause does not explain PR #3216's 40-minute stale read.

**Entry follow-ups** — 03647 優勝 (`food` tag), 05212 忍耐 (off-list `endurance`/
`psychology`), 04651 関節痛 (`{痛|いたみ}`), 04165 先程 (先ほど orthography). Each was
**verified still-unfixed** at harvest time; entries already repaired by their filing run
(12305 官邸, 06965 不協和音) were checked and deliberately not listed.

**Backlog queue** — `collocation-heading-synonyms` and `furigana-okurigana-in-ruby` added;
`unknown-semantic-tags` re-measured 998 → **934**; `crossref-missing-from-notes-prose`
re-measured to **1,828**.

### Not filed, because measurement refuted the premise

- *"Notes open with a bare header and never state what the word means"* — 7,810 entries
  (26%) do open with a bare header, but **all 7,810 have a `definitions` field**. Escalated
  as a style ruling instead of filed as a detector.
- *"50 entry files"* of katakana ruby — a shell character-range artifact; the real figure is
  **271 instances / 225 files**, matching P60's standing 275/229.
- The transitivity `PAIR:`-paragraph template said to be shared by six named priority-lane
  verbs — **24 entries** dictionary-wide carry the shape, and **none of the six** is among
  them.

### Re-discoveries needing no new item

The zero-link block at 06947+ (tenth filing; Tooling 124 already addresses it); the
cross-references-from-notes-prose proposal (already open and `batch_ready`); the
sole-`general` and stale-`noentry` families (both already open).

## §4 self-check

**Not applicable** — this is a wiki-only run. `git status --porcelain -- entries/` was
empty, so no entries were created or modified and no independent-model pass was owed.
**No OpenRouter spend this run**; the ledger is unchanged at $0.2245 for the day.

## §5 metrics

Appended to `pipeline/metrics-history.jsonl`: entries 30,484; candidates 164; review queue
10,676; observations 1,446 lines; frontier 6,967; OpenRouter $0.2245.

**[H] Metrics trend not run**: 529 lines against `quality-metrics.md`'s last refresh at 521
runs — 8 new, below the ≥10 threshold. Due next wiki run.

## Escalated to the curator

Two items appended to `reviews/needs_curator.txt`:
1. **Notes-opening convention** — 7,810 entries (26% of the dictionary) open their notes with
   a bare all-caps header. Reported twice now; a style decision worth making once.
2. **CI-gate cross-check** — new evidence that Tooling 109's root cause explains only one of
   the two failure modes. Needs a prompt edit (CLAUDE.md MCP path step 5 and
   `prompts/routine2.md` §7.5.2), which wiki sessions cannot make.

## Cursors

No entry cursor moves in `wiki` mode. `planning/wiki/log.md` appended;
`index.md` unchanged (no new pages created — this session deepened existing ones).
`pipeline/routine-state.json` was advanced by the selector in §1.

## Build

**Skipped** per §6 — wiki-only run, markdown changes only, `docs/` untouched.

## CI gate observation (live test of Tooling 109)

Recorded below in the merge section: this run's own §7 gate is the natural place to test
whether `get_check_runs` reports staleness, and the result is noted there.

## Next

Next run's mode is the selector's call. The two highest-value queue items this harvest
produced are **P20 starting at 08000–08999** (201 entries, highest-precision flag family)
and **P64** (123 instances, per-entry).
