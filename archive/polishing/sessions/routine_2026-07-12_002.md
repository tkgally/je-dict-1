# Routine session — 2026-07-12 (002)

**Mode:** accuracy-review
**Selector reason:** "accuracy-review: highest scheduler debt among eligible modes"
**Params:** phase=furigana, start_id=13875, openrouter_session_budget_usd=2.50
**Range worked:** 13875–13974 (accuracy: glosses/translations/tags); furigana screening 13875–14039 (timed out at 1200s cap, exit 143 — partial results kept per §A), furigana deep review of all screening-flagged entries in 13875–13974.

## Pre-flight
- §0a rescue: no open PRs — nothing to rescue.
- §0b sweep: no open `claude/*` PRs to sweep.
- Branch `claude/modest-keller-jsp9op` had diverged (old pre-squash history already merged as #2926); reset to `origin/main` before starting.
- Lock acquired.

## Reviews run
- **Furigana screening** (`review_runner.py --pass screening --range 13875 14374`): 159 entries screened before the 1200s timeout; ~12% screening flag rate (expected noise).
- **Furigana deep** (`--pass deep`) on screening-flagged entries in 13875–13940 and 13941–13974: 16 flagged by screening, deep model cleared all but 2 genuine issues.
- **Accuracy** (`review_accuracy.py --range 13875 13974`): 100 entries, 24 flagged — **all in the `tags` dimension** (glosses and example translations came back clean).

## Changes applied (13 entries)

**Tag migrations — not-in-list semantic tags (APPLY by policy, 8):**
- 13883 `social`→`society`; 13885 `social`→`society` (kept `action`)
- 13888 `degree`→`quantity`; 13890 `role`→`person` (kept `politics`)
- 13896 `argument`→`expression`; 13899 dropped invalid `values` (kept `abstract`)
- 13902 `hobby`→`leisure` (kept `education`); 13907 `people`→`person`

**Tag category corrections — `body-part` clearly wrong for non-body-part nouns (APPLY, 3):**
- 13911 聴覚 (sense of hearing) `body-part`→`health`
- 13916 義足 (prosthetic leg, artificial device) `body-part`→`health`
- 13920 肥満 (obesity, a condition) `body-part`→`health`

**Furigana fixes (APPLY, 2):**
- 13938 `{真|まっと}うな` → `{真|ま}っ{当|とう}な` (真っ当な; まっと was misassigned to 真 and the 当 kanji was dropped) — deep model flagged as error.
- 13957 `{肌寒|はださむ}` → `{肌寒|はだざむ}` (one occurrence out of 24 lacked rendaku, inconsistent with headword はだざむい).

## Rejected (20)
- **Furigana:** 13926 祭→まつ (valid 祭り), 13927 良→い (valid いい), plus 5 screening false positives cleared by the deep pass (aggregated line, n=5).
- **Tags — in-list "too broad/narrow" substitutions (REJECT per §A policy):** 13877, 13886, 13913, 13914, 13918, 13923, 13925, 13929, 13932, 13947, 13968, 13969 (mostly `general`→specific or `food` too-narrow suggestions; all defensible in-list tags).
- **Formality:** 13876 経る `formal`→`neutral` rejected — the entry's own note says the time sense "is often literary or formal", contradicting the reviewer.

## §4 self-verification
Satisfied by §A: all changed entries fall within the range this run reviewed with an independent model, so the review itself was the check (no separate pass needed).

## Ledger / budget
- OpenRouter spend this run ≈ **$0.19** (screening ~$0.06 + deep $0.085 + $0.066 + accuracy $0.044). Daily ledger: $0.1931. Well within $2.50 session / $5.00 daily caps.
- All 29 adjudications logged to `reviews/decisions.jsonl` (13 apply, 15 per-entry reject, 1 aggregated reject n=5).

## Candidates / observations
- None added this run (accuracy-review adjudicated existing entries; no new missing words surfaced in the reviewed metadata).

## Cursors / next
- `polishing/tasks/cross-model-review/progress.txt` → `next: 13975`
- `reviews/queue.txt` pruned of 13875–13974 (13795 → 13738 lines).
- Metrics line appended to `pipeline/metrics-history.jsonl`.
