# Routine v2 session — 2026-06-10 (run 004)

**Mode:** `systemic-fix` (forced via `--force-mode systemic-fix` for manual v2 test; `state_persisted: false`, so `pipeline/routine-state.json` was NOT advanced this run).
**Selector reason:** "systemic-fix: forced via --force-mode". `params.backlog_item` = `artifact-teiru-brace` (priority 2, the top open batch-ready item). Signals: open_backlog_items 8, comprehensive_next 6000, remaining daily OpenRouter $4.4515.
**Backlog item:** `{ている}` furigana-brace artifact in notes/examples — `ideas/cleanup-backlog.md#priority-15`. fix_type `mechanical-safe`, verify `per-entry`, scope_estimate 49.

## Pre-flight (§0)
- §0a rescue: `list_pull_requests` returned no open PRs — nothing to rescue.
- §0b: `sweep-stranded-prs.py` closed 0 stranded PRs; `routine_lock.py acquire` succeeded (held for the run, released at end).

## §B systemic-fix work (semantic-verification-first)
- **Detector** (`build/check_artifacts.py --issue teiru-brace --json`): **49** records, all in the `notes` field, **0 in examples**. Raw `grep -rl '{ている}' entries/` cross-checked at 49. The detector matches the literal string `{ている}`.
- **Per-entry verification:** opened and confirmed all **49** entries before changing any. Every occurrence was the identical context — an `ASPECT ({ている}):` section header — followed by a coherent discussion of the ている aspect. Exactly one occurrence per file; no examples affected; no edge cases.
- **Fix applied:** `{ている}` → plain `ている` in `notes` (yielding `ASPECT (ている):`), plus `metadata.modified` bumped to `2026-06-10T04:16:48Z` on each. Applied via a guarded raw-text substitution (asserts: exactly one `{ている}` and one `"modified"` key per file; result is valid JSON with zero residual artifacts) so the diff is minimal — 49 files, 98/98 lines (the notes line + the timestamp line). Other plain-kana `ている` occurrences in the same notes were correctly left untouched.
- **Why mechanically safe but still verified:** `ている` is pure kana, so `{ている}` (no pipe) can never be valid furigana and removing the braces drops no reading. Per §B the per-entry confirmation was done anyway (override directive: open and confirm every flagged entry first).
- **Post-fix checks:** detector re-run → **0** remaining; `grep` → **0**; `validate.py --range 0 9314` → 9198/9198 valid.

## §4 self-verification (furigana-screener variant — format-only batch)
- `OPENROUTER_API_KEY` available; remaining daily budget $4.4515 (≥ $0.05) → self-check ran.
- `review_runner.py --pass screening --ids <49>` (gemini-2.5-flash): **35 screened, 14 skipped** (already screened earlier today), **5 flagged**, est **$0.0040**.
- **The edit itself was clean** — none of the 5 flags concern the `{ている}`→`ている` change; all 5 are about pre-existing furigana *readings* elsewhere in the touched entries, and **all 5 were REJECTED** as partial-reading false positives / a hallucination:
  - 03937_nejiru — model invented kanji `捩`/reading `ね`; entry actually uses `{捻|ねじ}る` (full reading ねじ + okurigana る). Correct → REJECT.
  - 04611_nogasu — `{逃|に}がす` is the intentional contrast form (逃がす=にがす); notes explicitly contrast it with `{逃|のが}す`. Correct → REJECT.
  - 04615_haneagaru — model's own text concludes "all pairs are correct"; `{跳|は}ね{上|あ}がる` right. Self-resolved → REJECT.
  - 09303_bateru — `{果|は}てる` (果=は, てる okurigana) correct; model flagged a partial reading, which the prompt says not to do → REJECT.
  - 09314_shirakeru — `{話|はなし}` (noun) and `{盛|も}り{上|あ}がる` both correct; model self-resolved → REJECT.
- **§4 outcome: clean — 0 applied / 5 rejected / 0 flagged** (the 5 rejects are all pre-existing-furigana false positives, not regressions from this run). All 5 logged to `reviews/decisions.jsonl` (src=furigana). One fix round budget unused (nothing to apply); no re-verify loop.
- Self-check spend recorded in `pipeline/openrouter-ledger.json` (phase `self-check`, 35 entries, $0.0040).

## §3 always-on capture
- Candidates added: none (the entries only reference existing words via inline links; no missing-entry words encountered).
- Observations appended to `polishing/observations.md` (1): `[pattern]` furigana self-check on a format-only batch returned 5/5 rejects — confirms the edit introduced nothing while showing the screener's near-zero precision on untouched partial-reading pairs; per-flag data now in `reviews/decisions.jsonl` for the metrics-trend table.

## §5 metrics
Appended to `pipeline/metrics-history.jsonl`: mode systemic-fix, entries_changed 49, applied 0, rejected 5, flagged 0, entries_total 28873, candidates 1464, review_queue_len 19450, openrouter_spent_today 0.5525.

## Spend (OpenRouter ledger)
One call this run: self-check screening $0.0040 (35 entries). Ledger total today **$0.5525** (prior $0.5485), far under the $5 daily cap.

## Backlog status updated (§B step 5)
- `planning/wiki/ideas/backlog-queue.json`: `artifact-teiru-brace` → `status: "resolved"`, `scope_estimate: 0`, `resolved_date: "2026-06-10"`, notes record the resolution; top-level `updated` bumped.
- `planning/wiki/ideas/cleanup-backlog.md` Priority 15: added a `**RESOLVED (2026-06-10).**` marker summarizing the fix, verification, and self-check.

## Cursors / state
- systemic-fix has no progress-file cursor; the "cursor" is the backlog item status, now `resolved` in both the queue JSON and the prose page.
- `routine-state.json` intentionally NOT advanced (forced-mode manual test).

## Next
Wrap-up: single `make build`, commit/push, PR `routine(systemic-fix): …`, wait for CI (Monitor, 30s/1200s), squash-merge on green, release lock.
