# Routine session 2026-06-17 #006

- **Mode**: `systemic-fix`
- **Selector reason**: "systemic-fix: highest scheduler debt among eligible modes"
- **Backlog item**: `artifact-missing-target-id` — cross_references / prominent_see_also entries without a `target_id` (priority 5, severity warn, scope_estimate 183).

## Pre-flight
- §0a: no open PRs to rescue.
- §0b: `sweep-stranded-prs.py` closed 0 (none stranded); routine lock acquired.

## Work done
Detector `check_artifacts.py --issue missing-target-id` reported **190 dangling
refs across 180 entries** at run start.

Built a resolution report by reading each flagged reference object and looking
it up in `build/word_id_lookup.json`. Key safety finding: the `by_reading`
fallback produces homophone false matches (有限/ゆうげん→幽玄, 小学/しょうがく→少額,
工夫/こうふ→工夫くふう), so resolution was restricted to **exact
headword + reading unique matches**.

Classification of the 190:
- **54 refs (51 entries)** — headword AND reading both uniquely match one
  existing entry → **filled `target_id`** (provably-correct exact-match). These
  were the cases where a real link had simply lost its id: antonym pairs
  (12053_jigoku→天国, 13002_yuugai→無害), transitivity/derivation pairs
  (00001_amaru→27283_amasu, 00097_hekomu→27319_hekomasu), same-kanji homophone
  triplets (20859_atsui→厚い/暑い/熱い), and related compounds
  (28075↔28076 nursing home, 29070↔29071 koki/kiju birthdays).
- **136 refs** — headword has no entry → left for a future run (drop-or-keep
  judgment per ref; some are *deliberate* no-entry pointers, e.g.
  00250_kufuu → 工夫/こうふ "laborer (homophone)" — must NOT be bulk-dropped).
- 2 review cases (00250 工夫/こうふ, 03959 長〜 prefix) left intact — intentional
  no-entry homophone/prefix pointers.

Application was a pure mechanical `target_id` insertion (last key, per the
00287_mizukara convention) + `modified` bump. A deep structural diff against
HEAD confirmed **every one of the 51 files differs ONLY in the added
`target_id` and the `modified` timestamp** — nothing else touched.
`validate.py`: 29102/29102 valid. Detector recount: **190 → 136**.

## §4 self-verification
Format-only change → furigana-screener path (`review_runner.py --pass screening`)
on the 51 changed IDs. The runner timed out (540s) after screening **24/51**;
partial per-entry results kept (logged as a `[tooling]` observation, the second
such timeout). 5 of the 24 flagged:
- **APPLIED 1 (pre-existing error the model caught)**: 14488_okuribi notes had
  `{五山|ございん}` → fixed to `{五山|ござん}` (Gozan no Okuribi).
- **REJECTED 4 (one aggregated noise-family line)**: 00097 (rendaku っぱ / partial
  reading), 06837 (添削=てんさく correct, model misread "てんさ"), 12134
  (防衛費=ぼうえいひ correct, compound-split false positive), 12677 (model
  self-rejected). All decisions logged to `reviews/decisions.jsonl`; screening
  spend (~$0.02, 24 entries) recorded in the OpenRouter ledger as `self-check`.

**§4 outcome: 1 applied, 4 rejected, 0 flagged** (24/51 externally screened;
remaining 27 are the same target_id-only change, deterministically verified by
the structural diff + validate).

## Always-on capture
- Observations appended: `[tooling]` screener-too-slow; `[pattern]` by_reading
  homophone-fallback hazard for any future target_id resolver.
- No candidates added (the 136 unresolved refs are mostly intentional
  antonym/variant/homophone pointers, not gaps; curator tops up candidates).

## State updates
- `backlog-queue.json`: `artifact-missing-target-id` scope_estimate 183 → 136,
  notes updated with progress + the do-not-bulk-drop caution. Status stays open.
- `cleanup-backlog.md` Priority 2: progress update added.
- `pipeline/metrics-history.jsonl`: snapshot appended (changed 51, applied 1,
  rejected 4).
- Selector advanced `pipeline/routine-state.json` in §1.

No per-mode progress cursor for systemic-fix beyond the backlog item.
