## Session: Unified Routine v2 — polish

Date: 2026-08-29
Mode: `polish` (selector reason: "polish: highest scheduler debt among eligible modes")
Selector signals: candidates 137, seen-in-entry 3, max entry ID 30773, comprehensive next 07043,
cross-model-review next 14330, unharvested observations 21, open backlog items 41,
OpenRouter remaining $5.00.

Pre-flight: no open PRs to rescue, no stranded `claude/*` PRs, no orphan branches
(`main` was the only branch on origin). Lock acquired and released normally.

### Priority lane (polishing/priority/notes.txt, lines 1–6)

Priority file regenerated 2026-08-27, so it was fresh; cursor started at line 1.
All six entries needed changes, so the rankings are not stale and were **not** regenerated.

- **00510 短い** — replaced a weak example (`短いメールをありがとうございます`, an unnatural
  use of を with ありがとうございます) with `返事が短くなってすみません`; added the 気が短い
  idiom to the notes with a pointer to its own entry; added `短め` and `気が短い` as
  cross-references (with back-links on both); added the `time-general` semantic tag for the
  duration sense.
- **00529 遠い** — **factual fix**: the notes documented `目が遠い` as "farsighted; hard of
  hearing". That is not a Japanese idiom; the real one is `耳が遠い` (hard of hearing).
  Replaced it. Linked `遠く` to its own entry (01680) instead of leaving it bare, and added
  cross-references to 遠く and 遠ざかる with back-links.
- **00763 うるさい** — added a SIMILAR WORDS section distinguishing 騒がしい (reports noise,
  more neutral, no "picky" sense) and やかましい (same two senses, blunter and more
  old-fashioned), plus synonym cross-references to both and a back-link on やかましい.
- **00959 黄色い** — **factual fix**: both the definition and the notes claimed that "many
  color words function as na-adjectives (like 緑)". Japanese colour words are nouns used with
  の, not na-adjectives, and 緑 is tagged `noun` in this dictionary. Rewrote both passages to
  give the real generalisation (most colours are nouns + の; only 赤い・青い・白い・黒い・
  黄色い・茶色い are true i-adjectives) with correct entry IDs, and added a cross-reference
  to 茶色い with a back-link.
- **00788 汚い** — cleared a stale `noentry` marker (汚らしい now has entry 29808); broadened
  sense 2 from "unfair, underhanded" to also cover crude/vulgar language, and retagged
  example 7 (`汚い言葉を使わないでください`) from sense 1 to sense 2, since it is not physical
  dirt; documented the 汚れる/汚す verb pair and their different よご〜 reading; added three
  cross-references with back-links.
- **03095 〜など** — retagged example 6 (`サッカーや野球などのスポーツ`) from sense 1 to
  sense 2, matching the other `〜などの N` examples; removed a furigana wrapper around
  katakana (`{サッカー|さっかー}` → `サッカー`); removed the `style: ["spoken"]` tag, which
  contradicted the entry's own note that なんか is the *spoken* variant of など.

### Frontier lane (07043–07050)

All eight had **zero** inline link coverage. Full tier-1 coverage was added to every example
sentence and every Japanese phrase in the notes, along with the fixes below.

- **07043 喫煙所** — full link coverage; cross-references to 喫煙, 喫煙席, 分煙 with
  back-links; 喫煙室 marked `noentry` and queued as a candidate.
- **07044 休憩所** — full link coverage; cross-references to 休憩, 休憩室, サービスエリア,
  パーキングエリア with back-links; semantic tag `geography` → `building` + `travel`
  (a rest area is a facility, not a landform).
- **07045 非常口** — full link coverage; cross-references to 避難経路, 非常階段, 誘導灯, 非常
  with back-links; sole `general` semantic tag → `building` + `daily-life`.
- **07046 指定席** — full link coverage; **furigana fix**: `{グリーン車|しゃ}` wrapped the
  whole word including katakana → `グリーン{車|しゃ}`; cross-reference to グリーン車 with
  back-link; sole `general` tag → `transportation` + `travel`. Queued 変更する as a candidate.
- **07047 自由席** — full link coverage; added an example note explaining the set phrase
  早い者勝ち; cross-reference to グリーン車 with back-link; sole `general` tag →
  `transportation` + `travel`. Queued 早い者勝ち as a candidate.
- **07048 素泊まり** — removed a **dead cross-reference** with no `target_id`
  (`一泊二食付き`, which has no entry) and replaced it with a real one to 朝食付き plus a
  `noentry` mention in the notes; full link coverage; cross-reference to 泊まり; back-link on
  朝食付き. Queued 一泊二食付き and 夕食付き as candidates.
- **07049 連泊** — full link coverage; cross-references to 一泊 and 素泊まり with back-links;
  sole `general` tag → `travel` + `time-period`. Queued 二泊 and 延泊 as candidates.
- **07050 送迎** — full link coverage; sole `general` tag → `transportation` + `travel`.
  Queued 結婚式場 and 送迎バス as candidates.

Entries touched in total: 34 (14 polished, 20 back-link-only on neighbours).

### §4 self-check

`python3 build/review_accuracy.py --ids <the 14 polished entries> --budget 0.40`
— **clean**: 14 entries reviewed, 0 issues raised, est. $0.0063. Nothing applied, rejected,
or escalated. Logged as one aggregated line in `reviews/decisions.jsonl`; ledger updated
(day total $0.0063 of the $5.00 cap).

### Candidates added

耳が遠い, 喫煙室, 変更する, 早い者勝ち, 一泊二食付き, 夕食付き, 二泊, 延泊, 結婚式場, 送迎バス
(10; queue now 147).

### Observations logged

- [pattern] 50 entries wrap katakana inside furigana braces — a safe mechanical systemic-fix batch.
- [pattern] The 07000-block January-2026 entries have zero inline links and sole `general` tags;
  budget 6–8 frontier entries per run while the frontier is in this block.
- [tooling] 44 cross-references with no `target_id` pass CI as a mere "note"; worth promoting
  to a validation error or a backlog item.
- [entry] The wrong "colour words are na-adjectives" claim may be copied into other colour entries.
- [entry] Invented idioms in FORMS/EXPRESSIONS blocks (the 目が遠い case) may deserve their own
  accuracy-review dimension.

### Next cursors

- comprehensive frontier: `next: 07051`
- priority lane: `line: 7`
