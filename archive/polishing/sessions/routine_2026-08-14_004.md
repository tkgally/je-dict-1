# Routine session — 2026-08-14 (004)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Params**: `phase: furigana`, `start_id: 10401`, `openrouter_session_budget_usd: 2.50`
**Branch**: `claude/determined-rubin-5bfgp0`

## Pre-flight

- §0a rescue: no open PRs — nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs.
- §0b orphan-branch sweep: `list_branches` returned `main` only — zero orphans.
- Routine lock acquired.

## Range worked

Entries **10401–10686** (286 entries reviewed by the accuracy pass; furigana
screening flags covered 10401–11000).

The accuracy reviewer runs at roughly 4.5 entries/minute, so the originally
planned 600-entry range (10401–11000) would have taken about two hours of wall
clock. The pass was stopped at 10686 to leave room for adjudication and the
merge tail, and the cursor was set accordingly.

## Furigana dimension (§A step 2)

Screening covered 10401–11000 (81 entries newly screened this run, the rest
already on file). **43 entries flagged, all 43 rejected as false positives.**
Every flag fell inside a documented false-positive family, verified against the
entry files directly:

- Model truncating the reading in its own report, then flagging the truncation
  it invented — `{犯罪|はんざい}` reported as "only はん", `{不足分|ふそくぶん}` as
  "ふそくぶ", `{年賀状|ねんがじょう}` as "ねんがじょ", also 10446, 10619, 10658,
  10698, 10811, 10882, 10899, 10927, 10929.
- Correct rendaku — `クレジット{払|ばら}い`, `やんちゃ{盛|ざか}り`, 生け花's `{花|ばな}`.
- Correct okurigana splits — `{素焼|すや}き`, `{軍艦巻|ぐんかんま}き`, `{急|せ}かす`,
  `{手入|てい}れ`.
- Readings the entry itself documents as variants — 10548's まぬか/まぬが note.

Per §A's known-noise shortcut the deep pass was skipped and the whole set was
bulk-rejected with one aggregated ledger line.

## Accuracy dimension (§A steps 3–4)

286 entries reviewed, 51 error-severity and 5 warn-severity issues adjudicated.
**33 entries changed** (37 applied fixes), **60 flags rejected**, **1 flagged
for the curator**.

### Applied — off-vocabulary semantic tags (correct by definition, §A policy)

| Entry | Headword | Before | After |
|---|---|---|---|
| 10484 | つけ麺 | `food-cooking` | `food` |
| 10487 | 外れ | `result-outcome`, `location-area` | `general`, `geography` |
| 10495 | 拍子 | `music-sound`, `time-moment` | `music`, `time-general` |
| 10613 | ぶら下げる | `action-physical`, `position` | `action` |
| 10485 | どんくさい | `personality-trait` | `personality` |
| 10528 | 目指す | `action-cognitive`, `movement` | `action`, `movement` |
| 10636 | もてはやす | `social`, `evaluation` | `society`, `evaluation` |
| 10661 | 名乗り | `social`, `communication`, `identity` | `society`, `communication` |

Note: 10613's `position` and 10661's `identity` are also off-vocabulary but the
reviewer only caught one tag per entry; both were migrated.

### Applied — sole-`general` tags replaced with a specific in-list tag

10431 価値観 → `cognition`; 10451 信条 → `cognition`; 10457 開催 → `action`;
10458 発送 → `action`; 10552 正座 → `culture`; 10558 転売 → `economics`;
10575 証拠 → `cognition`; 10577 起訴 → `law`; 10580 点滴 → `health`;
10581 麻酔 → `health`; 10584 漆 → `art`; 10591 秩序 → `society`;
10600 権限 → `law` (the reviewer suggested `politics`; `law` was chosen to
match 裁判).

### Applied — semantic mismatches

- 10420 名詞 `education` → `grammatical` (matches 動詞 and 助詞).
- 10453 音読み `education` → `language`.
- 10450 気性 `emotion` → `personality`.
- 10407 出来上がる `existence` → `action` (matches its transitive pair 仕上げる).
- 10466 合う `existence` → `action` (matches 合わせる and 一致する).
- 10493 一際 `quantity` → `descriptive` (degree adverb, not a number word).
- 10582 版画 `leisure` → `art` (matches 絵).
- 10626 寝癖 `body-part` → `appearance` (bed hair is not a body part).
- 10403 取り組み — added `sports`; sense 2 is a sumo bout, with its own example.
- 10429 絵本 — added `media` alongside `education`, matching 児童書.
- 10531 さようなら — politeness `polite` → `plain`, matching こんにちは (the word
  carries no です/ます marking).

### Applied — translation

- 10410 とっとと, example 1: 「とっとと{帰|かえ}れ。」 was "Get out of here already."
  → "Go home already." The original lost 帰る = go home, which is exactly the
  mapping a learner needs from that sentence.

### Rejected (60)

- **43** furigana screening flags (aggregated line, see above).
- **7** formality `formal` → `neutral` flags (10401 たやすい, 10421 著しい,
  10448 定める, 10548 免れる, 10657 手筈, 10671 立ち入り, 10675 非常勤). §A allows
  a formality change only when the entry's own notes contradict the label; here
  the notes either state the formal register outright ("formal and used in
  official/legal contexts", "somewhat formal … written contexts or news",
  "prohibition signs") or say nothing about register.
- **Narrowness nits between in-list tags**: 10550 猫舌 `descriptive` → `person`;
  10583 織物 `clothing` → `general` (a move to a broader tag is not a fix).
- **10479 しゃぶしゃぶ** — asked to add `onomatopoeia` as a semantic tag; the
  part-of-speech tags already record that, and the word's semantic field is food.
- **10520 振る舞い** — asked to remove `food`; sense 2 is "treat, feast,
  hospitality", so the tag is earned.
- **10531 さようなら** gloss "goodbye" → "farewell": "goodbye" is the standard
  gloss and "farewell" reads archaic in English.
- **10656 引き出物** gloss (flagged twice): the existing gloss "wedding favor,
  gift to guests" already generalizes past weddings, and the notes carry the
  full cultural context.
- **10486 馳せる** gloss and example-2 translation: the suggested translation was
  less natural than the existing one, and widening the sense inventory is out of
  scope for this run (see the curator flag below).

### Flagged for the curator (1)

- **10486 馳せる** — example 3, 「{名|な}を{馳|は}せる。」 ("To make a name for
  oneself"), is tagged as sense 1 ("to rush, to gallop, to hasten"), but that
  meaning is not covered by either of the entry's two senses. Adding a third
  sense or moving the example is an editorial call. Recorded in
  `reviews/needs_curator.txt`.

## §4 self-verification

Skipped by design: §4 step 3 exempts IDs already covered by §A in the same run,
and all 33 changed entries fall inside the 10401–10686 range the accuracy pass
just reviewed. The review itself was the independent check.

`build/validate.py --range 10400 10700` reports 301/301 valid, and
`validate_tags.py --check-no-new-unknown` passes (no new off-vocabulary tags —
this run removed eight).

## Candidates added

None. The accuracy reviewer works from entry text that was already checked for
missing-entry words by the polish frontier; nothing new surfaced.

## Observations logged

Four appended to `polishing/observations.md`:

- `[pattern]` Sole-`general` tags are widespread beyond this range — 権利, 義務,
  責任 are all sole-`general` and 法律 is tagged `work`. Good candidate for a
  systemic-fix backlog item, since `check_tag_drift.py` already detects the class.
- `[pattern]` Grammar-terminology entries drift to `education`; 形容詞 still has
  it while 動詞 and 助詞 use `grammatical`. Small closed set, one-shot sweep.
- `[tooling]` A new furigana screening false-positive family: the model truncates
  the reading in its own report and then flags the truncation. The screening
  prompt should require quoting the pair verbatim before judging it.
- `[tooling]` `review_accuracy.py` runs at ~4.5 entries/min; Routine ranges
  should be sized at ~250–300 entries.

## Spend

$0.1408 estimated this run (screening + accuracy, 367 model calls). Daily
ledger now at $0.4159 of the $5.00 cap.

## Cursors

- `polishing/tasks/cross-model-review/progress.txt` → `next: 10687`
- `reviews/queue.txt` pruned of 10401–10686 (10,461 lines remain)
- Metrics line appended to `pipeline/metrics-history.jsonl`
