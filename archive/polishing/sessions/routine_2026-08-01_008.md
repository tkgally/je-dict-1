# Routine session — 2026-08-01 (008)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Selector params**: `phase: furigana`, `start_id: 22900`, `openrouter_session_budget_usd: 2.50`
**Signals**: candidates 1001 (10 "seen in entry"), max entry ID 30317, comprehensive next 6739,
cross-model-review next 22900, unharvested observations 26, reviewed entries 586, open backlog items 15.

## Pre-flight

- §0a: `list_pull_requests` returned no open PRs — nothing to rescue.
- §0b: no `claude/*` strands to sweep. Lock acquired for `claude/determined-rubin-na5nx8`.

## Range correction (important)

The selector's `start_id` came from `polishing/tasks/cross-model-review/progress.txt`
(`next: 22900`), but **22900-23400 had already been accuracy-reviewed on 2026-07-31**
by runs #3076 (22767-23100) and #3081 (23101-23500), and adjudicated then (39 decisions
already in `reviews/decisions.jsonl` for that range). Run #3086 advanced the cursor to
`end+1` of *its own* range without checking that the block above was already covered,
so the cursor pointed backwards into finished work.

Accordingly the accuracy pass was redirected to the **genuine un-reviewed frontier,
23408-23607**, and the cursor was reset to the first entry with no
`reviews/accuracy/` file. Logged as a `[tooling]` observation.

## Work done

### Furigana screening — 22900-23040 (141 entries, new coverage)

Genuinely un-screened territory (screening coverage restarts at 23015+). Stopped at
23040 to free throughput for the accuracy pass rather than run the full 500.

**11 entries flagged, 0 applied.** Every flag fell inside a documented
false-positive family from `reviews/calibration_report.md`:

- okurigana splits — `{元請|もとう}け`, `{下請|したう}け`, `{街並|まちな}み`, `{下書|したが}き`,
  `{肌荒|はだあ}れ`, `{上|あ}がる`, `{供|そな}える`, `{弔|とむら}い` (all correct as written)
- compound sound change — `{青|さお}` in 真っ青 (まっさお)
- readings the entry itself discusses — `お{札|さつ}` inside 23002 (札／ふだ), whose notes
  explicitly explain the さつ reading
- model hallucination — 23329 flagged `{赤茶色|あかちゃ}` and `{赤茶色|あ}`; the entry
  contains only the complete `{赤茶色|あかちゃいろ}`
- 23331 and 23364 flagged pairs the model then confirmed as correct in its own concern text

Per §A step 2's known-noise shortcut these were bulk-rejected with one aggregated
§C line and **the deep pass was skipped**.

### Accuracy review — 23408-23607 (200 entries, first review)

200 reviewed, 69 flagged, est. $0.0885.

**Applied — 60 entries, off-vocabulary semantic tags migrated to `VALID_SEMANTIC`.**
73 off-vocab tags across 43 distinct labels in just 200 entries. Per the §A
semantic-tag policy ("not in the list is correct by definition"), all were migrated.
Mappings used beyond `check_tag_drift.py`'s 9-entry `TAG_MIGRATION`:

`housing`→building (`business` for 23533 引越し業者), `body`→body-part,
`mathematics`/`math`→number, `government`→politics (`law` for 23526 在留カード,
`society` for 23532 相談窓口), `banking`→finance, `academic`→education,
`programming`/`computing`/`internet`→technology, `organization`→society,
`identity`→person, `workplace`→work, `dining`→food, `sport`→sports,
`biology`/`physics`→science, `office`→tool, `psychology`/`philosophy`→cognition,
`plants`→plant-general, `degree`→quantity, `tools`→tool, `crime`→law,
`spatial`→direction, `object`→tool, `place`→building (dropped on 23519 アトラクション),
`safety`→work, `document`→communication, `order`→number, `ceremony`→culture,
`death`→society, `conflict`→military, `events`→society, `gardening`→dropped.
23539 タイム also gained `time-general` alongside `sports`.

Verified afterwards: 0 off-vocab tags remain in 23408-23607;
`validate_tags.py --check-no-new-unknown` passes; all 701 entries in 23000-23700 validate.

**Rejected — 17 flags.**

- **All 13 formality flags** (23415, 23416, 23435, 23451, 23460, 23520, 23526, 23532,
  23550, 23556, 23567, 23585, 23599). §A applies a formality flag only when the entry's
  own notes contradict the label; here the notes either *supported* it ("A formal,
  comprehensive term" for 魚介類, "literary" for 可憐な, "formal or written-style term"
  for 自家用車, "the formal technical name" for 発光ダイオード, "somewhat formal" for
  調味する) or said nothing about register.
- 23463 translation: model claimed 縦列駐車 means "tandem parking", not "parallel
  parking". It does mean parallel parking; the model is wrong.
- 23412 `food` on オーブントースター — a kitchen appliance sits legitimately in the food field.
- 23424 `body-part` on カール — hair is in this project's own `body-part` keyword list.
- 23460 "suru-verbs should carry `action` by convention" — no such convention exists;
  only 24% of the 4,574 `verb-suru` entries carry it.

### Re-review residue — 22900-23017 (118 entries)

Before the range problem was spotted, 118 already-reviewed entries were re-reviewed.
The fresh second opinion produced only 9 issues, adjudicated rather than discarded:

- **Applied (1)**: 23000 {乙|おつ} — headline gloss read "second rank; stylish; nice",
  omitting documented sense 3 entirely. Now "second rank; stylish; thanks for the hard
  work (internet slang)"; the vague "nice" was redundant with "stylish".
- **Rejected (8)**: 22937 (青信号 correctly renders as "green" in English),
  22942 (`abstract` is a semantic-field tag, not the antonym of 具象),
  22982 (notes call it literary — `formal` stands), 23000/23011/23016/23017 tags
  (in-list narrowness nits, which §A rejects by policy).

## §4 self-verification

**Skipped by mode rule** — §4 step 3 exempts IDs already covered by §A this run, and
every one of the 61 changed entries was reviewed by this run's own passes.
Deterministic verification stood in for it: `validate.py` (701/701 valid),
`validate_tags.py --check-no-new-unknown` (pass), and a re-scan confirming
0 remaining off-vocab tags in the range.

## Totals

- Entries changed: **61** (60 tag migrations + 1 gloss fix)
- Flags applied 61 · rejected 35 (17 accuracy + 8 re-review + 11 furigana aggregate incl. 1 aggregated line) · escalated to curator 0
- OpenRouter spend this run ≈ **$0.18** (459 entries touched); daily ledger now $0.579 / $5.00
- Candidates added: 0 (no un-entered words encountered — this mode reads tags and glosses, not new example text)
- Observations logged: 3 (`[tooling]` cursor regression, `[pattern]` off-vocab tag block above 23400, `[pattern]` formality-flag precision 0/13)

## Cursors

- `polishing/tasks/cross-model-review/progress.txt` → **`next: 23608`** (first entry with
  no `reviews/accuracy/` file)
- `reviews/queue.txt` pruned of 22900-23040 and 23408-23607 (13,466 → 13,172 lines)
- `pipeline/routine-state.json` advanced by the selector in §1

## Note for the curator

The un-reviewed frontier is now **23608-30317 (~5,400 entries)**, plus scattered gaps
below (6926-7179, 7262-7640, 8238-8528, 9850-10449, 13975-14186, 16167-16373). Given the
density found here (73 off-vocab tags per 200 entries), extending
`check_tag_drift.py`'s `TAG_MIGRATION` with the ~30 mappings used above would let that
whole block be swept deterministically instead of 200 entries per LLM run.
