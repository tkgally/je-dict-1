# Routine v2 session — 2026-07-28 (003)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: candidates 1,029 (8 "seen in entry"); max entry ID 30,182; comprehensive frontier 6,669; cross-model-review cursor 20,451; unharvested observations 20; reviewed entries 586; open backlog items 7
**OpenRouter**: $0.2735 spent today of $5.00 cap; no session budget set

## Pre-flight

- **§0a rescue**: `mcp__github__list_pull_requests` returned **no open PRs** — nothing to rescue.
- **§0b sweep**: no `claude/*` PRs open, so nothing to close. (`pipeline/sweep-stranded-prs.py` not run — it 403s in this environment; the MCP sweep is the working path.)
- **Lock**: acquired for `claude/determined-rubin-54zzm3`.
- Branch was level with `origin/main` at `2f735c777`.

## Work done

Wiki mode, so **no entry files were touched** and `make build` was skipped (markdown-only changes do not affect `docs/`).

### Activity F — harvest (16 observations, all cleared)

Sources: the 2026-07-27 routine polish run (00908–07441 priority lane / 06656–06668 frontier), the 2026-07-28 accuracy-review (19951–20450), and the 2026-07-28 polish run.

Three observations closed long-open diagnoses rather than reinforcing them:

| Filed to | What changed |
|---|---|
| **Tooling 20** | **Scorer bug #2's root cause found**: `normalize_pos()` tests `'verb' in pos.split(',')[0]`, and `"adverb"` contains `"verb"` — so every `adverb, …` entry is scored against the verb template and required to have TRANSITIVITY/ASPECT/COMMON PATTERNS sections it cannot have (00266_maido stuck at 54 after a full notes rewrite). Together with bug #1 (6,351 entries mis-flagged for bare kanji) this completes the diagnosis of the priority lane's no-op streak, and both halves are one-line fixes in the same file. |
| **Tooling 24** | **FP mechanism located in the source** — the screening prompt's `followed by:` context snippet is clipped at a fixed width, cutting mid-`{kanji|reading}`; measured at 47/48 flags (~2%) on 19951–20388 and 7/18 on *brand-new* entries 30165–30175, so it is not a property of stale data. Separately, the deterministic charset lint this item has proposed since June was run by hand and **beat the screener 7:1** (8 Latin-letter readings found dictionary-wide vs the screener's 1), with zero false positives. |
| **Tooling 11** | **Third consecutive cycle, and the filed gate is insufficient**: both new dead links point at *real entries for the wrong word* (何→`00294_motomoto`, でも→`09528_tanaka`), so they render as working links to unrelated pages. Existence-checking the target ID would not catch either; the complete check is base-form agreement via `word_id_lookup.json`. |

Also filed:

- **Tooling 17** — a *second* invented reviewer rule ("`verb-suru` entries must carry `action`") alongside the twenty-times-confirmed "too broad/narrow" family; 234 of 244 issues (96%) came from the `tags` dimension. Two independently-invented rules argue for a **positive closed-scope** prompt clause rather than a growing prohibition list.
- **Tooling 19** — ten more stale `noentry` markers, **seven in a single entry** (05091_ryoukai). The population is clustered by entry age, not uniform; run the detector entry-major, oldest-first.
- **Tooling 21** — thirteenth confirmation, widest ratio yet (~8–9/min vs ~55/min, ~6×): a 500-entry screening pass costs ~55 min of wall clock. Items 21 and 24 have converged — cost and noise share one home in `review_runner.py`.
- **NEW Tooling 41** — `manage_candidates.py` keys its duplicate check on (surface, reading) and so cannot queue a homograph; でも was refused as a duplicate of the conjunction 00925. Self-concealing, and it turns every homograph gap into a permanent dead `noentry`.
- **NEW Cleanup P29** — adjective examples teaching a phrase where the language uses a set compound (×青い信号 for 青信号). Invisible to validation, the note scorer, and the accuracy reviewer, because the phrase is grammatical and faithfully translated.
- **NEW Cleanup P30** — Latin characters inside furigana readings; 8 instances, all fixed by the observing run, class empty. Kept as a queue entry because it can refill and the check is free.
- **Cleanup P20 update** — the band holds at 44% into 19951–20450 (221/498), but the mappable/unmappable split has **inverted**: 99 of 221 have a 1:1 target vs ~93% one band earlier. Filed the cheap discriminating test (compare the distinct off-list tag *sets* of the two halves) before the next sweep is sized.
- **Entry Follow-ups** — 00719/02574 込む (duplicate lemma, plus 〜込む as a genuinely third item with no entry); でも (particle, unqueueable per Tooling 41); and a **correction** to the やさしい follow-up (below). 00304_nandemo needed no filing — fixed in-run, siblings verified clean.

### Verification note (やさしい)

Two polish runs reported *opposite* entries as the offender — one said 00765's ex6–10 were misfiled, the other said 02640's. Rather than pick a report, this harvest read all three files. **Both were right**: 00765 and 02640 are mirror images, each leading with its own headword's sense and then documenting the other word in full underneath, ten examples apiece, with several sentences literally shared (00765 ex1 = 02640 ex7; 00765 ex2 = 02640 ex8). 00475 is a third combined copy. The follow-up's table was corrected and the observation recorded as a general caution: an entry whose second sense belongs to a different lemma looks correct from the inside.

### Activity C — deepen `research/collocations.md`

Added the set-compound finding as the *blocking* case of L1-incongruence (the compound pre-empts the productive rule rather than substituting a wrong partner), why model-written examples are structurally prone to it, and why no correctness-checking instrument can detect it. Last-updated refreshed.

### Activity A — project counts

`project/overview.md` refreshed to **29,973 entries / 27,170 general**.

### Activity E — lint and sync

- `backlog-queue.json` resynced against a fresh `check_tag_drift.py --summary`: `unknown-semantic` 6,696→**6,218**, `sole-general` 3,876→**3,789**, `semantic-mismatch` 977→**825**, `concrete-noun-domain-mismatch` 6→**8**, `proverb-idiom-mismatch` 0→**1** (that check is live again rather than exhausted). Added the two new items (`adjective-set-compound-examples`, `furigana-latin-in-reading`).
- Ran a cross-reference lint over every wiki page: **7 broken anchors fixed across 6 files** — `multilingual-dictionary.md#9-phasing--rollout` (×4, in `llm-translation-quality-japanese-pairs.md`, `multilingual-rendering-architecture.md` ×2, `translation-sidecar-design.md`) and the P23/P28 cleanup-backlog anchors (×3, in `overview.md`, `quality-metrics.md`, `cleanup-backlog.md`). All anchors introduced this run verified resolving.

### Activity H — not due

374 metrics lines vs the 369 covered by `topics/quality-metrics.md` — **5 new, below the ≥10 threshold**. Deferred to the next `wiki` run.

## §3 always-on capture

No new candidate words: a `wiki` run reads backlog prose, not entries. The one word this run would have queued — the particle でも — **cannot be queued**, which is itself the subject of new Tooling item 41 and is filed as an Entry Follow-up instead.

## §4 self-verification

**Not applicable — no entry files were created or modified.** No OpenRouter spend this run; the ledger is unchanged at $0.2735 of the $5.00 daily cap.

## §5 metrics

One line appended to `pipeline/metrics-history.jsonl` (`--mode wiki --changed 0`).

## Cursors

- `pipeline/routine-state.json` — advanced by the selector in §1.
- No polishing cursor moved (wiki mode advances none).

## Routed to the curator

A `wiki` run may not modify prompts, skills, entries, or build scripts. Outstanding:

1. The **two one-line `score_note_quality.py` fixes** — and note they are incomplete without regenerating `polishing/priority/notes.txt` and resetting the priority cursor to line 1, since the current position indexes a ranking the buggy scorer produced.
2. The **`validate.py` base-form-agreement gate** (target existence alone is provably insufficient — see Tooling 11).
3. The **`review_runner.py` context-snippet fix** and the **Latin-letter CI lint** (the latter needs no review queue: any match is a failure).
4. The **`review_accuracy.py` closed-scope tag clause**.
5. The three entry follow-ups that turn on an entry-identity decision (やさしい ×3, 込む ×2 + 〜込む, でも).
