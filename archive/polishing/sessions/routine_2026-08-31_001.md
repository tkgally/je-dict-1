# Routine session — 2026-08-31 (001)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: 35 unharvested observation lines, 41 open backlog items, candidates 152,
frontier 07059, cross-model cursor 14639, max entry ID 30773, OpenRouter remaining $5.00.
**Branch**: `claude/intelligent-curie-8sa17g`

## Pre-flight

- §0a `mcp__github__list_pull_requests` (open): **0 PRs** — nothing to rescue, nothing to sweep.
- §0b orphan sweep: `mcp__github__list_branches` returned `main` only — **zero orphan branches**.
- Lock acquired; `git fetch origin main` showed the branch level with main (0/0).

## What this run did

Harvested all 35 observations from five runs (the 2026-08-27 metrics refresh, the 2026-08-27
polish run over 07023–07042, the 2026-08-28 new-entries run over 30754–30773, and the
2026-08-29/30 polish and accuracy-review runs), measuring every premise before filing it.

No metrics refresh: only **7** new `metrics-history.jsonl` lines since refresh 39 (2026-08-27),
against the ≥10 threshold.

### Measurements run (11)

| # | Question | Result |
|---|---|---|
| 1 | Are ALL-CAPS notes headers a January-cohort artifact? | **Refuted** — 21,073 / 30,564 (69%) dictionary-wide; 89% on 2026-03-28 vs 59% on 2026-01-18 |
| 2 | How many entries have substantive notes missing a required section? | **5,748** of 9,067 with a template (63%); only 2 are short-and-missing |
| 3 | What heading do they use instead? | **2,499 of 2,965** する-verb cases use `COMMON COLLOCATIONS` |
| 4 | Does that drive the notes priority list? | **76 of the worst 100** vs an 18.8% base rate (4×) |
| 5 | Shop nouns tagged sole-`building`? | **22** of 115 shop-shaped headwords (run guessed 15) |
| 6 | Copied "colour words are na-adjectives" family? | **Refuted** — 2 entries dictionary-wide, both correct |
| 7 | Register parentheticals in glosses? | **247** entries (run estimated 216) |
| 8 | Do 製/系/用 lack entries? | **Refuted** — 〜製 02001, 〜系 28466, 〜用 09842; 99 tilde headwords, 42 product-suffix `noentry` markers |
| 9 | Are the two katakana-brace queue items one population? | **Yes** — 273 instances / 227 entries, 270 with a hiragana reading |
| 10 | Cross-references with no `target_id`? | **44 / 40 entries** (queue said 12) |
| 11 | Katakana bases in `check_stale_noentry.py`'s mechanical bucket? | **22 of 123 pairs**, holding **8** sense mismatches |

### Filed

- **Tooling 140** — `score_note_quality.py` does not credit `COMMON COLLOCATIONS` as a patterns
  section (`needs-decision`: one-line alias vs a 2,499-entry rename).
- **Tooling 141** — the deep furigana pass runs ~1 entry / 15 min (`DEEP_MODELS` carries
  `gemini-2.5-pro`; rate limiting explains ~12 s of it), and a Routine that starts it cannot stop it.
- **Tooling 142** — `reviews/accuracy/*.json` carry no staleness marker.
- **Tooling 143** — demote katakana base forms out of `check_stale_noentry.py`'s mechanical bucket.
- **Cleanup P70** — shop nouns tagged sole-`building`, 22 entries.
- Updates to Tooling **112** (bare-suffix/tilde duplicate shape), **123** (bare numeric ID +
  `entries_index.json` dependency), **138** (filter `reviews/screening/` by `screened_at`),
  **111/118** (the `general`-is-too-broad family, fourth measurement: 37 of 38 tag flags).
- Rescoped `artifact-missing-target-id` 12 → 44; marked `katakana-hiragana-reading-wrappers` a
  duplicate of `furigana-katakana-base`.
- Entry Follow-ups: 28575 式's missing suffix sense; 00529's invented 目が遠い plus the
  notes-block idiom class; 13981 義弟's referentially unclear example 1.
- New page sections: **Shape 3** on `topics/inline-link-integrity.md` (the target entry silently
  covers a different sense); **`existence`** on `topics/schema-tag-reliability.md` (129 entries,
  no denotation).
- Backlog queue: 6 items added, 3 rescoped, 1 marked duplicate (96 items total).

### Curator escalations (3, in `reviews/needs_curator.txt`)

1. The notes-scoring alias — one line versus a 2,499-entry rename, and the notes priority list
   should not be planned from until it lands.
2. `newentries.md` bans register parentheticals in glosses while 247 entries carry them.
3. What `existence` denotes — narrow it to be/exist/remain, or accept the broad reading and put
   the definition in the reviewer prompt.

## §4 self-verification

**Not applicable** — this run changed **0 entry files** (`git status --porcelain -- entries/`
empty). No OpenRouter spend; the ledger is unchanged at $0.00 for the day.

## §5 metrics

Appended: `{"mode": "wiki", "entries_changed": 0, "entries_total": 30564, "candidates": 152,
"review_queue_len": 10817, "observations_lines": 1546, "comprehensive_next": 7059}`.

## Cursors

No mode cursor to advance (`wiki` mode). `polishing/tasks/comprehensive/progress.txt` stays at
`next: 07059`; the cross-model cursor stays at 14639. `pipeline/routine-state.json` was advanced
by the selector in §1.

## Notes for the next run

- The **notes priority list is unreliable for lane planning** until Tooling 140 is settled — its
  top stretch is ranking heading vocabulary, not note quality. A `polish` run drawing from
  `polishing/priority/notes.txt` should expect a high share of entries that need a heading
  renamed rather than notes written.
- The frontier at 07059 is inside the zero-inline-link block (`inline-link-block-06800-07100`),
  so the standing budget guidance of ~6–8 frontier entries per run still applies — but **not**
  the "full notes rewrite" part of it, which measurement 1 refuted.
