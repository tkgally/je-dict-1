# Routine session — 2026-06-28 #007

**Mode:** accuracy-review (phase: furigana)
**Selector reason:** "accuracy-review: highest scheduler debt among eligible modes"
**Range/params:** start_id 10888; openrouter_session_budget_usd 2.50

## What ran

- **Furigana correctness** — screening (`review_runner.py --pass screening`) over
  10888–11087 (190 entries with files). 6 entries flagged
  (10899, 10927, 10929, 11023, 11062, 11068). Deep pass
  (`--pass deep`, gpt-4.1 + gemini-2.5-pro) on the 6: **5 cleared**, 1 survived
  (11062). On inspection 11062 ex[3] is `お{腹|なか}が{空|あ}いた` — お腹 (おなか),
  where 腹 is correctly read なか; both the screener and gemini-pro misread it by
  ignoring the お prefix (gpt-4.1 did not flag). **All 6 furigana flags = false
  positives** (REJECT), consistent with the known 0–5% screening precision on
  already-polished ranges. No furigana changes.
  - The full 10888–11387 screening pass was stopped early: API latency (~7.5
    s/entry) made the full 500-entry pass impractical within a single
    completable run, so the reviewed furigana frontier was capped at the fully
    covered 10888–11087 block (§A "shrink the range").

- **Glosses/translations/tags** — `review_accuracy.py` over 10888–10947 (60
  entries), 31 flagged, all in the **tags** dimension. Cross-checked every
  flagged tag against `VALID_SEMANTIC` (build/validate_tags.py).

## §4 self-check

Skipped re-review for the 25 migrated entries: in accuracy-review mode, IDs
already covered by §A this run are not re-sent (the review itself was the
check), and the changes are deterministic off-list→in-list taxonomy migrations.

## Adjudication (decisions logged to reviews/decisions.jsonl)

- **APPLY ×25** — off-list semantic tags migrated to the in-list taxonomy
  (correct-by-definition per §A tag policy). Dense pocket in 10905–10934:
  `abstract-concept`→abstract (×9), `people-personality`→personality (×6),
  `food-fruit`/`food-ingredient`/`food-dish`→food, `nature-plant`→plant-general,
  `nature-geography`→geography, `place-description`/`people-description`→descriptive,
  `body-action`→action, `commerce`→business, `group`→society,
  `reasoning`→cognition, `time`→time-general. `modified` timestamps updated.
- **REJECT ×9** — "general too broad/too vague" in-list narrowness nits
  (10891, 10895, 10896, 10897, 10899, 10938, 10945; one aggregated ledger line
  n=7); 10890 formality ("not formal" — entry's own notes call it formal/literary,
  so the label is correct); 10937 food→health (food is defensible for a consumable
  supplement, in-list substitution).
- **REJECT ×6 (furigana)** — screening false positives, one aggregated ledger line.

Validation: 60/60 valid in range; `validate_tags --check-no-new-unknown` clean;
0 off-list tags remain in 10888–10947.

## Always-on capture

- Observation: `[pattern]` dense off-list-tag pocket around 10900 from a 2026-02
  pre-taxonomy batch; adjacent ranges likely hold more → candidate for an
  `unknown-semantic` systemic-fix sweep over 10800–11000.
- No missing-word candidates surfaced this run.

## Spend (OpenRouter ledger)

screening ~$0.10 (190) + deep $0.0563 (6) + accuracy $0.0261 (60) → daily
`spent_usd` 0.364 (cap $5.00). Well under budget.

## Cursors / state

- `cross-model-review/progress.txt` → `next: 11088` (furigana frontier covered
  through 11087).
- `reviews/queue.txt` trimmed of 10888–11087 (14698 → 14595 lines).
- Metrics line appended to `pipeline/metrics-history.jsonl`.

**Next:** accuracy-review resumes at 11088.
