# Routine session — 2026-08-06 (007)

- **Mode:** `systemic-fix`
- **Selector reason:** "systemic-fix: highest scheduler debt among eligible modes"
- **Signals:** candidates 997 (26 "seen in entry"), max entry ID 30453, comprehensive frontier 6809,
  cross-model-review next 27001, unharvested observations 23, open backlog items 21,
  OpenRouter remaining $4.4691
- **Backlog item:** `link-target-baseform-disagreement` (priority 2, severity error, `verify: per-entry`) — **sixth batch**

## Pre-flight

- §0a: `list_pull_requests` returned **no open PRs** — nothing to rescue.
- §0b: no `claude/*` strands to sweep. Lock acquired for `claude/determined-rubin-wrecb1`.

## Detector state

```
DISAGREE 114  (entries 86; resolvable 113; ambiguous 1)   before
DISAGREE 101  (entries 75; resolvable 100; ambiguous 1)   after
dead targets 0 -> 0
```

This batch took **everything outside the documented benign orthographic family**: 13 decision
families, **13 links across 12 entries**. The remaining 101 are the benign family itself.

## Changes — repairs applied

**Class (a): bound suffix mislinked to its free-standing homophone** (new this batch)

| Entry | Link | Was | Now |
|---|---|---|---|
| 00445_kaihou | 開放⟦的⟧ (note collocation) | 03546_teki 敵 "enemy" | **09839_teki 〜的** |
| 02627_gaikou | 外交⟦的⟧な言い方 (ex. 7) | 03546_teki 敵 | **09839_teki 〜的** |

The detector's proposal here (`19356_mato` 的 まと) was **rejected** — its reading does not match the
surface furigana てき — and the correct suffix entry was found by search. The reading filter from
batch 3 works in the negative direction too: it can rule a proposal out without naming a replacement.

**Class (b): kanji-variant pairs, repaired to the entry matching the written surface** (batch-2 rule)

| Entry | Surface / context | Was | Now |
|---|---|---|---|
| 03449_denshi | 電流を{生}む "generate" | 01899 産む "give birth" | **29531 生む** |
| 03585_hyoujou | 表情が{硬}い | 12090 固い | **01116 硬い** |
| 04014_teki | 目薬を二滴{差}す | 02466 挿す "insert" | **17637 差す** |
| 05047_tsukiakari | 月明かりが{差}す | 02466 挿す | **17637 差す** |
| 05480_monosashi | 物差しの{差}し (note glosses it "to apply") | 02056 指す "to point" | **17637 差す** |
| 05864_usugumori | 日が{差}した | 02468 射す | **17637 差す** |
| 05338_tebanasu | 手 + {放}す, etymology note | 19372 離す | **29986 放す** |
| 05653_gacchiri | がっちり{造}られている (ex. 3) | 00481 作る | **17751 造る** |
| 05786_nukunuku | ぬくぬくと{暖}まる | 02095 温まる | **01856 暖まる** |
| 06031_sutoriimingu | ストリーミングで映画を{観}る | 00283 見る | **22793 観る** |

**Base-label repair** (target already correct, label imprecise — third instance after お得 in batch 3
and 一本/一個 in batch 5): `05653_gacchiri` ex. 4, `⟦{作|つく}り→造る：00481⟧` → base label `作る`.

## Left alone, with reasons

- `00215_kenkyo` 控えめ→09160 控え目, `03704_hikkoshi` 見積もり→04699 見積り — same word, variant
  okurigana, and the dictionary has a second entry for each spelling (see below).
- `05051_ginga` 巨大な→02586 巨大 — na-adjective base vs. noun entry; the same accepted relationship
  as the suru-noun rule, which the detector does not yet normalize.
- `00877_ototoi` 一昨年→02895 おととし and `02895_ototoshi` 明後日→00828 あさって — same word under a
  kana headword, and the furigana matches; the proposals (15474 いっさくねん, 00301 みょうごにち) are
  *different readings* and would be wrong.

## §4 self-check

Format/link-only batch, so the furigana screener variant: `review_runner.py --pass screening` over the
12 changed IDs. **Clean — 0 flags** (3 entries screened fresh, 9 already had current screening results).
Est. cost $0.0004, recorded in the ledger as `phase: self-check`; nothing to adjudicate, so no new
`reviews/decisions.jsonl` lines. `validate.py` 30244/30244 valid; `check_link_targets.py` dead targets 0.

## Observations logged

- `[pattern]` bound suffixes mislinked to their free-standing homophone — a deterministic check is
  possible (base is a single kanji that also heads a `〜X` entry, but the target is the free noun).
- `[entry]` **the finding that redirects this backlog item**: the ~79 surviving benign findings are
  mostly a symptom of **near-duplicate entry pairs**, not link errors — 09160/17234 控え目・控えめ,
  04699/05153 見積り・見積もり, 08344/16735 焼き立て・焼きたて, 03937/04474 捻る・捩じる,
  04628/16734 追い掛ける・追いかける, 02586/25544 巨大・巨大な. Draining the rest of this queue is
  `consolidate_entries.md` work, and the planned `--count` ratchet should be armed at the
  post-consolidation floor rather than at 0.
- `[tooling]` `check_link_baseform.py` should accept a na-adjective normalization (strip trailing な
  before the headword-identity test), parallel to the existing suru-noun rule.

## Candidates added

None — every word involved in this batch already has an entry.

## State updated

- `planning/wiki/ideas/backlog-queue.json`: `scope_estimate` 114 → **101**, `scope_measured_at`
  2026-08-06, notes extended with the sixth-batch record. Status stays **open** (the remainder is real,
  but it is now consolidation work rather than link work).
- `planning/wiki/ideas/cleanup-backlog.md`: remaining count updated and a 2026-08-06 batch retro added.
- `pipeline/metrics-history.jsonl`: one line appended (`--mode systemic-fix --changed 12`).
- `pipeline/openrouter-ledger.json`: $0.5313 spent today of the $5.00 cap.

## CI note

The `Validate Entries` workflow did **not dispatch** for PR #3130: ~19 minutes after PR creation,
`get_check_runs` returned `total_count: 0` and `list_workflow_runs --branch claude/determined-rubin-wrecb1`
returned `total_count: 0` — no queued run at all, while the PR itself reported `mergeable_state: clean`.
Every previous routine PR today dispatched within a minute or two, so this is a missed webhook rather
than a slow or failing check. Re-triggered with a follow-up push (`synchronize` event).

Worth flagging for the curator: this PR touches entries 00445–06031, all **below** the comprehensive
frontier (6809), so if it had been left open the next run's §0b sweep would have closed it as
"superseded" and discarded the work. The sweep's max-entry-ID heuristic is sound for polish PRs but
misfires on `systemic-fix` PRs, which touch low IDs by nature. Logged as a `[tooling]` observation.
