## Session: Unified Routine v2 — polish

Date: 2026-07-26
Mode: `polish` (selector reason: "polish: highest scheduler debt among eligible modes")
Selector signals: candidates 1050 (9 "seen in entry"), max entry ID 30124, comprehensive frontier 06639, cross-model cursor 18653, 18 unharvested observations, 7 open backlog items, OpenRouter remaining $4.987

Pre-flight: no open PRs — nothing to rescue (§0a), nothing to sweep (§0b). Lock acquired.

### Priority lane (notes.txt, lines 59–66; file generated 2026-07-24, fresh)

Four of the eight lines in range were eligible; 06484, 06735, 07006, 02006 were skipped as
modified within the last 30 days.

- **00696 元気**: linked the copula て-form `で` in ex5 and the notes' parting phrase to `09496_da`
  (both previously pointed at the particle `00502_de` — wrong homograph); linked `よく` in ex10;
  completed inline links in the FORMS section (`な`/`だ`/`です`/`に`); added `health` semantic tag.
- **06730 書き留める**: linked `ておこう` in ex2 and `を` in the USAGE pattern; removed the
  inaccurate `colloquial` domain tag; added `communication` semantic tag.
- **06978 書き上げる**: linked `なければならない` in ex4 (`27600`); added `仕上げる` (`04664`) as a
  synonym cross-reference with a back-link on 04664 (which had none at all).
- **01005 妻**: notes had three bare `妻` occurrences and a bare `戸籍` — added furigana and linked
  `戸籍` (`12695`); added cross-references to `奥さん` (`02853`) and `嫁` (`03670`), with a back-link
  added on 03670 (02853 already had one).

All four priority entries needed changes, so the rankings are not stale — priorities were not
regenerated and the cursor advances normally.

### Frontier lane (06639 → 06644)

This block is the same zero-inline-link creation batch flagged for 06631–06638 in a previous run:
every entry had links in neither examples nor notes.

- **06639 雑談**: full link coverage across 3 examples and the whole notes field; added
  cross-references to `世間話` (`07021`, already back-linked) and `お喋り` (`01911`, back-link added).
- **06640 鈍感**: full link coverage (3 examples, 5 note lines); added `繊細` (`07607`) as contrast.
- **06641 敏感**: full link coverage (3 examples, 7 note lines); added `繊細` (`07607`) as related.
  Back-links to both 06640 and 06641 added on 07607, which had only `神経質`.
- **06642 不器用**: full link coverage across 6 examples and the notes; fixed the `愛情表現` note line
  to link its two components; `手つき` marked `noentry` and added as a candidate.
- **06643 頑固**: full link coverage across 6 examples and the notes; fixed a stray `}` typo in the
  COLLOCATIONS line (`{汚|よご}れ}`); added `意地っ張り` (`18869`) as a synonym.
- **06644 無口**: full link coverage; corrected the `おしゃべり` cross-reference headword to the
  target's actual form `お{喋|しゃべ}り`; added `饒舌` (`08651`) as an antonym with a back-link;
  changed `formality` from `formal` to `neutral` and `semantic` from `descriptive` to `personality`.

All inline-link targets in every changed file were verified to resolve against the entry filesystem
(`validate.py` does not check this — see the standing `[tooling]` observation).

### §4 self-check (cross-model, `review_accuracy.py`, 15 entries, ~$0.0067)

12 of 15 entries came back clean. Five flags adjudicated: **3 applied, 2 rejected, 0 flagged.**

- APPLY 07607 tags — `quality` is not in `VALID_SEMANTIC`; migrated to `descriptive` (kept `personality`).
- APPLY 08651 tags — entry had no `semantic` tag at all; added `personality`, `communication`.
- APPLY 03670 translation — `嫁と姑の関係` rendered "a wife and mother-in-law"; the 嫁姑 dyad is
  specifically daughter-in-law vs. mother-in-law, so the old wording was ambiguous.
- REJECT 03670 tags — model proposed formality `archaic/derogatory`; not a valid enum value, and 嫁
  for one's own wife is casual/regional rather than archaic. `informal` stands.
- REJECT 03670 translation — model wanted `息子の嫁` as "my daughter-in-law"; "my son's wife" mirrors
  the Japanese compositionally and is not wrong.

### Candidates added

- 手つき (てつき): hand movements, the way one uses one's hands; seen in entry 06642

### Observations logged

- [entry] 気持ち has two live entries (01385 basic / 02485 core) with the same gloss and differing
  headword furigana wrapping — a consolidation candidate that forces every polisher to pick arbitrarily.
- [pattern] The 066xx creation batch has zero inline links in examples *or* notes; a batch-scoped
  sweep of 06600–06700 would clear it far faster than the sequential frontier.
- [pattern] Personality na-adjectives in this batch carry sole `descriptive` where `personality` is
  the accurate in-list tag — a candidate `check_tag_drift.py` sub-check.
- [tooling] Copula て-form `で` after a na-adjective stem is systematically mislinked to the particle
  `00502_de`; the skill should state the rule and a detector could flag the pattern.

### Next cursors

- Frontier: 06645
- Priority lane: line 67
