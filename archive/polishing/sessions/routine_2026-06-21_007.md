# Routine session — 2026-06-21 (007)

- **Mode**: `systemic-fix`
- **Selector reason**: "systemic-fix: highest scheduler debt among eligible modes"
- **Backlog item**: `artifact-missing-target-id` — cross_references / prominent_see_also entries without a target_id
- **Pre-flight**: §0a no open PRs to rescue; §0b sweep closed 0 stranded PRs; routine lock acquired.

## Work done

Detector at run start: 82 target-less refs across 79 entries.

### 1. Filled the 5 newly-resolvable refs (semantic, per-entry verified)

Another curator restock created entries 29368–29383, making 5 previously target-less
refs resolvable by **exact headword+reading match**. Each target entry was read and
semantically confirmed before filling:

| Source entry | ref | → target | type |
|---|---|---|---|
| 06214_butanishinju (豚に真珠) | 猫に小判 | 29383_nekonikoban | synonym (proverb) |
| 12651_tashinkyou (多神教) | 一神教 | 29371_isshinkyou | antonym |
| 12671_kyuuzou (急増) | 急減 | 29369_kyuugen | antonym |
| 13899_bitoku (美徳) | 悪徳 | 29368_akutoku | antonym |
| 16910_koushitsu (硬質) | 軟質 | 29370_nanshitsu | antonym |

`modified` timestamps updated on all 5. Structural change limited to target_id + modified.

After fills: detector 82 → 77, and **all 77 remaining are confirmed non-resolvable**
(referenced word has no entry) — verified by checking each ref's reading+surface
against `build/word_id_lookup.json`.

### 2. Detector convergence fix (tooling)

Implemented the recommendation logged by the 2026-06-19 and 2026-06-20 runs.
`build/check_artifacts.py --issue missing-target-id` now flags **only refs whose
referenced word actually has an entry** (resolvable = some entry shares the ref's
reading AND furigana-stripped surface). Intentional target-less pointers
(homophone notes, antonym/contrast/synonym display labels, transitivity-pair
pointers to words with no entry) are no longer re-surfaced every run. Added
`--include-intentional` to restore the full audit list (77 pointers). Falls back to
the old flag-all behavior if `word_id_lookup.json` is unavailable.

- Default `check_artifacts.py --summary`: **0 instances** (converged).
- `--include-intentional`: 77 (unchanged audit view).

### §4 self-check

Furigana screener on the 5 changed entries (target_id-only change → format check):
**1 flag, rejected (false positive)** — model claimed `大切` furigana was `たいせ`,
but entry 13899 has the correct `{大切|たいせつ}`. Logged to `reviews/decisions.jsonl`;
ledger updated (phase self-check, $0.0006).

### §3 always-on capture

- Candidates added (10): 妊娠する, 創造する, 肯定する, 推薦する, 企画する, 罷免, 悪者, 対義語, 鎮める, 取り付く.
- Observations: 1 `[tooling]` + 1 `[pattern]` line in `polishing/observations.md`.

## State updates

- `backlog-queue.json`: item `artifact-missing-target-id` → status **resolved**, batch_ready false, scope_estimate 0.
- `cleanup-backlog.md`: added the 2026-06-21 RESOLVED update.
- Metrics snapshot appended (entries_changed 5, rejected 1).
- Selector state advanced in §1.

## Outcome

Backlog item **resolved**: 0 resolvable target-less refs remain dictionary-wide, and
the detector now converges instead of re-flagging ~80 permanent intentional pointers.
It re-opens automatically only if a future ref's referenced word gains an entry.
