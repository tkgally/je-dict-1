# Routine v2 — 2026-08-12 run 007 (`polish`)

**Mode**: `polish`
**Selector reason**: "polish: highest scheduler debt among eligible modes"
**Signals**: candidates 170 (7 "seen in entry"), max entry ID 30594, comprehensive frontier 6896,
cross-model review frontier 8851, unharvested observations 6, reviewed entries 586, open backlog
items 32
**OpenRouter**: $0.1443 spent today of the $5.00 cap at start; this run added $0.0046 (self-check
only) → $0.1489

## Pre-flight

- §0a rescue: `list_pull_requests` returned **zero open PRs**. Nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs to evaluate.
- §0b orphan-branch sweep: `list_branches` returned **only `main`**. No orphan `claude/*` branches.
- Lock acquired for session `claude/determined-rubin-r7iluv`.

## Priority lane (notes.txt, lines 80–118)

Priority file regenerated 2026-08-11 (1 day old) — used as-is. Cursor started at line 80.
**Lines 80–110 were all skipped** as modified within the last 30 days — an unbroken run of 31
(logged as a `[pattern]` observation; see below). Four eligible entries were opened, and **all four
needed changes**, so the ranking is not stale and the priority file was not regenerated.

- **02865 女の子** (basic): one naked, furigana-less 女子 in the notes was linked to 03350; added
  two cross-references (少女 related — this fixed an existing one-way link from 03233, and 子供
  related). Entry was otherwise clean.
- **02117 しゃべる** (core): had **no cross-references at all** despite naming three neighbours in
  its notes — added 話す (contrast), 語る (related), おしゃべり (related), with back-links on all
  three. Completed inline-link coverage (こと ×2, 一晩中, 3時間, and the naked おしゃべりな in the
  notes). Retagged `semantic` from the fallback `action` to `communication`.
- **05985 ざわざわ** (core): retagged `semantic` from `descriptive` to `onomatopoeia` (the entry's
  own notes open by calling it an onomatopoeia); added the missing ざわめく cross-reference named in
  the notes, plus its back-link. Link coverage was already complete.
- **06703 振り切る** (core): the heaviest item — **zero inline links across 15 examples and the
  notes**. Added full coverage to all 15 (plus four `noentry` markers), fixed a malformed furigana
  wrapper `{しがらみ}` (braces with no reading), and rewrote the notes: dropped three bullets that
  merely restated the conjugation table, added a USAGE section, and linked every Japanese word.

## Frontier lane (06896–06901)

All six are body-part idiom entries from the zero-inline-link block that run 006 identified as
starting exactly at the frontier. Each got **full inline-link coverage in examples and notes**
(none had a single link before), notes tightened, and cross-references added where the notes
already named the neighbours.

- **06896 肩をすくめる**: 5 examples linked; notes reorganised (gesture meanings, cultural note,
  related gestures); 3 cross-references added (肩を落とす, 肩, 仕方がない).
- **06897 眉をひそめる**: 5 examples linked; added a REGISTER paragraph explaining that 顔をしかめる
  is the everyday equivalent; 3 cross-references (しかめる, 眉, ひそめる); `semantic` `expression`
  → `idiom`.
- **06898 足を運ぶ**: 5 examples linked; notes restructured around register and patterns; 3
  cross-references (運ぶ, 行く contrast, いらっしゃる); `semantic` `expression` → `idiom`. Also
  the one entry the self-check flagged — see below.
- **06899 顔を出す**: 6 examples linked; notes rewritten to document **both** senses (showing up,
  and physically peeking out — the second sense had three examples but no note); 3 cross-references.
- **06900 胸を張る**: 5 examples linked; notes rewritten around the potential form 胸を張れる, which
  is at least as common as the plain form; 3 cross-references; `semantic` `emotion` → `idiom,
  emotion`.
- **06901 腰を据える**: 10 examples linked; notes split into the two uses (committing to a task,
  settling in a place); 3 cross-references.

**Back-links**: 20 neighbour entries received a single reciprocal cross-reference each (and a
`modified` bump). No neighbour was polished further and no recursion was done.

## §4 self-verification

Sent the 10 substantively-changed entries to the independent accuracy reviewer
(`review_accuracy.py --ids … --budget 0.25`, est. $0.0046, logged to the ledger as `self-check`).
**Nine of ten came back clean.** One entry drew two `error`-severity tag flags:

- 06898 足を運ぶ, `politeness: humble` → **APPLIED** (set to `polite`). The phrase is not 謙譲語,
  and in its own examples it most often describes the *listener's* visit — the humility in
  足を運んでいただく comes from いただく, not from 足を運ぶ. The reviewer suggested `neutral`; the
  register is clearly marked, so `polite` is the closest correct value in the keigo enum.
- 06898 足を運ぶ, `formality: formal` → **REJECTED**. The entry's own notes describe it as polite
  and slightly formal, which is what the label says. Per the §A policy a formality flag is applied
  only when the entry's own register description contradicts it.

Both decisions logged to `reviews/decisions.jsonl`. Nothing escalated to the curator.

## Candidates added (5)

- 追いすがる (おいすがる) — seen in 06703
- 追跡者 (ついせきしゃ) — seen in 06703
- しがらみ (しがらみ) — seen in 06703
- スパート (すぱーと) — seen in 06703
- 本腰 (ほんごし) — seen in 06901

Queue: 170 → 175.

## Observations logged (3)

- `[pattern]` The head of `notes.txt` is self-blocking: 31 consecutive lines skipped as
  recently-modified, because the list ranks on note *scores* and cross-reference/inline-link
  polishing does not move that score. Suggests an `--exclude-recent` filter in
  `build/prioritize_polishing.py`.
- `[pattern]` Idiom entries in the 06880–06980 block and their body-part nouns never link to each
  other in either direction — a deterministic, detector-shaped gap covering the whole block.
- `[entry]` 06899 顔を出す: the top-level `gloss` advertises only sense 1 although the definitions
  list two senses; wants a convention ruling before editing.

## §5 metrics

Snapshot appended (mode `polish`, 30 entries changed, 1 flag applied, 1 rejected): entries 30,385;
candidates 175; review queue 10,279; frontier 6,902.

## Cursors

- `polishing/tasks/comprehensive/progress.txt` → `next: 06902`
- `polishing/tasks/comprehensive/priority-cursor.txt` → `line: 119`

Priority list **not** regenerated: all four priority-lane entries opened needed changes.
