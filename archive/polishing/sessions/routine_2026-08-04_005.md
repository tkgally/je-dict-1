# Routine session — 2026-08-04 (005)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Signals**: candidates 986 (12 "seen in entry"), max entry ID 30396, comprehensive frontier 6775,
cross-model-review cursor 25101, unharvested observations 28, reviewed entries 586, open backlog items 17
**Params**: `phase: furigana`, `start_id: 25101`, `openrouter_session_budget_usd: 2.50`

## Pre-flight

- §0a: `list_pull_requests` returned zero open PRs — nothing to rescue.
- §0b: no `claude/*` strands to sweep. Lock acquired for `claude/determined-rubin-6rb0rj`.

## Range worked

**25101–25600** (495 entries with files).

### Pass 1 — furigana screening (`review_runner.py --pass screening`)

Screened **25101–25330** (217 entries) before the pass was stopped at a clean point; 11 flagged (5.1%).
**Every flag was a documented false-positive family**, verified against the source entries:

- okurigana splits — `{手入|てい}れ`, `{鉢植|はちう}え`, `{爆|は}ぜる`, 万引き, 気持ち, 行う (25101, 25109, 25231, 25248, 25297)
- katakana/kanji compounds where furigana covers only the kanji — ラジオ`{体操|たいそう}`, オームの`{法則|ほうそく}` (25172, 25186)
- rendaku — 鳴き`{声|ごえ}`, 羽ばたく (25275, 25297)
- parse failures recorded as flags (25207, 25213, 25268)

Precision **0%**, consistent with the 0–5% measured 2026-06-10/11 over already-polished ranges.
Per §A step 2's known-noise shortcut the **deep pass was skipped** and all 11 were bulk-rejected
with one aggregated §C line. The screening pass was stopped at 25330 rather than run to 25600
because it was returning no signal at ~9 s/entry.

### Pass 2 — accuracy (`review_accuracy.py`)

Reviewed all **487** entries in 25101–25600. 225 flagged (46%), 240 issues:
136 `tags`/error, 99 `tags`/warn, 3 `gloss`/error, 1 `translation`/error.

The 46% flag rate is normally the §A "reviewer noise" threshold, but here it is a **genuine corpus
defect**. Checking the entries directly against `VALID_SEMANTIC` (not trusting the model's claim)
found **201 of 495 entries carrying off-vocabulary semantic tags** — more than the reviewer caught.
Top offenders: `body` 19, `time` 14, `transport` 8, `people` 7, `material` 7, `quality` 6,
`medicine` 6, `relationship` 6, `economy` 6.

Per the 2026-06-11 semantic-tag policy ("a flag that a tag is not in the list is correct by
definition"), all 201 were migrated to in-list tags: `check_tag_drift.py`'s 1:1 map where it
applied, plus **52 per-entry overrides** decided by reading each headword and gloss
(e.g. 呼吸する `body`→`body-internal`, つま先立ち `body`→`movement`, 咀嚼する `body`→`consumption`,
子豚 `animal`→`animal-mammal`, 親鳥 `animal`→`animal-bird`, 温泉旅館 `place`→`travel`).
Where no in-list equivalent existed and another valid tag already carried the sense, the off-vocab
tag was dropped rather than replaced with a vague one. **0 off-vocab tags remain in the range.**

Content fixes (all APPLY, all genuine):

| Entry | Fix |
|---|---|
| 25192 {酒麹} | gloss "sake koji; **sake lees with koji**" → "sake koji; rice koji for brewing sake" — 酒粕 is lees, 酒麹 is the rice koji; the entry's own definition already said so |
| 25509 {撮り直す} | ex2 "could you take one more photo?" → "could you retake it?" — the translation dropped the headword's retake sense |
| 25555 {融解する} | gloss "to melt; to thaw; **to dissolve**" → "to melt; to thaw; to liquefy" (also in definitions[0]) — dissolve is 溶解, a different word |

## §4 self-check outcome

Sent the 3 content-edited entries plus the 52 override-decided tag entries (56 total) to an
independent model. **9 flagged → 2 applied, 7 rejected:**

- **APPLY** 25143 つま先立ち — my own slip: `body-part` for a posture; changed to `movement`.
- **APPLY** 25375 警戒する — pre-existing `weather` tag drawn from an example, not the headword; removed.
- **REJECT** ×4 — a model-invented convention that every suru-verb needs an `action` tag (25326, 25333, 25341, 25546).
- **REJECT** 25455 和食店 (`food` is valid for a restaurant), 25478 住処 (`nature` supported by the habitat sense), 25490 親鳥 (the gloss itself includes "old hen (in cooking)", so `food` is justified) — all in-list breadth nits, rejected per §A step 4.

## Ledger and metrics

- OpenRouter spend this run: **$0.264** (screening $0.0234 / accuracy $0.2161 / self-check $0.0245),
  758 entry-reviews. Daily total now **$0.2854** of the $5.00 cap.
- Metrics line appended: 206 entries changed, 240 flags applied, 18 rejected, 0 to curator.
- `reviews/queue.txt` drained of 25101–25600: 12566 → 12071.

## Candidates and observations

- No new missing-word candidates surfaced (this mode reads reviewer reports rather than composing examples).
- Three observations appended to `polishing/observations.md`: `[pattern]` off-vocab tag density in the
  25xxx block, `[tooling]` screening parse failures recorded as flags, `[pattern]` 0% furigana screening precision.

## Next cursors

- `polishing/tasks/cross-model-review/progress.txt` → **next: 25601**
  (the accuracy dimension covered the full 25101–25600; furigana screening covered 25101–25330 only,
  and was stopped for 0% precision rather than left incomplete by accident)
- Selector state advanced by `routine_next.py` in §1.
