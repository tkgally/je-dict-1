# Routine session — 2026-07-01 #008

**Mode:** accuracy-review
**Selector reason:** "accuracy-review: highest scheduler debt among eligible modes"
**Range worked:** entries 11573–11646 (74 entries), phase = furigana
**Session budget:** OpenRouter $2.50 session / $4.84 remaining daily at start

## What ran

- **Furigana screening** (`review_runner.py --pass screening`, gemini-2.5-flash)
  over 11573–12000, stopped at 11646 to keep the run bounded (throughput
  ~3–8 entries/min). 74 entries covered, **3 flags** (11580, 11600, 11643).
  All three were **false positives**: the entries carry the correct full
  readings (`{食料|しょくりょう}`, `{優勝|ゆうしょう}`, `{入門|にゅうもん}`);
  the model truncated the reading in its own read-out and flagged its own
  truncation. Rejected all three; **deep pass skipped** (known model-misread
  FP family per `reviews/calibration_report.md`).
- **Accuracy review** (`review_accuracy.py`, gloss/translation/tags) over the
  same 11573–11646. 74 reviewed, **14 flagged** (~19%), all `tags`-dimension,
  severity error. Est. cost $0.0325.

## §4 self-check outcome

Accuracy-review mode: per §4 step 3, the §A review IS the independent check for
the entries changed this run (all 5 changed IDs were in the reviewed range).
No separate OpenRouter self-check call. **Result: 5 applied, 12 rejected
(9 accuracy-tags + 3 furigana), 0 flagged to curator.**

## Adjudication (tags)

Every current tag was already in `VALID_SEMANTIC`, so the "not-in-list
migration" rule did not trigger; these were in-list→in-list judgments. Applied
only genuine miscategorizations; rejected `general`-fallback and defensible
in-list choices.

**APPLIED (5):**
- 11587 テクノロジー `general → technology` (exact category for "technology")
- 11588 トレード `leisure → business` (trade/exchange is not leisure)
- 11613 作画 `leisure → art` (drawing/animation is art, not recreation)
- 11624 先進国 `geography → society` (developed nation is socioeconomic)
- 11642 入浴 `consumption → daily-life` (bathing is not consumption)

**REJECTED (9):** 11579, 11582, 11586 (tool defensible for tube), 11601, 11603
(building wrong; general ok), 11617, 11619 (model suggested `neutral`, a
formality value not a semantic tag), 11641, 11645 — all `general`-fallback or
defensible in-list choices.

All 14 decisions + 3 furigana rejects logged to `reviews/decisions.jsonl`.

## Candidates added

None (this range's examples had no un-entered words surfaced during review).

## Observations logged

- `[pattern]` accuracy-review 11573–11646: recurring miscategorization of
  abstract/loanword nouns onto oddly-specific tags (トレード/作画→leisure,
  入浴→consumption); worth a targeted tag-drift sweep on katakana/suru-noun
  entries. Furigana screener FPs all from model truncating its own readout.

## Spend & cursors

- OpenRouter ledger: +$0.037 (screening) +$0.0325 (accuracy) → $0.2276 today.
- `polishing/tasks/cross-model-review/progress.txt` → `next: 11647`
- `reviews/queue.txt` pruned of 11573–11646 (14623 → 14591 lines)
- Metrics line appended to `pipeline/metrics-history.jsonl`.
