# Routine session — 2026-06-26 #002

- **Mode:** `accuracy-review`
- **Selector reason:** "accuracy-review: highest scheduler debt among eligible modes"
- **Params:** phase=furigana, start_id=9815, session budget $2.50 (daily remaining $4.9951)
- **Range worked:** 09815–09849 (35 entries)

## Pre-flight
- §0a rescue: no open PRs — nothing to rescue.
- §0b sweep: no open `claude/*` PRs to sweep.
- Lock acquired for `claude/modest-keller-rpvp3r`.

## Furigana screening (§A step 2)
- The first screening pass over the full 585-entry selector range (9815–10414) ran
  ~14 s/entry and then stalled mid-pass under API rate-limiting; range was narrowed
  to keep the run bounded. Screening results for 9815–9849 (35 entries) were already
  written to `reviews/screening/`.
- 1 flag: **09838** (買い物かご). Verified directly — all furigana readings correct
  (買か, 物もの, 取と, 持も, 入い, 置お, 場ば); the model was confused by truncated gloss
  text in the prompt context, a documented false-positive family. **Rejected; deep
  pass skipped** (screening precision on already-polished ranges is ~0–5%).

## Accuracy review (§A step 3) — glosses / translations / tags
- `review_accuracy.py --range 9815 9849` (gemini-2.5-flash): 35 reviewed, 20 flagged,
  est. $0.0151. All flags were `tags` dimension.
- A full scan of the reviewed range surfaced **21 entries** carrying off-vocabulary
  semantic tags (2 the model itself missed: 09828 `service`, 09834 `medicine`).
- **21 APPLY** — migrated off-list semantic tags to in-list tags per the §A
  semantic-tag policy (off-list = correct-by-definition to migrate):
  - Removed redundant off-list tag, kept existing in-list tag: 09815/09817/09818
    (smartphone→technology), 09816 (internet→technology), 09819 (contract→finance),
    09820/09821/09822/09823 (marketing→shopping), 09824 (restaurant→food),
    09825/09826/09835 (retail→shopping), 09828 (service→communication),
    09831/09832 (bathing→leisure/health), 09833 (delivery→food),
    09834 (medicine→health), 09837 (environment→shopping).
  - Replaced (no in-list tag remained): 09829 (housing/security→building),
    09830 (housing→building).
  - All updated `metadata.modified`. `validate.py` + `validate_tags.py` clean; ratchet passes.
- **1 REJECT** — 09848 〜さん politeness flag: model suggested politeness `neutral`
  (a formality value), confusing the formality/politeness axes; `polite` is correct
  for さん.
- Decisions logged to `reviews/decisions.jsonl` (22 lines: 21 apply + 1 reject;
  plus the furigana reject for 09838).

## §4 self-verification
- accuracy-review mode: the §A review **is** the independent-model check for these
  IDs (§4 step 3 — skip IDs already covered by §A this run). No separate call. The
  changes were pure removal/migration of provably off-list tags.

## Always-on capture (§3)
- Observation logged to `polishing/observations.md`: `[pattern]` — entries
  09815–09849 (a recently-created shopping/tech vocabulary block) widely used an
  off-vocabulary tag set; 21/35 needed migration. Suggests a sweep of the
  surrounding range and a note in the new-entry skill.
- No new missing-word candidates surfaced (review was tag-focused, not deep reading).

## Spend / ledger
- Screening ~$0.01 + accuracy $0.0151 → daily `spent_usd` now $0.03 (cap $5).

## Cursors / state
- `polishing/tasks/cross-model-review/progress.txt` → `next: 09850`.
- `reviews/queue.txt`: removed 9815–9849 (14961 → 14951 lines).
- Metrics snapshot appended to `pipeline/metrics-history.jsonl`.
- Selector state already advanced in §1.

## Next
- accuracy-review resumes at 09850. The selector-suggested range continues to 10414;
  the rate-limiting that truncated this run may recur — keep ranges bounded (~100/run)
  until API throughput recovers.
