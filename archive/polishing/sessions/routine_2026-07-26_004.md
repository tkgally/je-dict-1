# Routine session — 2026-07-26 (004)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Selector signals**: candidates 1051 (10 "seen in entry"), max entry id 30124, comprehensive_next 6645, cross_model_review_next 18653, unharvested observations 22, reviewed entries 586, open backlog items 7
**Params**: `phase: furigana`, `start_id: 18653`, `openrouter_session_budget_usd: 2.50`
**Range worked**: 18653–19200 (548 entries; furigana screening reached 19178)

## Pre-flight

- §0a rescue: `list_pull_requests` returned **no open PRs** — nothing to rescue.
- §0b MCP sweep: no `claude/*` PRs open, so nothing to close.
- Lock acquired for `claude/determined-rubin-45sdt3`.

## §A step 2 — furigana screening

`review_runner.py --pass screening --range 18653 19200 --budget 0.60`

- **437 of 548 entries screened** before the runner's 3000 s wrapper killed it at
  entry 19178. Per §A's resilience clause the per-entry results already written
  were kept, no retry was attempted, and a `[tooling]` observation was logged.
- **49 flags, all rejected — 100% false-positive rate**, consistent with the
  0–5% precision measured 2026-06-10/11 on already-polished ranges. Every flag
  fell into a documented family:
  - **Truncated-token** (the large majority): the model reports a reading as
    incomplete (魅力→みりょ, 正体→し, 姑息→こそ, 強制執行→き, 見破→みや) when the
    entry in fact carries the full correct reading. Verified directly against
    18715, 18719, 18680, 18726, 18996, 18749, 19178 — all correct as written.
  - **Documented variant / compound reading**: 18677 白金 はっきん vs しろかね,
    18692 焦らす じ vs 焦る あせ, 18722 東奔西走 走→そう, 18782 追従, 18801 四隅,
    19084 遺言, and 18990 物悲しい — where the flagged ものがな occurs **only** in
    the notes line that explicitly labels it a variant reading.
  - Individually spot-checked the four flags that were not obviously truncation
    (18990, 19059 四十日, 18917 香典返し, 18802 東アジア); all four correct as written.
- **Deep pass skipped** under §A step 2's known-noise shortcut; bulk-rejected
  with one aggregated §C line (`n: 49`). Deep spend: $0.00 of the ~$0.83 cap.

## §A step 3–4 — gloss / translation / tags

`review_accuracy.py --range 18653 19200` — 548 reviewed, 155 flagged (28%),
$0.2388. The 28% rate is above §A's 20% "reviewer noise" threshold; logged as a
`[tooling]` observation with the causing family identified.

### Semantic tags — 51 entries fixed

A **deterministic scan** of `metadata.tags.semantic` against `VALID_SEMANTIC`
found **51 entries in range carrying off-vocabulary tags**; the cross-model
reviewer had flagged only 32 of them. Per the 2026-06-11 semantic-tag policy
("not in the list is correct by definition"), all 51 were migrated to the best
in-list tag:

| off-vocab tag | n | migrated to |
|---|---|---|
| `time` | 5 | `time-general` |
| `transport` | 5 | `transportation` |
| `body` | 5 | `body-part` |
| `interpersonal` | 4 | `society` / `communication` |
| `thought` | 4 | `cognition` |
| `event` | 3 | `entertainment` / `person` / `abstract` |
| `commerce` | 2 | `shopping` / `business` |
| `crime` | 2 | `action` / `tool`+`law` |
| `state` | 2 | `descriptive` / `existence` |
| 22 singletons | 22 | see diff (`frequency`, `decision`, `cleaning`, `competition`, `career`→`occupation`, `degree`, `death`, `agriculture`→`animal-bird`, `legal`→`law`, `situation`, `difficulty`, `character`→`personality`, `attitude`, `place`, `infrastructure`→`building`, `planning`, `housing`, `animal`→`animal-mammal`, `life`→`daily-life`, `people`→`person`, `space`→`size`, `energy`→`nature`) |

Also fixed 3 **wrong-category in-list** tags (a different class from the
breadth nits rejected below): 18758 健康体 and 18805 空咳 were tagged
`body-part` though neither is a body part (→ `health`), and 18825 渋面 (→
`expression`, `emotion`).

`validate_tags.py` now reports **no unknown semantic tags in 18653–19200**, the
`--check-no-new-unknown` ratchet passes, and
`build/data/unknown_semantic_baseline.json` was regenerated (6600 tolerated
uses across 5361 entries, down from the previous baseline).

### Gloss / translation — 6 applied, 6 rejected

**Applied**
- **18657 安楽** — example 3 illustrated the compound 安楽死 (euthanasia), not the
  headword, and duplicated what the notes already say. Replaced with a genuine
  安楽 example.
- **18675 水深** — "The water depth is deep." → "The water is deep."
- **18783 戦艦** — gloss "battleship; warship" → "battleship" (warship is 軍艦;
  the pairing misleads). Semantic tag `transportation` → `military`.
- **18805 空咳** — example 6 "cleared his throat" → "gave a deliberate cough"
  (sense 2 is a deliberate cough, not throat-clearing).
- **19071 前々日** — gloss "two days before; the day before yesterday (relative)"
  contradicted the entry's own definition, which states 前々日 is *not* relative
  to today unlike 一昨日. Narrowed to "two days before (a specified event or
  date)". Example 2 "by two days in advance" → "at least two days in advance".

**Rejected**
- 18701 やり過ごす — reviewer claimed "to overdo" was the primary gloss; it is
  already third. Model misread.
- 18714 不自由する (×3) — glosses are accurate; "language barrier" is a faithful
  rendering of 言葉に不自由する. Stylistic.
- 18934 霜降り (×2) — semicolon-joined multi-sense gloss is house style, and
  "heathered fabric" is the correct textile term for 霜降りの生地.
- **95 tag flags** bulk-rejected as one family: in-list breadth substitutions
  (`general` → `finance`/`politics`/`art`/`law`/…). `general` is a valid
  terminal tag and §A rejects breadth substitutions between in-list tags.
- **10 formality/politeness flags** bulk-rejected: none was contradicted by the
  entry's own register description, which §A makes the condition for applying.

## §4 self-check

The 51 tag migrations were themselves §A output, so they are skipped by §4's
"already covered by §A this run" rule. The **5 entries whose content I changed**
were re-reviewed independently (`review_accuracy.py --ids 18657,18675,18783,18805,19071`,
$0.0022): 18657 and 18675 came back **clean**; the other three returned tag
flags only — 2 applied (18783 `transportation`→`military`, 18805
`body-part`→`health`, both genuine wrong-category), 1 rejected (19071
formality, notes do not contradict the `formal` label). One pass, one fix
round, no re-verification.

## Ledger and metrics

- OpenRouter spend this run: screening ~$0.1750 (437 entries) + accuracy
  $0.2388 (548) + self-check $0.0022 (5) = **$0.4160**. Daily ledger total
  $0.4357 of the $5.00 cap.
- 63 + 1 decision lines appended to `reviews/decisions.jsonl`
  (55 applies, 160 rejects across 2 aggregated + individual lines).
- Metrics line appended: 58 entries changed, review queue 12480 → 12352.

## Wrap-up state

- `polishing/tasks/cross-model-review/progress.txt` → **`next: 19179`**
  (set to where furigana screening actually stopped, not where the accuracy
  pass finished, so 19179–19200 get a furigana pass next run; their accuracy
  results are already on disk and re-reviewing 22 entries is negligible).
- `reviews/queue.txt`: 18653–19178 removed (12480 → 12352 lines).
- 4 observations appended to `polishing/observations.md` (2 `[tooling]`,
  2 `[pattern]`).
- No candidates added — no missing-entry words surfaced in this mode.
- `reviews/needs_curator.txt` unchanged (0 flags escalated).

## For the curator

The headline finding is methodological: **the deterministic off-vocab tag scan
outperformed the cross-model reviewer on the one defect class that mattered**
(51 found vs 32 flagged, at zero cost and in seconds), while the reviewer's tag
dimension spent 95 of its 128 error-severity flags on breadth substitutions
that policy rejects outright. Both points are written up as observations for
the `wiki` mode to harvest.
