# Routine session — 2026-08-01 (002)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: candidates 1003 (7 `seen in entry`), max entry ID 30297, comprehensive_next 6723,
cross_model_review_next 22334, **unharvested observations 35**, reviewed entries 586, open
backlog items 13. OpenRouter: $0.011 spent today, $4.989 remaining, no session budget.
**Branch**: `claude/determined-rubin-88zl36`

## Pre-flight

- §0a: `mcp__github__list_pull_requests` returned **0 open PRs** — nothing to rescue, nothing to
  sweep. No `claude/*` strands outstanding.
- §0b: lock acquired cleanly.

## Work done

**[F] Harvest — 31 observations cleared** from the two 2026-07-31 accuracy-review runs, the two
2026-07-31 polish runs, the 2026-07-31 systemic-fix run, the 2026-08-01 polish run, and the
previous harvest's own raised notes.

Two observations arrived proposing new backlog items with scope guesses. Both were measured
against the corpus before filing, and the measurements went in opposite directions:

| Observation | Measured | Outcome |
|---|---|---|
| Stale `noentry` markers, "estimated hundreds" | **3,797 of 7,320 (52%)** now resolve; **2,887** safely, **883** are homograph traps | Filed as **Cleanup P35** at ~10× the estimate, with the exclusion that makes it safe |
| "The 067xx block was created with zero inline links — a batch that predates the requirement; suggest `check_link_coverage.py` + a targeted backfill" | Zero-link is 0–1.2% below ID 06000, 62.7% in 06000–07999 (frontier = 06723), ~100% above 08000 | **Closed with no action** — a cliff at the polish frontier, not a batch |

The second had been reported by two separate runs and read as a defect both times. `CLAUDE.md`
states links are never added at creation time, so an unpolished block having none is the
documented design — the observation had inverted the causality. Recorded as a Cleanup
Informational so it is not filed a fourth time.

**Filed this run**:
- **New page `topics/inline-link-integrity.md`** — the run's main content contribution. All six
  inline-link defect/coverage classes with measured scopes in one place, plus the strategic
  finding that 23,294 unlinked entries against a frontier lane advancing 4–8 entries/run cannot
  be closed by the lane (Tooling 49's read-only suggester attacks the ~90% of a linking slot
  that is dictionary lookup).
- **`topics/homographs.md` deepened** — neighbour substitution documented across three
  instruments in one week: the reviewer substituting a homophone (23075 講読 → 購読) and a
  homograph (23166 激高/げきたか → げきこう, 4 error flags; 23182 悪感 → おかん, 5 flags), and the
  corpus's own 87 links pointing at homophones' entries. Prediction that falls out: an entry
  documenting a homograph's minority reading reliably draws a full sweep of error-severity
  flags. Corollary: a rejected flag is still a coverage signal (激高/げきこう genuinely missing →
  C22661; 悪寒/おかん already at 12670).
- **`topics/register.md`** — the datedness gap (23060 外套): `formal` is a register axis,
  datedness a currency axis; the reviewer proposed overwriting a correct `formality` value
  because the schema has nowhere else to put the observation. Curator decision.
- **New Tooling 53** (`review_accuracy.py` empty `description` on all 26 issues in one run),
  **54** (candidate-list noise filter — ~1 usable headword per 10), **55** (detector for contrast
  words in notes prose absent from `cross_references`).
- **Tooling 24 update** — fourth and fifth consecutive post-fix screener runs at zero yield
  (cumulative **0 applied of ~35 flags across five runs**); the shared cursor means its ~2
  entries/min pins the accuracy pass's ~13.
- **Tooling 46 update** — two more blocks: 110 of 118 and 38 of 54 reviewer tag flags were
  already found by the free `VALID_SEMANTIC` diff. Five consecutive blocks agree.
- **Cleanup P31 update** (na-adjective half confirmed; blocked on a curator decision, not
  detection). **Entry follow-ups** for 00759 飛ぶ, 00711 かかる, 06718 かりかり, and the calendar
  family's 9 remaining entries.
- **`backlog-queue.json`**: +4 items (`inline-link-stale-noentry` priority 2 batch-ready,
  `crossref-calendar-family`, `candidate-list-noise-filter`, `crossref-missing-from-notes-prose`);
  scopes resynced — **`unknown-semantic` 5,519 → 5,009**, `semantic-mismatch` 845→865,
  `sole-general` 3,790→3,791, wrappers held at 762.
- **`project/overview.md`** counts refreshed: 30,088 entries / 27,285 general / 117,462 examples
  / **17,068 cross-references** (past 17,000).
- **[H] Metrics trend NOT due**: 404 lines vs the 399 covered by `topics/quality-metrics.md` — 5
  new, below the ≥10 threshold.

## §4 self-verification

**Skipped — no entries were created or modified** (`git status --porcelain -- entries/` empty).
Wiki-only run: markdown plus one JSON index. **OpenRouter untouched, $0 spend this run**; the
ledger is unchanged.

**Verification that was done**: the wiki link lint written for this run initially reported 14
broken anchors. Its own calibration check against three anchors known to work in the repo
**failed**, and the cause was in the lint — it stripped `_` from heading slugs, which GitHub
keeps. After the fix, calibration passed on all three and **all links in the eight changed files
resolve: 0 problems**. Recorded because it is the same failure mode `topics/instrument-defects.md`
case 6 documents, reproduced by a run that had read the page; the calibration step is what caught
it, and acting on iteration 1 would have "fixed" 14 working links into broken ones.

## Always-on capture

- No new candidates (a wiki run reads no entries).
- Observations raised for the next harvest: **none new** — the two adverse metrics carried
  forward from the twenty-eighth refresh (rising review queue, 337-item escalation backlog) are
  restated in the cleared-marker block rather than re-raised, since they need a curator decision
  rather than another harvest.

## Curator queue (a `wiki` run may not modify prompts, skills, entries, or build scripts)

The datedness schema question; the screener retire-or-downsample decision (Tooling 24, five data
points); the P31 adjective FORMS-block decision; the `manage_candidates.py sync` stale-`noentry`
hook (Tooling 19), re-prioritised by the P35 measurement showing 85% of the population is at most
a few months old; the candidate-list filter (54); the `review_accuracy.py` empty-`description`
bug (53); a reviewer-prompt line making the headword's kanji *and stated reading* jointly
authoritative; and the two ownerless adverse metrics.

## Wrap-up

- §5 metrics line appended (mode=wiki, changed=0).
- **No `make build`** — wiki-only run, `docs/` untouched.
- Cursors: none advanced (`wiki` mode owns no entry cursor); the selector already advanced
  `pipeline/routine-state.json`.

**Next**: whatever the selector picks. If `systemic-fix`, `inline-link-stale-noentry` is now the
highest-priority batch-ready item at 2,887 mechanically-fixable links.
