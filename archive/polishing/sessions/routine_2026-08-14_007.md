# Routine session — 2026-08-14 (007)

**Mode**: `systemic-fix`
**Selector reason**: "systemic-fix: highest scheduler debt among eligible modes"
**Selector signals**: candidates 153, seen_in_entry 7, max_entry_id 30653, comprehensive_next 6930,
cross_model_review_next 10687, unharvested observations 15, open backlog items 35
**Backlog item**: `inline-link-stale-noentry` (P35) — stale `noentry` inline links whose word now
has an entry
**Branch**: `claude/determined-rubin-q36sv8`
**OpenRouter at start**: $0.4323 spent today, $4.5677 remaining of the $5 daily cap

## Pre-flight (§0)

- §0a rescue: `mcp__github__list_pull_requests` returned **0 open PRs**. Nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs, so nothing to close.
- §0b orphan-branch sweep: `mcp__github__list_branches` returned `main` only. **Zero orphans.**
- Lock acquired for `claude/determined-rubin-q36sv8`.

## Work done — band 03757–04458 swept

Fifth per-entry batch on the short-base (2–3 character) remainder of P35. **174 A1/A2 pairs,
210 marker instances, across 141 entries.** Every pair was judged against a ±60-character
context window around the marker before substituting `：noentry⟧` with the resolved target ID.

**Zero rejections** — the third consecutive fully clean band. Running false-positive rate across
the 893 hand-verified short-base pairs (2026-08-09 → 2026-08-14): **~0.2%**.

### The one new question: compound-element markers

Four pairs sat on one half of a lexicalized compound, pointing at that half's standalone entry,
where the target's *gloss* did not express the compound sense:

| Marker | In | Target | Target gloss |
|---|---|---|---|
| 込む | 04226 持ち込む, 04229 打ち込む, 04237 押し込む | `00719_komu` | "to be crowded" |
| 思い | 03916 思い浮かぶ | `10248_omoi` | "thought, feeling" |

All four **applied** after opening the target entries: `00719`'s notes carry an explicit
`〜{込|こ}む (compound suffix: into)` line, and `10248`'s notes list 思い出/思い入れ/思い込み under
COMMON COMPOUNDS. This is the 年寄り臭い → `01133_kusai` precedent (2026-08-13), and it now
generalises: **for a compound-element marker, the test is whether the target entry's notes
document the compound role, not whether its gloss expresses it.** Gloss-only screening would have
rejected all four. Recorded in the backlog page and the queue item's notes.

### Families verified as groups

Morphological-decomposition notes in compound-verb entries (取り除く = 取る + 除く, 振り返る =
振る + 返る, 再生 = 再〜 + 生); kendo/karate equipment and scoring terms (小手/組み手/面); Tokyo
district names used as places (上野/浅草/新宿/成田); medical term lists
(心不全/狭心症/心電図/弁膜症/見当識); plant and food compounds (甘栗/毬栗/白桃/甘柿/芍薬/藤棚);
katakana loanwords whose target entry carries the needed sense (ツボ "the right spot", オール
"oar", エース "top player") — the same shape as 2026-08-13's ベスト/カード family.

`04379` 銀杏 is a clean demonstration of the detector's class-R reading test: the entry documents
both readings (いちょう the tree, ぎんなん the nut) and only the `{銀杏|ぎんなん}` marker was
linked to `28490_ginnan`.

## §4 self-verification

`build/review_accuracy.py --ids <36-entry stratified sample of the 141 changed> --budget 0.25` —
every fourth changed entry in ID order. Reviewed 36, flagged 13 entries / 17 error-severity
issues, est. **$0.0162**, logged to the ledger with phase `self-check`.

**No flag concerned a link target** — the fifth consecutive run with that result. Every flag was a
pre-existing gloss or tag defect on an entry this sweep happened to touch.

**11 applied, 6 rejected, 0 escalated.**

Applied (all tag defects the deterministic detectors also track):

| Entry | Fix |
|---|---|
| `03922` 削る | formality `formal` → `neutral` |
| `03989` 武士 | sole-`general` semantic → `history` |
| `04077` 火傷 | semantic `body-part` → `health` (a burn is an injury, not a body part); formality `formal` → `neutral` |
| `04210` 親類 | sole-`general` → `family` |
| `04222` 取り除く | formality `formal` → `neutral` |
| `04267` 眉 | sole-`general` → `body-part` |
| `04369` 花弁 | sole-`general` → `plant-flower` |
| `04379` 銀杏 | sole-`general` → `plant-tree` |
| `04407` 笹 | sole-`general` → `plant-general` |
| `04419` 認知症 | sole-`general` → `health` |

Rejected:

- `03989` 武士, `04210` 親類, `04369` 花弁 (formality `formal` → `neutral`): each entry's own notes
  state the word *is* the formal member of its set — 武士 vs 侍, 親類 vs 親戚, 花弁(かべん) vs
  花弁(はなびら). Per §A, a formality flag applies only when the notes contradict the label; here
  they support it.
- `03888` 芯 (`tool` → `general`/`abstract`): an in-list narrowness nit, and `tool` fits pencil
  lead well. §A rejects in-list substitutions of this kind.
- `04026` 〜発 (gloss "combines two distinct meanings"): the entry already has two numbered
  definitions with separate glosses; the reviewer only saw the summary `gloss` field.
- `04452` 剣道 (`leisure` → `sports`): in-list lateral substitution, not a clear error.

All 17 decisions logged to `reviews/decisions.jsonl` with `src: self-check`.

## §3 capture

No new candidate words (this mode reads existing markers rather than encountering undefined
vocabulary). Four observations appended to `polishing/observations.md`:

- `[pattern]` the compound-element rule above.
- `[tooling]` `03995_atena` has a katakana headword wrapped in furigana braces with a hiragana
  reading (`{ラベル|らべる}`) — no checker flags furigana supplied where none is wanted. Possible
  cheap detector cut; scope unmeasured.
- `[pattern]` `formality: formal` mis-set from a *neighbouring* word's register note (03922, 04077,
  04222 all show it) — possibly mechanically detectable.
- `[entry]` sole-`general` tags are dense in 03900–04450: 7 of 36 sampled (~19%), all with obvious
  in-list destinations. Worth a targeted P13 pass.

## §5 metrics

`python3 pipeline/metrics_snapshot.py --mode systemic-fix --changed 141` → appended
`{"ts": "2026-08-14T18:24:09Z", "mode": "systemic-fix", "entries_changed": 141, "flags_applied": 11,
"flags_rejected": 6, "flags_to_curator": 0, "entries_total": 30444, "candidates": 153,
"review_queue_len": 10526, "observations_lines": 1386, "comprehensive_next": 6930,
"openrouter_spent_today_usd": 0.4485}`.

## State updated

- `planning/wiki/ideas/backlog-queue.json` — `inline-link-stale-noentry` scope 920 → **751**,
  `scope_measured_at` 2026-08-14, cursor now "everything below entry 04459 is swept for A1+A2",
  band notes appended. Item stays **open** (`verify: per-entry`).
- `planning/wiki/ideas/cleanup-backlog.md` — P35 section gains a 2026-08-14 subsection with the
  compound-element rule, the verified families, and the updated false-positive rate.
- OpenRouter ledger: **$0.4485** spent today of the $5.00 cap ($0.0162 this run).

## Next

Resume P35 at the lowest remaining entry_id in `check_stale_noentry.py --mechanical --json`
(entries from **04459** up; **751 pairs / 865 instances** left, all short-base).

## For the curator

Nothing needs you. `reviews/needs_curator.txt` was not appended to this run.
