# Routine v2 session — 2026-07-27 (006)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Selector signals**: candidates 1,041 (seen-in-entry 17), max entry ID 30,164, comprehensive frontier 6,661, cross-model-review cursor 19,951, **unharvested observations 21**, reviewed entries 586, open backlog items 7
**OpenRouter**: $0.1858 spent today, $4.8142 remaining, no session budget (mode does not call the API)
**Branch**: `claude/determined-rubin-1gmnrx`

## Pre-flight (§0)

- **§0a rescue**: `mcp__github__list_pull_requests` returned **0 open PRs** — nothing to rescue.
- **§0b sweep**: no `claude/*` PRs open, so the MCP strand sweep was a no-op. (`pipeline/sweep-stranded-prs.py` not run — it 403s in this environment by design.)
- Branch level with `origin/main` (0 ahead / 0 behind) after `git fetch origin main`.
- Lock acquired: `ACQUIRED routine.lock for session 'claude/determined-rubin-1gmnrx'`.

## Work performed

Wiki-mode run per `planning/maintain-knowledge-base.md`: the observation harvest plus three
additional activities (H, A, E) and a backlog-queue sync.

### Harvest (activity F) — 17 observations, all cleared

Sources: the 2026-07-27 routine polish run (00908–07441 priority lane, 06650–06655 frontier),
the systemic-fix run (22000–23499 wrapper sweep), and the accuracy-review run (19701–19950).

| Destination | What was filed |
|---|---|
| **Cleanup new P28** | Mixed bullet markers (`・` vs `- `) inside `notes` — measured this session at **18,272 line-initial instances across 2,524 entries** (~1/12 of the dictionary). Doubly-anchored normalization rule, sequenced behind a sample check. |
| **Cleanup P20 update** | 19701–19950 off-vocab band at **124/250 entries (49.6%)**, 143 occurrences / 83 distinct tags; the ~50 safe 1:1 renames belong in `TAG_MIGRATION` (covers 9), only the ~7% residue needs a model. |
| **Cleanup P17 update** | Seventh formality sub-family — 〜甲斐 nouns auto-tagged `informal` (06653/06654); a morphological cluster, so an unusually cheap detector cut. |
| **Cleanup P21 update** | Zero-link band unbroken through **06655**; `・` bullets co-occurring, cross-linked to P28. |
| **Tooling new 38** | `review_accuracy.py --ids` overwrites `reviews/accuracy/{id}.json`, so §4 destroys the §A record for exactly the entries that changed. Fix: `--phase` suffix. |
| **Tooling new 39** | Cross-reference-pair tag consistency check (06528 連勝 `formal`/`action` vs 06529 連敗 `neutral`/`leisure`) — free, and a disagreement proves one of two entries wrong. |
| **Tooling new 40** | Conjugation-vs-headword invariant: 22070 走り続ける's whole-word wrapper doubled the stem in all 33 generated forms, validating cleanly since 2026-04-04. |
| **Tooling 8 update** | No-pipe brace spans measured dictionary-wide: **887 instances / 616 entries**, with the notation reject family documented. |
| **Tooling 24 update** | A 102-entry screen at 13 flagged / **0 applicable**; ~5× slower than `review_accuracy.py` and throttling it when concurrent; two deterministic fixes specified. |
| **Tooling 11 update** | Second consecutive cycle of a polish run improvising a link checker and finding real dead links (00494_aru + six wrong IDs) while `validate.py --id` reported valid. |
| **Tooling 6 update** | The ~50-rename `TAG_MIGRATION` extension + a free `domain-tag-in-semantic-slot` sub-check (19769/19782/19798/19814/19847). |
| **Tooling 19 update** | Four more dead `noentry` markers (03754→28285; 06529→29125/29124/29129) + 03682 百万人 as a splittable compound. |
| **Tooling 20 update** | The exact heading strings behind scorer bug #2 ("SIMILAR WORDS:", "REGIONAL USE:") and why widening the matcher and documenting the headings are complements. |
| **Tooling 23 update** | Candidate pool quantified: **~1,030 of 1,044** are corpus residue in four decidable families. |
| **Tooling 34 update** | Fourth report; `CLAUDE.md` attribution **re-verified false** (`formality_variant` occurs only at `prompts/comprehensive_polish.md:107`). |
| **Entry Follow-ups** | 03794/12672 悪口 (variant-reading consolidation); 08169/17946 中火 (17946 carries なかび, the reading of 中日). |
| **Already filed** | The P9 no-pipe + over-wrapped-headword findings were written into Cleanup P9 by the observing systemic-fix run; this harvest added only their tooling counterparts. |

### Activity H — twenty-sixth metrics refresh

`topics/quality-metrics.md` refreshed for runs 354–369 (16 new lines, 353→369 runs,
~9,923 adjudicated flags; window: 790 flags — 448 applied / 340 rejected / 2 escalated;
452 error / 338 warn).

- The 26-run **escalation-free window ends at 2 flags** (18554 訓読 sense split; 06637 tactile
  tag with no in-list target) — incidence, not volume, so not a return to the runs 104–118 pattern.
- **`tags` apply-among-decided 71.1%** (413/168), highest since runs 313–327, on the 19701–19950
  off-vocab cohort. By source: accuracy 70.9%, self-check 42.4%, furigana screening 1.6%.
- **Furigana screening 2.1%** (3/143) — a fourth consecutive refresh with a measured value in the
  1–2% band.
- Entry total 29,895 → **29,955**; candidates 1,063 → 1,041; frontier 6,631 → **6,661**; review
  queue **new floor 12,352**; OpenRouter daily peak **$0.856** (new high, 17% of cap).
- Detector depths 2026-07-27: `furigana_format` 875/587 entries (from 1,032/708), `artifacts` 12,
  `tag_drift` 10,948 (unknown-semantic 6,325, sole-general 3,790, semantic-mismatch 824);
  `needs_curator.txt` 61 lines.

### Activity A — sync

`project/overview.md` counts refreshed (29,955 entries / 27,152 general, tier split
801 basic / 1,982 core / 27,152 general / 20 pending backfill).

### Activity E — lint

Re-verified the recurring `formality_variant` claim by grepping `CLAUDE.md`, `prompts/`,
`.claude/` and `build/schema.json`: it occurs in exactly one place. The `CLAUDE.md`
attribution circulating in the observation stream is false for the second cycle.

### Backlog-queue sync

Added `furigana-no-pipe-span` (needs-detector, scope 887) and `notes-bullet-nakaguro`
(needs-decision, scope 18,272); `updated` → 2026-07-27. JSON re-validated after write.

## §4 self-verification

**Skipped — no entries created or modified.** `git status --porcelain -- entries/` is empty;
this run touched only `planning/`, `polishing/`, and `pipeline/` files. OpenRouter was not
called, so the daily ledger is unchanged at $0.1858.

## §5 metrics

`pipeline/metrics_snapshot.py --mode wiki --changed 0` appended.

## Cursors / state

- `pipeline/routine-state.json` — advanced by the selector in §1.
- No polishing cursors touched (wiki mode does not advance the polish or review frontiers).
- `polishing/observations.md` — 17 observations removed, replaced by the dated harvest summary.

## Routed to the curator

1. `prompts/comprehensive_polish.md:107` names `formality_variant` and `transitivity_pair`,
   both rejected by `build/schema.json` — **fourth consecutive cycle**. One-line fix either way.
2. The exact expected note headings should be documented in the `vocabulary-notes` skill
   (Tooling 20) — a `wiki` run may not modify skills.
3. Both Entry Follow-ups (悪口, 中火) turn on a reading/headword decision that changes an
   entry's identity, so neither is a Routine-side call.

## Notes for the next run

- Nothing left unharvested in `polishing/observations.md`.
- The cheapest outstanding work identified this window is all deterministic and already filed:
  the `TAG_MIGRATION` extension (Tooling 6) would take the 19xxx off-vocab band off the
  OpenRouter budget entirely, and the `validate.py` link gate (Tooling 11) has now caught two
  consecutive polish runs introducing dead links.
