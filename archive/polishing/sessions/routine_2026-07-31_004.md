# Routine session — 2026-07-31 (run 004)

**Mode**: `systemic-fix`
**Selector reason**: "systemic-fix: highest scheduler debt among eligible modes"
**Signals**: candidates 1006 (3 "seen in entry"), max entry 30278, comprehensive next 6713,
cross-model-review next 22276, unharvested observations 16, open backlog items 13.
**Backlog item worked**: `link-target-baseform-disagreement` — "Inline links whose declared
entry_id resolves, but not to the link's own base form (real entry, wrong word)".

## Pre-flight

- §0a: no open PRs — nothing to rescue.
- §0b: no `claude/*` strands to sweep. Lock acquired.

## What was done

### 1. Detector built and committed (§B step 2)

The item's `detect` was a prose rule with no implementation, so the run built one:
**`build/check_link_baseform.py`** (read-only, never modifies entries). Same CLI shape as its
sibling `check_link_targets.py`: `--summary`, `--json`, `--count`, `--by-base`, `--resolvable`,
`--ambiguous`, `--entry-ids`, `--range`.

Baseline over the whole dictionary — 265,173 links:

| | |
|---|---|
| agree | 256,094 |
| `noentry` | 7,310 |
| no lookup hit | 870 |
| dead target | 0 (owned by `check_link_targets.py`) |
| accepted — headword identity | 227 |
| accepted — する-verb → noun | 267 |
| accepted — affix lookup key | 0 |
| **DISAGREE** | **405** (272 entries; 393 resolvable, 12 ambiguous) |

The item specified two normalizations (affix `〜`, する-noun). A third was needed and turned out to
subsume the first: comparing the base form against the **declared entry's own headword** — split on
`／`, tilde stripped — accepts 227 links and drops the affix path to zero, because normalizing
`〜ころ` → `ころ` catches what the tilde-prefixed lookup key was meant to catch. The affix branch is
kept as a lookup-key fallback. The item's 418 estimate held (405 on the current corpus).

### 2. Triage: the queue is two families, only one is a defect

- **(a) Wrong word** — base and declared headword are different words sharing a reading.
  `機能`→`昨日`, `状況`→`上京`, `電気`→`伝記`, `性格`→`正確`, `福祉`→`副詞`, `会社`→`外車`.
- **(b) Benign orthographic indexing** — the declared entry *is* the same word under a kana
  headword or a variant spelling: `頃`→`〜ころ` (24), `上げる`→`あげる` (18), `通り`→`どおり` (5),
  `焼きたて`→`焼き立て` (4). Repairing these would be a regression. This is exactly why the item
  was filed `verify: per-entry`, and it is the reason the detector reports rather than fixes.

### 3. Batch applied: the compound-homophone slice (87 links / 64 entries)

Bounded to the slice that is entirely family (a) and entirely unambiguous — base **and** declared
headword both all-kanji, 2+ characters, exactly one lookup candidate. **Every one of the 87 was
read in its own sentence/notes context before applying**; every repair was 1:1.

Families repaired (count → declared target that was wrong → correct target):

| base | was linked to | corrected to | n |
|---|---|---|---|
| 性格 | 03372 正確 | 03371_seikaku | 6 |
| 急速 | 02587 休息 | 02634_kyuusoku | 4 |
| 見方 | 03743 味方 | 10072_mikata | 4 |
| 成長 | 00455 生長 | 03290_seichou | 3 |
| 人工 | 01618 人口 | 11573_jinkou | 3 |
| 背景 | 12790 拝啓 | 28348_haikei | 3 |
| 政策 | 04320 制作 | 10070_seisaku | 3 |
| 就職 / 医療 / 協議 / 人名 / 通貨 / 好調 / 起動 / 行動 | 修飾 / 衣料 / 競技 / 人命 / 通過 / 校長 / 軌道 / 講堂 | — | 2 each |
| 46 further one-off pairs | incl. 機能→昨日, 状況→上京, 電気→伝記, 福祉→副詞, 会社→外車, 結婚式→結婚 | — | 1 each |

Entries changed: 00099, 00102, 00105, 00114, 00196, 00198, 00200, 00201, 00205, 00214, 00230,
00365, 00370, 00439, 00441, 00442, 01346, 02035, 02404, 02503, 02530, 02628, 02629, 02630, 02632,
02773, 02775, 03872, 03876, 04164, 04168, 04419, 04475, 04478, 04484, 04486, 04679, 04840, 04868,
04872, 04873, 04874, 04875, 04876, 04878, 05297, 05650, 05653, 05671, 05777, 05779, 05787, 05788,
05789, 05790, 05791, 05798, 05813, 05979, 05984, 06054, 06090, 06091, 09586.

After: **405 → 318 disagreements** (223 entries). `check_link_targets.py --count` still 0;
`validate.py --range 0 9999` clean at 9,878/9,878.

## §4 self-check outcome — **1 applied / 26 rejected / 0 flagged**

`review_accuracy.py --ids <64> --budget 0.40` → 64 reviewed, 25 flagged, est. $0.0282.

**Zero findings on the dimension this run changed.** The reviewer raised 25 `tags` and 1
`translation` issue, none of them about a link target — which is itself the finding: this defect
class is invisible to the accuracy instrument (logged as a `[pattern]` observation).

- **APPLY (1)** — 04874 アプリケーション `formality: informal` → `neutral`. Policy-compliant: the
  entry's own notes say the clipped form アプリ is used "in casual speech", which contradicts
  `informal` on the full form.
- **REJECT (24, aggregated)** — semantic-tag flags. `validate_tags.py` confirms **none** of the
  flagged entries carries an off-`VALID_SEMANTIC` tag, so all 24 are in-list narrowness
  substitutions ("replace `general` with `society`"), which §A step 4 says to reject.
- **REJECT (1)** — 00196 箇所 `formal`: not contradicted by the entry's own notes.
- **REJECT (1)** — 00102 examples[1] translation of 首を捻って考えた. The literal "I tilted my head
  and thought about it" is the right choice for an entry about 捻る; the idiomatic rewrite is a
  stylistic preference.

All four decisions logged to `reviews/decisions.jsonl` (one aggregated line for the tag family).
Ledger updated: `self-check` phase, 64 entries, $0.0282 → daily spend **$0.2466** of $5.00.

## Observations logged (4, in `polishing/observations.md`)

- `[tooling]` detector shipped; headword identity subsumes the affix normalization.
- `[pattern]` the disagreement population is two families, only one a defect.
- `[pattern]` the compound-homophone class is invisible to every semantic instrument — argues for
  a `--count` ratchet once the queue is triaged.
- `[tooling]` `review_accuracy.py` returned an empty `description` on all 26 issues this run.

Candidates added: none (no entry-less words encountered; the run touched link metadata only).

## Backlog state (§B step 5)

`link-target-baseform-disagreement` — **still `open`**, `scope_estimate` 405 → **318**,
`scope_measured_at` 2026-07-31, `detect` replaced with the real command, `filter` rewritten as the
(a)/(b) triage rule, `batch_ready: true`. Prose page `ideas/tooling-backlog.md` item 11 updated
with the shipped-detector section, the three findings, and the ratchet recommendation.

**Next batch**: take the kanji-variant verb pairs one base form at a time — `立てる`→`建てる` (11),
`治る`→`直る` (7), `合う`→`会う` (6), `越える`→`超える`, `量る`→`測る` — each needs the example
sentence read for sense. Leave family (b) alone; the 12 ambiguous findings belong to the curator.

## Cursors

None of the mode cursors advance for `systemic-fix` (no frontier). Selector state advanced in §1.
