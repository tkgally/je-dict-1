# Routine session 2026-06-21_004 — `wiki` mode

**Mode**: `wiki` (selector reason: "wiki: highest scheduler debt among eligible modes")

**Selector signals**: candidate_count 1268, seen_in_entry_count 0, max_entry_id 29389,
comprehensive_next 6222, cross_model_review_next 8038, unharvested_observations 14,
reviewed_entries 296, open_backlog_items 8. OpenRouter remaining $4.7671 (no spend this run —
wiki mode makes no model calls).

**Pre-flight**: §0a found 0 open PRs to rescue. `sweep-stranded-prs.py`: 0 stranded
(main `next: 06222`). `routine_lock.py acquire` exited 0 (lock held cleanly).

## Work done (3–4 activities)

### F — Observation harvest
Processed all unharvested observations from the 2026-06-20 routine(polish) session and the
three 2026-06-21 accuracy-review / polish / new-entries runs. Filings:
- **Cleanup P20 update** — new 7815–8037 cohort 73% out-of-taxonomy (163/223); 120 entries
  still invalid; enumerated 1:1-mappable migration families; recommended a 7000–8500
  systemic-fix sweep. (The single most significant new finding this period.)
- **Tooling item 6 update** — concrete `check_tag_drift.py` migration-map expansions for that sweep.
- **Tooling new item 26** — embed the valid `formality`/`politeness` enums in the
  accuracy-review prompt (formality analogue of the resolved item-14 gap).
- **Cleanup P21 update** — zero-inline-link band unbroken ~06150→06214+; 06200–06250 backfill block.
- **Tooling item 20 update** — eighth 6/6 priority-lane no-op.
- **Tooling item 23 update** — fallback lane skipped two runs running; run `clean_up_candidates_list.md`.
- **Entry Follow-ups** — 00304_nandemo sense 3 dubious-usage review.
- Reinforcement-only: furigana-screener ~0% precision (item 24), candidate-junk (item 23 / Open Issues).
- Pruned all observations from `polishing/observations.md`; harvest summary recorded there.

### H — Metrics trend (seventh refresh, due: 14 new lines, 89→103 runs)
Refreshed `topics/quality-metrics.md`: new run-by-run tables (rest of 2026-06-19, full
2026-06-20, partial 2026-06-21), recomputed all-time tables and a new runs-90–103 period
column from `reviews/decisions.jsonl` (window apply=164 / reject=250 / flag=1, verified
against the metrics-history flag sums). Headline: `tags` 49.4% (range-state thesis fourth
consecutive period, on the densest contamination yet); review queue reached its structural
floor (15,298–15,436, net −33). Rewrote Finding §5 and the queue implication accordingly.
No metric moving the wrong way → no new `[pattern]` observation.

### E — Lint
Refreshed "Last updated" on cleanup-backlog, tooling-backlog, entry-followups,
quality-metrics, and the wiki index; new cross-references resolve.

## §4 self-verification
N/A — wiki-only run, **no entry files changed** (markdown only). No model calls, no ledger spend.

## §5 metrics
`metrics_snapshot.py --mode wiki --changed 0` appended successfully.

## Wrap-up
- No `make build` (wiki-only run — markdown changes don't touch `docs/`).
- Next cursor: n/a (wiki mode); selector already advanced `routine-state.json` in §1.
