# Routine session — 2026-08-08 — mode: `wiki`

**Branch**: `claude/determined-rubin-8ihgsg`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: 20 unharvested observations · 21 open backlog items · comprehensive frontier 06826 · cross-model-review next 27646 · 985 candidates (14 "seen in entry") · max entry ID 30493
**OpenRouter**: $0.00 spent today of the $5.00 cap; **$0.00 spent this run**

## Pre-flight (§0)

- **§0a rescue**: `mcp__github__list_pull_requests` (state `open`) returned **0 open PRs**. Nothing to rescue.
- **§0b stranded-PR sweep**: no open `claude/*` PRs to consider. No-op.
- **§0b orphan-branch sweep**: `mcp__github__list_branches` returned **`main` only** — zero `claude/*` branches on origin, so no orphans, no `prune-branch` lines added to `reviews/needs_curator.txt`.
- **Lock**: acquired cleanly (`routine_lock.py acquire`, exit 0).
- Branch was level with `origin/main` at `7695f683d`.

## Work performed (§2, mode `wiki`)

Followed `planning/maintain-knowledge-base.md`. Activities: **[F]** harvest, **[A/F]** three corpus measurements, **[C]** deepen `topics/furigana-wrapper-anomalies.md`, **[E]** index/log maintenance.

### Harvest

All **20** loose observations cleared from `polishing/observations.md` — from the 2026-08-07 polish run (`claude/determined-rubin-rltni4`), the 2026-08-07 accuracy-review run (`claude/determined-rubin-1nnj89`), and the 2026-08-07 priority+frontier polish run. Three premises were measured against the whole corpus before filing; **all three changed their item, and one retired it.**

### Measurement 1 — Tooling 8's two June rules, sized separately for the first time

| Rule (proposed 2026-06-17, never sized) | Measured | Disposition |
|---|---|---|
| (b) fields with unequal `{`/`}` counts | **34 instances / 33 entries** (20 notes, 14 examples) | **Ship** → new Cleanup **P45** + queue `furigana-unbalanced-braces` |
| (a) `{…}` spans containing no `\|` | **931 instances / 623 entries**, ~100% false | **Retire** |

Rule (b) is visible on the live site: `08385` renders "**ぎ} tends to be used for**" in English prose. `04471` is worse than cosmetic — `かき{混→かき{混：noentry⟧|ま}ぜ` interleaves a furigana wrapper and an inline link so neither parses; that one needs re-authoring, not a brace fix.

Rule (a) collides with an **undocumented convention**: braces double as **mention-quotes** around a word, reading, or character under discussion. Decomposition: 855 kana mentions in notes prose · 168 grouping wrappers (`{お{正月|しょうがつ}}`) · 54 kanji mentions · 22 inside `⟦…⟧` link surfaces · 7 `{WO}`/`{NI}` placeholders. The obvious repair ("flag only spans with kanji") also fails — 00319 exists to contrast 匂 and 臭, which share the reading にお, so adding readings would erase the note's own point. Convention documented for the first time in `topics/furigana-wrapper-anomalies.md`.

### Measurement 2 — the 27447 wrong-reading wrapper class

Proposed as a detector split; measures at **5 instances corpus-wide** against 339 cosmetic (27447 itself already fixed by the observing run). They are three shapes, not one: 2 genuine wrong readings (23394 `{カキ|がき}`, 28929 `{に|み}`) and 3 empty reading slots (`{X|}` — 00961, 23799, 25376). The most serious, **28929, is a dropped kanji** (苦悶に満ちた声 lost the 満 from `{満|み}ちた`), which the proposed category would have filed as a cosmetic kana wrapper. Filed as a fix-on-sight list in Entry Follow-ups, not a detector.

### Measurement 3 — stale-`noentry` affix tilde forms

**159 instances / 47 distinct bases** resolve only via `〜X`/`X〜` (against 3,536 bare-headword and 3,686 unresolved) — essentially all of it the **28xxx affix cohort** created after the entries referencing those morphemes. Filed as queue item `noentry-affix-tilde-forms` with **`status: blocked`**: applying it writes bare-label links at tilde-headword targets, exactly what `check_link_baseform.py` flags, so Tooling 83 must accept affix-tilde normalization first or the sweep manufactures 159 new findings.

### Filed this run

- **New**: Cleanup **P45**; Cleanup Informational (brace mention-quote convention); Tooling **84** (`RATE_LIMIT_INTERVAL = 6.0` bounds accuracy-review coverage — ~6.6 vs ~19 entries/min, concurrent, no throttling, $0.31), **85** (§A 20%-noise heuristic misreads tag-contaminated blocks: 41% blended → gloss 3.4% / translation 0.9% after removing off-vocab tag flags), **86** (Hangul-outside-loanword-etymology validator line, scope 1).
- **Queue items**: `furigana-unbalanced-braces` (open, 34), `noentry-affix-tilde-forms` (blocked, 159), `reviewer-flag-rate-carve-out` (needs-decision).
- **Updates**: P35, P42 (8-of-8 and 7-of-7), P43 (7 of 7 zero-link at the frontier, ~4× cost), Tooling 19 (filed a 5th and 6th time), Tooling 76.
- **Entry Follow-ups**: the five mismatch wrappers; 00823/01525 若い (also the entry missing its conjugation table for the same over-wrapped headword); 02005/02458 にくい; 30493 百十番's figure-form headword.

### Activity H (metrics trend) — not run

5 new metrics lines since the thirty-second refresh (454 → 459), below the ≥10 threshold.

## §3 Always-on capture

No new candidate words encountered (wiki mode touches no entry examples). No new observations logged — this run *drains* `observations.md` rather than adding to it.

## §4 Self-verification — skipped (no entries changed)

`git status --porcelain -- entries/` was empty for the whole run. Markdown and one JSON index only, so there was nothing for an independent model to check. **OpenRouter untouched: $0.00 this run**, ledger unchanged.

## §5 Metrics

Appended: `mode=wiki, entries_changed=0, entries_total=30284, candidates=985, review_queue_len=11004, observations_lines=816, comprehensive_next=6826, openrouter_spent_today_usd=0.0`. `metrics-history.jsonl` now **460** lines.

## §7 Wrap-up

- **No `make build`** — wiki-only run; `docs/` untouched (per §6).
- Cursors: none to advance (`wiki` mode advances only `pipeline/routine-state.json`, already persisted by the selector in §1).
- Lock released at the end of the run.

## Curator items

1. **Proper-noun scope** (C22806 夏目漱石, C22820 成田) — **second filing**. The dictionary holds no person or place names, so these can never be created, and every new-entries run will keep re-picking them until a policy exists in the `find-candidates` skill.
2. **Figure-form headwords** — 30493 was created as {百十番|ひゃくとおばん} because an inline link referenced it that way; the standard orthography is 110番. This is the same question as the already-routed number+counter one (C22795 三十人) and should be answered once for both.
3. Carried forward: the taxonomy question (spatial/positional + document-type tags, 322 instances); the frontier-versus-growth rate (fifth filing).
