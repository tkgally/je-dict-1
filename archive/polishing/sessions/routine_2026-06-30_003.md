# Routine session — 2026-06-30 (003)

- **Mode:** accuracy-review
- **Selector reason:** "accuracy-review: highest scheduler debt among eligible modes"
- **Range worked:** entries 11301–11393 (cross-model accuracy review of glosses / example translations / semantic tags)
- **OpenRouter session budget:** $2.50; daily ledger spent today now $0.0551 (well under $5 cap)

## What ran

- **Furigana screening (§A step 2):** `review_runner.py --pass screening --range 11301 11800`
  stalled on a single OpenRouter request after screening ~38 entries (11301–~11338)
  and survived its outer `timeout` wrapper; SIGKILLed by name. Per the §A resilience
  rule, kept the per-entry results already written and continued. 6 entries flagged
  in the completed portion (11310, 11314, 11317, 11318, 11324, 11329) — **all verified
  false positives** (okurigana/partial-reading splits and the calibration report's
  reading-truncation/index-confusion family; actual furigana `{中傷|ちゅうしょう}`,
  `{不安定|ふあんてい}`, `{中立|ちゅうりつ}` are all correct). Bulk-rejected via one
  aggregated decisions line; deep pass skipped (known-noise shortcut).
- **Accuracy review (§A step 3):** `review_accuracy.py --range 11301 11500` (gloss,
  translation, tags). Completed 11301–11393 (93 entries, contiguous) before being
  halted at a clean point for wall-clock/context budget. 37 issues raised
  (36 tags.semantic, 1 gloss).

## §A step 4 adjudication

- **APPLY — 23 tag migrations** (off-vocabulary semantic tag → in-list tag; verified
  per entry against `metadata.tags.semantic`, headword, and gloss):
  - `people-description`→`descriptive` (11345, 11349); `object-stationery`+`disaster`→`tool` (11346);
    `relationship`→`family` (11347); `people`→`person` (11348, 11357); `safety`→`society` (11348);
    `quality`→`descriptive` (11350, 11359, 11362, 11364, 11365); `time-manner`→`time-general` (11351);
    `information`→`cognition` (11352); `place`→`geography` (11353, 11367); `food-cuisine`→`food` (11353);
    `position-direction`→`direction` (11356, 11366, 11371); `time`→`time-general` (11358);
    `emotion-feeling`→`emotion` (11363, **caught by my own range scan — model missed it**);
    `nature-water`→`geography` (11368); `rank`→`society` (11370); drop invalid `body`→`abstract` (11373).
- **APPLY — 1 gloss fix:** 11354 串カツ "deep-fried skewered meat and vegetables" →
  "…meat, seafood, or vegetables" (definition already specified seafood).
- **REJECT — 11 in-list nits** (one aggregated decisions line): `general`→action/finance/society/etc.
  broadness suggestions (11310, 11311, 11321, 11327, 11331, 11338, 11339, 11343, 11382) and
  in-list substitution/removal nits (11322 add culture/geography; 11372 remove `science`).
  Per §A semantic-tag policy: in-list narrowness/broadness substitutions are rejected;
  sole-`general` is handled by the deterministic systemic-fix lane, not here.
- **FLAG — 1:** 11325 ヘルメット tagged `clothing` (reviewer suggests `tool`) → `reviews/needs_curator.txt`
  (genuine category question — a helmet is worn but is protective equipment).

## §4 self-verification

Not a separate pass: every changed entry (11345–11373, 11354) lies inside the §A
reviewed range 11301–11393, so the review itself was the check (§4 step 3). Also
avoided re-triggering the OpenRouter hang. `validate.py --range 11300 11400` → 101/101
valid; `validate_tags.py --check-no-new-unknown` → no new off-vocab tags (migrations
reduced the baseline by ~26 off-vocab tag occurrences).

## Always-on capture

- No new candidate words encountered (tag-focused review; examples not deep-read).
- Observations appended to `polishing/observations.md`: `[tooling]` review-script hang
  past `timeout`; `[pattern]` off-vocab tag families clustered in the 11300s 不-/中-/下-/両- block.

## State updated

- `polishing/tasks/cross-model-review/progress.txt` → `next: 11394`
- `reviews/queue.txt`: removed 11301–11393 (40 lines), 14605 → 14565
- `reviews/decisions.jsonl`: +26 lines (23 tag applies, 1 gloss apply, 1 flag, 1 aggregated reject n=11)
- `pipeline/openrouter-ledger.json`: +$0.043 (screening $0.012 / accuracy $0.0383), today total $0.0551
- `pipeline/metrics-history.jsonl`: one accuracy-review line (24 changed)

## Next cursor

`cross-model-review next: 11394`
