# Routine session — 2026-08-06 (008)

- **Mode**: `systemic-fix` (selector reason: "systemic-fix: highest scheduler debt among eligible modes")
- **Backlog item**: `link-target-baseform-disagreement` — batch 7, **closeout**
- **Signals**: candidates 997 (26 "seen in entry"), max entry ID 30453, comprehensive frontier 6809,
  cross-model review next 27001, unharvested observations 23, open backlog items 21,
  OpenRouter remaining $4.4691
- **Entries changed**: 2 (`03654_yutaka`, `04230_uchiakeru`)

## Pre-flight

- **§0a rescue**: one open PR, **#3130** (`routine(systemic-fix)`, batch 6 of this same item).
  Not rescuable: `get_check_runs` returns `total_count: 0` — GitHub never dispatched a
  workflow run for that branch, across two trigger events, so there was no green to merge.
- **Not swept, and not left stranded either.** Its entry IDs (00445–06031) are all below the
  comprehensive frontier, so the §0b max-ID heuristic would have closed it as "superseded" when
  it is nothing of the kind. Instead its two commits were **merged into this run's branch**, so
  its 13 link repairs get CI coverage as part of this PR and reach main with it. PR #3130 should
  be closed as absorbed once this merges.
- Lock acquired; no other Routine active.

## Work: closing out the link-baseform backlog item

**The two deferred judgment cases — both decidable, both repaired.**

| Entry | Link | Was | Now | Why |
|---|---|---|---|---|
| `03654_yutaka` | 石油や天然ガスがたくさん{採|と}れる | `00565_toru` 取る | **`20862_toru` 採る** | 採る sense 1 is "to pick or gather natural things … **or minerals** from their environment" — exactly this sentence |
| `04230_uchiakeru` | etymology note 打つ + {明|あ}ける "(to open)" | base label 明ける | **base label 開ける** (target `00563_akeru` unchanged) | base-label repair: the target was already right, only the label disagreed. 4th of its kind after お得, 一本/一個, 作る/造る |

`03654` had been deferred twice as "neither entry fits" — but only 取る and the detector's
proposal `02376_toreru` "to come off" had ever been compared. **When both the declared target and
the proposal are wrong, look for a fourth entry before reaching for `noentry`.**

**The remaining 99 findings are all correct, so the item is finished as a link problem.** Family
by family they are one word reached under a different spelling (頃/〜ころ 24, 上げる/あげる 18,
付ける/つける 7, 通り/どおり 5, いい/良い 4, 羽/羽根 3, 被る 3, 空く 2, plus 12 singletons).
They are now enumerated as verified `(base, target)` pairs in
**`build/data/link_baseform_allowlist.json`**, each with a written reason and a class, and the
detector accepts them as a fourth normalization path.

**Ratchet armed** — `check_link_baseform.py --gate` added to `.github/workflows/validate.yml`,
absolute at 0, alongside the semantic-tag ratchet. The item has been calling for this since
2026-07-31.

- **Why an allowlist and not a count ratchet at 99**: the benign families *grow with normal entry
  creation* — every new entry linking 頃 adds one — so a numeric floor would have failed ordinary
  new-entry PRs while still tolerating a real wrong-word link that displaced an allowlisted one.
  A ratchet on a growing population is not a ratchet.
- **Negative-tested**: dropping the 頃 pair from the allowlist reproduces a 24-link `FAIL`
  (exit 1), so the green is evidence rather than a no-op. `--no-allowlist` still prints the raw
  99 — classified, not hidden.

**7 of the 99 are a different defect class**: 捻る/捩じる, 焼き立て/焼きたて, 翳む/霞む,
追い掛ける/追いかける, 控え目/控えめ, 見積り/見積もり, and the とき pair 02918/10077 are
**near-duplicate entries** — both link targets are defensible because the word is held twice, and
no link work converges them. Tagged `near-duplicate-pair` in the allowlist and filed as a new
backlog item **`entry-pair-consolidation`** (`needs-decision`: merging touches live URLs, so the
keep/merge call is the curator's).

## §4 self-check

`review_accuracy.py --ids 03654,04230` → **clean, 0 issues** ($0.0009, logged to the ledger as
`phase: self-check`). Nothing to adjudicate, so no `decisions.jsonl` lines were written.
The furigana screener skipped both IDs (prior screening results on file); the accuracy pass is the
substantive check for a link-target change in any case.

`validate.py`: **30244/30244 valid**. `check_link_baseform.py --gate`: **OK, 0 ≤ 0**
(raw queue 99, all allowlisted). Dead link targets: 0.

## State updates

- `backlog-queue.json`: `link-target-baseform-disagreement` → **`resolved`**, scope 101 → 0;
  new item `entry-pair-consolidation` (needs-decision, scope 7).
- `planning/wiki/ideas/cleanup-backlog.md`: closeout batch written up under Priority 27's
  follow-on section; the "Remaining 101 links" line updated to RESOLVED.
- Observations: 6 appended (`[tooling]` ×4, `[pattern]`, `[entry]`).
- Candidates added: none (no unlinked words encountered — this batch touched 2 entries).
- Metrics snapshot appended.

## Next

`systemic-fix` should take a different backlog item; this one is closed. The natural successors
are the P11 tag-drift items or `example-headword-missing`. `entry-pair-consolidation` is curator
work, not routine work.

## CI outcome (appended after the wrap-up)

**PR #3131 left open, not merged: GitHub never dispatched a workflow run.** Polled
`get_check_runs` 8 times over ~11 minutes (past routine2.md's ~8 min cap) — `total_count: 0`
throughout, and `list_workflow_runs` shows **no run for this branch at all**.

This is repo-wide and predates this run: the most recent `validate.yml` run of any kind is
**15:43 UTC**, roughly six hours before this session. PR #3130 hit the identical symptom at
18:28 UTC. So this is an Actions dispatch outage on GitHub's side, not anything about either PR
— which also means #3130's commits, carried here, still have no CI.

Local equivalents of what CI would run are green: `validate.py` **30244/30244**,
`check_link_baseform.py --gate` **OK 0 ≤ 0**, dead link targets **0**. That is not a substitute
for CI and neither PR was merged without it.

**Do not sweep #3131 or #3130.** Both touch only entries below the comprehensive frontier
(03654/04230 here; 00445–06031 in #3130), so the §0b max-ID heuristic reads them as "superseded"
when they are simply waiting for CI. The next Routine run should merge #3131 via the §0a rescue
once checks appear, and close #3130 as absorbed at that point. If dispatch is still dead,
re-running the workflow manually from the Actions tab is enough.
