# Routine session — 2026-08-02 (006)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: candidates 998 (14 "seen in entry"), max_entry_id 30337, comprehensive_next 6751,
cross_model_review_next 23908, unharvested_observations 23, reviewed_entries 586,
open_backlog_items 16. OpenRouter: $0.1711 spent today, $4.8289 remaining, no session budget.
**Branch**: `claude/determined-rubin-34kgoo`

## Pre-flight

- §0a `mcp__github__list_pull_requests` (open) returned **[]** — no predecessor to rescue.
- §0b MCP stranded-PR sweep: nothing to sweep (no open `claude/*` PRs).
- Lock acquired cleanly.

## Work done

Harvested all **18** loose observations from four sections of `polishing/observations.md` (the
day's metrics-refresh note, two polish runs, the accuracy-review run, the new-entries run) and
ran three corpus measurements before filing anything new.

### Measurements

1. **`noentry` provenance split.** Comparing each resolving marker's target `created` date
   against its source entry's: **447 markers / 317 entries wrong when written** (a floor) vs
   **3,362 that went stale**; 3,515 still unresolved. The wrong-when-written subclass is closed —
   zero above ID 07000 — and 301 of 447 are full-headword/unique-target. **No new detector
   filed**; Cleanup P35 + Tooling 19 gain a `wrong_when_written` column instead.
2. **`Xする` link-label convention.** 822 links target a real `Xする` entry; 441 label the base
   `Xする` while targeting the bare noun, of which **174 have no `Xする` entry to target**. Only
   267 decidable. Filed as a Cleanup Informational (curator decision).
3. **Tableware tag coherence.** 32 vessel entries, **12 distinct tag-sets, no plurality**. Filed
   as new Cleanup P38 + queue item `tag-lexical-family-incoherence`.

### Filed

- **Cleanup**: P35 provenance update; **new P38** (lexical-family tag incoherence); **new
  Informational** (`Xする` base labels); P20 update (forced vs context-dependent migration
  classes, `drop` as the safe default, `interrogative` residue on 00534/00543/23898); the
  zero-link Informational marked as filed a fourth and fifth time.
- **Tooling**: **new 63** (`validate_tags.py` warning breakdown), **new 64**
  (`manage_candidates.py add` rejection message), **new 65** (`validate.py` link base-form
  ratchet); updates to 19, 24 (fourth zero-precision run, itemised), 54 (candidate ratio
  re-measured).
- **Entry Follow-ups**: 06742 告白 (告白記 → likely 告白文/告白録, second opinion), 04477 大皿
  (`→得：07739_otoku` label/target mismatch).
- **Topics**: `deterministic-vs-semantic-tasks.md` deepened (first update since April) with the
  measured sort of the four paid review dimensions by whether *detection* is mechanical;
  `inline-link-integrity.md` gained both link measurements and the partial-vs-zero coverage
  distinction.
- **Index/queue/log**: `index.md` header + two page descriptions; `backlog-queue.json` +1 item,
  one re-scoped, `updated` bumped; `planning/wiki/log.md` entry appended.

### Lint (activity E)

- Wiki relative-link lint: **0 broken**.
- Three anchor typos I had introduced were caught and fixed against the real heading text
  (`#priority-11-batch-creation-semantic-tag-transportation-misapplied`,
  `#priority-13-overuse-of-general-as-sole-semantic-tag`, `check_semantic_clusterspy`).

## §4 self-verification

**Skipped — no entries were created or modified this run** (`git status --porcelain -- entries/`
empty). OpenRouter untouched; **$0.00 spent**, ledger unchanged at $0.1711 for the day. No
`reviews/decisions.jsonl` lines (no external flags adjudicated).

## §5 metrics

Appended: `mode=wiki, entries_changed=0, entries_total=30128, candidates=998,
review_queue_len=13217, observations_lines=698, comprehensive_next=6751,
openrouter_spent_today_usd=0.1711`.

## Cursors

No entry cursors advanced (wiki mode). `pipeline/routine-state.json` advanced by the selector in
§1. `polishing/observations.md` reduced to its harvest record.

## Build

**Skipped** — wiki-only run, markdown plus two JSON files, `docs/` untouched (§6).

## Candidates added

None — this mode reads the wiki and the corpus, not entries, so no unentered Japanese words were
encountered in examples or notes.

## Observations raised for the next harvest

None as loose items. Three questions were **routed to the curator** in the wiki log rather than
left as observations, because no Routine run can act on them: the `Xする` convention, the P20
step-1 sweep of the nine already-shipped `TAG_MIGRATION` mappings (unswept after three harvests
recommending it), and the `container`/`tableware` taxonomy question that partly blocks P38.
