# Routine v2 session — 2026-07-25 (006)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Branch**: `claude/determined-rubin-yegzzu`

## Selector signals

| Signal | Value |
|---|--:|
| candidate_count | 1,063 |
| seen_in_entry_count | 2 |
| max_entry_id | 30,104 |
| comprehensive_next | 6,631 |
| cross_model_review_next | 18,346 |
| unharvested_observations | 19 |
| reviewed_entries | 586 |
| open_backlog_items | 6 |
| OpenRouter remaining today | $4.5828 of $5.00 |

## Pre-flight (§0)

- **§0a rescue**: `mcp__github__list_pull_requests` returned **0 open PRs** — nothing to rescue.
- **§0b sweep**: no `claude/*` PRs open, so nothing to close. `pipeline/sweep-stranded-prs.py` deliberately not run (403s under the agent proxy).
- **Lock**: acquired cleanly.
- Branch was level with `origin/main` (0 commits either way) at start.

## Work done (§2 — `wiki` mode, per `planning/maintain-knowledge-base.md`)

### Activity F — harvest (15 observations, all cleared)

Every claim was checked against the repository before filing. Three were **corrected** in the process:

| Observation claim | Verified value |
|---|---|
| "44 entries" write the inline-link base form with furigana braces | **39** entries |
| verb-godan/-eru scan: "36 genuine godan" FP family | **31** residual (re-measured after this run's two fixes) |
| "CLAUDE.md documents `formality_variant`/`transitivity_pair`" | Not in `CLAUDE.md` at all — the live source is **`prompts/comprehensive_polish.md:107`** |

Also measured, where the observation gave only a single entry's count: kanji-written auxiliaries across the dictionary — て行く 222 occurrences / 200 entries, て来る 203/34, て見る 168/138, て置く 7/7. And `ている：noentry` — **37** entries.

**New pages/sections created:**

- **Cleanup P24** — inline-link base forms written with furigana braces (39 entries; genuinely mechanical-safe, regex anchored on both `→` and `：`).
- **Cleanup P25** — fabricated conjugation tables from a mis-assigned verb class (06624 甘える, 09361 バックレる; both fixed in the observing run).
- **Cleanup P26** — auxiliary verbs written in kanji in examples (filed **not** batch-ready: the counts are a superset including correct main-verb uses; only the 7-entry ておく row is a clean batch, the rest needs a house-convention decision first).
- **Cleanup Informational** — `ている` has no entry and is `noentry` in 37 ASPECT notes: a standing curator scope decision, not a workable backlog item.
- **Tooling 34** — `comprehensive_polish.md:107` names two cross-reference types `schema.json` rejects; open since 2026-05-09 because wiki runs may not edit prompts.
- **Tooling 35** — verb-class misassignment detector, specified as **self-contradiction** (an entry whose own examples show a form its declared class cannot produce) rather than reading-shape, because the latter has a permanent 31-entry FP family and the former has none.
- **Expository Articles** — `ている` and the te-form auxiliaries added as a High-priority article topic.

**Existing items deepened:** Tooling 11 (nine silent dead links; the documented check is the broken one), 24 (screener noise diagnosed to prompt-builder context truncation — the stray `)` tell — ~37 of 48 flags), 21 (first side-by-side throughput measurement, ~8 vs ~29 entries/min), 23 (seen-in-entry logger's missing reading check), 7 (worked sense-vs-entry-split example); Cleanup P8 (new sub-family + the 気持ち duplicate pair), P20 (18250–18300 pocket, detector 6,721→6,696), P21 (band confirmed *past* the frontier at 06627–06632; first per-entry cost figure, 10–15 min).

**Entry Follow-ups:** 01385_kimochi / 02485_kimochi (duplicate 気持ち entries, merge candidate); 00969_mata (sense 3 fixed in-run, with the generalisation to other bare-adverb-with-compound-only-sense entries).

### Activity E — lint and maintain

- Moved two P21 updates that the 2026-07-24 and 2026-07-25 wiki runs had mis-appended to the **Priority 23** section into Priority 21, with a filing note.
- `ideas/backlog-queue.json`: `unknown-semantic-tags` scope 6,721 → **6,696**; three new items queued with honest flags — `inline-link-braced-base-form` (open, batch_ready, scope 39), `verb-class-fabricated-conjugation` (needs-detector), `kanji-written-auxiliary-verbs` (needs-decision). Only the first is genuinely mechanical; the other two are deliberately gated so the systemic-fix selector cannot pick them up prematurely. JSON re-validated after the edit.

### Activity A — sync

`project/overview.md` General-tier and total counts refreshed: 27,092 tagged `general` (+ the 20 untiered 29181–29200) of **29,895** total.

### Activity H — metrics trend, twenty-fifth refresh

Due at 12 new lines (341 → 353). `topics/quality-metrics.md` updated with the runs 342–353 window (2026-07-24 03:25 → 2026-07-25 09:27, ~1.3 days; polish ×4, accuracy-review ×3, wiki ×2, new-entries ×2, systemic-fix ×1):

- **366 flags adjudicated: 159 applied / 207 rejected / 0 escalated** — a second consecutive escalation-free refresh, **26 straight runs (328–353)**.
- `tags` apply-among-decided **62.0%** (142/87) as the off-vocab cohort ran into 17911–18345; rejects remain the standing in-list `general`-narrowness family (item 17, twenty-fourth confirmation).
- gloss 4/8, translation 1/4 (small samples); **self-check** 11 applied / 20 rejected across all three content dimensions.
- Furigana screening **1 of 88 (1.1%)** — but the news is diagnostic: the FP family is now localised to prompt-builder context assembly.
- Entries **29,867 → 29,895**; candidates 1,086 → 1,063; frontier 6,614 → 6,631; review queue **new floor 12,496** (settled 12,523).
- OpenRouter roughly doubled to **~$0.44/day**, still under 9% of the $5 cap.
- Detectors (2026-07-25): `furigana_format` **1,032** / 708 entries (from 1,057), `artifacts` **12** (stable), `tag_drift` **11,339** total — unknown-semantic 6,696, sole-general 3,823, semantic-mismatch 813. `needs_curator.txt` 59 lines.

**Metric moving the wrong way**: one, and it is **off-ledger** — the nine dead inline links a polish run introduced silently, which no reviewer dimension can see because they are not a reviewer flag. Filed to Tooling item 11 this run with the evidence, rather than logged as a fresh `[pattern]` observation that would only round-trip back to the next harvest.

## §3 always-on capture

No new Japanese words encountered needing candidates (wiki mode reads project files, not entries). No new systemic observations logged — every adverse signal found this run was filed directly to the backlogs above.

## §4 self-verification

**Skipped — no entries created or modified** (`git status --porcelain -- entries/` empty). OpenRouter untouched this run; ledger unchanged at $0.4172 spent today.

## §5 metrics

Appended: `mode=wiki, entries_changed=0` (run 354).

## Routed to the curator

A `wiki` run may not edit prompts, skills, or entries, so two items need the curator:

1. **`prompts/comprehensive_polish.md:107`** tells polishers to use `formality_variant` and `transitivity_pair`; `schema.json` admits neither. One-line fix (`→ keigo`/`related` and `→ pair`). Open since 2026-05-09; every polish run hits it.
2. **`ている` scope decision** — grammar-form entries for core auxiliaries, a documented never-link convention, or an expository article. 37 entries are waiting on the answer.

## Next cursors

- `polishing/tasks/comprehensive/progress.txt` — unchanged at `next: 06631` (wiki mode does not advance it).
- Selector state advanced by `routine_next.py` in §1.
- Next wiki-mode metrics refresh (Activity H) due at **≥363** metrics lines.
