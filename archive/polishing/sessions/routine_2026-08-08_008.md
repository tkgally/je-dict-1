# Routine v2 session — 2026-08-08 (008)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: candidates 983 (12 "seen in entry"), max entry ID 30511, comprehensive frontier
`next: 06842`, cross-model review cursor 28901, unharvested observations 27, open backlog items 22.
**OpenRouter**: $0.5508 spent today of the $5.00 cap; no session budget set (wiki mode makes no
external calls).

## Pre-flight (§0)

- **§0a rescue**: `list_pull_requests` returned **zero open PRs** — nothing to rescue.
- **§0b stranded-PR sweep**: no open `claude/*` PRs, so nothing to close.
- **§0b orphan-branch sweep**: `list_branches` returned **`main` only**. Zero orphans; the
  previous run's branch was cleaned up by the auto-delete-on-merge setting as expected.
- **Lock**: acquired for `claude/determined-rubin-uyd0hh`.

## Work done (§2, wiki mode)

Followed `planning/maintain-knowledge-base.md`. Activities: **[F] harvest**, **[A/F] five
corpus measurements**, **[H] quality-metrics refresh**, **[C] backlog-page deepening**.

### Harvest — 23 loose observations, all cleared

From the 2026-08-08 polish run (priority + frontier lanes), the 2026-08-08 new-entries run, and
the 2026-08-08 accuracy-review (28351–28900). Five premises were measured against the whole
corpus before filing; **four came back a different size or shape than proposed**, and one
inverted its own causal story.

| Premise (as observed) | Measured | Outcome |
|---|---|---|
| Detector for "notes linked, examples bare" would find the rest of that block | **33 entries**; 32 of them *behind* the frontier in six contiguous runs | **Cleanup P46** — diagnosis inverted: session-shape, not creation-era |
| Flag adjacent link pairs whose concatenation is an existing headword | **1,579 pairs / 1,296 entries**; 847 are correct content+particle links | **Cleanup P47** at **443 pairs / 391 entries** with the ≥2-char filter; already documented skill policy |
| 17 malformed `⟦surface→noentry⟧` links | **17 instances / 7 entries**; 2 have no arrow at all | **Cleanup P48**; 15 mechanical, 03022's 2 need a lookup |
| `domain` needs a `check_tag_drift.py`-style pass | **3,593 instances / 3,278 entries**; `business` 1,162 (32%) | Filed; **proposed method cannot work** — no off-vocabulary values to key on |
| `VALID_SEMANTIC` lacks a sound category (3 instances) | **896 entries** with a sound/voice/acoustic gloss | Merged into the standing curator taxonomy question |

Also sized in passing: the `〜的` templated linker — **281 entries, 47 already linking
`09839_teki`, 234 not** — filed as an Informational sizing, not a queue item.

**Also filed**: **Cleanup P49** (wrong furigana inside inline-link surfaces, 40 pairs; third
confirmed member of the Tooling 47 "fields outside examples/notes fall through every furigana
instrument" family), **Tooling 87** (`review_accuracy.py` parallel workers — ~2.4 entries/min
serial vs ~30/min across four processes), **Tooling 88** (`get_next_id.py` should print the
target directory; the 500-boundary trap cost a session).

**Updates**: **P11** — largest single migration yet (236 of 549 entries in 28351–28900), with
**1,902 entries dictionary-wide** still carrying baselined off-vocab tags; **Tooling 84** — a
second, larger throughput bound (cross-pass concurrency starves the furigana screener to
~1.2 entries/min vs ~19), so §A should run the passes sequentially or on distinct models;
**reviewer formality noise** 5/5 for a third window; **P43** at 9/9; **P42** reinforced.

**Entry Follow-ups**: 28655's contradictory headword readings; the proper-noun scope
contradiction (six live place-name entries vs. the "no place names" policy); 06830's
`domain: medical`.

### Activity H — quality-metrics refresh #33 (Finding §15)

12 new metrics lines since the last refresh (454 → 466), above the ≥10 threshold.

- **Frontier-versus-growth broke its 44–50% band to 76% — on the supply side.** The frontier
  column barely moved (26 → 29 IDs); growth fell 59 → 38 because the "seen in entry" candidate
  lane drained. The lane refills from comprehensive polish, so the mechanism that closed the gap
  will reopen it. 44–50% stays the planning number.
- **§14's mixture-weight reading reproduced, prediction first.** Pointed at a 43%-contaminated
  block, blended `tags` rose 71.0% → **76.2%** while components held: off-vocabulary **97.2%**
  [495/14], in-list-and-other **5.0%** [7/134], formality **18.2%** [2/9].
- **Third consecutive record review-queue floor: 10,297** (−839, after −882 and −607).
- Escalations light (`needs_curator.txt` 319 → 323). Furigana screening 3/31 (**9.7%**).
  OpenRouter peak day **$0.551** of $5.
- Caveat logged in §15: this refresh's family split was keyword-classified over free-text notes,
  so its 5.0% is not strictly comparable to last refresh's 28.1%. A `"fam"` key on `tags` lines
  in `decisions.jsonl` would remove the classifier; filed as a `[tooling]` observation.

## §4 self-verification

**Not applicable — no entry files were created or modified this run.** A wiki-mode run touches
only markdown and JSON under `planning/`, `polishing/`, and `pipeline/`. No OpenRouter calls
were made; the ledger is unchanged at $0.5508.

## §5 metrics snapshot

Appended: `{"mode": "wiki", "entries_changed": 0, "entries_total": 30302, "review_queue_len":
10537, "comprehensive_next": 6842, "openrouter_spent_today_usd": 0.5508}`.

## Cursors

- No entry cursors advanced (wiki mode does not touch `comprehensive` or `cross-model-review`).
- `pipeline/routine-state.json` advanced by the selector in §1.
- `planning/wiki/log.md` appended; `index.md` unchanged (**no new pages created** — all work
  deepened existing pages).

## Observations left for the next harvest

- `[tooling]` `decisions.jsonl` needs a `"fam"` key on `tags` lines so the metrics refresh stops
  keyword-classifying free-text notes.
- `[pattern]` The 76% frontier-versus-growth reading is a supply artifact; the frontier itself
  did not speed up.

## Build

**Skipped** — wiki-only run, no `docs/` impact (§6).

## Next

Selector picks the next mode. The curator items carried forward: the proper-noun scope question
(third filing, now with corpus evidence against the policy), the taxonomy question (three gaps),
and the frontier-versus-growth rate (sixth filing).
