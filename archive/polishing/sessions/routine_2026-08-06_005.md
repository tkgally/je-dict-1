# Routine session — 2026-08-06 (005)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Params**: `phase: furigana`, `start_id: 26701`, `openrouter_session_budget_usd: 2.50`
**Signals**: candidates 990 (19 "seen in entry"), max entry ID 30453, comprehensive
frontier 6802, cross-model-review cursor 26701, unharvested observations 13,
reviewed entries 586, open backlog items 21.

## Pre-flight

- §0a rescue: no open PRs — nothing to rescue.
- §0b sweep: no open `claude/*` PRs — nothing superseded to close.
- Lock acquired for `claude/determined-rubin-1i4fgp`; branch already level with
  `origin/main` (a0d921ea0).

## Range worked

**26701–27000** (299 entries with files).

Two passes run concurrently against `google/gemini-2.5-flash`:

| Pass | Coverage | Flagged |
|---|---|---|
| `review_runner.py --pass screening` (furigana) | 296 entries screened | 12 real concerns + 10 `Parse failure` |
| `review_accuracy.py` (gloss / translation / tags) | 320 entries reviewed | 149 issues |

(3 entries in the range carry no furigana pairs and were skipped by the screener.)

**Deep furigana pass skipped** under §A step 2's known-noise shortcut: every one
of the 12 screening concerns falls inside a documented false-positive family.

## Furigana adjudication — 12 rejected, 0 applied

| Entry | Concern | Verdict |
|---|---|---|
| 26761 公爵夫人 | model confused 公爵 with 侯爵 | reject — こうしゃくふじん is correct |
| 26765 干し果物 | `{柿\|がき}` "should be かき" | reject — rendaku inside 干し柿 |
| 26767 女役 | 女形 おやま vs おんながた | reject — the entry itself contrasts both |
| 26800 外向き | `{外向\|そとむ}き` | reject — correct compound/okurigana split |
| 26745 外装材 | `{板張\|いたば}り` "incomplete" | reject — correct okurigana split |
| 26786 黒子 | `{黒子\|ほくろ}` | reject — the note explicitly discusses that reading |
| 26890 大根おろし | `{焼\|や}き{魚\|ざかな}` ×2 | reject — correct okurigana split + standard rendaku |
| 26899 亭主関白 | `{天下\|でんか}` | reject — かかあ天下 genuinely reads でんか |
| 26927 ざるそば | `{手打\|てう}ち`, `{大盛\|おおも}り` | reject — correct okurigana splits |
| 26932 潔癖 | `{好\|ず}き` | reject — 綺麗好き = きれいずき |
| 26943 戯言 | ざれごと vs たわごと | reject — the entry presents both |
| 26993 マニュアル | `{通\|どお}り` | reject — マニュアルどおり rendaku |

The 10 `Parse failure` flags are unparsed model responses, not entry defects;
logged as a `[tooling]` observation.

## Accuracy adjudication — 113 applied, 36 rejected, 0 flagged

**Applied (113 decisions across 129 entry files changed):**

1. **Off-vocabulary semantic tags → in-list tags (83 entries, 92 tags).**
   `check_tag_drift.py --check unknown-semantic --range 26701 27000` found 92
   tags outside `VALID_SEMANTIC` across 32 distinct labels (`people` ×14,
   `description` ×9, `time` ×8, `interpersonal` ×7, `body` ×5, `category` ×4,
   plus 26 more). Per §A's semantic-tag policy these are correct by definition.
   Each target was chosen against the entry's own headword + gloss +
   explanation, not applied from a blanket map — e.g. 26771 佳曲 → `music`
   (not `art`), 26911 保育士 → `occupation` (not `person`), 26827 避難する →
   `movement`, 26924 叫び声 → `communication`. Range now reports 0
   unknown-semantic findings.

2. **Vague or wrong semantic tags → specific (44 entries).** Sole-`general`
   replaced where a specific in-list tag clearly applies (Cleanup P13):
   26736 已然形 → `grammatical`, 26753/26752/26764 → `geography`,
   26766 巡洋艦 → `military`, 26744/26747/26750/26751 → `technology`,
   26980/26981 → `food`, 26984 パリッとする → `onomatopoeia`, and others.
   Two outright mis-tags: 26854 馬鹿げる `emotion` → `evaluation`,
   26987 出産する `body-part` → `health` (childbirth is not a body part),
   26739 育児嚢 `nature` → `animal-mammal` + `body-part`.

3. **Gloss (1 entry).** 26737 バイブ: `vibrate; vibration mode` →
   `vibrate setting; vibration mode` — a bare verb form was heading a noun entry.

4. **Translation (1 entry).** 26972 一貫する example 4:
   設計から製造まで一貫して自社で行っている → "We handle the entire process
   in-house, from design through manufacturing." The previous "consistently"
   read as *reliably* rather than *end-to-end*, which is the sense of 一貫して here.

5. **Formality (1 entry).** 26789 第三者 `formal` → `neutral`; the entry's own
   notes say it is common in "legal, business, and everyday contexts".

**Rejected (36):**

- 27 tag flags — in-list narrowness or additive nits: `daily-life` → `action`
  on 26885/26849, removing `sports` from 26726 起用法 and 26891 最終回 (both
  genuinely have sports senses — 最終回 is also "last inning"), removing
  `society` from 26821 うねり (the metaphorical social sense is real), and the
  9 formality flags whose entries' own notes support the `formal` label.
- 6 translation flags — 26721 和字 ×4 (the model wanted "Japanese-made kanji"
  substituted for the equally-correct "Japanese-made characters"), 26795
  頭を下げる ("I take my hat off to" is an accurate idiomatic rendering),
  26883 一泊二日 (spelling out "one night and two days" would be stilted and
  the entry already glosses it as "overnight trip").
- 2 gloss flags — 26721 和字; the top-level gloss legitimately summarises both
  senses, and sense 2's gloss is accurate.
- 2 aggregated furigana lines covering the 12 rejects above.

All 150 decisions are in `reviews/decisions.jsonl`. **Precision by dimension
this range**: tags 110/137 applied (80%), gloss 2/4 (50%), translation 1/7
(14%), furigana 0/12 (0%).

The furigana dimension has now produced zero applicable corrections over a
polished range for the third consecutive measurement, consistent with the
0–5% precision recorded on 2026-06-10/11. The `tags` dimension is carrying
essentially all of this lane's value.

## §4 self-verification

**Skipped by rule, not by omission.** §4 step 3 says to skip IDs already covered
by §A in the same run — every entry changed this run is inside 26701–27000, the
range both review passes just covered, so the review *was* the check. No
`self-check` spend recorded.

## Budget

| Phase | Entries | Est. cost |
|---|---|---|
| accuracy (gloss/translation/tags) | 320 | $0.1321 |
| furigana screening | 296 | $0.12 (estimated) |

Run total ≈ $0.25; daily ledger at $0.5239 of the $5.00 cap, well inside the
$2.50 session cap. The deep furigana pass — normally the expensive third — was
not needed. The screening estimate is conservative: the runner was stopped once
it cleared the range, so its own `Est. cost` summary was not printed.

Wall-clock, not dollars, was the binding constraint this run: the screener's
6-second per-request rate limit caps it near 7 entries/minute, so 300 entries
took ~45 minutes while the accuracy pass covered the same range in ~10. A
future run wanting 500+ entries would need either parallel workers on disjoint
sub-ranges (which would race on `screening_status`) or a lower interval.

## Candidates added

None. `accuracy-review` reads flag reports rather than walking entry prose, so
no new missing-word encounters this run.

## Observations logged

Four appended to `polishing/observations.md`: the `Parse failure` screening
tooling bug; the density of off-vocabulary tags in this block (28% of entries)
and the likelihood that ranges above 27000 match it; sole-`general` (P13) being
tractable through the accuracy-review lane rather than a dedicated sweep; and
the accuracy reviewer's formality dimension running ~10% precision because it
does not consult the entry's own register description.

## Cursors

- `polishing/tasks/cross-model-review/progress.txt` → `next: 27001`
- `reviews/queue.txt` — 26701–27000 drained; depth 11589 → 11290
- `pipeline/routine-state.json` — advanced by the selector in §1
