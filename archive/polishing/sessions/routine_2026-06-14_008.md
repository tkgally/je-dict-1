# Routine session — 2026-06-14_008

- **Mode**: `systemic-fix` (selector reason: "highest scheduler debt among eligible modes")
- **Backlog item**: `example-headword-missing` — Noun examples that never contain their headword
- **Pre-flight**: §0a found no open routine PRs to rescue; `sweep-stranded-prs.py` closed 0; routine lock acquired.

## Work done

Ran `build/check_example_headword.py --json`: 53 suspect examples across 31 noun
entries. Adjudicated every flagged entry per-entry:

### Genuine fix (1 entry)
- **22875 出回り (でまわり)** — ex2 used the *verb* 出回る (`{新米}の{出回}る{時期}だ`),
  not the noun headword. Replaced with the noun collocation documented in the
  entry's own notes: `{新米}の{出回}りの{時期}になった。` ("The season when new
  rice appears on the market has arrived.") Matched the entry's existing
  unlinked example style (entry has not been through inline-link polishing).

### Skipped — out of scope (U+FFFD encoding corruption, owned by tooling-backlog #16)
20478, 20670, 21769, 22101, 22152, 22153, 22265, 22266, 24702, 24726, 24747,
24795, 25000, 25168, 25169, 25573, 25630, 25631, 26262, 26636 — the detector
flags these only because the kanji headword is mojibake in the example text.
These need the dedicated replacement-character repair pass, not a per-entry
semantic fix.

### Skipped — legitimate orthography / detector false positives (correct examples)
- 01336 ご馳走 → ごちそう (kana orthography; explicitly cited in the backlog as a skip)
- 02430 結び → おむすび / 塩むすび (documented rice-ball sense, kana)
- 03094 〜時 → 時 present in kanji; ～-prefix headword the matcher can't bind
- 03906 冠 → うかんむり (sense-3 "crown radical" — legitimate)
- 05203 山羊 → ヤギ, 05426 染み → シミ, 06106 鰹 → カツオ, 10786 牡蠣 → カキ
  (animal/seafood katakana orthography)
- 07558 届け → 退職届 / 婚姻届 / 欠席届 (documented compound forms in the notes)
- 28789 皺寄せ → しわ寄せ (rare-kanji standard kana orthography)

**Conclusion**: the clean-entry semantic-fix frontier for this item is
exhausted. Set `batch_ready: false` and `scope_estimate: 0` in
`backlog-queue.json`, and recorded the status in the prose page
(`cleanup-backlog.md` Priority 19). Genuine headword-absent cases now hidden
behind mojibake will re-surface for this lane only after tooling-backlog #16
repairs the corrupted text.

## §4 self-check
`review_accuracy.py --ids 22875` (gemini-2.5-flash, ~$0.0004): 2 error-severity
tag flags, both genuine pre-existing errors the model caught. Verified against
`VALID_SEMANTIC` (source of truth): both tags were out-of-list. **Applied** both
migrations per the semantic-tag policy — `commerce → economics` (supply-side
market circulation, not consumer `shopping`), `food-drink → food`. Logged 2
`apply` lines to `reviews/decisions.jsonl`; ledger updated with self-check spend.
Outcome: **2 applied, 0 rejected, 0 flagged.**

## Capture
- Candidates added: none (replacement used only common existing words).
- Observation logged: `[tooling]` on `check_example_headword.py` noise families
  (skip U+FFFD entries; strip leading ～ from headword; treat katakana-of-reading
  as a match for animal/seafood nouns) — would shrink the queue to near zero.

## Metrics
`metrics_snapshot.py --mode systemic-fix --changed 1` appended (2 flags applied).

## Cursors
- systemic-fix has no ID cursor; backlog item state updated as above.
- selector state advanced in §1.
