# Routine v2 — 2026-08-27, run 001 (`wiki`)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: candidates 146, seen-in-entry 8, max entry ID 30753, comprehensive frontier 07023,
cross-model cursor 13900, unharvested observation lines 29, reviewed entries 586, open backlog
items 40. OpenRouter: $0.00 spent today, $5.00 remaining.

## Pre-flight

- §0a `mcp__github__list_pull_requests` (open) → **0 PRs**. Nothing to rescue, nothing to sweep.
- §0b `mcp__github__list_branches` → **`main` only**. Zero orphan `claude/*` branches.
- Lock acquired on `claude/intelligent-curie-e79b5e`.

## Work done

Wiki-only run. **No entries changed**, so §4 self-verification does not apply and OpenRouter was
not called ($0.00 this run). No `make build` (markdown + two JSON index files; `docs/` untouched).

### [F] Harvest — 29 observations from three runs, all cleared

Sources: the 2026-08-24 accuracy-review run (12913–13412), the 2026-08-24 polish run
(priority lane + frontier 07005–07022), and the 2026-08-26 accuracy-review run (13413–13899).

Filed with measurements:

| Filing | Scope |
|---|---|
| **Tooling 137** — `screening_status.json` is a strict subset of the results on disk | short by **12,016 of 22,991** |
| **Tooling 138** — screening skips print nothing; the "glob order" diagnosis refuted at the source | logging fix |
| **Tooling 139** — parse failures recorded as flags (69 of 1,974) + verdicts inferred from prose | 2 clerical inflators |
| **Tooling 114 update** — per-entry `screened_at` has existed all along; whole-disk pre/post-fix rates | 8.6% → 5.7% (1.5×, not 3×) |
| **Tooling 118 update** — the reviewer misreports `politeness`, not just `semantic` | 13450 |
| **Tooling 111/118 update** — `general`-is-too-broad measured a third time | 53 of 93 tag flags |
| **Tooling 20 update** — the note scorer credits ALL-CAPS headers only | ranking artefact |
| **Tooling 134 update** — the self-declaration test measured, plus the refinement that keeps the 6 good pairs | 7 of 175, 1 TP |
| **Tooling 135 update** — a second, 4×-divergent throughput number; timeouts lose the index | wall-clock |
| **`TAG_MIGRATION` gap** — all ten proposed off-vocab mappings absent from a 9-row map | ~840 entries |
| **Cleanup P50 re-measure** — zero links below the frontier, unchanged, and in blocks | **57**, in runs of 21 and 9 |
| **Cleanup P68** — sole-`education` on discipline names | ~20 of 461 |
| **Cleanup P69** — kana-only furigana braces | 753 instances / 529 entries |
| **Entry follow-ups** — 07015/16878 duplicate; P19's substring blind spot; 07010's invented 髪抜き | 16 entries in P19's queue |
| **Prompt recommendations** (curator; wiki sessions do not edit prompts) | 3 |

**Refuted**: `existence` as a batch-fixable catch-all (112 lead-tag entries, but most are correctly
tagged; the crisp cut is 6). **Re-discoveries needing no new item**: `・` bullets (P62, third
filing), the duplicate conjugation stub in verb notes (P31/P54 + Tooling 132, sixth filing),
sole-`general` clusters (polish-lane side task by standing ruling), and the zero-link band above
the frontier (eighth re-derivation of the do-not-file ruling).

### [H] Metrics refresh 39 (runs 537–548)

12 new `metrics-history.jsonl` lines since refresh 38, above the ≥10 threshold. Written as §22 of
`planning/wiki/topics/quality-metrics.md`.

- **`furigana`: third consecutive exactly-zero window** — 0 applied of 81; 221 flags across three
  windows, zero corrections — now with a mechanism rather than a hedge (69 parse-failure flags,
  plus Tooling 119's truncation family; 516 of 1,974 flagged concerns contain their own acquittal).
- **§14 reproduces a seventh time at its widest separation**: headline `tags` 28.3% = off-vocab
  migration 100% [42/0] + individually-adjudicated 91.7% [22/2] (residual **97.0%**) against three
  policy-settled families at **1.2%** [2/165]. 165 of the 167 rejects are eight aggregated lines.
- `gloss` **54.5%** [12/10], best since June; `translation` 45.5%; self-check 61.1% [11/7].
- Entries 30,504 → **30,544**; frontier 6,985 → **7,023** (+38 against +40 growth); review queue
  +33 on 343 entries changed; candidates **146** (below the 150 restock threshold); OpenRouter peak
  day **$0.2377**, the lowest in two months.

Two `[pattern]` observations logged for the next harvest (the furigana yield/cost imbalance; the
queue metric's unreadability until Tooling 94 ships).

### [E] Wiki link and anchor lint

10 genuinely broken anchors repaired, 0 broken file targets. The first pass reported 48 — 38 were
the linter's own error, because GitHub does not collapse whitespace runs when slugging a heading.
The real ten were rename drift, two truncated anchors, and one this session had just created.
The lint also caught a **duplicate filing before it landed**: the zero-link finding had been
written as a new P68 and a new queue row, and is P50.

### [A] Backlog-queue sync

Four items added (`screening-status-rebuild`, `tag-education-disciplines`,
`furigana-kana-only-braces`, plus the P50 row re-measured), one removed as a duplicate. Queue now
holds 90 items.

## §4 self-verification

**Not applicable** — no entries created or modified this run. OpenRouter untouched; ledger
unchanged at $0.00 for the day.

## §5 metrics

Appended: `mode=wiki, changed=0, entries_total=30544, review_queue_len=10803, candidates=146,
comprehensive_next=7023`.

## Cursors

No cursor advances (wiki mode carries no range cursor). `pipeline/routine-state.json` advanced by
the selector in §1.

## Next

`candidates` is due on the selector's own signal — the queue stands at 146, below the
`candidates_low` threshold of 150.
