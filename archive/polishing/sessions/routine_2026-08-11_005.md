# Routine v2 — wiki mode — 2026-08-11 (005)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: candidates 97, seen-in-entry 9, max entry ID 30574, comprehensive next 6876,
cross-model-review next 8066, unharvested observations 31 (27 real + 4 template lines),
reviewed entries 586, open backlog items 32.
**OpenRouter**: spent today $0.2506 of $5.00 cap; **$0.00 spent this run** (no API calls —
wiki mode changes no entries).

## Pre-flight

- §0a rescue: `list_pull_requests` returned **no open PRs**. Nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs, so nothing to close.
- §0b orphan-branch sweep: `list_branches` returned **`main` only**. Zero orphans.
- Lock acquired for `claude/determined-rubin-ygpg51`.
- Branch was level with `origin/main` at `0e5a35326`.

## Work done

Harvested all **27** observations from `polishing/observations.md` (the 2026-08-10 metrics
refresh, the 2026-08-10 polish (008) and stale-`noentry` runs, the 2026-08-11 polish of
06865–06875, and the 2026-08-11 accuracy-review of 07566–08065), then three further activities.

### Two filed observations failed verification

**1. "A contiguous block of 25 entries (07832–07861) with no semantic tags at all."** Does not
exist. Re-measured across all 30,365 entry files: **zero** such entries in 07566–08065. The
dictionary-wide population is **79** (19 basic / 4 core / 56 general), down from 105 on
2026-08-04; the real clusters are 08635–08659 (13), 03948–03969 (12), 02814–02924 (10),
08812–08840 (10). Cause is in the reporting run's own `[pattern]` note: its census read
`entry["tags"]` instead of `entry["metadata"]["tags"]`, which returns `{}` silently — the same
bug that made it report "0 off-vocabulary tags" for a band that had 208. Filed as a bounded
79-entry queue item (`tag-missing-semantic-entirely`, needs-detector) plus Tooling 101.

**2. "The furigana screener's prompt builder still truncates readings" (85 flags, 0 true
positives, third consecutive sweep).** The prompt builder is correct — `trim_context()`
(`build/review_runner.py:152`) was fixed 2026-07-30, and the `{kanji|reading}` pair was never
windowed. The quoted flag is in `reviews/screening/07586.json` dated **2026-06-19**. Measured
across the whole cache:

| Stored screening results | Count | Flagged | Flag rate |
|---|---|---|---|
| Before the 2026-07-30 fix | 19,368 (85%) | 1,950 | 10.1% |
| After the fix | 3,454 (15%) | 170 | 4.9% |

In the 07566–08065 band the "85 flags / 60 entries" report was reading 54 pre-fix flags and 7
post-fix ones. The fix worked and halved the flag rate; nothing invalidated the pre-fix cache, so
three sweeps have re-adjudicated June output and attributed its 0% precision to current code.
Consequence: **every post-fix precision figure on record is contaminated**, including the
"0 applied of ~158 flags across eight consecutive runs" underpinning the retire-the-screener
case. Filed as instrument-defects **case 8** and **Tooling 102**; Tooling 24's
retire-or-downsample decision marked not-yet-evaluable.

### Files changed

| File | Change |
|---|---|
| `planning/wiki/project/open-issues.md` | Candidate-list quality → RESOLVED (purge + per-word vetting + the raw-count-is-usable-count consequence); proper-names section → RESOLVED, rewritten around the decision |
| `planning/wiki/ideas/dictionary-growth.md` | Decision table: no schema change, no relaxed examples, ordinary tags; priority verdict superseded |
| `planning/wiki/project/vocabulary-tiers.md` | Person-name question answered (sixth filing) |
| `planning/wiki/ideas/cleanup-backlog.md` | Updates 2026-08-11: the 79-entry correction, P20-vs-P11 separated, P11 continuity, P43 third cohort, formality as a cost argument |
| `planning/wiki/ideas/tooling-backlog.md` | New items 101–104; updates to 24, 77, 90, 94, 95 |
| `planning/wiki/ideas/entry-followups.md` | Proper-noun thread closed; 03515 日光; prohibitive な |
| `planning/wiki/ideas/backlog-queue.json` | `unknown-semantic-tags` → 1,603; `tag-sole-general` → 3,681; formality item annotated; new `tag-missing-semantic-entirely` |
| `planning/wiki/topics/instrument-defects.md` | New case 8 + summary-table row + sequel note on case 3; date → 2026-08-11 |
| `planning/wiki/log.md` | Session entry |
| `polishing/observations.md` | All 27 observations cleared with a harvest record |
| `reviews/needs_curator.txt` | One escalation (below) |

### Escalated to the curator

The **"general is too broad"** reviewer-flag family — ~25–30% of tag-flag volume every sweep,
rejected wholesale under standing §A policy, **fifth filing**, three runs having written that it
needs a decision rather than another observation. Escalated as a two-option choice: teach the
reviewer prompt that `general` is an accepted fallback, or rule `general` unacceptable as a sole
tag and hand the family to the free `check_tag_drift.py --check sole-general` detector.

## §4 self-verification

**Not applicable** — wiki mode changed no entry files (`git status --porcelain -- entries/` is
empty). $0.00 OpenRouter spend this run; the ledger is unchanged.

## §5 metrics

Appended: entries_total 30,365, candidates 97, review_queue_len 10,238, comprehensive_next 6876,
observations_lines 1,170, OpenRouter spent today $0.2506.

**Activity [H] (quality-metrics refresh) deliberately skipped**: `metrics-history.jsonl` stands
at 487 lines against the 34th refresh's 480 — **7 new lines against the ≥10 threshold**. The next
wiki harvest should expect to run it.

## Candidates added

None. No Japanese words lacking entries were encountered — this run read wiki pages, backlog
files, and instrument code, not entries.

## Cursors

- `wiki` mode has no ID cursor; `planning/wiki/log.md` is the state and was appended to.
- No entry-polishing cursor touched. `polishing/tasks/comprehensive/progress.txt` stays at
  `next: 6876`.
- `pipeline/routine-state.json` was advanced by the selector in §1.

## Build

**Skipped** per §6 — wiki-only runs change markdown and JSON under `planning/` and touch no
entries, so `docs/` is unaffected.
