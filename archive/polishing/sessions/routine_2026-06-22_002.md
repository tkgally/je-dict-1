# Routine session — 2026-06-22 (002)

- **Mode:** accuracy-review (furigana phase)
- **Selector reason:** "accuracy-review: highest scheduler debt among eligible modes"
- **Params:** start_id 8238, phase furigana, session budget $2.50 (daily remaining $4.99 at start)

## Pre-flight
- §0a rescue: no open PRs — nothing to rescue.
- §0b sweep: 0 stranded `claude/*` PRs closed.
- Routine lock acquired for `claude/modest-keller-7jzbx2`.

## Work done (§A furigana phase)
- Ran `review_runner.py --pass screening --range 8238 8838` (google/gemini-2.5-flash).
  The 25-min wrapper timeout killed it after **221/601 entries** (8238–8458);
  per-entry results were kept per §A resilience (no retry).
- **30 entries flagged** by screening (~14%). Independently verified every one against
  the actual entry file: **all 30 are false positives.** Families:
  - **Truncated-example artifact (dominant):** the screening model received example
    text cut off mid-word and flagged the truncation as an "incomplete reading"
    (e.g. 昇格→しょ, 最高→さい, 視聴率→しちょうり, 永久凍土→えいき, 復号→ふく, 勤務先→きんむさ,
    不戦勝→ふせんし, 出場→しゅつじょ; several concerns even render `)` where the entry has `}`).
    The actual entries all carry correct, complete readings (昇格→しょうかく, etc.).
  - **Okurigana / partial-reading splits (correct by design):** 剥がす 剥→は,
    食べた/食う/食い 食→た/く/ぐ, 消える 消→き — documented FP family.
  - **Model hallucinations:** 08395 採決 (alleged 採血 "typo"; entry headword/reading
    both さいけつ, correct), 08330 熱中症 (misread the notes-format "{…|ねっちゅうしょう}
    refers to…" as a "redundant reading").
- Per §A known-noise shortcut: bulk-rejected as ONE aggregated `reviews/decisions.jsonl`
  line; **deep pass skipped** (0% precision in range).

## §4 self-check
- No entries created or modified this run (all flags rejected) → self-verification not
  applicable. Clean.

## Capture
- Candidates added: none (no missing-entry words encountered while reviewing furigana).
- Observations: 2 `[tooling]` lines added to `polishing/observations.md` —
  (1) the screening-prompt truncation artifact (recommend sending example fields
  untruncated / truncating only at furigana-pair boundaries to cut this dimension's
  noise); (2) screening throughput (~6 s/entry here) overran the 25-min timeout —
  future runs should size the screening range to ~200 entries.

## Budget
- OpenRouter ledger: +$0.11 (screening, 221 entries) → day total $0.1182 of $5.00 cap.

## Wrap-up state
- Cursor `polishing/tasks/cross-model-review/progress.txt` → `next: 08459`
  (advanced only to where screening actually reached).
- `reviews/queue.txt` left untouched: this run covered only the **furigana** dimension,
  not the gloss/translation/tag accuracy dimensions, so the range stays queued for a
  later full accuracy pass.
- Metrics snapshot appended (entries_changed 0, flags_rejected 30).
- No `make build` (no entry/`docs/` changes).

## Next
- accuracy-review continues from 08459. Consider a smaller screening range (~200) and
  the prompt-truncation fix before the next furigana phase.
