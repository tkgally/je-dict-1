# Routine session — 2026-07-28 (001)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Params**: `start_id: 19951`, `openrouter_session_budget_usd: 2.50`
**Signals**: max_entry_id 30182, comprehensive_next 6664, cross_model_review_next 19951,
candidates 1029, unharvested observations 10, open backlog items 7

## Range worked

- **Gloss / translation / tag review** (`review_accuracy.py`): **19951–20450**, 498 entries,
  complete. 220 entries flagged (44%), 244 individual issues.
- **Furigana screening** (`review_runner.py --pass screening`): **19951–20388**, 423 entries.
  The pass was cut short by its wall-clock timeout at ~20320 (exit 143). Per §A step 2's
  resilience rule the per-entry results already written were kept and the run continued;
  the pass was not retried. Deep furigana pass **skipped** under the known-noise shortcut
  (see below).

Both passes ran concurrently to halve wall-clock; they write to different directories.

## Changes made — 111 entries

**Semantic tag migrations (99 entries).** 221 of the 498 entries in range carried
off-vocabulary semantic tags. Applied only where a 1:1 target exists:
the project's own `TAG_MIGRATION` map from `build/check_tag_drift.py`
(`time`→`time-general`, `people`→`person`, `social`→`society`,
`description`→`descriptive`, `medical`/`medicine`→`health`,
`transport`→`transportation`, `economy`→`economics`) plus pure orthographic variants
of an existing valid tag (`daily life`→`daily-life`, `arts`→`art`, `sport`→`sports`,
`grammar`→`grammatical`, `emotions`→`emotion`, `season(s)`→`time-season`,
`food-drink`→`food`). The 24 entries in the `body` family were read individually and
assigned `body-part` / `body-internal` / `health` / `appearance` / `expression` per
headword. The apply script refuses to write any tag not in `VALID_SEMANTIC` unless it
was already present, so the change can only improve tag vocabulary.
`validate_tags.py --check-no-new-unknown` passes; all 498 entries validate.

**Gloss / translation fixes (5 entries).**

| ID | Change |
|---|---|
| 20022 {金品} | example 3: "money and gifts" → "money and valuables" (金品 is neutral/broader) |
| 20150 {片側} | example 2: "a one-lane road on each side" → "a road with one lane in each direction" |
| 20173 {多文化} | gloss: dropped "multiculturalism" (that is 多文化主義); now "multicultural; relating to multiple cultures" |
| 20274 {食べ歩く} | main gloss omitted sense 2; added "to eat while walking" |
| 20418 {雑食} | noun headword had an adjective-only gloss; added the nominal sense "omnivory" |

**Latin letters in furigana — 8 entries, class now empty dictionary-wide.**
The screener flagged `{旅|たbi}` in 19961. A regex scan over every entry found 7 more:
`{間違|まちga}` (08579), `{形式|けいしiki}` (25195), `{敷金|しikikん}` (06080),
`{らいはいどう|raihaidou}` (27034), `{雅楽|yǎyuè}` (27354), `{X用|ようi}` (26505),
`{〜たる|taru}` (24398). Each was read in context before fixing; the last four were
wrapper misuse (kana or a placeholder wrapped as if it were kanji) rather than a bad
reading. Rescan now reports **0** furigana readings containing `[A-Za-z]`.

## Adjudication summary

- **Applied 112**: 99 tag migrations, 5 gloss/translation, 8 furigana.
- **Rejected 80**:
  - 47 furigana screening flags — one family. The model reads the truncated
    "followed by" context preview (clipped mid-word, e.g. `{記者会見|きしゃか}`) as the
    entry's actual reading and reports it as incomplete. Spot-checked nine against the
    real files: 20260, 20279, 20334, 20122, 20212, 20083, 20052 and 20273 are all correct
    in the entry. Screening precision this run ≈ 2% (1 of 48), matching the 0–5% measured
    on already-polished ranges — so the deep pass was skipped per the known-noise shortcut.
  - 20 "too broad / too narrow" substitutions between two in-list tags — §A rejects these.
  - 5 formality flags (20118, 20087, 19977, 20346, 20004) — each entry's own notes
    explicitly describe the word as formal/business/academic, so the label stands.
  - 4 "suru-verbs must carry the `action` tag" — an invented rule, not project convention.
  - 4 gloss/translation nits on 19970 {弾み}: "bounce" and "chance occurrence" are genuine
    senses, and the existing example-5 translation is accurate and more natural than the
    suggested replacement.
- **Flagged to curator 129**: entries in range still carrying off-vocabulary semantic tags
  with no 1:1 target (`position` 7, `relationship` 6, `character` 5, `physical` 5,
  `agriculture` 5, `perception` 4, `math` 4, `disaster` 4, `location` 4, `quality` 4,
  `commerce` 4, `personal-quality` 4, …). These need a taxonomy decision — extend
  `VALID_SEMANTIC` or map to existing tags — not per-entry guessing. Also flagged: a
  suggested CI check rejecting any furigana reading containing `[A-Za-z]`.

All decisions logged to `reviews/decisions.jsonl` (§C).

## §4 self-check

The 104 changed entries inside 19951–20450 were covered by §A's own review, so they were
skipped per §4 step 3. The 7 furigana fixes **outside** the range were sent to the
independent screener: **clean — 0 flags on all 7** ($0.0009).

## Candidates added

None. No unknown Japanese words were encountered — this mode reads review reports and
existing entries rather than drafting new text.

## Observations logged

Four appended to `polishing/observations.md`: the off-vocab tag backlog as the dominant
defect in this band `[pattern]`; the resolved Latin-furigana class `[pattern]`; the
`review_accuracy.py` tag-dimension over-flagging and its two invented rules `[tooling]`;
the screener's truncated-preview false positive family and its ~8–9 entries/min throughput
vs ~55/min for the accuracy reviewer `[tooling]`.

## OpenRouter spend

$0.2685 of the $5.00 daily cap — accuracy $0.2176 (498 entries), screening ~$0.05
(423 entries), self-check $0.0009 (7 entries). Session budget was $2.50; well under.

## Next cursor

- `polishing/tasks/cross-model-review/progress.txt` → **next: 20451**
- `reviews/queue.txt` pruned of 19951–20450 (12,659 → 12,590 lines)

Note: the furigana screening only reached 20388, while the accuracy review covered the
full range. The cursor follows the accuracy pass, which is the mode's primary instrument;
the un-screened tail 20389–20450 is low-value to revisit given ~2% screening precision.
