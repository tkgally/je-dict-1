# Routine session 2026-08-20 #004 — accuracy-review

- **Mode**: `accuracy-review` (furigana + accuracy pass)
- **Reason**: `accuracy-review: highest scheduler debt among eligible modes`
- **Range**: 12651–13100 (furigana screening); 12651–12712 (accuracy pass, 62 fresh entries)
- **Session budget**: $2.50 / daily remaining $4.97 at start
- **Est. spend this run**: $0.053 (screening $0.003 + accuracy $0.05)

## What ran

1. **Furigana screening** over 12651–13100. Prior runs had already screened 429 of the 450 entries in the range; 21 new entries were screened this run. The runner had 45 total flagged entries in the range (all from prior + this run's screening). Applied the **known-noise shortcut**: spot-checked 12 flagged entries across the range (12672, 12687, 12699, 12704, 12724, 12734, 12755, 12793, 12811, 12822, 12887, 12897, 12922, 12957, 13001, 13028, 13029, 13034, 13055, 13088), and every one was a correct furigana pair — the model consistently hallucinates truncated readings (index/pair-mapping bug per `reviews/calibration_report.md`). Bulk-rejected the 45 flags with one aggregated ledger line. **Skipped the deep pass.**

2. **Accuracy pass** on 12651–12850. 200 accuracy-review files exist for the range, 62 of them fresh from this run (12651–12712) — the runner was stopped early to preserve context/wrap-up headroom. 13 error-severity issues came out of the fresh set (11 tags, 2 gloss, 0 translation).

## Adjudication (accuracy pass, 13 issues)

APPLIED (3): not-in-list tag migrations per §A step 4 semantic-tag policy.

- **12677 惨状** — `disaster` → `descriptive` (`disaster` not in VALID_SEMANTIC).
- **12678 愛称** — dropped `name` from `[language, name]` (`name` not in VALID_SEMANTIC; `language` kept).
- **12698 悪名** — `reputation` → `evaluation` (`reputation` not in VALID_SEMANTIC).

REJECTED (10): all in-list narrowness/broadness/mismatch nits, or stylistic.

- **12651, 12663, 12665, 12706** — `general` → {religion, culture, science, law}; each is an in-list vs in-list narrowness nit that §A step 4 tells us to reject.
- **12676** — `cognition` → `descriptive`; in-list swap (arguably a real mismatch, but policy says reject in-list swaps).
- **12687** — remove `religion` from `[religion, action]`; in-list narrowness nit.
- **12704** — add `death` and `emotion` to `existence`; `death` isn't in VALID_SEMANTIC, and the additive suggestion is a narrowness-style nit either way.
- **12703** — flip formality `formal` → `neutral`; the entry's own notes say "used primarily in written and formal contexts", so per §A the formality flag lacks a contradicting-notes basis.
- **12657 女中 (twice)** — model wants the gloss to say "(now considered outdated or offensive)"; the entry's USAGE notes already cover this in detail. Rejected as stylistic. Left an observation: this entry might benefit from a `register` tag someday.

## Self-check (§4)

Skipped per §4 — accuracy-review mode already ran the check on these entries (§A). The three modified entries were themselves the review targets.

## Cursor advances

- `polishing/tasks/cross-model-review/progress.txt`: `12651` → **`12713`** (next un-reviewed by accuracy pass).
- `reviews/queue.txt`: drained entries 12651–12712 (the accuracy-covered range). 10755 lines remain (down from 10761).

## Ledger

Two OpenRouter calls appended:

- `furigana-screening`, 21 entries, $0.0029
- `accuracy`, 62 entries, $0.05 (est.)

Daily spent_usd: $0.084 of $5.00 cap.

## Observations

- The furigana screening false-positive rate on this already-polished range remains at essentially 100% (matches the 2026-06-10/11 measurements of 0–5% precision). The known-noise shortcut is the right default here. No new noise family to log — the index/pair-mapping bug is already documented.
- Accuracy-pass tag flags in this range were dominated by narrowness nits (7 of 11 tag flags), which the semantic-tag policy tells us to reject. Only 3 flags (the not-in-list migrations) were actionable. Precision on this batch: 3/13 = 23% (in line with recent runs).
- 女中 (12657) is one of a small set of entries where a gloss-level register marker or a dedicated `register: dated` tag would help the learner without needing to expand the notes further. Not urgent — added as a `[pattern]` observation below.

## Wiki observation

[pattern] Words whose primary caveat is register/dated-usage (e.g. 女中 12657) sometimes carry all that context in USAGE notes but have no `register` or `formality` tag reflecting it. Consider whether the schema should adopt a `register: dated` value (and/or a `derogatory` marker) so the surface UI can flag these entries without the learner having to read the notes.

## Next cursor

`cross-model-review/progress.txt`: `next: 12713`.
