# Routine session — 2026-08-15 (006)

**Mode:** `polish`
**Selector reason:** "polish: highest scheduler debt among eligible modes"
**Selector signals:** candidates 154, seen-in-entry 8, max entry ID 30673,
comprehensive next 06942, cross-model-review next 11501, unharvested
observations 4, reviewed entries 586, open backlog items 36.
**Params:** `start_id: 6942`. Priority file `polishing/priority/notes.txt`
dated 2026-08-11 (3 days old, fresh), priority cursor at line 182.

## Pre-flight

- §0a rescue: no open PRs — nothing to rescue.
- §0b stranded-PR sweep: no open `claude/*` PRs.
- §0b orphan-branch sweep: `list_branches` returned only `main`. Zero orphans.
- Lock acquired for `claude/determined-rubin-8hpdv5`.

## Priority lane (8 entries, lines 182–191)

Every entry on this stretch of the list scored exactly 77, and in every case
the missing 10 points came from the same thing: the notes used `・` for bullet
lists where `.claude/skills/vocabulary-notes/SKILL.md` mandates `- `
(hyphen-space). Converting line-initial `・` to `- ` moved all eight from 77 to
87. Skipped 00503_hataraku and 00538_aruku (line 182 and 190) — both modified
2026-07-30, inside the 30-day window.

- 00508 kaeru (帰る): bullets converted. Notes claimed "No pair verb" but the
  transitive pair 帰す exists — rewrote that line, added `prominent_see_also`
  to 02030_kaesu, added the missing antonym back-link to 00833_dekakeru.
- 00513 deru (出る): bullets converted. Stale `noentry` on 会 relinked to
  27493_kai. Added `prominent_see_also` to 01087_dasu and upgraded the existing
  `see_also` cross-reference to type `pair` (01087_dasu linked here but got
  nothing back). Two example translations corrected (see self-check below).
- 00519 hashiru (走る): bullets converted; otherwise clean.
- 00521 okiru (起きる): bullets converted; back-link added on the neighbour
  01234_okosu, which pointed here but had no reciprocal link.
- 00526 oshieru (教える): bullets converted; otherwise clean.
- 00531 hajimeru (始める): bullets converted; otherwise clean.
- 00532 kariru (借りる): bullets converted; added the missing antonym
  back-link to 09648_kaesu (返す).
- 00555 tomaru (止まる): bullets converted, plus a structural fix found by the
  self-check — see below.
- 01234 okosu (起こす) — neighbour touched for back-link symmetry only, plus a
  gloss fix from the self-check.

More than half of the priority-lane entries needed changes, so the priority
files were **not** regenerated and the cursor advances normally to line 192.

## Frontier lane (5 entries, from 06942)

All five were grammar-pattern / conjunction entries with **zero inline word
links** in examples or notes — the largest tier-1 gap in this stretch. Each
got full link coverage across every example and every Japanese phrase in the
notes, plus `・` → `- ` bullet conversion and cross-references.

- 06942 samonaito (さもないと): 5 examples fully linked; notes linked;
  related-link to 06943.
- 06943 sousuruto (そうすると): 10 examples fully linked; notes linked;
  synonym 01627_suruto, related 05785_sorenara and 06942.
- 06944 nikanshite (に関して): 5 examples fully linked; notes linked and the
  trailing arrow-fragment lines rewritten as a sentence.
- 06945 nisaishite (に際して): 3 examples fully linked; notes linked.
- 06946 womotte (をもって): 6 examples fully linked; notes linked; added
  related/contrast cross-references to を and で (it had none).

## §4 self-check (independent model, 14 entries, ~$0.0065)

`build/review_accuracy.py` over exactly the entries changed this run.
Eleven of fourteen came back clean, including all five newly-linked frontier
entries. **4 applied, 1 rejected, 0 flagged** (plus one aggregated apply line
covering the five migrated examples).

- **00555 tomaru — real structural error, applied.** The entry was headed
  止まる but carried a whole second sense, "to stay overnight", written with a
  different kanji (泊まる) and backed by five examples. 泊まる already has its
  own entry, 10445_tomaru, which even links back here as a homophone. Removed
  sense 2 and its five examples from 00555, migrated the three that 10445
  didn't already cover (10445 goes 3 → 6 examples), and added the reciprocal
  homophone link plus the transitivity pair 00678_tomeru, which was missing.
- **00513 deru — two translations corrected, one rejected.** 声が出ません was
  glossed "I can't make a sound" → "My voice won't come out"; 答えが出ましたか
  was "Did you find the answer?" (implies searching, which 出る doesn't) →
  "Did the answer come out?". Rejected the suggestion to change "I got a fever"
  to "I developed a fever" — stylistic only.
- **01234 okosu — gloss widened**, "to wake (someone) up, to cause" → "to wake
  (someone) up, to raise, to cause". 起こす does carry the raise/set-upright
  sense and the entry's own examples use it.

All seven adjudications logged to `reviews/decisions.jsonl` with
`src: "self-check"`; spend recorded in the ledger under `phase: "self-check"`.

## Candidates added (6)

From `noentry` markers written during frontier linking:

- さもなければ — otherwise; or else (formal, written); seen in 06942
- そうしないと — if you don't do that; otherwise; seen in 06942
- じゃないと — otherwise; if not (casual); seen in 06942
- でないと — or else; if not; seen in 06942
- だとすると — if that is the case; assuming that; seen in 06943
- ということは — that means; which is to say; seen in 06943

## Observations logged

- `[pattern]` 2,507 entries use `・` bullets against the documented `- `
  standard, which silently costs each of them 10 note-quality points and
  distorts the notes priority ranking. Proposed as a `systemic-fix` batch item.
- `[tooling]` `word_id_lookup.json` doesn't index slash-separated headword
  variants (`速い／早い`), producing false `noentry` results.
- `[tooling]` `review_accuracy.py` emits `"description": null` on every issue.
- `[entry]` 06946: 三月末 left as `noentry` (compositional, no candidate filed).

## Cursors

- `polishing/tasks/comprehensive/progress.txt` → `next: 06947`
- `polishing/tasks/comprehensive/priority-cursor.txt` → `line: 192`
