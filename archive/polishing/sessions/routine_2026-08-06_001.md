# Routine session — 2026-08-06 (001)

**Mode:** `accuracy-review`
**Selector reason:** "accuracy-review: highest scheduler debt among eligible modes"
**Params:** `phase: furigana`, `start_id: 26189`, `openrouter_session_budget_usd: 2.50`
**Signals:** candidates 991 (20 "seen in entry"), max entry ID 30433, comprehensive next 6800,
cross-model-review next 26189, unharvested observations 25, reviewed entries 586, open backlog items 21

## Pre-flight

- §0a: no open PRs — nothing to rescue.
- §0b: no `claude/*` strands to sweep. Lock acquired.

## Range worked

**26189–26700 (512 entries).**

| Pass | Coverage | Flagged | Est. cost |
|---|---|---|---|
| `review_accuracy.py` (gloss/translation/tags) | 512/512 | 158 entries, 179 issues | $0.2242 |
| `review_runner.py --pass screening` (furigana) | 230/512 (26189–26418) | 9 | $0.0247 |
| §4 self-check (`review_accuracy.py --ids`) | 22 | 4 | $0.0096 |
| **Total** | | | **$0.2585** |

Screening was **truncated at 26418** and the deep furigana pass **skipped** under §A step 2's
known-noise shortcut: 8 of its 9 flags fell squarely in the documented false-positive families
(rendaku in compounds, okurigana/compound reading splits, pair-index confusion), matching the
0–5% precision measured 2026-06-10/11. It also runs ~4x slower per entry than the accuracy pass,
so finishing it would have doubled the run's wall-clock for near-zero expected yield. Logged as a
`[tooling]` observation. **The accuracy sweep covered the full range**; the cursor advance below
reflects that, and the furigana gap for 26419–26700 is noted here rather than silently absorbed.

## Changes applied (77 entries)

### Gloss / translation corrections (6, all APPLY)

| Entry | Change |
|---|---|
| 26287 乱筆 | main gloss `hastily written` → `hastily written letter` (adjective phrase in a noun gloss; def0 already had the noun form) |
| 26310 毛根 | main gloss `hair root; hair follicle` → `hair root; root of a hair` (the follicle is 毛包/毛嚢, a different structure) |
| 26517 自宅学習 | dropped `distance learning` from the main gloss (that is 遠隔教育/通信教育) |
| 26557 噴く | ex2 "Steam is **blowing**…" → "**spouting**" — "blowing" directly contradicted the entry's own 吹く/噴く contrast note |
| 26228 素材感 | ex2 "the **quality** of the ingredients" → "the **natural character** of the ingredients" (drifted from the headword's texture/material-feel sense) |
| 26297 外皮 | ex0 "To peel the skin of a banana." → "Peel the skin…" (bare infinitive against the imperative house style) |

### Furigana correction (1, APPLY)

- **26418 編纂者** — `{歴史書|れきしょ}` → `{歴史書|れきししょ}` in the COMMON COLLOCATIONS block.
  The single true positive from the whole screening pass, and it was in notes rather than a
  headword or example.

### Semantic tag migrations (70 entries, APPLY)

70 of the 512 entries (13.7%) carried semantic tags outside `VALID_SEMANTIC`. Per the
2026-06-11 semantic-tag policy a not-in-list flag is correct by definition, so each was migrated
to the closest in-list tag, verified per entry against the headword and gloss (not applied
mechanically). `build/check_tag_drift.py`'s `TAG_MIGRATION` map covered only 9 of the ~60 distinct
off-list tags; the rest were mapped by hand, e.g.

`architecture/construction→building`, `crafts→creation`, `character→personality`,
`weapons→military`, `botany→plant-general`, `physics/biology→science`, `aviation/traffic→transportation`,
`historical→history`, `tools→tool`, `equipment→tool`, `games→entertainment`, `commerce→business`,
`psychology→health`, `mental→cognition`, `spatial→direction`, `lifestyle→daily-life`,
`calendar/time→time-general`, `idiomatic→idiom`, `martial-arts→sports`, `fishery→animal-fish`,
`counter→number`, `question→grammatical`, `consumer→shopping`, `performing-arts/design/literature→art`.

Five off-list tags (`audio`, `materials` ×2, `agriculture` ×2, `safety`, `news` ×2) were **dropped**
rather than remapped, because an in-list tag already on the entry covered the concept and no
honest in-list target existed.

`build/data/unknown_semantic_baseline.json` regenerated afterwards: 2880 → 2530 tolerated entries
(my 70 plus ~280 that earlier runs had already fixed without regenerating). Strictly stricter for
the CI ratchet.

## §4 self-check outcome

**22 entries re-reviewed by an independent model (the 7 content/furigana edits plus a 15-entry
sample of the tag migrations): 4 flagged → 2 applied, 2 rejected, 1 flagged to curator.**

All six gloss/translation fixes and the furigana fix came back **clean**. The two applies were
**my own migration slips**, which is exactly what this gate is for:

- **26662 近年稀にみる** — I had mapped `degree→quantity`; the expression is evaluative, not a
  quantity. Corrected to `evaluation`.
- **26695 ミドルエイジ** — I had mapped `loanword→language`; loanword-ness is not a semantic
  field. Tag dropped, leaving `time-period`.

## Rejected (adjudicated, not applied)

- **Tags, 83 flags** — bulk-rejected as one family: in-list "too broad / too narrow" substitutions
  (mostly "replace `general` with something more specific"). Policy explicitly rejects narrowness
  nits between valid tags.
- **Furigana, 8 flags** — bulk-rejected as one family (rendaku, okurigana splits, index confusion).
- **26680 一眼カメラ** — model asked for "single-lens camera"; it is already the first gloss.
- **26589 風車小屋** — model wanted "windmill house" throughout; "windmill" is the natural English
  term for the building and is already paired with "windmill house" in the gloss.
- **26552 長孫** — model insisted on "eldest grandson"; 長孫 is the eldest *grandchild*
  (特に嫡男の長男), so the gender narrowing is unsupported.
- **26296 水系** — model said the gloss conflates two senses; the entry already splits them into
  def0 and def1.
- **26297 外皮 ex4 / 26556 溶く ex0** — "dumpling wrapper" and "beat the eggs" are the standard
  English renderings.

## Flagged to curator (`reviews/needs_curator.txt`)

- **26326 解像** — glossed "resolution (of an image)", but that sense belongs to 解像度
  (existing entry **04888_kaizoudo**), and all three examples use 解像度 rather than the headword.
  The entry's own notes concede 解像 "almost never appears alone". Needs a scoping decision
  (rescope as a bound morpheme / merge into 04888 / delete) — beyond an accuracy-review fix round.
- **26473 右党** — glossed "tea drinker; teetotaler; person who doesn't drink alcohol". 右党 is the
  counterpart of 左党: someone who prefers sweets/tea *to* alcohol (≈甘党), not necessarily a total
  abstainer. Gloss and three example translations all lean on "teetotaler/non-drinker", and one
  example's Japanese (お酒は一切飲まない) would need rewriting too, so this is an example-rewrite
  job rather than a gloss edit.

## Decision ledger

24 lines appended to `reviews/decisions.jsonl` (17 accuracy + 2 furigana + 5 self-check),
including two aggregated family-reject lines (`n:83` tags, `n:8` furigana) and one aggregated
apply line (`n:72` off-list tag migrations).

## Candidates added

None. No entry-less Japanese words were encountered — the range is dense compound nouns whose
components already have entries.

## Observations logged

Four appended to `polishing/observations.md`:

- `[tooling]` screening runs ~4x slower per entry than the accuracy pass; Routine runs should size
  the furigana range separately from the accuracy range.
- `[pattern]` screening precision 1/9 on this block; the single true positive was in a notes block,
  the least-swept furigana surface.
- `[pattern]` off-list semantic tags cluster at 13.7% in 26400–26700 vs an 8.4% dictionary-wide
  baseline — smells like one batch-creation session tagging from memory rather than the closed list.
- `[tooling]` extending `TAG_MIGRATION` with the ~55 mappings derived here would make the remaining
  ~2500 baselined entries batch-fixable instead of hand-adjudicated.

## Cursors

- `polishing/tasks/cross-model-review/progress.txt` → **`next: 26701`**
- `reviews/queue.txt` trimmed of 26189–26700: 12003 → 11491 lines
- `pipeline/openrouter-ledger.json` → $0.2585 spent today (cap $5.00)
- `pipeline/metrics-history.jsonl` → one line appended (77 entries changed, 81 applied,
  101 rejected, 2 to curator)
