# Routine session — 2026-07-27 (005)

**Mode**: `accuracy-review`
**Selector reason**: "accuracy-review: highest scheduler debt among eligible modes"
**Selector params**: `phase: furigana`, `start_id: 19452`, `openrouter_session_budget_usd: 2.50`
**Selector signals**: candidates 1041 (17 "seen in entry"), max entry ID 30164,
comprehensive_next 6661, cross_model_review_next 19452, unharvested observations 18,
reviewed entries 586, open backlog items 7

## Pre-flight

- §0a rescue: `list_pull_requests` returned **no open PRs** — nothing to rescue.
- §0b sweep: no `claude/*` PRs open, so no strands to close.
- Lock acquired for `claude/determined-rubin-ocmy1l`.

## Range worked

**Effective range: 19701–19950 (250 entries).**

The selector's `start_id` of 19452 was **stale**: the cursor
`polishing/tasks/cross-model-review/progress.txt` read `next: 19452`, but PR #3042
("cross-model review of 19179-19700, 160 entries corrected") had already reviewed
through **19700** without advancing the cursor past 19452. The first ~10 minutes of
this run re-reviewed already-adjudicated ground (19452–19528 accuracy, 19452–19554
furigana screening) before that was spotted. Those duplicate `reviews/accuracy/`
writes were reverted with `git checkout`; the run restarted at the true frontier,
19701. **The freshly-written screening results for 19452–19554 were kept** — furigana
screening had only ~7 files in that band, so that coverage is genuinely new.

Range sized to 250 rather than §A's nominal 400–600 because wall-clock, not dollars,
was the binding constraint (`review_accuracy.py` ran ~5 entries/min; total spend was
only $0.186 against a $2.50 session budget).

## Furigana pass (§A step 2)

Screening over 19452–19554 (102 entries): **13 flagged (12.7%), 0 applicable.**
All fell inside documented false-positive families, so per §A's known-noise shortcut
the **deep pass was skipped** and the family was bulk-rejected with one aggregated
§C line. Families seen:

- **Truncated-reading hallucination** (now the dominant family): the model misquotes
  the wrapper — e.g. reports `{両腕|りょう)` / `{会食|か)` / `{習慣化|しゅうか)` when the
  entries actually contain `{両腕|りょううで}`, `{会食|かいしょく}`, `{習慣化|しゅうかんか}`.
  Verified against the entry files for 19455, 19462, 19481, 19503, 19506, 19528.
  This is the entry-18448 index-confusion bug in `reviews/calibration_report.md`.
- **Okurigana splits**: `{除|のぞ}いてから`, `{手作|てづく}り`, `{面持|おもも}ち` — correct.
- **Rendaku in compounds**: `{尽|づ}` in 四百字詰め / 愛想尽かし — correct.
- **Readings the entry itself documents**: 19530 carries both `{愛想|あいそ}` and
  `{愛想|あいそう}` because its own VARIANTS section documents あいそう as a variant
  pronunciation and 愛想尽かし (あいそづかし) as the noun form. Correct as written.

Screening was also ~5× slower than `review_accuracy.py` and appeared to throttle it
when both ran concurrently, so screening was stopped after 19709 in the second
segment to let the higher-value accuracy pass finish. Logged as a `[tooling]`
observation with a concrete post-filter proposal.

## Accuracy pass (§A step 3)

`review_accuracy.py --range 19701 19950` — **250 reviewed, 121 entries flagged,
131 issues, $0.109.** Dimension split: `tags` 130 (62 error / 68 warn), `gloss` 1
(error), `translation` 0.

### The finding: off-vocabulary semantic tags

**124 of 250 entries (50%)** carried at least one semantic tag outside
`VALID_SEMANTIC` — **83 distinct off-list tags, 143 occurrences**. Per §A's
semantic-tag policy a "tag not in the list" flag is correct by construction, so
these were applied by migrating each to its in-list equivalent. Note that
**warn-severity was not noise here** — it was the same applicable family as error
severity, so it was worked rather than sampled.

Applied via a curated map in three classes:

- **~50 unambiguous synonym renames**: `time`/`time-sequence`/`calendar`→`time-general`,
  `body`/`body-eye`/`body-build`/`body-voice`→`body-part`, `body-condition`→`health`,
  `thought`/`perception`/`logic`→`cognition`, `social`→`society`,
  `society-government`/`government`→`politics`, `grammar`→`grammatical`,
  `food-drink`/`food-and-drink`/`food-dining`→`food`, `people`→`person`,
  `person-family`→`family`, `person-trait`→`personality`, `transport`→`transportation`,
  `housing`→`building`, `legal`/`crime`→`law`, `description`→`descriptive`,
  `nature-weather`→`weather`, `nature-water`→`nature`, `emotion-*`/`emotions`→`emotion`,
  `medicine`/`medical`→`health`, `actions`→`action`, `animals`→`animal-general`,
  `economy`→`economics`, `industry`→`business`, `measurement`/`degree`→`quantity`,
  `culture-tradition`/`folklore`→`culture`, `abstract-concept`→`abstract`,
  `object-daily`/`cleaning`→`daily-life`, `plant`→`plant-general`,
  `art-photography`/`photography`→`art`, `academic`/`school`→`education`,
  `fashion`/`accessories`/`accessory`→`clothing`, `stationery`→`tool`, `office`→`work`,
  `games`/`hobby`→`leisure`, `energy`→`science`, `quality`→`evaluation`,
  `quality-appearance`→`appearance`, `place`→`geography`, `craft`→`creation`,
  `conflict`→`military`, `yojijukugo`→`expression`.
- **Dropped as non-semantic** (etymology / rhetorical mode, not categories):
  `figurative`, `loanword`.
- **30 per-entry judgment calls** where no clean synonym existed and the model's own
  suggestion was wrong or too crude, decided by reading each headword, gloss and notes:
  19701 垢 → `daily-life` (grime is not a body *part*, and `health` overreaches);
  19708 化かす → `culture, action`; 19725 慶祝 → `culture`; 19729 ピンボケ → `art,
  communication`; 19737 度数 → `quantity, number` (the food-drink/health tags came from
  example topics, not the headword); 19740 勧善懲悪 → `culture, art, idiom`;
  19748 手順書 → `business, work`; 19763 やむなく → `descriptive` (describes manner, not
  an act of deciding); 19767 花畑 → `nature, plant-flower`; 19770 珍事 → `daily-life`;
  19776 引き締め → `economics, health`; 19777 軽油 → `transportation, technology`;
  19781 流血 → `military, health`; 19782 止血 → `health`; 19800 通読 → `language,
  education`; 19832 芳しい → `descriptive, abstract`; 19859 非常ベル → `tool, building`;
  19873 真珠 → `nature` (a pearl is not `clothing`, as the model proposed);
  19878 手仕事 → `work, creation`; 19885 置物 → `daily-life, art`; 19910 板場 →
  `building, occupation`; 19911 山札 → `leisure`; 19924 ぱらぱら → `onomatopoeia,
  weather, daily-life`; 19940 がっしり → `descriptive, appearance`; 19943 酷似 →
  `descriptive`; 19946 高音 → `music`.

Residue after the map: **0 off-list semantic tags remain in 19701–19950.**

Three in-list appropriateness flags were also applied after reading the entries:
19707 包囲する dropped `law` (not a legal term) and gained `action` per the suru-verb
convention; 19858 無機質な → `descriptive, science` (it describes an absence of warmth
rather than being an emotion); 19913 植え付ける gained `plant-general`.

**Rejected (6):** 19732 言うまでもなく and 19904 事細かに and 19763 やむなく formality
flags — each entry's own notes *support* `formal` ("less formal" contrast with
もちろん; "somewhat literary"; "slightly formal or stiff quality"), and §A only permits
a formality change when the notes contradict the label. 19748 手順書 formality — notes
say "standard workplace term", which does not contradict `formal`. 19836 模擬
(`education` "too narrow") and 19913's phrasing — narrowness nits between in-list tags,
which §A directs be rejected. 19840 帰化 — the suggestion was to add `biology`, which
is not in `VALID_SEMANTIC`.

### Gloss fix (1)

**19879 内職** — sense 2's gloss read "doing something else secretly; working on the
side". In English "working on the side" means a side job, which is *sense 1*, so the
gloss blurred the two senses. Rewritten to "doing something else secretly; doing
unrelated work during class or a meeting", matching the sense's own explanation.

## §4 self-verification

All 128 changed entries fell inside the range §A had just reviewed, so §4 step 3
permits skipping. Because the tag migration was a large systematic change, a
**targeted self-check was run anyway** on the 30 entries where the destination was my
own judgment rather than the model's suggestion (`--dimensions tags`, $0.0125,
independent pass).

Result: **10 flagged → 7 applied, 3 rejected.** Applied: 19701 (drop `health`),
19707 (add `action`), 19781 and 19782 (drop `body-internal` — blood is not an internal
organ), 19858 (`science` for the inorganic sense), 19924 (broader than `weather`),
19943 (drop `evaluation` — a state, not an act of judging). Rejected: 19748 and 19763
formality (notes do not contradict / actively support `formal`), 19879 (`work` → `occupation`
is a narrowness nit, and sense 2 is not an occupation at all). One pass, one fix round,
no re-verification.

The self-check also caught a **bug in my own migration script**: entries whose tags
were all already in-list short-circuited before the per-entry override table was
consulted, so the 19707 override silently did not apply. Fixed by hand for 19707,
19858 and 19913, and the three ledger lines were corrected from `reject` to `apply`.

## Totals

- **128 entries changed** (124 tag migrations + 19879 gloss + 19707/19858/19913),
  all 250 in range revalidated: `250/250 valid`.
- `validate_tags.py --check-no-new-unknown`: **no new off-vocabulary tags**.
  `build/data/unknown_semantic_baseline.json` regenerated after the migration
  (5,109 entries / 6,325 tolerated uses still baselined dictionary-wide).
- Decisions logged to `reviews/decisions.jsonl`: 131 accuracy lines + 10 self-check
  lines + 1 aggregated furigana bulk-reject line.
- OpenRouter spend this run: **$0.186** ($0.143 review + $0.0125 self-check, plus the
  aborted first segment) against a $2.50 session budget and $5.00 daily cap.
- Candidates added: none — no un-entered Japanese words were encountered, since this
  mode reads review reports and tag metadata rather than example prose.
- Observations logged: 3 (`[pattern]` off-vocab tag scope + migration recommendation;
  `[tooling]` screening precision/throttling with a post-filter proposal; `[tooling]`
  `VALID_DOMAIN` values misfiled in the `semantic` slot).

## Cursors

- `polishing/tasks/cross-model-review/progress.txt` → **`next: 19951`**
- `reviews/queue.txt` trimmed of 19701–19950: 12,581 → 12,506 lines
- `pipeline/routine-state.json` advanced by the selector in §1

## Note for the curator

Two things worth a decision:

1. **The off-vocab tag backlog is ~5,100 entries.** At 250 entries per
   accuracy-review run that is ~20 runs for this band alone. The ~50 safe renames used
   here should be folded into `check_tag_drift.py`'s `TAG_MIGRATION` (which currently
   covers only 9) and promoted to a `systemic-fix` backlog item; only the ~7% ambiguous
   residue needs per-entry judgment.
2. **The cross-model-review cursor drifted behind its own work** (19452 vs 19700
   actually reviewed), costing this run ~10 minutes of duplicate review. Whatever
   advanced it in PR #3042 set it to the start of its range rather than the end.
