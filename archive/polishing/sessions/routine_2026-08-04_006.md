# Routine session — 2026-08-04 (006)

**Mode**: `wiki`
**Selector reason**: "wiki: highest scheduler debt among eligible modes"
**Signals**: candidates 986 (12 "seen in entry"), max entry ID 30396, comprehensive frontier 6775,
cross-model-review cursor 25601, **unharvested observations 31**, reviewed entries 586,
open backlog items 17. OpenRouter: $0.2854 spent today of $5.00 cap.

## Pre-flight

- §0a: `mcp__github__list_pull_requests` returned **0 open PRs** — nothing to rescue, nothing to
  sweep (the MCP stranded-PR sweep had no candidates). Branch level with `origin/main`
  (`c85b64d02`), working tree clean.
- §0b: lock acquired on `claude/determined-rubin-smc81b`.

## Work done

Followed `planning/maintain-knowledge-base.md`. **Harvested all 27 loose observations** from the
2026-08-03 metrics refresh, the 2026-08-03 polish and new-entries runs, the 2026-08-04
systemic-fix run (link-target-baseform batch 4), the 2026-08-04 polish run (06769–06774), and the
2026-08-04 accuracy-review (25101–25600). Four proposed detectors were measured against the corpus
before filing:

| Premise | Proposed as | Measured | Filed as |
|---|---|---|---|
| free-text `part_of_speech` has no house style | mechanical normalization from `tags.pos` | 401 distinct values / 30,187 entries; 6,573 deviate; **but the field is the sole record of transitivity for 50, `proverb` for 8, `idiom` for 13, "verb phrase" for 19** | **P22 + Tooling 29 updates** (premise refuted); queue item `pos-freetext-transitivity-backfill` (71) |
| `body-part` applied to non-body-parts | dictionary-wide detector rule | **41** verb-POS entries; 30 with no body part in the headword (pure drift), 11 that name one (convention question) | **new Cleanup P40**; queue item `tag-bodypart-non-denotational` |
| potential-of-a-potential conjugation rows | suppression flag | できる → **できられる/できろ** (basic tier), 見える, 聞こえる; shape test unusable (9, mixed), self-declaration test **6/6 true positives** | **new Cleanup P41**; queue item `conjugation-potential-of-potential`; **Tooling 70** |
| literal `\n` stored in notes | `check_consistency.py` check | **exactly 1 entry** (17662) | **Tooling 74** as a permanent guard, not a sweep; entry follow-up |

Other activities:

- **[C]** `topics/entry-consistency.md` deepened (first update since 2026-05-17): added the
  **entry-internal contradiction** class — two fields of the same entry disagreeing — with six
  measured instances, and why it is nearly free to detect, has no false-positive family, and is
  generator-created (hence ratchets, not sweeps).
- **[E]** wiki relative-link + anchor lint: **0 broken** across all pages.
- **Queue sync**: `ideas/backlog-queue.json` 47 → **50 items**; `updated` → 2026-08-04.
- **New tooling items 69, 71, 72, 73, 74**; updates to Tooling **20, 24, 29, 54, 59**; cleanup
  updates to **P13** (350 katakana sole-`general`), **P20** (fourth consecutive block at 41%
  off-vocabulary density), **P37** (24 katakana entries tagged `formal`), and the zero-inline-link
  Informational (eighth and ninth filing; cost estimate now agrees across three takes).
- **Entry follow-ups**: 17662 格安SIM (literal `\n`), 03654 豊か (採れる link with no correct
  target).
- **Activity H (metrics trend) not run** — 9 new metrics lines since the thirtieth refresh
  (2026-08-03), below the ≥10 threshold. Next `wiki` run should expect to do it.

## §4 self-verification

**Skipped — no entries were created or modified this run** (wiki mode changes markdown and one
JSON index only). OpenRouter untouched: **$0.00 spent this run**, ledger unchanged at $0.2854.

## §5 metrics

Appended: `mode=wiki, entries_changed=0, entries_total=30187, candidates=986,
review_queue_len=12275, observations_lines=719, comprehensive_next=6775`.

## Candidates added

None — `wiki` mode reads the wiki and observation stream, not entries.

## Observations logged

None new. All 27 existing loose observations were cleared into the backlog pages; the harvest note
appended to `polishing/observations.md` records where each went.

## Cursors

- `polishing/tasks/comprehensive/progress.txt` — unchanged (`next: 06775`); no polish lane ran.
- `pipeline/routine-state.json` — advanced by the selector in §1.
- No `make build` (wiki-only run; `docs/` untouched).

## Routed to the curator

The `part_of_speech` house-style choice that blocks the normalizer (the backfill does not need
it); the topical-vs-denotational tag convention that blocks 11 of the 41 P40 entries; the
furigana-screener retire-or-gate decision (eight zero-precision windows plus a throughput
argument); the frontier-versus-growth scheduling gap (third refresh running, still ownerless); and
the standing P20 step-1 sweep of the nine shipped `TAG_MIGRATION` mappings.
