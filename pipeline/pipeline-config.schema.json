{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pipeline-config.schema.json",
  "title": "je-dict-1 Pipeline Configuration",
  "description": "Defines a queue of dictionary maintenance tasks for automated or semi-automated execution.",
  "type": "object",
  "required": ["tasks"],
  "properties": {
    "description": {
      "type": "string",
      "description": "Human-readable description of this pipeline run."
    },
    "branch": {
      "type": "string",
      "description": "Default git branch for commits. Individual tasks can override this.",
      "default": "main"
    },
    "on_failure": {
      "type": "string",
      "enum": ["stop", "skip"],
      "description": "Default behavior when a task fails validation. 'stop' halts the pipeline, 'skip' moves to the next task.",
      "default": "stop"
    },
    "tasks": {
      "type": "array",
      "description": "Ordered list of tasks to execute.",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/task"
      }
    },
    "parallel_slots": {
      "type": "array",
      "description": "Parallel task slot definitions for the orchestrator.",
      "items": {
        "$ref": "#/$defs/parallel_slot"
      }
    },
    "schedule": {
      "type": "object",
      "description": "Scheduling configuration for the orchestrator.",
      "properties": {
        "max_concurrent": {
          "type": "integer",
          "minimum": 1,
          "description": "Maximum number of parallel slots to run concurrently."
        },
        "daily_budget_usd": {
          "type": "number",
          "minimum": 0,
          "description": "Maximum daily spend in USD."
        },
        "cooldown_minutes": {
          "type": "integer",
          "minimum": 0,
          "description": "Minutes to wait between launching new sessions."
        },
        "max_runs_per_day": {
          "type": "integer",
          "minimum": 1,
          "description": "Maximum number of session launches per day."
        }
      },
      "additionalProperties": false
    },
    "coordinator": {
      "type": "object",
      "description": "Post-session coordination settings.",
      "properties": {
        "post_session_build": {
          "type": "boolean",
          "description": "Run make build after merging a session's results."
        },
        "auto_merge": {
          "type": "boolean",
          "description": "Automatically merge valid session branches (false = create PR for review)."
        },
        "notify_on_failure": {
          "type": "boolean",
          "description": "Log a notification when a session fails."
        }
      },
      "additionalProperties": false
    }
  },
  "$defs": {
    "task": {
      "type": "object",
      "required": ["type", "count"],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "corpus-harvesting",
            "new-entries",
            "new-candidates",
            "clean-candidates",
            "inline-links",
            "example-sentences",
            "furigana-completeness",
            "furigana-correctness",
            "semantic-labels",
            "noentry-resolution",
            "expand-short-notes"
          ],
          "description": "The task type, which maps to a prompt file in prompts/."
        },
        "count": {
          "type": "integer",
          "minimum": 1,
          "description": "Number of times to invoke this task (each invocation is one Claude session)."
        },
        "branch": {
          "type": "string",
          "description": "Git branch for this task's commits. Overrides the top-level branch."
        },
        "on_failure": {
          "type": "string",
          "enum": ["stop", "skip"],
          "description": "Behavior when this task fails validation. Overrides the top-level on_failure."
        },
        "parameters": {
          "type": "object",
          "description": "Optional task-specific parameters passed to the prompt.",
          "properties": {
            "batch_size": {
              "type": "integer",
              "minimum": 1,
              "description": "Number of entries to process per invocation."
            },
            "entry_ids": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Specific entry IDs to target (e.g., [\"01234\", \"05678\"])."
            },
            "tier": {
              "type": "string",
              "enum": ["basic", "core", "general"],
              "description": "Restrict processing to entries in this vocabulary tier."
            },
            "pos": {
              "type": "string",
              "description": "Restrict processing to entries with this part of speech (e.g., \"verb\", \"noun\")."
            }
          },
          "additionalProperties": true
        }
      },
      "additionalProperties": false
    },
    "parallel_slot": {
      "type": "object",
      "required": ["id", "task_type", "prompt", "batch_size", "enabled"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for this slot."
        },
        "task_type": {
          "type": "string",
          "description": "Task type from the task queue (e.g., furigana, notes, cross_refs)."
        },
        "prompt": {
          "type": "string",
          "description": "Path to the prompt file for this slot's sessions."
        },
        "batch_size": {
          "type": "integer",
          "minimum": 1,
          "description": "Number of entries to claim per session."
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether this slot is active for orchestration."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
